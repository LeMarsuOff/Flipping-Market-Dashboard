# ROADMAP.md — Flipping Market Dashboard

> Single source of truth for task tracking. Updated by agents at the end of every session.
> Supersedes `docs/current-roadmap.md` (kept for historical reference only).

---

## In Progress

_(none)_

---

## TODO — Active

- [ ] **Migrate remaining ~26 inline `connectionType === 'notion'` checks to `INTEGRATIONS` registry** — Strangler Fig follow-up to the 2026-05-19 registry refactor. One micro-fix done at [dashboard.js:5336](dashboard.js:5336) (`_reloadJournalProfileSelection` `isNotionWithDb` now uses `getIntegration(p).isReady(p)`). Rest deferred until MT5 ships — abstracting from a single concrete implementation risks getting the methods wrong.

- [ ] **Extend `.claude/hooks/post-edit.sh` to bump `share.html` / `share.js` / `share.css` cache-bust** — Today the hook only handles `dashboard.css` / `dashboard.js`. Manual `?v=N` bumps in `share.html` per edit during Phase 2/3 worked but are error-prone. Mirror the existing `dashboard.js` branch with a `share.js` branch (no `node -c` validation needed since share.js is small + parsed live in Preview) and same for `share.css`.

- [ ] **Install `pg_cron` extension + schedule weekly purge of expired shares** — Spec'd in [docs/share-link-feature.md §7](docs/share-link-feature.md). RLS already hides expired rows (`expires_at > now()` SELECT policy) so this is pure garbage-collection. Run `select cron.schedule('shares-purge-expired', '0 3 * * 0', $$delete from public.shares where expires_at < now() - interval '1 day';$$)` after enabling the extension via Supabase dashboard. Defer until row count justifies it (~10k shares).

- [ ] **Journal Evolution P1 — Custom props on existing trades** — next phase after P0. Scope : UI "Add custom property" (name, type, validation) dans le Data Setup → écrit `user_schemas` ; rendu inline des custom props dans la trade card du drawer ; edit-in-place (click → input → save → upsert `trade_overlays` via `tradeId`) ; enrichissement de `_normalizeAPITrade` avec les overlays au read. Estimé : 3–4 sem focalisées. **Bloquant aucun** — l'infra Supabase est en place depuis 2026-06-09 (P0). Spec : [docs/journal-evolution.md](docs/journal-evolution.md) §4.

- [ ] **Journal Evolution P2 — Native trade CRUD** — phase suivante après P1 : form de saisie complet (date, pair, setup, RR, screenshots upload), édition inline, soft-delete + UI Corbeille. À ce moment-là le dashboard est vraiment autonome (Notion devient optionnel). Estimé : 4–6 sem. Spec : [docs/journal-evolution.md](docs/journal-evolution.md) §4. **Ne pas démarrer avant P1 stabilisé** (les overlays valident le pattern jsonb sans risquer des données natives).

- [ ] **Journal Evolution P0 follow-up — Retention cron body** — `pg_cron` extension pas encore installée (TODO Active séparé : "Install pg_cron extension"). Une fois installée, écrire le body du cron weekly (cf. [docs/journal-evolution.md](docs/journal-evolution.md) §10 retention policy) : hard-delete profiles `deleted_at < now() - 30 days`, warning email à 180 j d'inactivité, soft-delete à 365 j. Sans le cron, la retention reste passive (le schéma supporte l'état, rien ne GC automatiquement). Pas urgent — tant que la base n'a pas de comptes inactifs, no-op.

- [ ] **Journal Evolution P0 follow-up — Server-side quotas (trades_native + storage)** — déclaré dans spec (10 000 trades/user, 500 Mo media/user) mais pas enforcé en BDD. Implémenter via trigger BEFORE INSERT sur `trades_native` (count check) + edge function de pré-signature pour les uploads Storage. Pas urgent tant que P2 n'a pas démarré (aucun trade natif possible aujourd'hui).

- [ ] **PDF export — handle non-CORS (Notion-hosted) screenshots in the jsPDF download** — the trade-card PDF export (`_pdfBuildDoc`/`_exportTradesPdf`, 2026-06-09) embeds screenshots via html2canvas, which can only rasterise images whose host allows cross-origin reads. Supabase-public-bucket screenshots embed fine; a screenshot still hosted directly on Notion (upload to Supabase not yet done) comes out blank in the downloaded PDF. The in-tab preview still shows them (plain `<img>`, no CORS needed). Options to fix: (a) route Notion image URLs through the Vercel proxy with permissive CORS before capture, (b) force-migrate any pending screenshots to Supabase before allowing export, (c) fetch→dataURL each image with a CORS-allowed fallback. Low urgency — most synced profiles have migrated screenshots; flagged so it isn't mistaken for a bug.

---

## TODO — Deferred

- [ ] **Grouped filters — builder dédié style Notion** (idée discutée 2026-06-10, parked) — système de filtres combinés pour exprimer des conditions multi-dim AND'ées que les chips mono-dim actuels ne permettent pas. Exemple cible : « exclure les setups Confirmation qui se sont passés à 8h » (au lieu d'exclure tous les Confirmation ou toutes les 8h). **Direction validée avec Max** : builder dédié (panneau side-drawer style Theme editor) plutôt que lasso multi-chips. **Décisions UX/archi à valider avant de coder** (proposées, pas encore tranchées) : (1) sémantique = groupes s'ajoutent **en plus** des chips existants (intersection finale), pas remplacement ; (2) profondeur = **plat** v1 (1 groupe = `(A AND B AND C)`, plusieurs groupes OR'és, chacun include/exclude), imbrication façon Notion `((A AND B) OR (C AND D))` en backlog v2 ; (3) opérateurs numériques (hour, rr_initial, rsi) = **égalité only** v1, `≥/≤/range` en v2. **Persistance** : réutiliser la clé `comboFilters` déjà présente dans `presetLiveFilters_v1` + snapshot dans `flipping_preset_snapshots_v2.comboFilters` (scope per-preset comme les chips). **Intégration `filterDataset()` (~13386)** : étape supplémentaire **après** le filtrage par chips — trade matchant un groupe `exclude` viré, trade ne matchant aucun groupe `include` (quand ≥1 include) viré. Ne touche pas la logique chips. **UI** : bouton "+ Group filter" en bas de la sidebar des chips → drawer ; chaque groupe actif rend une pastille composite (ex `⊗ Confirmation × 08h`) dans la sidebar avec tooltip et click→édition. **Prochaine étape quand repris** : trancher les 3 décisions ci-dessus puis écrire mini-spec `docs/grouped-filters.md` (schéma LS, pseudo-code évaluateur, wireframe ASCII builder) avant tout code.

- [ ] **MT5 / MT4 / cTrader — actual implementation** — registry stubs (`available: false`) exist since 2026-05-19. Next steps before code: (1) decide MetaApi.cloud SaaS bridge vs. self-built MQL5 EA (cost vs. control); (2) extend Vercel proxy or move to Supabase edge functions for per-broker secret handling (current static-page architecture can't hold per-user API keys safely); (3) spike one platform end-to-end on a single test account before promising to community. cTrader is the easiest technically (OAuth Open API) but smaller audience; MT5 covers most of Max's prop-firm users.

- [ ] **Backend OAuth: dynamic frontend redirect** — `notion-dashboard-api-2` Vercel function uses `DASHBOARD_URL` env var hardcoded to a single front URL (`https://lemarsuoff.github.io/Flipping-Market-Dashboard/`). Conséquence : impossible de tester l'OAuth Notion en local sans flipper la variable (et casser la prod). Fix : à `/api/notion/oauth/start`, lire l'`Origin`/`Referer` de la requête, valider contre une allowlist (`github.io/Flipping-Market-Dashboard/`, `localhost:*`), encoder dans le `state` OAuth, puis y rediriger au callback au lieu de toujours utiliser `DASHBOARD_URL`. Garder `DASHBOARD_URL` comme fallback. Nécessite accès au repo backend `notion-dashboard-api-2` (pas sur cette machine). Contexte complet dans [DECISIONS.md](DECISIONS.md) § 2026-05-19 — Vercel `DASHBOARD_URL` is single-tenant prod.

- [ ] **Drawdown Intelligence section** — DD by context (setup/session/pair decomposing max DD), Recovery Factor (Total R / |Max DD|), DD Duration distribution, Risk of Ruin estimate. Optional: Underwater curve, Streak/DD scatter, MAR/Calmar (~14 months of data, low priority). Deferred pending community priority signal.

- [ ] **Hardcoded font-sizes (~180 remaining)** — deferred pending community feedback on typography editor coverage.

- [ ] **BYOT + export/import config** — open the dashboard to all users without requiring their Notion credentials. Recommended step before full OAuth rollout. ~1.5 days dev vs weeks for OAuth. Deferred until post-Notion overhaul stabilization.


- [ ] **Orphaned localStorage keys after profile deletion (audit M2)** — `_deleteJournalProfileById` does not purge scoped keys (`apiTradesCache_*`, `outcomeValueMapping_*`, etc.).

- [ ] **Share view — bottom summary strip** — `WIN DAYS / LOSS DAYS / NO TRADES / BEST DAY / WORST DAY / AVG PER TRADE`. Visible in reference designs, not yet implemented.

- [ ] **Yellow border highlighting on clicked cells** — P&L Calendar, Pair × Session, Monthly P&L in Hover mode.

- [ ] **Security migration — repo privé + hosting Cloudflare Pages + minification bundle (Niveau 1+2)** — surfaced 2026-05-20. Aujourd'hui le repo est public sur GitHub → un voleur fait `git clone + deploy` en 5 min. Plan en 2 étapes faisables en ~3 h en une session : (a) passer le repo en privé + migrer le hosting GH Pages → Cloudflare Pages (free tier, accepte repos privés via GitHub Actions auto-deploy) ; (b) ajouter un step `npx terser dashboard.js -o dashboard.min.js -c -m` dans le CI, `index.html` pointe sur `.min.js` en prod, source reste lisible en local. Workflow dev inchangé (édition locale + refresh). Bénéfice : passe de "clone trivial en 5 min" à "reverse-engineer pénible d'un bundle minifié". **Timing libre — pas urgent aujourd'hui** (audience = 600 community members distribuée par Max, pas viral, pas monétisé, pas indexée). À faire **avant** un des 4 triggers : (1) lancement public visible (Twitter / YouTube / Product Hunt), (2) monétisation, (3) lancement MT5 (élargit l'audience), (4) repost externe non contrôlé. Niveaux 3 (auth gate via Supabase Auth) et 4 (secret sauce côté serveur via Edge Functions) restent réservés au pivot SaaS payant — sur-engineering tant que freemium community.

- [ ] **Add LICENSE file (MIT recommended)** — currently the public repo has no license, so legally all rights reserved by default. Not urgent since users don't access GitHub directly, but worth adding for legal clarity.

- [ ] **Add README.md** — public-facing landing page on GitHub. Low priority since users access the dashboard via GitHub Pages link, not the repo itself. Would matter if/when the repo becomes a public-facing project (contributions, forks, etc.).

- [ ] **Auto-rename session on `/end` — feature request to revisit if Anthropic ships the hook** — investigated 2026-05-21 via the claude-code-guide agent: programmatic auto-rename of the current Claude Code session is **not possible today**. (1) No CLI command like `claude session rename <id> "<title>"` exists. (2) Stop hooks have no access to the agent's final message text — they only receive tool execution events. (3) Claude Code does not parse OSC codes or any agent-emitted marker that would trigger a rename. The current pattern (agent proposes `Tu peux remplacer le titre par : …` line, user copy-pastes into `/rename`) is the only reliable path. Revisit if Anthropic ever exposes (a) a CLI/IPC endpoint for session-rename, (b) enriches the `Stop` hook payload with the agent's final turn, or (c) introduces a marker convention agents can emit. Until then, status quo.

---

## Done — Last 10

- [x] **Journal Evolution P0 — Foundation : Supabase infra + tradeId unifié + retention infra + export JSON v1 + UI button** (2026-06-09) — Pivot stratégique acté avec Max : on s'éloigne de Notion (read-only en import désormais), le dashboard devient autonome (CRUD natif + custom props à venir en P1/P2). Spec mise à jour [docs/journal-evolution.md](docs/journal-evolution.md) (§1 vision révisée, §4 P3 Notion two-way déclassé, §9 marqué résolu, §10 retention policy ajoutée, §11 nouvelle séquence P0→P1→P2). **(1) Migration Supabase appliquée prod** ([supabase/migrations/20260609000000_journal_evolution_p0.sql](supabase/migrations/20260609000000_journal_evolution_p0.sql), 266 lignes) : 6 nouvelles tables — `user_retention` (per-user `last_active_at` + soft/hard delete state, backfillée avec 5 lignes depuis `auth.users`), `trades_native` (UUID, jsonb props, schema_version, FK sur journal_profiles, GIN index `jsonb_path_ops`), `trade_overlays` (PK composite `user_id/profile_id/source_trade_id`, jsonb props), `user_schemas` (versionné, props_def jsonb), `audit_log` (append-only, action enum, jsonb diff), `pending_emails` (queue retention, RLS read-own seulement) ; `journal_profiles.deleted_at` ajoutée (soft-delete per-journal, NULL pour les 6 lignes existantes) ; RLS active partout (`auth.uid() = user_id`, soft-deleted hidden par défaut) ; triggers `updated_at` sur les 3 tables mutables. **(2) Hardening `auto_bump_updated_at`** (migration de suivi) : `search_path = ''` + `security invoker` posés, résout le seul advisor security introduit par la migration P0. **(3) `tradeId` unifié côté JS** sur 5 chemins de normalisation : `_normalizeAPITrade` ([dashboard.js:9417-9424](dashboard.js)) ajoute `_resolvedTradeId = _notionId || notionId || id || \`idx:${_rawRowIndex}\`` ; `loadBuiltinCSV` ([dashboard.js:11193](dashboard.js)) → `demo:N` ; 3× CSV import paths (lignes 30577, 30678, 30776) → `csv:N`. Préfixés pour éviter les collisions cross-source. Vérifié live : 100/100 demo trades portent un `tradeId` non-vide unique. **(4) `_SW.touchLastActive()`** ([dashboard.js:1524-1551](dashboard.js)) debouncé 1×/jour via LS flag `lastActiveTouch_v1` ; upsert dans `user_retention` ; appelé depuis les handlers SIGNED_IN ET INITIAL_SESSION (ligne 495 + 540) ; no-op propre quand pas signed in (pas de LS write). **(5) Export JSON v1** ([dashboard.js:9534-9598](dashboard.js)) — `_buildFullDataExport()` retourne `{ version:1, exported_at, profile_id, profile_meta, schema, custom_widgets, trade_count, trades }` ; `downloadFullDataExport()` déclenche le download via Blob URL + filename `flipping-export-<profile>-<stamp>.json` ; `window.flippingExportData` bound pour invocation devtools ; `_handleExportAllData()` wired sur le bouton avec toast confirmation via `showThemeToast`. **(6) UI bouton "Export"** ([index.html:1322-1329](index.html), [dashboard.css:15598-15642](dashboard.css)) : placé dans le panel Data Setup à droite du bouton Guide, palette muted gray pour rester secondaire (Guide reste l'action visuelle primaire), hover bascule sur accent-primary, icône SVG download-arrow. `data-action="export-all-data"` câblé sur le dispatcher (ligne 4056). **Vérifié live en preview** (port 8000) : bouton rendu (26 px, visible, "Export"), wiring confirmé via `data-action` + `_handleExportAllData` global, export shape complète, 0 erreur console au boot. `node -c` OK, brace delta 0. **Note importante reconnue avec Max en fin de session** : P0 = fondation invisible (rien ne change côté UX utilisateur final, hors le bouton Export). L'autonomie réelle (saisie trades, edit inline, custom props) viendra en P1 (~3–4 sem) et P2 (~4–6 sem). Décision tracée dans DECISIONS.md du jour. Cache JS `?v=950 → 962` · CSS `?v=307 → 308`.

- [x] **Trade-card PDF export — "One per page" layout overhaul (vertical screenshots, share-style cover, bigger fonts, consistent cards, discreet labels)** (2026-06-09) — Itération Max-driven en plusieurs passes sur le mode **One per page** du PDF export (`_pdfBuildDoc` / `downloadPdf`, [dashboard.js](dashboard.js)). **(1) Screenshots empilés** : en page layout, `.shots` passe de la grille 3-colonnes inline à une seule colonne (`grid-template-columns:1fr`) → les 3 captures s'empilent verticalement et la carte devient haute, donc le scaler page-fit la grossit pour remplir toute la hauteur de page → grandes images. **(2) Page de garde stylée** (remplace les bandes header) : nouveau bloc `.cover` (caché en inline, affiché en page layout ; `doc-head`/`ctx-strip` masqués en page layout) reprenant le watermark du share — pill de marque, eyebrow = **vrai nom de dimension** (`ctx.dim`, ajouté à `_pdfCtx`), grand titre de sélection, 5 tuiles colorées **Trades / Total R / Win Rate / Avg R / Profit Factor** (Avg R + Profit Factor calculés depuis le set). **(3) Bandes noires éliminées** : les zones transparentes devenaient NOIRES en JPEG → la cover ne capture plus que `.cover-inner` (pas de gouttières) et tous les blocs (cover + cartes) sont rasterisés sur un fond `backgroundColor` = page-bg (coins arrondis fondus). **(4) Polices lisibles indépendamment de la largeur d'onglet** : l'onglet d'export s'ouvre plein écran → carte rendue très large → polices minuscules une fois scalées ; fix = `.page` épinglé à `max-width:780px` en page layout (taille de police prévisible) + bump des polices de carte. **(5) Cartes uniformes** : tentative de collapse des slots vides annulée à la demande de Max → chaque carte garde **toujours 3 slots** (trou placeholder si vide) → hauteur de carte identique quel que soit le nombre de captures. **(6) Bande setup retirée** : le séparateur de groupe `.setup-sep` n'est plus dessiné en page layout (redondant avec le setup déjà dans la carte + source de la bande noire). **(7) Labels discrets** : `H4/M15 BEFORE/AFTER` passent du gros encadré noir (`rgba(0,0,0,.6)`, bold) à un pill subtil (`rgba(0,0,0,.28)`, weight 600) — appliqué au **style de base `.shot-lbl`** donc valable dans **tous les modes** (inline + raw gallery + page), pas seulement One per page. **(8) Alignement raw gallery** : en mode single-screenshot multi-cartes, les têtes de carte ont des hauteurs variables (listes d'obstacles) → les screenshots démarraient à des Y différents ; fix = la grille `body[data-img-filter] .page` passe en `align-items:stretch` (cartes d'une rangée à hauteur égale) + carte en `display:flex;flex-direction:column` + `.shots{margin-top:auto}` → screenshot ancré en bas → captures alignées en haut ET en bas à travers la rangée (le delta de hauteur de tête est absorbé par un gap au-dessus de l'image). **Validé en preview** (mesures iframe à largeur d'onglet réaliste : cover full-width sans bandes, hauteurs de carte égales 2-shots vs 3-shots, polices ~2× plus grandes, labels adoucis tous modes, raw gallery → 3 captures tops=404 bottoms=744 identiques malgré têtes 1 vs 6 obstacles) — rendu JPEG/PDF final à confirmer live par Max. `node -c` OK, brace delta 0, 0 erreur console. Cache JS `?v=933 → 950`.

- [x] **Share link — dynamic Open Graph preview (rich card in Discord/Slack/Twitter) via SSR + generated og:image** (2026-06-09) — Demande Max : qu'un lien de partage collé dans Discord affiche une carte riche (nom du chip + stats) au lieu d'une URL nue. GitHub Pages ne peut pas faire de SSR et le domaine `*.supabase.co` des Edge Functions **neutralise le HTML** (force `text/plain` + CSP `sandbox` → les crawlers n'unfurlent pas), donc l'archi a 3 morceaux : **(1) Image dynamique** — Supabase Edge Function `share` ([supabase/functions/share/index.ts](supabase/functions/share/index.ts), `verify_jwt:false`) génère un PNG 1200×630 via `og_edge` (Satori + resvg-wasm) avec polices Google chargées en TTF (trick no-UA, woff2 non lu par Satori) ; design = watermark du viewer (pill de marque, eyebrow = **vrai nom de dimension**, grand nom du chip, 5 tuiles `Trades/Total R/Win rate/Avg R/Profit factor` colorées par palier). Lit `shares` en anon (RLS respecte l'expiration). **(2) Page HTML SSR** — projet Vercel **standalone** `flipping-share-og` ([docs/share-og-vercel/](docs/share-og-vercel/)) `https://flipping-share-og.vercel.app/share?id=` : sert du vrai `text/html` avec les `<meta og:*>`/Twitter dynamiques + `og:image` → la fonction Supabase, et un `<base href>` vers github.io pour que le viewer interactif (share.css/js) tourne à l'identique. Servi à tous (humains + crawlers, pas de UA-sniffing). **(3) Vrai nom de propriété** — `opts.dim` câblé dans `openWidgetDrawer` → `_shareCtx.dim` → snapshot `stats.dim` ([dashboard.js](dashboard.js) `_createShareRow`), wiré aux drawers (barres perf Setup/Session/Pair/Day/M15-H4 Obstacle/Session Time/custom via `_barChartDimLabel`, donut→Outcome, Monthly→Month, Pair×Session, calendrier→Date, RR bubble→RR Max). L'eyebrow (image + viewer share.js) préfère `stats.dim`, fallback sur le type `chip_kind`. Lien généré basculé de `github.io/share.html?id=` → `…vercel.app/share?id=`. **Validé live** : endpoint Vercel renvoie `text/html` + OG injectés (testé `id=OGtest01`), PNG 1200×630 généré (eyebrow « SESSION », stats London), page humaine rend watermark + drawer + 2 trade cards dans Chrome, assets github.io 200, `node -c` OK, braces 0. Cache JS `?v=922 → 933` · share.js `?v=9 → 10`.

- [x] **Trade-card PDF export — new drawer button → self-contained export tab (inline screenshots grouped by setup) with true client-side PDF download (jsPDF + html2canvas)** (2026-06-09) — Demande communauté : un bouton pour exporter les trades en PDF avec l'intégralité des screenshots, triés par setup, 1 trade = 1 ligne de 3 captures. Construit comme un **document HTML autonome** (`_pdfBuildDoc(ctx)` @~23060) ouvert dans un nouvel onglet depuis le widget-drawer (`_exportTradesPdf`, lit `drawer._pdfCtx`, stashé indépendamment de l'auth dans `openWidgetDrawer`). Bouton PDF ajouté à côté du bouton Share du drawer ([dashboard.css](dashboard.css) `.wd-drawer-pdf-btn`). **(1) Document** : en-tête (date/sélection/mode TP), bandeau stats (trades/WR/total R), groupage par setup avec séparateurs, cartes reprenant `_wdTradeCard` (paire, 🔗 Notion, direction, date, session/heure, setup, RR Max, TP hits, obstacles H4/M15, R effectif coloré, outcome) + 3 screenshots inline 16:9 bord-à-bord. **(2) Toolbar du doc** : sélecteur d'image (All / les 3 noms de slots mappés via `_getTradeMediaStepLabel`), sélecteur grille 1/2/3 par ligne (visible seulement en mode single → vue galerie d'un timeframe à travers plusieurs trades), multi-select « Card fields » (toggle des propriétés affichées — standards cochées par défaut = cartes habituelles + props Notion custom via `loadCustomProps()`/`t.extras`, décochées par défaut), bouton Download. **(3) Lightbox** : clic sur une image → agrandissement plein écran. **(4) Vrai download PDF** via **jsPDF 2.5.1 + html2canvas 1.4.1 (cdnjs, nouvelles deps CDN approuvées par Max)** : rendu **par bloc** (header/séparateurs/chaque carte capturés séparément) → reste sous la limite canvas ~32767px sur gros exports + carte jamais coupée entre 2 pages ; le moteur de pagination PDF reproduit la grille 1/2/3 colonnes de l'écran. **(5) Contraintes html2canvas** : `color-mix()` pré-calculé en `rgb()` (helper `_pdfHexToRgb` + `mix()` depuis les tokens thème live), `aspect-ratio` remplacé par padding-top 56.25%, visibilité des champs en pur CSS via attribut `data-show~="key"` → respectée à l'identique écran/PDF. Texte du doc **100% anglais** (règle projet). **Validé en preview** sur données démo (5–20 trades) : pipeline PDF complet (5 pages/1,6 Mo pour 20 trades, hauteur canvas max 748px), grille 3-col (cartes x=14/206/398), lightbox, toggle champs (standard + custom « Confidence » simulé), 0 erreur console, `node -c` OK, brace delta 0. **Caveat CORS** (nouveau TODO Active) : captures encore hébergées sur Notion → blanches dans le PDF (Supabase OK). **(6) Sélecteur Layout (mode All)** : nouveau `<select>` Inline / One per page (visible en mode All, mutuellement exclusif avec le sélecteur de colonnes du mode single, attribut body `data-layout`). En `page`, `downloadPdf` branche sur une boucle dédiée : header+stats sur une page de garde, puis **chaque carte sur sa propre page** (agrandie au max, ratio préservé, centrée, label du setup re-dessiné en tête) ; à l'écran un séparateur pointillé matérialise les sauts de page. Validé en preview : 5 trades → 6 pages PDF (1 garde + 5 × 1 trade), 395 Ko. Cache JS `?v=877 → 922` · CSS `?v=306 → 307`.

- [x] **Post-Drawdown Recovery — new Distribution view (toggle Scatter ↔ Histogram), percentile selector P50/P60/P75/P90, bucket drilldown, Theme Editor wired typography** (2026-06-02) — Demande Max : afficher la distribution des profondeurs de drawdown (mock à barres horizontales fourni) en complément de la vue scatter existante du widget Post-Drawdown Recovery. Spec verrouillée avec lui en amont : toggle dans la toolbar (mirror des segments calendrier), buckets adaptatifs (1R wide jusqu'à 5R, 2R wide au-delà), sélecteur de percentile, click bar → drawer existant. **(1) Refacto partagé** ([dashboard.js:21070](dashboard.js:21070) `_computeRecoverySequences`) : extraction du calcul des `recoveries[]` (objet `{ddDepth, trades, startIdx, troughIdx, endIdx, isActive}`) du scatter, partagé avec le nouveau histogram → garantit que les deux vues lisent exactement le même dataset. **(2) State LS-persisted** : `_recoveryViewMode` (`scatter`/`histogram`, clé `recoveryViewMode_v1`) + `_recoveryPercentile` (50/60/75/90, clé `recoveryPercentile_v1`, défaut 90) — par machine, pas de sync cross-device. **(3) Dispatcher** ([dashboard.js:21130](dashboard.js:21130) `renderRecovery`) : route vers `renderRecoveryScatter` (code existant intact) ou `renderRecoveryHistogram` selon le mode, swap les surfaces canvas/HTML via `.is-hidden`. **(4) Histogram HTML** ([dashboard.js:21380](dashboard.js:21380) `renderRecoveryHistogram`) : buckets via `_recoveryBuckets(maxDepth)` (convention `lo<=d<hi`, labels `0–0.9R, 1–1.9R, 2–2.9R, …, 5–6.9R, 7–8.9R, …` non-ambigus), percentile via `_recoveryQuantile(values, pct)` (linear interp, comme NumPy default), grid layout `[label 64px][bar 1fr][meta 96px][badge 38px]` avec sub-grid `28px 10px 1fr` pour count/sep/% (pixel-perfect alignement vérifié à `x=573/601/669`). Footer : 2 stats (P_n + DDMAX) avec valeurs en `var(--a)` / `var(--r)`. **(5) Drilldown réutilise l'existant — zéro nouveau code** : click bar → `openRecoveryDrawer` ([dashboard.js:23498](dashboard.js:23498)) étendu pour reconnaître `bucket.x` range-string ("5–7") vs numeric, et — fix critique trouvé en cours — brancher la branche active/recovered sur `occ.isActive` par-occurrence ([dashboard.js:23566](dashboard.js:23566)) au lieu du `opts.isActive` global, sinon un bucket mixte (3 recouvrés + 1 ongoing) rendait les 4 cards comme "Active Drawdown". **(6) Selection state aligné Performance Bars** : `box-shadow: inset 0 0 0 2px var(--gold)` (même pattern que `.bar-row.lb-active` et `.pp-gchip.lb-active`) — pas de fill background ; cleared par `closeWidgetDrawer` ([dashboard.js:23834](dashboard.js:23834)) qui gère maintenant scatter + histogram dans le même bloc. **(7) Color polish** (3 itérations Max-driven) : (a) ditched le marqueur dashed-gold "DD actif" qui était illisible — l'info reste dans le sub du drawer (`N occurrences · K ongoing`) ; (b) bucket percentile passe de `var(--gold)` à `var(--a)` (orange/amber) — `--gold` se résolvait teal/blue sur les light themes (Dawn/Flipping) et lisait off-context pour un highlight drawdown ; (c) bar fill aligné Performance Bars : `var(--r)` flat + `var(--bg3)` track (était gradient `--r/--r2/--r3` trop pâle). **(8) Leading-empty trim** : si `0-0.9R` (et suivants) sont vides, ils sont retirés ; gaps internes (ex `7-8.9R: 0` entre buckets populés) restent visibles pour préserver l'échelle. **(9) Theme Editor integration** ([dashboard.css:5295-5440](dashboard.css:5295)) : toutes les typos câblées sur 8 groupes existants — title→G04 widget-title, sub→G05 widget-sub, label→G08 chart-axis, meta→G09 chart-val, badge→G15 micro, foot lbl→G07 kpi-lbl, foot val→G06 kpi-val, foot note→G05 widget-sub — vérifié `computed.fontSize === root --typo-<group>-size` pour tous les 8 sites. Aucun nouveau token, aucun changement nécessaire dans le code du Theme Editor. **Tentative thème "Ember"** (palette warm-dark salmon+amber inspirée du mock initial) : implémentée puis retirée par Max (jugé "hideux"). **Convention ancrée en mémoire** ([conventions.md](.claude/projects/.../memory/conventions.md)) : à l'avenir, jamais hardcoder font-size/color/weight ; toujours router via tokens Layer A (couleurs) + 1 des 15 groupes typo (G01-G15) ; si aucun groupe ne fit sémantiquement, demander création avant. **Validé live en preview** (port 8000) sur données démo (12 recoveries, DDMAX -11.60R) : toggle round-trip propre, percentile switch re-highlight le bon bucket, drawer s'ouvre avec sub "N occurrences · K ongoing" correct sur bucket mixte, click bar puis backdrop → selection cleared. `node -c` OK, 0 console error, brace delta 0. Cache JS `?v=865 → 873` · CSS `?v=292 → 306`.

- [x] **Mapping reverts to template default — durable fix : dirty-flag local-edit-wins (défauts 1+3 fermés, testé E2E contre Supabase prod)** (2026-06-01) — 4e passe sur le bug de revert de mapping signalé par un membre : « hard refresh + re-map → re-login → revient au défaut » et surtout « après quelques minutes loin de la page, ça revient tout seul ». **Diagnostic** : les 3 verrous précédents (`9c6f577`/`a57f58f`/`2edf43a`) ne traitaient que le côté ÉCRITURE (queue + force-flush). Le bug venait de deux défauts jamais corrigés : **(1)** pull remote-wins inconditionnel — `syncFromRemote` ([dashboard.js:775](dashboard.js:775)) écrase le mapping local par la valeur cloud à chaque login/refresh/**refocus** (le « tout seul » = `visibilitychange` >10 s → `_forcePull` → `syncFromRemote`) ; **(3)** le reconcile ([dashboard.js:871](dashboard.js:871)) ne pousse que les clés ABSENTES du cloud, jamais une présente-mais-périmée → le cloud du membre restait bloqué sur le défaut template (RR Max + screenshots, même clé `apiFieldOverrides_v1_<profile>` → reviennent ensemble). Aggravant trouvé : le court-circuit dedup de `safeSetLocalStorage` ([dashboard.js:9727](dashboard.js:9727)) saute le push Supabase quand le LS contient déjà la valeur → un seul échec transitoire laisse le cloud en retard définitivement. **Fix dirty-flag local-edit-wins** (6 points) : store local-only `flipping_mapping_dirty_v1` (non synchronisé, survit au hard refresh) ; `_saveAPIFieldOverrides` marque la clé override dirty AVANT le push ; `_SW.set` nettoie dirty sur confirmation RPC ; `syncFromRemote` ET `_onUserData` : si clé mapping dirty → ne clobber PAS, seede le seen-cache avec l'`updated_at` distant + re-pousse le local (RÉPARE le cloud périmé) ; reconcile nettoie dirty sur push réussi. Scope serré : seul `apiFieldOverrides_v1_<profile>` est protégé (les field-names sont auto-dérivés du fetch). **Validé E2E contre le vrai Supabase prod** (user de test jetable créé puis supprimé, 0/0/0) : cycle save→push→clear OK ; scénario membre (cloud périmé + local bon + dirty → pull) → local NON reverté + cloud réparé + dirty nettoyé ✓ ; non-régression (pas dirty → adopte le cloud normalement) ✓ ; 0 erreur console. Compromis assumé (validé avec Max) : l'édition locale non confirmée gagne sur le cloud (LWW classique en cas d'édition multi-device quasi-simultanée). **Note membre** : re-saisir le mapping une dernière fois après deploy — cette fois prouvé que ça atteint le cloud et ne reverte plus, même si le push initial échoue (le dirty re-pousse au pull suivant). Cache JS `?v=855 → 861`.

- [x] **Mapping reverts to template default on profil re-sync — round 2 : pencil-pick force-flush la file `_pendingSyncWrites` pour garantir la remontée cloud même fenêtre apply-remote ouverte indéfiniment** (2026-06-01) — Suite directe du fix matin (commit `a57f58f`) : un membre signalait que le bug revenait encore après re-sync + refresh. **Trou identifié dans le mécanisme de queue du 2026-05-31** : la file `_pendingSyncWrites` est flushée par un `setTimeout(1500ms)` posé par `_enterApplyingRemote` ([dashboard.js:1560](dashboard.js:1560)), mais **chaque nouvel appel à `_enterApplyingRemote` reset le timer** via `clearTimeout`. Conséquence : une cascade d'events Realtime (preset/theme/blob) arrivant à <1,5 s d'intervalle keep-alive la fenêtre indéfiniment → la file grossit, jamais flushée → les pencil-picks de l'utilisateur restent uniquement en LS, jamais Supabase. Au prochain remote-wins pull (refresh / sign-in / refocus), `syncFromRemote` re-tire la valeur cloud stale (template default) → la mapping "revient toute seule au défaut" malgré la re-saisie. **Fix** ([dashboard.js:8084](dashboard.js:8084)) : `_saveAPIFieldOverrides` appelle maintenant `_flushPendingSyncWrites()` immédiatement après `safeSetLocalStorage`. Tout pencil-pick force la file à drainer dans le même tick, qu'on soit dans une fenêtre apply-remote ou pas. No-op quand la file est vide (happy path → un appel de fonction). Garantit que le write atteint Supabase dans la même action utilisateur, peu importe combien de temps la fenêtre apply-remote reste ouverte. **Validé en preview** (port 8000, v=854→855, 0 erreur console au boot, `node -c` OK). Tests E2E nécessitent une session signed-in avec un compte Notion réel — à confirmer live par Max (Test 1 : pencil-pick → Network tab montre l'upsert RPC immédiat ; Test 2 : event Realtime dans onglet B + pencil-pick dans onglet A pendant la fenêtre → upsert immédiat aussi ; Test 3 : refresh + Sync ↻ → mapping ne revient plus au défaut). **Note membre** : le cloud déjà corrompu d'avant les 3 verrous reste sur l'ancien défaut tant qu'une re-saisie ne le réécrit pas — désormais cette re-saisie est garantie d'atteindre Supabase. Cache JS `?v=854 → 855`.

- [x] **Mapping reverts to template default on profil re-sync — auto-template-map was re-writing defaults whenever the override set read empty + screenshot auto-map ignored user picks** (2026-06-01) — Suite du fix cross-device de la veille : un membre (sur code à jour, sa propre base) signalait que le bug revenait **lors d'un re-sync du profil** (bouton `↻` Sync ou Full resync). Investigation : le re-sync passe par `_loadNotionTrades` qui ne re-tire PAS le cloud (`syncFromRemote` absent du chemin) — donc le revert venait des deux auto-mappers internes lancés à chaque sync. **Deux causes distinctes, deux verrous.** **(1) Garde anti-revert dans `_maybeAutoApplyTemplateMapping`** ([dashboard.js](dashboard.js) ~8598) : la fonction n'écrit les défauts template QUE si le set d'overrides se lit **vide**. Sur un profil déjà synchronisé, un set vide est anormal (la perso devrait être là) — quelle qu'en soit la cause transitoire (cloud `{}`, wipe LS, race de contexte), écrire les défauts cimentait le revert (RR Max → `RR Max`, toutes les dims non-screenshot → défaut). Nouvelle règle : **jamais d'écriture automatique de défauts sur un profil ayant déjà sync** (`profile.notionLastSync || notionTradeCount>0 || notionSyncState==='synced'`) ; on décline l'écriture et on garde le set en l'état. Nouveau param `opts.forceWrite` ajouté à la signature : le bouton "Reapply template defaults" ([dashboard.js](dashboard.js) ~9103) le passe à `true` pour rester le seul chemin sanctionné de re-seed à la demande. Un `console.warn('[MappingRevert] blocked…')` se déclenche uniquement quand la garde bloque (silencieux pour les users normaux). **(2) Appartenance utilisateur dans `_autoMapScreenshotsByRawKeys`** ([dashboard.js](dashboard.js) ~8770) : l'auto-map screenshot (lancé à chaque sync ET sur le reload post-sync via [dashboard.js:7434](dashboard.js:7434)) appliquait sa règle "F&M gagne" **sans jamais consulter** le set d'appartenance `flipping_auto_mapped_dims_v1` → il ré-écrasait des overrides screenshot que l'utilisateur avait explicitement pencil-pickés (symptôme : screenshots se remappent / se dédoublent au re-sync). Fix : lecture de `_getAutoMappedDims()` avant la boucle + skip de tout dim `present mais PAS auto-mappé` (= choix user authoritaire). Les dims encore auto-mappées restent éligibles à la dérivation (first-connect + upgrade URL→F&M intacts). **Validé en preview** (port 8000, v=854, 0 erreur console) via tests stubbés : profil établi + overrides vides → **bloqué** (set reste vide) ✓ ; first-connect → écrit la baseline ✓ ; `forceWrite` (Reapply) → écrit les défauts ✓ ; override user présent → court-circuit préserve ✓ ; screenshot choisi par user → préservé ✓ ; screenshot auto-mappé → re-dérivé ✓. **Effet** : un re-sync (incrémental ou full) ne peut plus réinitialiser le mapping aux défauts, même si le cloud est encore corrompu. Le membre re-saisit son mapping une fois (le fix cross-device l'envoie au cloud) et plus aucun sync ne le casse. Cache JS `?v=846 → 854`.

- [x] **Mapping settings revert to template default (RR Max + screenshots) — cross-device sync was silently dropping mapping-config uploads** (2026-05-31) — Bug remonté par un membre de la communauté : ses réglages de Mapping pour RR Max et les liens screenshots ne se sauvegardaient pas — à chaque update ils revenaient à l'état par défaut, soit au refresh, soit "tout seul" en cours d'utilisation. Screenshots Before/After fournis : RR Max passe de son champ custom `RR max (between ranges)` (5.8, 76/76) au défaut template `RR Max` → "no values detected", et les deux dims screenshot (`img_m15` + `img_h4_before`) se retrouvaient mappées sur le même champ "M15 link" (doublon, "4H link" perdu). **Root cause** (investigation, 3 défauts en cascade dans le sous-système de synchro cross-device) : (1) **Pull remote-wins inconditionnel** — `syncFromRemote` ([dashboard.js:775](dashboard.js:775), "Remote toujours prioritaire sur local") + `_onUserData` ([dashboard.js:1601](dashboard.js:1601)) écrasent la clé locale `apiFieldOverrides_v1_<profile>` / `apiFieldNames_v1_<profile>` avec la valeur cloud, à chaque sign-in/refresh/refocus/reconnect/event Realtime, sans vérifier qui est le plus récent. (2) **Upload local droppé** — le garde anti-boucle-d'écho du monkey-patch `localStorage.setItem` ([dashboard.js:1995](dashboard.js:1995)) saute `_SW.set` pendant la fenêtre `_isApplyingRemote` de 1,5 s (ouverte par n'importe quel event preset/thème/trades-blob — [dashboard.js:1632](dashboard.js:1632), [dashboard.js:1739](dashboard.js:1739)) ; un pencil-pick fait dans cette fenêtre ne touchait que le LS, jamais Supabase. (3) **Aucune réparation** — le pass de réconciliation ([dashboard.js:871](dashboard.js:871)) ne pousse que les clés ABSENTES du cloud, donc une ligne présente-mais-périmée n'est jamais corrigée. Net : le cloud restait bloqué sur le défaut template du premier sync, et chaque pull remote-wins ramenait ce défaut par-dessus la perso locale. **Fix ciblé** (défaut 2, le déclencheur) : nouvel helper `_isMappingConfigKey()` (matche `apiFieldOverrides_v1_*` / `apiFieldNames_v1_*`, variantes `_h4` incluses) ; le monkey-patch, au lieu de jeter l'écriture pendant la fenêtre, **met en file** les clés de mapping (les presets/thèmes restent droppés → protection anti-écho intacte, car `_onUserData` ne déclenche pas de re-render pour les clés de mapping = pas de boucle possible) ; le timer `_enterApplyingRemote` **vide la file** via `_flushPendingSyncWrites` (qui contourne le monkey-patch) à la fermeture de la fenêtre. Le cloud reçoit donc toujours la perso → le remote-wins devient inoffensif. **Validé en preview** (port 8000) via test stubbé : pendant la fenêtre, écriture mapping mise en file puis flushée (`flushedMappingKey: true`), écriture preset bien droppée (`flushedPresetKey: false`), aucun envoi synchrone pendant la fenêtre. Helper classifie correctement override/field-names vs preset/thème/null. `node -c` OK, brace delta 0, 0 erreur console au boot. **Round-trip Supabase réel non testable en preview** (nécessite session connectée) — à confirmer live par Max. **Note membre** : le cloud déjà corrompu ne s'auto-répare pas (cf. défaut 3, nouveau TODO Active) → re-saisir le mapping une fois après deploy suffit, ensuite ça tient. Cache JS `?v=843 → 846`.

- [x] **Share view — default to Month mode on the current calendar month** (2026-05-31) — Demande Max : dans l'onglet "Share your data", le toggle Year/Month démarrait sur **Year** ; il voulait **Month** par défaut, positionné sur le **mois courant**. Le défaut était entièrement câblé sur 'year' en trois endroits cohérents : (1) `_shareMode = 'year'` (module-level, pas de persistence LS) → `'month'` ([dashboard.js](dashboard.js) ~43257) ; (2) bouton `sv-btn-year` avait la classe `active` en dur + `sv-btn-month` non → bascule de `active` vers Month ([index.html](index.html)) ; (3) `<select id="sv-month-sel" class="is-hidden">` masqué par défaut (car mode year) → `is-hidden` retiré. Pour "le mois actuel", `_svPopulateMonthSel` défaultait sur `sorted[0]` (mois le plus récent) ; ajout d'une préférence pour le mois calendaire courant en miroir exact de `_svPopulateYearSel` (qui préférait déjà l'année courante) : `const currentMonth = String(new Date().getMonth()+1).padStart(2,'0')` puis ordre `prev valide → currentMonth si présent → sorted[0] → ''`. Fallback gracieux : si le mois courant n'a pas de trades (cas des données démo qui s'arrêtent en avril 2026 alors qu'on est le 31 mai), retombe sur le mois le plus récent. **Validé live en preview** (port 8000) : ouverture de "Share your data" → mode Month, bouton Month actif, sélecteur de mois visible, année 2026, label "April 2026" (mai absent de la démo → fallback récent ; sur les vraies données Notion avec trades du mois en cours, se calera sur le mois actuel). `node -c` OK, brace delta 0. Cache JS `?v=841 → 843`.

---

## Update rules for agents

At the end of every coding session:
- Move completed items → Done (keep last 10 max, oldest fall off)
- Add any new TODO discovered during the session (with context)
- Update "In Progress" if work is mid-flight
- Commit `ROADMAP.md` in the same commit as the related code change
