# ROADMAP.md — Flipping Market Dashboard

> Single source of truth for task tracking. Updated by agents at the end of every session.
> Supersedes `docs/current-roadmap.md` (kept for historical reference only).

---

## In Progress

_(none)_

---

## TODO — Active

- [ ] **Migrate remaining ~26 inline `connectionType === 'notion'` checks to `INTEGRATIONS` registry** — Strangler Fig follow-up to the 2026-05-19 registry refactor. One micro-fix done at [dashboard.js:5336](dashboard.js:5336) (`_reloadJournalProfileSelection` `isNotionWithDb` now uses `getIntegration(p).isReady(p)`). Rest deferred until MT5 ships — abstracting from a single concrete implementation risks getting the methods wrong.

- [ ] **Extend `.claude/hooks/post-edit.sh` to bump `share.html` / `share.js` / `share.css` cache-bust** — Today the hook only handles `dashboard.css` / `dashboard.js`. Manual `?v=N` bumps in `share.html` per edit during Phase 2/3 worked but are error-prone. Mirror the existing `dashboard.js` branch with a `share.js` branch (no `node -c` validation needed since share.js is small + parsed live in Preview) and same for `share.css`.

- [ ] **Install `pg_cron` extension + schedule weekly purge of expired shares** — Spec'd in [docs/share-link-feature.md §7](docs/share-link-feature.md). RLS already hides expired rows (`expires_at > now()` SELECT policy) so this is pure garbage-collection. Run `select cron.schedule('shares-purge-expired', '0 3 * * 0', $$delete from public.shares where expires_at < now() - interval '1 day';$$)` after enabling the extension via Supabase dashboard. Defer until row count justifies it (~10k shares).

- [ ] **Journal Evolution P1 — Custom props on existing trades** — next phase after P0. Scope : UI "Add custom property" (name, type, validation) dans le Data Setup → écrit `user_schemas` ; rendu inline des custom props dans la trade card du drawer ; edit-in-place (click → input → save → upsert `trade_overlays` via `tradeId`) ; enrichissement de `_normalizeAPITrade` avec les overlays au read. Estimé : 3–4 sem focalisées. **Bloquant aucun** — l'infra Supabase est en place depuis 2026-06-09 (P0). Spec : [docs/journal-evolution.md](docs/journal-evolution.md) §4.

- [ ] **Journal Evolution P2 — Native trade CRUD** — phase suivante après P1 : form de saisie complet (date, pair, setup, RR, screenshots upload), édition inline, soft-delete + UI Corbeille. À ce moment-là le dashboard est vraiment autonome (Notion devient optionnel). Estimé : 4–6 sem. Spec : [docs/journal-evolution.md](docs/journal-evolution.md) §4. **Ne pas démarrer avant P1 stabilisé** (les overlays valident le pattern jsonb sans risquer des données natives).

- [ ] **Journal Evolution P0 follow-up — Retention cron body** — `pg_cron` extension pas encore installée (TODO Active séparé : "Install pg_cron extension"). Une fois installée, écrire le body du cron weekly (cf. [docs/journal-evolution.md](docs/journal-evolution.md) §10 retention policy) : hard-delete profiles `deleted_at < now() - 30 days`, warning email à 180 j d'inactivité, soft-delete à 365 j. Sans le cron, la retention reste passive (le schéma supporte l'état, rien ne GC automatiquement). Pas urgent — tant que la base n'a pas de comptes inactifs, no-op.

- [ ] **Journal Evolution P0 follow-up — Server-side quotas (trades_native + storage)** — déclaré dans spec (10 000 trades/user, 500 Mo media/user) mais pas enforcé en BDD. Implémenter via trigger BEFORE INSERT sur `trades_native` (count check) + edge function de pré-signature pour les uploads Storage. Pas urgent tant que P2 n'a pas démarré (aucun trade natif possible aujourd'hui).

- [ ] **Preset id remap migration — repair presets already stuck at an out-of-range id (≥ PRESET_MAX)** — follow-up au fix `_nextPresetId` du 2026-06-11. Le fix empêche tout NOUVEAU preset de dépasser la plage `[0, PRESET_MAX)`, mais un membre qui a déjà aujourd'hui un preset bloqué à un id ≥ 10 (via cycles création/suppression sous l'ancien allocateur `maxId+1`) garde ce preset **vierge** (son snapshot/override n'a jamais été persisté) tant qu'il ne le supprime + recrée pas manuellement. Migration optionnelle au boot (après les loaders) : pour chaque preset `p.id >= PRESET_MAX` ou `< 0`, trouver le plus petit id libre `< PRESET_MAX`, migrer les slots runtime survivants (le live slot survit — `loadPresetLiveFilters` le charge par clé string ; le snapshot/override sont déjà perdus → migrent en blank), réassigner `p.id`, mettre à jour `appState.presets.activeId` si concerné, re-persister. **Proposé à Max le 2026-06-11, pas tranché** — plus sensible (touche les ids + la persistance de l'active id + dropdowns compare) donc demander confirmation avant. Le cas du reporter (Max) est déjà couvert par le seul fix `_nextPresetId` (ses 7 presets sont en plage 0..9).

- [ ] **Migration optionnelle des valeurs détail bloquées dans le chip natif `setup`** (noté 2026-06-14, suite du fix « exclusion mode détail → chip custom ») — Avant le fix de ce jour, les exclusions de valeurs setup-detail atterrissaient dans le chip natif `setup` (qui dual-filtre `[t.setup, t.setupDetail]` ~[dashboard.js:15393](dashboard.js:15393)). Les valeurs déjà présentes chez un membre (observé sur la donnée de Max : `NEM type A/B` dans `chips.setup.excluded`) continuent de filtrer mais restent dans le natif, **pas** dans la propriété custom → résiduel de désync pour l'ancien état. Le fix ne migre rien (ne touche pas l'état live existant, volontairement). Option : au boot, pour chaque valeur de `chips.setup.{excluded,included}` qui n'est **pas** une valeur de `t.setup` mais **est** une valeur de `t.setupDetail` ET dont la colonne est déclarée comme propriété custom, la déplacer vers le chip `custom:<key>`. Sensible (touche l'état de filtre live + persistance preset/snapshot) → demander confirmation avant. Contournement simple en attendant : le membre retire ces valeurs du chip Setup et les ré-exclut depuis le graphique (route désormais correctement).

- [ ] **PDF export — handle non-CORS (Notion-hosted) screenshots in the jsPDF download** — the trade-card PDF export (`_pdfBuildDoc`/`_exportTradesPdf`, 2026-06-09) embeds screenshots via html2canvas, which can only rasterise images whose host allows cross-origin reads. Supabase-public-bucket screenshots embed fine; a screenshot still hosted directly on Notion (upload to Supabase not yet done) comes out blank in the downloaded PDF. The in-tab preview still shows them (plain `<img>`, no CORS needed). Options to fix: (a) route Notion image URLs through the Vercel proxy with permissive CORS before capture, (b) force-migrate any pending screenshots to Supabase before allowing export, (c) fetch→dataURL each image with a CORS-allowed fallback. Low urgency — most synced profiles have migrated screenshots; flagged so it isn't mistaken for a bug.

---

## TODO — Deferred

- [ ] **Prop Firm Optimizer — vrai pic interne via budget de trades/jours + règles firme avancées** (noté 2026-08-12, non retenu cette session) — Aujourd'hui le widget `w-prop-optimizer` n'a **pas de limite de temps** → avec un edge positif la proba de passer est monotone décroissante en risque (le max de proba brute pointe vers le risque minimum). C'est pourquoi le titre affiche le « risque le plus agressif encore safe » (plus haut risque ≥ seuil). Un **vrai optimum interne** apparaîtrait en ajoutant : (a) un **budget de trades ou une deadline en jours** (un risque trop bas échoue faute d'atteindre la cible dans le budget) — option B proposée à Max, non prise ; (b) **jours de trading minimum** ; (c) **règle de consistance** (aucun jour > X % du profit total). Les hooks sont là (`_pfRunPhase` tracke déjà `trades`, `_PF_MAX_TRADES` cap). À reprendre si Max veut modéliser des firmes à deadline/consistance. **avg loss configurable** (fixé à −1R aujourd'hui) et **saisie directe de l'avg win** (au lieu de dérivé de l'expectancy) = quick wins optionnels du même chantier.

- [ ] **Simultaneous Positions — 3 recommandations non retenues cette session (2026-07-04)** — proposées à Max, il a pris #2/#5/#6, laissé ces trois. **(#1 Exposition « live »)** — la section ne voit que les trades **clôturés** (`exitDate` requis via `_expoValidTrades`) ; elle ne peut pas répondre à « combien de positions ouvertes maintenant, pour quel risque ». Traiter les trades sans `exitDate` comme ouverts jusqu'à `today` débloquerait un KPI « Open right now » + barre « today » sur Timeline/Over Time. C'est le manque le plus gros pour le risk-management réel. **(#3 Risque agrégé % au pic)** — « max simultaneous » compte les positions à égalité ; 5 positions à 0.5% ≠ 5 à 2%. Si la donnée risk-per-trade existe (ou hypothèse fixe), afficher le **% de compte à risque en simultané** au pic — donne du sens concret à la teinte de corrélation déjà en place. **(#4 Persister période + mode par profil)** — `_expoPeriodMode` / `_expoOverMode` / `_expoTimelineMode` sont **session-only** → reset au reload ; les scoper par profil (comme les autres prefs) = quick win.

- [ ] **Concurrent Positions — vraie granularité intra-jour (chevauchement à l'heure/minute)** (noté 2026-07-04, suite de l'approximation heure-d'entrée livrée ce jour) — L'approx livrée décale **visuellement** le début des barres Gantt par `t.hour`, mais le **modèle de concurrence reste jour-level** (la sortie n'a aucune donnée d'heure). Pour un vrai calcul de chevauchement intra-jour il faut : (a) une colonne datetime de **sortie** côté Notion + son mapping ; (b) préserver l'heure d'entrée **et** de sortie à la normalisation — aujourd'hui `date`/`exitDate` sont sliced en `YYYY-MM-DD` ([dashboard.js:9621](dashboard.js:9621) + [9696](dashboard.js:9696)), l'heure jetée ; `t.hour` n'existe qu'en **entrée**, entier (0–23), sans minutes ; (c) refondre `_expoComputeRange` en résolution horaire. **Bloqué** tant que Max n'a pas de colonne heure-de-sortie exploitable. Non urgent. Bonus optionnel accueilli en même temps : teinter aussi la **peak-line** du widget Over Time par corrélation (aujourd'hui seule la caption du KPI 'Max simultaneous' l'est).

- [ ] **Clarification UX du widget Optimal RR (contrefactuel vs perf réelle)** (noté 2026-06-16, proposé à Max non retenu cette session) — Un membre a confondu le « Cumul +163R / Optimal 2.4R » du panneau ORR avec sa perf réelle (+391R) affichée en haut, croyant à une incohérence/bug. En réalité le panneau est un **contrefactuel** : « si tu fermais 100% à un seul TP fixe » — il plafonne les gagnants et, en mode Strict Loss, compte toute sortie sous le TP comme perte pleine → **forcément < perf réelle** quand le trader laisse courir. Pas un bug, mais trompeur visuellement (gros « +163.20R » sous le « +391.5R » sans étiquette « hypothèse »). Proposer un sous-titre/tooltip explicite (ex. « contrefactuel : TP fixe unique fermé à 100%, distinct de tes sorties réelles ») pour couper court à la confusion chez les 600 membres. Max a préféré ne traiter que l'affichage de la colonne source en Data Setup cette session.

- [ ] **Revoir le système « Locked » et tout ce qu'il recouvre** (à reprendre un jour, noté 2026-06-11) — Aujourd'hui le compteur « LOCKED » du hero Data Setup ([dashboard.js:34065](dashboard.js:34065) `_updateDataSetupHero`) ne reflète **qu'une seule chose** : le mode **Personalised TP** (`appState.ui.tpConfig.mode === 'personalised'`) avec un mapping TP incomplet (`_getPersonalisedMappingStatus().complete === false`) → `lockedCount = 1`, pastille rouge `is-bad`, sinon masquée. C'est piloté par `FEATURE_REQS['personalised-tp']`. **À revoir** : (1) la sémantique exacte de `FEATURE_REQS` et quelles features devraient « locker » (aujourd'hui quasi rien d'autre que personalised-TP ; Multi-TP est explicitement exclu — cf. pitfall #12) ; (2) l'UX : un membre voit « 1 LOCKED » sans comprendre quoi est verrouillé ni comment le débloquer (pas de drill-down, pas de lien vers le mapping manquant) — surfacer *quelle* feature et *quel* champ manque ; (3) cohérence avec le ring de couverture + le badge « X to fix » de l'attention pipeline ; (4) décider si « Locked » doit rester un simple compteur ou devenir une liste actionnable. **Déclencheur** : revu quand un membre a remonté un « 1 LOCKED » incompris (2026-06-11). Pas urgent — informatif, ne bloque aucune donnée. Faire un audit complet de `FEATURE_REQS` + tous les sites qui lisent/écrivent « locked » avant de retoucher.

- [ ] **Grouped filters — quick-add depuis les widgets (clic-droit, option D)** (proposé 2026-06-12, non retenu cette session) — point d'entrée alternatif pour créer un groupe directement depuis un graphique : clic-droit sur une barre (Setup, Pair, Session…) → « Exclure ce setup en groupe » / « Inclure ». Seed un groupe pré-rempli avec la dim+valeur cliquée, ouvre le drawer pour affiner. Plus gros chantier (hook context-menu sur tous les renderers de barres + donut + heatmaps). Le système de groupes (drawer builder, opérateurs, mini-stats) est déjà en place — il ne manque que ce raccourci de création. À reprendre si Max veut accélérer la création depuis l'analyse.

- [ ] **MT5 / MT4 / cTrader — actual implementation** — **SPEC WRITTEN 2026-07-04 : [docs/mt5-integration.md](docs/mt5-integration.md)**. Décision d'archi tranchée avec Max : **bridge côté serveur via MetaApi.cloud** (pas l'EA) car la majorité des membres sont **mobile-only** → un EA (qui tourne dans le terminal MT5 desktop) ne les sert pas ; seule une connexion serveur↔broker via **mot de passe investisseur lecture seule** (validé par Max) fonctionne device-independent. EA MQL5 reclassé bonus Phase 2 (power-users desktop). Modèle économique laissé ouvert (§5) : socle technique identique que ce soit BYO-token gratuit (chaque membre 1 compte MetaApi gratuit) ou managé/premium payant — à trancher à l'exposition (P5). Schéma : 2 tables (`mt5_connections` sans secret, `trades_synced` générique) + réutilise `trade_overlays` (couche discrétionnaire SMC). Limite produit clé : le R n'est auto que si le trader pose ses SL sur le broker (§7.3). **Prochaine étape = P1 spike sur le FTMO de Max** (nécessite accès Supabase auth + compte MetaApi — non faisable en session non-interactive). **Chantier non démarré — Max ne veut pas le lancer pour l'instant (2026-07-04).** cTrader = chemin différent (OAuth Open API natif, pas de coût bridge), accueilli par la table générique.

- [ ] **Backend OAuth: dynamic frontend redirect** — `notion-dashboard-api-2` Vercel function uses `DASHBOARD_URL` env var hardcoded to a single front URL (`https://lemarsuoff.github.io/Flipping-Market-Dashboard/`). Conséquence : impossible de tester l'OAuth Notion en local sans flipper la variable (et casser la prod). Fix : à `/api/notion/oauth/start`, lire l'`Origin`/`Referer` de la requête, valider contre une allowlist (`github.io/Flipping-Market-Dashboard/`, `localhost:*`), encoder dans le `state` OAuth, puis y rediriger au callback au lieu de toujours utiliser `DASHBOARD_URL`. Garder `DASHBOARD_URL` comme fallback. Nécessite accès au repo backend `notion-dashboard-api-2` (pas sur cette machine). Contexte complet dans [DECISIONS.md](DECISIONS.md) § 2026-05-19 — Vercel `DASHBOARD_URL` is single-tenant prod.

- [ ] **Drawdown Intelligence section** — DD by context (setup/session/pair decomposing max DD), Recovery Factor (Total R / |Max DD|), DD Duration distribution, Risk of Ruin estimate. Optional: Underwater curve, Streak/DD scatter, MAR/Calmar (~14 months of data, low priority). Deferred pending community priority signal.

- [ ] **Hardcoded font-sizes (~180 remaining)** — deferred pending community feedback on typography editor coverage.

- [ ] **BYOT + export/import config** — open the dashboard to all users without requiring their Notion credentials. Recommended step before full OAuth rollout. ~1.5 days dev vs weeks for OAuth. Deferred until post-Notion overhaul stabilization.


- [ ] **Orphaned localStorage keys after profile deletion (audit M2)** — `_deleteJournalProfileById` does not purge scoped keys (`apiTradesCache_*`, `outcomeValueMapping_*`, etc.).

- [ ] **Share view — bottom summary strip** — `WIN DAYS / LOSS DAYS / NO TRADES / BEST DAY / WORST DAY / AVG PER TRADE`. Visible in reference designs, not yet implemented.

- [ ] **Yellow border highlighting on clicked cells** — P&L Calendar, Pair × Session, Monthly P&L in Hover mode.

- [ ] **Security migration — repo privé + hosting Cloudflare Pages + minification bundle (Niveau 1+2)** — surfaced 2026-05-20. Aujourd'hui le repo est public sur GitHub → un voleur fait `git clone + deploy` en 5 min. Plan en 2 étapes faisables en ~3 h en une session : (a) passer le repo en privé + migrer le hosting GH Pages → Cloudflare Pages (free tier, accepte repos privés via GitHub Actions auto-deploy) ; (b) ajouter un step `npx terser dashboard.js -o dashboard.min.js -c -m` dans le CI, `index.html` pointe sur `.min.js` en prod, source reste lisible en local. Workflow dev inchangé (édition locale + refresh). Bénéfice : passe de "clone trivial en 5 min" à "reverse-engineer pénible d'un bundle minifié". **Timing libre — pas urgent aujourd'hui** (audience = 600 community members distribuée par Max, pas viral, pas monétisé, pas indexée). À faire **avant** un des 4 triggers : (1) lancement public visible (Twitter / YouTube / Product Hunt), (2) monétisation, (3) lancement MT5 (élargit l'audience), (4) repost externe non contrôlé. Niveaux 3 (auth gate via Supabase Auth) et 4 (secret sauce côté serveur via Edge Functions) restent réservés au pivot SaaS payant — sur-engineering tant que freemium community.

- [ ] **Add LICENSE file (MIT recommended)** — currently the public repo has no license, so legally all rights reserved by default. Not urgent since users don't access GitHub directly, but worth adding for legal clarity.

- [ ] **Add README.md** — public-facing landing page on GitHub. Low priority since users access the dashboard via GitHub Pages link, not the repo itself. Would matter if/when the repo becomes a public-facing project (contributions, forks, etc.).

- [ ] **Auto-rename session on `/end` — feature request to revisit if Anthropic ships the hook** — investigated 2026-05-21 via the claude-code-guide agent: programmatic auto-rename of the current Claude Code session is **not possible today**. (1) No CLI command like `claude session rename <id> "<title>"` exists. (2) Stop hooks have no access to the agent's final message text — they only receive tool execution events. (3) Claude Code does not parse OSC codes or any agent-emitted marker that would trigger a rename. The current pattern (agent proposes `Tu peux remplacer le titre par : …` line, user copy-pastes into `/rename`) is the only reliable path. Revisit if Anthropic ever exposes (a) a CLI/IPC endpoint for session-rename, (b) enriches the `Stop` hook payload with the agent's final turn, or (c) introduces a marker convention agents can emit. Until then, status quo.

---

## Done — Last 10

- [x] **Query Widgets (Data Setup) — clic-droit Include/Exclude manquant sur bars + heatmap** (2026-08-18) — Max remonte : les widgets créés via le Query Widget (Data Setup → Create Widget) s'affichent bien (bar chart, heatmap, table), mais le **clic-droit sur la donnée n'ouvre pas le menu Include/Exclude** comme sur les widgets natifs. **Root cause** : le menu contextuel est câblé différemment par renderer et 2 des 3 ne le branchaient pas. **(bars)** le handler `contextmenu` global ([dashboard.js:36323](dashboard.js:36323)) ne connaît que les containers natifs (`_BAR_CONTAINER_TO_CHIP`) ; les bars custom vivent dans `bars-w-cust-*` → non mappé → `return` immédiat. **(heatmap)** `renderCustomHeatmap` ([dashboard.js:18254](dashboard.js:18254)) ne passait pas `onCellContextMenu` à `_renderSharedHeatmapGrid` → aucun listener posé. **(table)** déjà câblé ([dashboard.js:18776](dashboard.js:18776)), OK. **Fix** : (1) bars — dans le handler global, si le container commence par `bars-w-cust-`, résoudre le def via `_getCustomBarChartDef(_getBarChartIdFromContainerId(id))` puis le chip via `_cwEnsureFilterableChipKey` (même chemin lazy que la Table : chip natif, ou custom prop auto-déclarée en Additional Filter au 1er clic-droit) ; (2) heatmap — ajout `onCellContextMenu` qui construit **2 dims indépendantes** (axe ligne + colonne), chacune résolue via `_cwEnsureFilterableChipKey` (field2 via def-like `{field:field2, fieldLabel:field2Label, inferredType:''}`), puis `_showHeatmapContextMenu(dims, title, e)` → chaque axe filtre séparément (pas un combo-filter, absent des custom heatmaps). Réutilise les handlers natifs éprouvés. **Vérifié en preview** (démo 100 trades, 3 widgets créés via les vraies fns puis clic-droit réel dispatché) : bars → menu « EURAUD ＋Include −Exclude », clic Include → `chips.pair` mode=include +EURAUD ; heatmap → menu 2 axes « Pair: AUDUSD » + « Session: Asian », Exclude Session → `chips.session` exclut Asian, pair reste neutre ; table → « Setup: Re-confirmation Y » OK. Widgets test supprimés, filtres remis neutres, reload sans erreur console. `node -c` OK, brace delta 0, cache JS `?v=1420`. **Confirmé validé live par Max.**

- [x] **Prop Firm Risk Optimizer — nouveau widget `w-prop-optimizer` (Monte Carlo à barrières sur la vraie distribution R)** (2026-08-12) — Demande de Max : un widget qui détermine le **risque idéal pour passer un prop firm** (fonction du win rate, des séries de pertes, du max DD firme, des cibles P1/P2). **Moteur** : Monte Carlo à barrières — pour chaque niveau de risque (0.1→3 % par pas de 0.1), 2 000 challenges bootstrappés sur la **distribution R réelle** du preset actif (pas un win-rate+RR paramétrique comme le MC existant `w-montecarlo`) ; course à deux barrières cible-profit (pass) vs max/daily drawdown (blow). Réutilise l'infra MC (moteur async MessageChannel chunké, compounding/fixe) mais boucle de sim neuve. Placé sous le MC en section `optimal-rr` (auto-visible pour les 600 membres via le merge `{...OPTIMAL_RR_LAYOUT}`). **Paramètres firme** : preset FTMO-style / 1-step, account size, cible P1+P2, max DD (static/trailing), daily DD, toggles daily/2-phases/compounding. **Objectif « risque le plus agressif encore safe »** (tranché avec Max) : le titre = le plus haut risque dont la proba ≥ seuil réglable (défaut 90 %), pas le max de proba brute (qui dégénère vers le risque minimum quand l'edge est positif + pas de limite de temps). Curseur de seuil qui re-choisit l'optimal **en direct** sans re-simuler. **Edge auto + éditable (pattern Monte Carlo)** : bloc unique « Your edge » (win rate / expectancy / trades-month) **auto-rempli depuis les vrais trades**, éditable par-dessus → sim sur la distribution réelle tant qu'intact, bascule sur modèle binaire paramétrique dès qu'un champ est modifié (statut explicite + lien « Reset to my data ») ; fallback saisie manuelle pour membre sans journal (détection auto, zéro bouton de mode). Sortie : courbe pass/blow-up + ligne-repère seuil, cartes (risque optimal, proba, blow-up, trades moyens ≈ mois, max DD moyen). **Vérifié en preview** (démo 100 trades → optimal 0.80 % / 91 % pass / 64 trades ≈ 9 mois ; override custom 55 %/0.5R → 2.10 % ; seuil 70/90/99 → 1.6/0.9/0.4 % live ; auto-fill/override/reset/no-journal ; 2 bugs corrigés : note non affichée (sélecteur trop restreint) + lien Reset visible à tort (`all: unset` écrasait `hidden`)). `node -c` OK, brace delta 0, caches JS `?v=1418` / CSS `?v=397`. **À valider live par Max** sur ses vraies données Notion. Voir [DECISIONS.md](DECISIONS.md) 2026-08-12.

- [x] **Data Setup — le mapping custom de « RR TP 1 » (`r`) était ignoré → 2.4 hardcodé pour tous les trades TP** (2026-07-21) — Max remonte : sur un journal lambda (rien à voir avec la stratégie de base), il mappe « RR TP 1 » (dim `r`, Required) sur sa colonne « RR respect » (TP variables 0.8/3/2…), mais le dashboard affiche **2.4 pour tous** ; seul « RR max » (mappé « RR max atteint ») s'adaptait. **Root cause** : dans `_normalizeAPITrade`, `r` était le **seul** champ mappable à ne pas passer par `_pick('<dim>', …)` — il lisait en dur `t.rrTp1 ?? t['RR TP 1']` ([dashboard.js:9728](dashboard.js:9728)), ignorant l'override. La DB custom n'ayant pas de colonne littérale `RR TP 1`, `_mapR` tombait sur le fallback système `_SYSTEM_TP_R = 2.4` (TP) / `-1.0` (SL). `rrMax` fonctionnait car il a son bridge d'alias `_copyMappedAliasPatch` ([dashboard.js:8621](dashboard.js:8621)) → seule donnée R réelle qui circulait, d'où l'impression « s'adapte uniquement via RR max ». **Fix** (1 ligne) : `r: _mapR(oc, _pick('r', t.rrTp1 ?? t['RR TP 1']))` — consulte l'override `r` d'abord, retombe sur les candidats standards sinon → zéro régression templates Flipping, journaux custom OK. **Vérifié en preview** (repro isolée du chemin `_pick('r',…)`+`_mapR`) : sans override TP→2.4 (bug reproduit) ; avec `r`→« RR respect » chaque trade lit sa vraie valeur (0.8 / 3.0 / −1.0). `node -c` OK, cache JS `?v=1379`. **Confirmé validé live par Max.**

- [x] **Trade cards — bouton « Card fields » (volet widget) : choisir les propriétés affichées, dynamique depuis les colonnes Notion du profil** (2026-07-15) — Max voulait un bouton à côté du Share/PDF du volet pour afficher/masquer les propriétés des trade cards, **sauvegardé par profil** et **identique pour tous les presets**. **3 itérations** : (1) toggle des 8 champs déjà dessinés ; (2) ajout d'un sous-ensemble hardcodé de `JOURNAL_DIMS` (BE Management, Position Type, Exit date, Day, Bad feeling) — Max : « ça a l'air hardcodé, il m'en manque plein, chacun a des noms différents » ; (3) **refonte dynamique** : panneau = **8 champs cœur stylés** (direction/date/session+hour/setup/rrMax/tphits/h4/m15, ON par défaut) **+ section dynamique bâtie sur `t.extras`** (que `_bakeAllDetectedPropsToExtras` remplit avec **toutes** les propriétés Notion détectées, nommées comme dans la DB du membre), clés `x:<col>`, OFF par défaut, rendues en lignes `Label : valeur`. **Dédup** via `_wdConsumedCoreColumns()` (overrides de mapping) → une colonne déjà portée par un champ cœur/anchor/screenshot est retirée de la liste **et** du rendu (jamais de doublon). Clés système Notion + screenshots filtrés. Labels = nom custom-prop convivial sinon `_humanizeWidgetPropertyLabel`. Stockage `getProfileScopedKey('tradeCardFields_v1')` (map creuse `{key:bool}`, vide = rendu par défaut), synchro cross-device via préfixe `tradeCardFields_v1_`, cache invalidé au switch de profil. Paire/R/Outcome = ancres toujours visibles. **Gotcha corrigé** : checkbox dans son `<label>` toggle non fiable vs le handler délégué (action native après le listener, double-fire possible) → handler config-authoritative qui **régénère le popover depuis la config** à chaque changement (déterministe). **Vérifié en preview** (démo + colonnes injectées façon bake Notion) : panneau 8 cœur + séparateur + colonnes dynamiques triées noms exacts ; `notionId` exclu ; activation → `Emotion : Calm` / `Conviction : 1/5` / `BE Management : No BE` ; dédup override → colonne retirée liste+carte ; cohérence case↔config↔carte ; persistance après reload ; 0 erreur console. `node -c` OK, caches JS `?v=1378` / CSS `?v=389`. **Confirmé validé live par Max.** Voir [DECISIONS.md](DECISIONS.md) 2026-07-15.

- [x] **Grouped filters — add-condition sur le 2ᵉ groupe atterrissait sur le 1ᵉ (collision d'id après hydratation preset)** (2026-07-09) — Max remonte : un groupe A existant fonctionne, mais dès qu'on crée un groupe B, « Add condition » sur B ajoute la condition sur A. **Root cause** : collision d'id. Le compteur `_gfNextId` n'est re-synchronisé qu'**une seule fois au boot** ([dashboard.js:48787](dashboard.js:48787)) via `_gfBumpIdCounterFromGroups`, mais `applyPreset` ré-hydrate les groupes sauvegardés d'un preset ([dashboard.js:13909](dashboard.js:13909)) **sans** toucher au compteur (idem les autres sites d'hydratation 23352/42854…). Après un switch vers un preset qui contient déjà `g_1`, le compteur reste à `1` → le groupe B suivant reçoit lui aussi `g_1`. Comme `_gfAddCondition` résout le groupe via `_gfFindGroup(id)` (= premier match), la condition atterrit sur A. **Fix** : re-sync du compteur (`_gfBumpIdCounterFromGroups(groups)`) juste avant de générer l'id dans les deux fonctions de création — `_gfAddGroup` ([dashboard.js:14508](dashboard.js:14508)) et `_gfDuplicateGroup` ([dashboard.js:14547](dashboard.js:14547)) → le nouvel id est toujours strictement supérieur à tous les ids vivants, quelle que soit la voie d'hydratation (preset switch, snapshot restore, import). **Vérifié en preview** (localhost, démo) : scénario reproduit (g_1 hydraté + compteur stale → création B) → ids distincts `g_1`/`g_2`, add-condition « setup » atterrit bien sur B, A garde sa condition ; mécanisme de collision démontré (`.find` renvoie le premier des deux `g_1`) ; 0 erreur console. `node -c` OK, brace delta 0, cache JS `?v=1354`. **Confirmé validé live par Max.**

- [x] **Grouped filters — le bouton Clear restaure un groupe sauvegardé supprimé (parité avec les standard filters)** (2026-07-08) — Max remonte : sur un preset avec un groupe de filtres sauvegardé, si on supprime le groupe **sans** faire Update preset, il disparaît (normal) ; mais **Clear** est censé le remettre comme avant (comme les chips standards) — or le groupe supprimé restait supprimé. **Root cause** dans `_filtersClearAll` ([dashboard.js:22985](dashboard.js:22985)) : le bloc grouped-filters ne parcourait que les groupes **présents en live** et se contentait de réappliquer leur `enabled` depuis le snapshot → un groupe sauvegardé **supprimé** n'étant plus dans le tableau live, la boucle ne le voyait jamais → jamais ré-ajouté. Les chips n'ont pas ce bug car leur Clear passe par `_hydrateChipsFromSnapshot` qui **reconstruit** depuis le snapshot. **Fix** : Clear reconstruit désormais les groupes depuis le snapshot du preset — clones frais via `_sanitizeGroupedFilters(snapGroups)` (groupes sauvegardés **ré-ajoutés même si supprimés**, remis à leurs conditions/enabled sauvegardés), puis les groupes **live-only** (absents du snapshot) sont **conservés mais désactivés** (`enabled=false`) pour arrêter de filtrer sans perdre le travail (comportement inchangé). Persistance dans le live slot via `_gfPersistAndRender`. **Vérifié en preview** (démo localhost) : groupe sauvegardé supprimé → revient (enabled + conditions intactes) ; groupe live-only → gardé+désactivé ; live slot = `g_saved1:true`/`g_live_only:false` ; état de test nettoyé. `node -c` OK, cache JS `?v=1352`. **À valider live par Max** sur ses vrais presets.

- [x] **Screenshot lightbox — outils Position Long / Short type TradingView (Entry/SL/TP + ratio R)** (2026-07-05) — Nouvelle famille de formes `pos` dans le calque d'annotation (`_lbAnn*`), demandée par Max pour visualiser un setup directement sur ses screenshots. **Deux outils** dans la toolbar de dessin (à droite du pinceau) : **Position Long** (flèche haut) et **Position Short** (flèche bas). **Un clic** dépose la position au point d'entrée avec une boîte 1:2 R par défaut ; **3 niveaux réglables** Entry / SL / TP ; **zones colorées** vert=reward / rouge=risk (littéraux sémantiques `_LB_POS_GREEN/RED/ENTRY`, theme-independent comme le set fib) ; **ratio R affiché en direct**, recalculé à chaque drag (`reward/risk` en distance verticale). Coords normalisées 0..1 → survivent resize/mobile/share ; persistance + sync cross-device **gratuites** via le même `_lbAnnPersist` (blob-sync scopé profil×HTF, zéro schéma Supabase). **Itérations d'affinage same session** : (1) largeur défaut −60% (`halfW` 0.10→0.04) ; (2) **mire qui suit pendant le clic maintenu** — listener overlay `mousemove`→`pointermove` car `_lbAnnPointerDown` fait `preventDefault()` qui coupe les events souris de compat (la mire gelait mid-drag) ; (3) **poignées** — fusion du point central d'entrée avec le point gauche en une poignée-coin `(x1,ey)` (règle hauteur + bord gauche), suppression du texte « Entry » → 4 poignées ; (4) hauteur d'entrée réglable **uniquement** via le point gauche (retrait du hit-stroke `pe` sur la ligne d'entrée → clic sur la ligne = move, plus de fausse manip) ; (5) **label R ancré hors de la ligne SL** (sous SL pour un long, au-dessus pour un short) pour ne jamais mordre dans le price action ; (6) **formes des poignées** — point gauche fusionné = rond, SL/TP/bord-droit = carrés (`<rect class="lb-ann-h">`, style partagé). **Vérifié en preview headless** (math R symétrique/asymétrique 2.0/3.5/5.8, dir long/short zones inversées, persistance LS, drag combiné x1+ey, 4 poignées rond+3 carrés, R au bon y long/short, aucune régression trend/autres shapes toujours ronds, 0 erreur console) + rendus visuels Long/Short. **Suivi UX même journée (3 fixes lightbox)** : (7) **boîte à outils persistante au clic** (`4fd0491`) — `openImgLightbox` remettait `_lbDrawMode=false` à chaque appel, donc cliquer un autre screenshot désactivait le mode dessin (le clavier passait par `_lbShow`, préservé) ; gaté sur un flag `freshOpen = !overlay` → reset uniquement à la première ouverture, sticky sinon (draft pending toujours purgé au changement d'écran) ; (8) **mire conservée en mode dessin** — `_lbAnnUpdateCursor` forçait le curseur `default` (flèche) sur le SVG en mode select, masquant la mire ; ajout garde `if (_lbCrosshair) cursor='crosshair'` + appel dans `_lbToggleCrosshair` pour maj immédiate ; (9) **suppression du thème clair de la mire** (indiscernable du sombre) — bouton colour-flip retiré, `_lbSetCrosshairTheme`/`_lbToggleCrosshairTheme` supprimés, `_lbCrosshairTheme` figé à `'dark'` (const), règles CSS `.ch-theme-light` + `.img-lightbox-ch-theme-btn` retirées ; toolbar mire = 1 seul bouton. Caches `dashboard.js ?v=1351` / `dashboard.css ?v=388`. **À valider live par Max** sur ses vrais screenshots (data démo sans images). Voir [DECISIONS.md](DECISIONS.md) 2026-07-05.

- [x] **Trade cards — l'en-tête de mois épinglé ne rogne plus les cartes (share + volet dashboard) + label paire/date rendu visible dans la lightbox** (2026-07-05) — Trois demandes de Max sur les trade cards. **(1 & 2 — anti-rognage share + volet dashboard)** : le header de section `.wd-day-sep` est `position: sticky` par design → les cartes défilent dessous, donc le haut d'une carte est rogné en cours de scroll. Max voulait **garder le header épinglé mais sans rognage**. Solution CSS scroll-snap : `.wd-drawer-body` (share) / `.wd-drawer-body.has-day-sep` (dashboard) → `scroll-snap-type: y proximity` + `scroll-padding-top: 36px` (≈ hauteur du header sticky) ; `.wd-trade-card` → `scroll-snap-align: start`. Au repos du scroll la carte se cale 4px **sous** le header au lieu de dessous → header toujours épinglé, plus de rognage ; `proximity` (pas `mandatory`) garde le défilement libre naturel. Côté dashboard, gaté à `has-day-sep` (ajouté sur le body **uniquement** quand le volet est groupé, [dashboard.js:26756](dashboard.js:26756)) → les volets à plat / cohortes (`wd-drawer-body-recovery`) n'ont aucun inset parasite. Mobile share géré (le document scrolle → snap+offset portés sur `html`). **(3 — label paire/date invisible dans la lightbox screenshots)** : `.img-lightbox-label` utilisait `color: var(--white)` → **résout en texte foncé dans le thème clair** → la ligne « PAIRE · DATE » était rendue mais invisible sur le fond sombre de la barre (seul le compteur « n/total » en blanc littéral restait visible → Max ne voyait que « 3/9 »). Passé en `rgba(255,255,255,.95)` littéral théme-indépendant, comme le compteur/step et comme le share (`#fff`). **Vérifié en preview** : share — carte snappée 4px sous le header épinglé (`cardClearsHeader: true`, `gap: 4px`) ; dashboard — snap+36px actifs sur `grouped`, `none`/`auto` sur `flat`+`cohort` ; lightbox — label passe de `rgb(16,12,6)` (noir) → `rgba(255,255,255,.95)` (blanc), affiche « USDJPY · 2026-06-12 ». Caches dashboard.css `?v=386` / dashboard.js `?v=1326` (auto) + share.css `?v=19` (manuel). **Poussé** (`e6e8c12`) — rendu GitHub Pages du lien share à re-valider live par Max.

- [x] **Simultaneous Positions — win-rate sorti en widget autonome + groupe typo dédié dans le theme editor** (2026-07-05) — Deux demandes de Max sur l'ex-section « win-rate collé aux KPIs ». **(1) Widget autonome `w-expo-winrate`** : le bloc « Win rate by simultaneous positions » (barres WR bucketées par concurrence-à-l'entrée, cliquables → drawer cohorte) était rendu **dans** le body des KPIs (`renderExpoKpis`) ; extrait dans sa propre tuile GridStack — nouvelle carte `data-gs-id="w-expo-winrate"` avec ses propres contrôles de période (suffixe `w`), nouvelle fonction `renderExpoWinrate`, câblée dans `_expoRerenderAll` + le render principal, layout par défaut (pleine largeur sous les KPIs, dist/duration/timeline/overtime décalés), icône mini-grid `bars`. **Zéro casse pour les 600 membres** : le merge `{ ...CONCURRENT_POSITIONS_LAYOUT, ...saved }` ([dashboard.js:46290](dashboard.js:46290)) place auto le widget chez les layouts existants. **Bug corrigé au passage** : le handler des selects année/mois ([dashboard.js:4778](dashboard.js:4778)) filtrait sur `[kduto]` → excluait le suffixe `w` du nouveau widget (dropdowns morts) ; élargi en `[kwduto]`. **(2) Groupe typo dédié « Simultaneous Positions »** (réponse à « bouger Stats du calendrier bouge aussi mes KPIs ») : la section partageait les tokens `--fs-cal-*` du P&L Calendar ; nouveau set `--fs-expo-*` (5 tokens, défauts identiques), 20 sélecteurs `.expo-*` basculés (+ override `.cc-sub` scopé section pour ne pas fuiter sur Setup/Pair), nouveau groupe G16b (5 sliders) dans `TYPO_GROUPS`, enregistré dans `WIDGET_SCOPED_TYPO_VARS` (auto-scale) + `THEME_DEFAULT` (baseline reset). **Migration one-shot idempotente** `_migrateCalTypoToExpo` (boot + import thème) : copie `--fs-cal-* → --fs-expo-*` si absent → un membre ayant customisé la typo calendrier garde la taille de sa section. **Vérifié live** (preview, données démo + exit dates injectées) : widget rend 4 tranches + drill-down OK, plus dans les KPIs ; groupe rendu avec 5 sliders, P&L Calendar toujours là ; **découplage confirmé** (bouger `--fs-expo-stats`→16px suit le widget, bouger `--fs-cal-stats`→20px ne touche plus la section) ; migration backfill+preserve OK ; 0 erreur console. `node -c` OK, brace delta 0, caches JS `?v=1325` / CSS `?v=382`. Couleurs déjà 100% couvertes (tokens globaux) — chantier typo-only. **À valider live par Max.**

- [x] **Simultaneous Positions — refonte section : rename + 6 tranches + période 3 modes + range sync bidirectionnel + drill-downs + win-rate/corrélation/label** (2026-07-04) — Grosse session en 2 vagues sur l'ex-« Concurrent Positions ». **Vague 1 (4 demandes)** : (1) **Rename Concurrent → Simultaneous** UI-only — onglet, 3 titres widgets, `_SECTION_LABEL_MAP` (`Simultaneous`), empty-state, commentaires ; ids internes `concurrent-positions`/`w-expo-*`/`_expo*` **conservés** (renommer casserait les clés LS de layout de section). (2) Donut **Share of Open Positions** → `1 · 2–3 · 4–5 · 6–8 · 9–10 · 11+` ; **Position Duration** → `1 · 2–3 · 4–7 · 8–10 · 11–14 · 15+` ; rampe couleur généralisée N-stops (`_expoRampColors(n)` + `_expoLerpHex`, source unique labels/bornes `_EXPO_CONC_*`/`_EXPO_DUR_*`). (3) **Range sync Timeline↔Over Time** : fenêtre visible du Gantt (modes wide) mirrorée en bande sur Over Time (plugin `expoViewport`) ; source de vérité unique = `scrollLeft` de la Timeline. (4) Période **3 modes `Month · Year · Overall`** (`_expoPeriodMode`, flag `wide`=year|overall) : Year borné aux données réelles de l'année, flèches steppent l'année, month-select masqué ; handoff Year→Month conserve l'année. **Vague 2 (recommandations 2/5/6 + drag bars)** : **drag de la bande scrolle la Timeline en line ET bars** (seuil 4px distingue clic-sur-barre vs drag, clic parasite avalé via `_expoSuppressNextOverClick`) ; clic barre **Duration** → drawer trades de la tranche ; clic tranche/légende **donut** → drawer **« occurrences »** (runs contigus, façon Streak/DD-recovery, `wd-occ` collapsibles) ; **Win rate par concurrence** dans les KPIs (`_expoWinRateByConcurrency` — trades bucketés par concurrence-à-l'entrée, largeur barre=WR, couleur=signe expectancy, lignes cliquables → drawer entrants) ; **corrélation au-delà de la devise** (`_EXPO_FACTORS` indices/crypto/energy → symboles sans leg commun corrèlent, fix latent `NAS100` mal splitté) ; **label de plage de dates** sur la bande. **Vérifié en preview** (dataset synthétique multi-années + démo réel avec exit dates injectées) : ordre boutons, 6 tranches (donut 11+, duration 15+), Year+handoff, bande+drag line/bars (clic vs drag), 3 drawers, win-rate différencié (1:37% → 4-5:25%/−0.2R rouge), corrélation « 12/13 indices ». `node -c` OK, brace delta 0, 0 erreur console, cache JS/CSS auto-bumpés. **Décision archi** : win-rate intégré aux KPIs plutôt qu'un 6e widget (évite la plomberie layout de section). Voir [DECISIONS.md](DECISIONS.md) 2026-07-04. **À valider live par Max.**

---

## Update rules for agents

At the end of every coding session:
- Move completed items → Done (keep last 10 max, oldest fall off)
- Add any new TODO discovered during the session (with context)
- Update "In Progress" if work is mid-flight
- Commit `ROADMAP.md` in the same commit as the related code change
