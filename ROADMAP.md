# ROADMAP.md — Flipping Market Dashboard

> Single source of truth for task tracking. Updated by agents at the end of every session.
> Supersedes `docs/current-roadmap.md` (kept for historical reference only).

---

## In Progress

_(nothing — Refacto Total shipped 2026-05-24 in 2 commits, see Done below)_

---

## TODO — Active

- [ ] **Migrate remaining ~26 inline `connectionType === 'notion'` checks to `INTEGRATIONS` registry** — Strangler Fig follow-up to the 2026-05-19 registry refactor. One micro-fix done at [dashboard.js:5336](dashboard.js:5336) (`_reloadJournalProfileSelection` `isNotionWithDb` now uses `getIntegration(p).isReady(p)`). Rest deferred until MT5 ships — abstracting from a single concrete implementation risks getting the methods wrong.

---

## TODO — Deferred

- [ ] **Supabase Realtime push for "2 min after" without refresh (Session 4, optional)** — surfaced 2026-05-23. Today after PC A pushes a write to Supabase, PC B sees it only on next sign-in / hard refresh. For a truly magical "PC A → 30 s → PC B updates without F5", subscribe PC B to Supabase Realtime channels on `user_data` (for metadata) + a small `trades_meta` table (timestamp watcher for the trades blob, since Storage has no native realtime). On push event: re-download the affected key/blob, invalidate local caches, re-render. Cost: 1 WebSocket connection per open tab, Supabase free tier handles plenty. Implementation effort: ~2-3h. Defer until #3.10 ships + Max confirms he wants the no-refresh experience (current "refresh = instant sync" is already a huge UX upgrade vs pre-2026-05-23).

- [ ] **`gs_layout_active` + `gs_layout_<preset>` twin-write** — surfaced 2026-05-20 during Bloc 4. `_saveActiveSlotLive` ([dashboard.js:35937](dashboard.js)) writes the same ~13.5KB payload to two LS keys on every drag/resize (autosave slot + per-preset slot). Semantically different (autosave vs preset-bound state) so global dedup wrap can't help. 27 KB/drag is inherent cost. Reducing requires either: (a) lazy-write per-preset slot only on preset switch (risk of crash-loss), (b) ref `dynamicWidgets` separately to dedupe between slots (major refactor of preset/layout architecture). Touch when preset architecture is next visited.

- [ ] **`_saveActiveSlotLive` over-fires on drags without layout change** — surfaced 2026-05-20 during Bloc 4. ~50% of `gs_layout_*` writes are dedup'd by the LS wrap → meaning the GridStack `resizestop dragstop` handler fires from drag operations that didn't actually move/resize anything (snap-back, micro-clicks). The dedup wrap absorbs the IO cost but JSON.stringify of 13.5 KB still runs. Investigate whether GridStack emits these events spuriously or whether a `_syncGridToLive` no-op check would short-circuit cleanly. Low priority since dedup absorbs the storage cost.

- [ ] **Retention policy design — to spec before any server-side trade storage ships (e.g. MT5)** — today Supabase only holds metadata (~100KB/user) + media; trades live in localStorage so an abandoned user costs nothing. The moment MT5 EA pushes trades to a server endpoint, abandoned accounts start accumulating real volume (~6–30 MB/user). Direction agreed during the 2026-05-19 session: (a) disconnect integration = soft (token revoked, metadata/trades retained), (b) profile manually deleted = soft → hard at 30 days, (c) user inactive 6 months = warning email, archive at 7 months, hard-delete at 12 months, (d) "Delete my account" = soft with 30-day grace period. Implementation: Supabase `pg_cron` or scheduled edge function, runs weekly. Add `last_active_at` column to user/profile rows when the schema is touched next.

- [ ] **MT5 / MT4 / cTrader — actual implementation** — registry stubs (`available: false`) exist since 2026-05-19. Next steps before code: (1) decide MetaApi.cloud SaaS bridge vs. self-built MQL5 EA (cost vs. control); (2) extend Vercel proxy or move to Supabase edge functions for per-broker secret handling (current static-page architecture can't hold per-user API keys safely); (3) spike one platform end-to-end on a single test account before promising to community. cTrader is the easiest technically (OAuth Open API) but smaller audience; MT5 covers most of Max's prop-firm users.

- [ ] **Backend OAuth: dynamic frontend redirect** — `notion-dashboard-api-2` Vercel function uses `DASHBOARD_URL` env var hardcoded to a single front URL (`https://lemarsuoff.github.io/Flipping-Market-Dashboard/`). Conséquence : impossible de tester l'OAuth Notion en local sans flipper la variable (et casser la prod). Fix : à `/api/notion/oauth/start`, lire l'`Origin`/`Referer` de la requête, valider contre une allowlist (`github.io/Flipping-Market-Dashboard/`, `localhost:*`), encoder dans le `state` OAuth, puis y rediriger au callback au lieu de toujours utiliser `DASHBOARD_URL`. Garder `DASHBOARD_URL` comme fallback. Nécessite accès au repo backend `notion-dashboard-api-2` (pas sur cette machine). Contexte complet dans [DECISIONS.md](DECISIONS.md) § 2026-05-19 — Vercel `DASHBOARD_URL` is single-tenant prod.

- [ ] **[Post-readonly] Trading Journal evolution** — manual trade entry, custom schemas, Notion two-way sync. Spec complète dans `docs/journal-evolution.md`. **Ne pas démarrer avant que le read-only soit clos.** Bloquants identifiés : XSS fix ✅ (2026-05-19), Ghost trades ✅ (2026-05-19), Mappings cross-device sync (audit M1) — restent listés dans le doc §9.

- [ ] **Drawdown Intelligence section** — DD by context (setup/session/pair decomposing max DD), Recovery Factor (Total R / |Max DD|), DD Duration distribution, Risk of Ruin estimate. Optional: Underwater curve, Streak/DD scatter, MAR/Calmar (~14 months of data, low priority). Deferred pending community priority signal.

- [ ] **Hardcoded font-sizes (~180 remaining)** — deferred pending community feedback on typography editor coverage.

- [ ] **BYOT + export/import config** — open the dashboard to all users without requiring their Notion credentials. Recommended step before full OAuth rollout. ~1.5 days dev vs weeks for OAuth. Deferred until post-Notion overhaul stabilization.


- [ ] **Orphaned localStorage keys after profile deletion (audit M2)** — `_deleteJournalProfileById` does not purge scoped keys (`apiTradesCache_*`, `outcomeValueMapping_*`, etc.).

- [ ] **Share view — bottom summary strip** — `WIN DAYS / LOSS DAYS / NO TRADES / BEST DAY / WORST DAY / AVG PER TRADE`. Visible in reference designs, not yet implemented.

- [ ] **Yellow border highlighting on clicked cells** — P&L Calendar, Pair × Session, Monthly P&L in Hover mode.

- [ ] **Security migration — repo privé + hosting Cloudflare Pages + minification bundle (Niveau 1+2)** — surfaced 2026-05-20. Aujourd'hui le repo est public sur GitHub → un voleur fait `git clone + deploy` en 5 min. Plan en 2 étapes faisables en ~3 h en une session : (a) passer le repo en privé + migrer le hosting GH Pages → Cloudflare Pages (free tier, accepte repos privés via GitHub Actions auto-deploy) ; (b) ajouter un step `npx terser dashboard.js -o dashboard.min.js -c -m` dans le CI, `index.html` pointe sur `.min.js` en prod, source reste lisible en local. Workflow dev inchangé (édition locale + refresh). Bénéfice : passe de "clone trivial en 5 min" à "reverse-engineer pénible d'un bundle minifié". **Timing libre — pas urgent aujourd'hui** (audience = 600 community members distribuée par Max, pas viral, pas monétisé, pas indexée). À faire **avant** un des 4 triggers : (1) lancement public visible (Twitter / YouTube / Product Hunt), (2) monétisation, (3) lancement MT5 (élargit l'audience), (4) repost externe non contrôlé. Niveaux 3 (auth gate via Supabase Auth) et 4 (secret sauce côté serveur via Edge Functions) restent réservés au pivot SaaS payant — sur-engineering tant que freemium community.

- [ ] **Add LICENSE file (MIT recommended)** — currently the public repo has no license, so legally all rights reserved by default. Not urgent since users don't access GitHub directly, but worth adding for legal clarity.

- [ ] **Add README.md** — public-facing landing page on GitHub. Low priority since users access the dashboard via GitHub Pages link, not the repo itself. Would matter if/when the repo becomes a public-facing project (contributions, forks, etc.).

- [ ] **Auto-rename session on `/end` — feature request to revisit if Anthropic ships the hook** — investigated 2026-05-21 via the claude-code-guide agent: programmatic auto-rename of the current Claude Code session is **not possible today**. (1) No CLI command like `claude session rename <id> "<title>"` exists. (2) Stop hooks have no access to the agent's final message text — they only receive tool execution events. (3) Claude Code does not parse OSC codes or any agent-emitted marker that would trigger a rename. The current pattern (agent proposes `Tu peux remplacer le titre par : …` line, user copy-pastes into `/rename`) is the only reliable path. Revisit if Anthropic ever exposes (a) a CLI/IPC endpoint for session-rename, (b) enriches the `Stop` hook payload with the agent's final turn, or (c) introduces a marker convention agents can emit. Until then, status quo.

---

## Done — Last 10

- [x] **REFACTO TOTAL du pipeline extras — 5 étapes, 2 commits, bug structurellement impossible** (2026-05-24) — Continuation de la marathon debug d'hier. Audit obligatoire complet du pipeline AVANT toute modif (1443 lignes de cartographie via Explore agent : 3 writers concurrents, 2 readers avec fallback chain, 4 storage modes, 3 hooks recovery/guards défensifs, 2 entry points boot concurrents, 8-stage pipeline). Synthèse identifie le défaut conceptuel : **clé d'extras non canonique** (`prop.name` éditable par user vs `prop.key` camelCase) avec writers et readers qui ne partagent pas la même convention → rename = chips cassent. 5 étapes appliquées et validées en preview après chaque étape. **Étape 1** (`eedf882`) : `customProps` gagnent un champ immuable `notionSourceName` (= clé Notion canonique, capturée à +Add-from-Detected time depuis `detectedColKey`, fallback `name.trim()` pour Add manuel). Migration boot-time dans `loadCustomProps` copie `name → notionSourceName` pour tous les props existants et re-persiste. `updateCustomProp` n'écrase JAMAIS `notionSourceName`, même si user rename. **Étape 2** (`eedf882`) : suppression de `_bakeCustomWidgetExtrasIntoCache` (~55 LOC, redondant avec `_bakeAllDetectedPropsToExtras` qui est strict superset). Nouveau helper `_eagerBakeAfterWidgetCreate` remplace les 3 call sites widget-create (bars/donut/heatmap) + suppression de la boucle per-widget redondante en fin de `_injectTrades`. **Étape 3** (`eedf882`) : readers déterministes — `_customEntryPasses`, `_npCoverageForProp`, `_scanUniqueValuesFor` lisent UNIQUEMENT `t.extras[prop.notionSourceName]`, plus de fallback chain `?? prop.key`. Writer `_normalizeAPITrade` arrête d'écrire le legacy `extras[p.key]` (camelCase). `_collectCustomExtras` + `_collectCustomExtrasFromCSV` switchés sur `notionSourceName`. `_extractWidgetValueForExtras` préserve les arrays pour multi-select (sinon downstream `_customArrayOk` casserait). `_bakeAllDetectedPropsToExtras` gagne deep-equality check sur arrays. `deleteCustomProp` drop les DEUX slots (nsn + legacy key) pour cleanup. **Étape 4** (`151a823`) : boot single entry point. Ordre : (1) blob first (parsed Storage blob est la source canonique), (2) LS uniquement en fallback offline, (3) Notion API en dernier recours. L'IIFE async raw-blob reste fire-and-forget pour widget-create + mapping-change later, mais ZÉRO catch-up bake post-hydration (le blob carry déjà extras canoniques). Tue la race lost-update entre sync LS-first et async IIFE qui produisait le "124 → 0 extras non-déterministe". **Étape 5** (`151a823`) : suppression du scaffolding défensif — `_blobPayloadIsHealthy` + son call dans `_scheduleBlobUpload` (anti-corruption guard), `_ensureExtrasForNewProp` ~60 LOC + son call dans `addCustomProp` (recovery hook au +Add), anti-stale LS guard (≥30% threshold). Net : ~190 LOC supprimées, code aligné sur target DECISIONS.md (single source of truth = blob serveur, single boot entry point, readers déterministes, zéro fallback guards). Cache JS `?v=597` → `?v=619`. **Validation live** : boot clean (100 demo trades, 29 body children, 0 console errors) à chaque étape ; test final intégration prouve que le bug Max est **structurellement impossible** : addCustomProp avec `notionSourceName='M15 Setup'`, extras bakés sous cette clé, `_scanUniqueValuesFor` trouve 3 uniques, rename `name → 'My Custom Setup Label'`, `nsn` reste `'M15 Setup'`, re-scan trouve TOUJOURS 3 uniques. Filter pipeline `_customEntryPasses` avec include={Bullish} → [true,false,true] OK. Coverage 3/3 OK. **Restant** : delete edge functions Supabase `diag-blob-peek` + `diag-bake-fix` (Active TODO ci-dessus, manuel ~30s).

- [x] **Marathon debug pipeline extras — 9 commits, bug non résolu, décision refacto total** (2026-05-24) — Long stint de débogue d'un symptôme reporté par Max : custom-field chips affichent "no values found / 0/658" malgré data en mémoire. **9 commits poussés** sans résoudre : `afddb84` (élim dual-write `_repopulateExtrasForProp`), `30f4235` (anti-corruption guard sur upload blob), `a71f5da` (revert régression cacheKey), `388685f` (anti-stale guard sur boot path), `1441fdf` (recovery hook `_ensureExtrasForNewProp` au moment du Add), `7a1e3de` (explicit `renderCustomFilterChips()` post-bake), `395a6e7` (bake sur appState.trades.items en plus de parsedMem). **Investigations via accès direct Supabase + Vercel + Notion MCP** : (a) blob serveur sain confirmé (397 KB, 148 props × 658 trades, anti-corruption guard tient depuis 00:35), (b) DL manuel client retourne 124 props sur trade 0, (c) 1ère diag montrait `item0_extrasCount: 124`, 2ème diag montrait `extrasKeysCount: 0` entre deux refresh consécutifs — système non-déterministe. **Edge functions diag déployées** sur Supabase : `diag-blob-peek` (lecture blob avec décompression gzip + extras union) et `diag-bake-fix` (server-side bake fix qui réécrit le blob — utilisé 2× pour rattraper les écrasements client). **Root cause architecturale identifiée par audit Explore** : pipeline extras a 8 étages (LS cache → 4 storage modes → blob parsed → blob raw → _injectTrades clone → bake parsedMem OR appState → render → renderCustomFilterChips) et chaque patch ajoute un guard sans résoudre le défaut conceptuel. **Décision** (cf [DECISIONS.md § 2026-05-24](DECISIONS.md)) : arrêter les patches incrémentaux, démarrer la next session par un refacto total + audit obligatoire 100% du code avant de toucher quoi que ce soit. **Cache JS** `?v=586` → `?v=597`. Session close sur reconnaissance explicite : on tourne en rond, fatigue (Max 20h ces 3 jours, agent 12 fix tries), un round dédié + propre est nécessaire.

- [x] **Custom widgets leak across profile switch — DOM cleanup before re-inject** (2026-05-23) — Profile A's custom widget DOM elements stayed attached to `#gs-container` after switching to Profile B, even though `_reloadProfileScopedState` correctly invalidated the def cache and reloaded B's defs. Result: B's grid showed B's widgets PLUS A's leftover widgets — same widget pool visible on every profile. Root cause: `_ensureCustomWidgetInGrid` ([dashboard.js:38669](dashboard.js:38669)) is additive only (`return false` when item exists) and no path removed stale DOM. **Fix** — new § 3.4 block in `_reloadProfileScopedState` ([dashboard.js:9146](dashboard.js:9146)) walks every `#gs-container .grid-stack-item[gs-id]`, identifies custom widgets via `data-cw-id` / `.cw-widget` markers (standard widgets like `w-equity` / `w-monthly` are profile-agnostic and skipped), and removes any whose `gs-id` isn't in the freshly-loaded def list via `_grid.removeWidget(item, true)` (same pattern as the delete-widget path at line 38806). Runs BEFORE the existing `_ensureCustomWidgetInGrid` loop so the next inject pass rebuilds only the widgets the new profile actually owns. **Tested in preview** (mocked 2 profiles): widget A visible on A ✓, removed on switch A→B ✓, restored on switch B→A ✓, full round-trip A→B→A with B owning its own widget keeps each profile to its own widget set ✓. Cache `?v=578` → `?v=579`. **Validated live by Max** after his 3-day frustration with sync/cross-device issues.

- [x] **Eager-bake ALL detected Notion props into extras at sync (Task #3.11)** (2026-05-23) — Architectural simplification surfaced by Max's mental model: "all Notion properties detected should always be in Supabase, regardless of refresh, like the standard filters". Until now only properties referenced by an existing custom widget def were baked into `t.extras` — a brand-new device that downloaded the parsed blob saw extras only for the widgets the source device had previously created. Creating a NEW custom widget on a different prop on the fresh device required a full Sync before data appeared. The eager bake closes that gap: every detected non-system Notion property is now baked at sync, on inject, and post-hydration. New helper `_bakeAllDetectedPropsToExtras(trades, rawCache, source)` ([dashboard.js:1450](dashboard.js:1450)) walks every raw prop for every trade, flattens via `_flattenApiWidgetValue`, writes to `t.extras[propName]`. **Skip list**: system keys (`id`, `_notionId`, `created_time`, `last_edited_time`, `created_by`, `last_edited_by`, `icon`, `cover`, `parent`, `archived`, `url`, `public_url`, `object`, `in_trash`), Notion File & Media shapes (detected via `[{file:{url}}]` / `[{external:{url}}]` heuristic — image URLs are routed through the existing `imgM15/imgH4Before/imgM15After` + media cache pipeline, no need to double the payload), oversized values (>2KB cap to keep blob lean). **Empty-value semantics**: deleting `t.extras[key]` when raw returns empty/null mirrors `_repopulateExtrasForProp` so a cleared Notion column doesn't leave stale extras. **Hook points (3 hot paths)**: (1) `_loadNotionTrades` ([dashboard.js:10690](dashboard.js:10690)) after `mergedParsed` is built and BEFORE `setCachedAPIData` — covers manual Sync + incremental + full-resync, the bake lands in LS cache and blob upload. (2) `_injectTrades` ([dashboard.js:9266](dashboard.js:9266)) when raw is available — covers boot-from-LS-cache where the bake at sync time happened on a previous session. (3) The post-hydration IIFE in `_reloadJournalProfileSelection` ([dashboard.js:6360](dashboard.js:6360)) — replaces the per-widget bake added in the #3.10 commit; forces `setCachedAPIData(parsedMem, src, { fromBlobDownload: true })` after the bake to bypass the `_bakeCustomWidgetExtrasIntoCache` `changed`-flag aliasing trap. **Tested in preview**: 8 baked + 1 deleted per pipeline pass, screenshot dims excluded ✓, system keys excluded ✓, >2KB values capped ✓, empty values delete existing extras ✓, full boot path: no widget def required → all 6 mock Notion props baked into extras + persisted to LS ✓. **Trade-off**: parsed blob grows by ~50% (every trade carries every Notion prop value, not just widget-referenced) — for Max's 595 trades × ~10 extra props × ~50 bytes ≈ +300 KB raw, ~+80 KB compressed, well within Supabase Storage free-tier budget. Cache JS `?v=574` → `?v=578`. **Net effect**: Max's mental model — "Notion = all properties detected = all data connected 24/7 cross-device, like standard filters" — now matches the code's behaviour exactly.

- [x] **Raw Notion cache cross-device sync (Task #3.10) — option (b) URL substitution + boot-time hydration + post-bake LS persist** (2026-05-23) — Closes the last gap of the cross-device overhaul. Before: a fresh device that signed in could see parsed trades + screenshots instantly (via Task #3.4 parsed blob) but `_rawAPICacheMemory` stayed empty by design, so creating a custom widget or changing a mapping required a full Sync (~5-10s "no data") before the bake could populate extras. After: the raw payload propagates via a sibling Storage blob, and the device hydrates `_rawAPICacheMemory` at boot in parallel with the parsed blob. **(1) Helpers** — `_SW.uploadRawTradesBlob` / `downloadRawTradesBlob` ([dashboard.js:756](dashboard.js:756)) mirror the existing parsed blob helpers, path `<uid>/<profile>_<src>_raw.json.gz` in the same `notion-trades` bucket (no new RLS), reuse `_compressString`/`_decompressBlob`. **(2) URL substitution (option b + a fallback)** — `_substitutePermanentImageUrls(rawTrades, source)` ([dashboard.js:8100](dashboard.js:8100)) swaps Notion S3 signed URLs (`prod-files-secure.s3...` / `secure.notion-static.com` / `amazonaws.com`, all ~1h expiry) for permanent Supabase Storage URLs from the media cache (looked up by `_notionId` + slot via the current field overrides). Orphan trades (not yet in media cache, e.g. screenshot < 30s old) get their image properties stripped to `[]` rather than left as Notion URLs — no bucket pollution with dead links. Non-image props and external URLs (imgur, cloudinary, etc.) pass through untouched. **(3) Debounced upload** — `_scheduleRawBlobUpload(profileId, source)` ([dashboard.js:1015](dashboard.js:1015)) at 1.5s, slot-aligned via `_getJournalProfileCacheContext` (NOT `getActiveJournalProfile`) so the fire-time slot lookup matches the write slot regardless of mid-debounce profile switches. Skips for `__demo`. **(4) Setter hook** — `_setRawAPICache` ([dashboard.js:6843](dashboard.js:6843)) gains an `options.fromBlobDownload` flag; every Notion-side write (sync, incremental, fetch fallback) schedules an upload, downloads suppress to avoid the loop. **(5) Boot hydration (fire-and-forget IIFE)** — at the top of `_reloadJournalProfileSelection` ([dashboard.js:6326](dashboard.js:6326)), before the LS-cache early-return, an `(async () => {...})()` block kicks off the raw blob download asynchronously and re-uses `_setRawAPICache(..., { fromBlobDownload: true })` to hydrate memory without blocking first-paint. Two guards (pre-fetch + post-await) skip the call when raw is already populated, preventing clobber races against a concurrent Sync. **(6) Post-hydration bake catch-up** — same IIFE, after raw is hydrated: re-loads every custom widget def and re-runs `_bakeCustomWidgetExtrasIntoCache` for each. Then **explicitly** calls `setCachedAPIData(parsedMem, src, { fromBlobDownload: true })` to force LS persist — needed because the bake's internal `if (changed) setCachedAPIData(...)` flag only flips when Loop 2 (parsed cache) writes new extras, but when `appState.trades.items` and `_parsedAPICacheMemory[slot]` hold the same references after `_injectTrades`, Loop 1's in-place mutations short-circuit Loop 2 → `changed` stays false → LS not updated → next hard refresh shows widget with no data. Forcing the write closes that gap. Finally `render()` to surface the freshly-baked extras. **The regression Max kept hitting for 3 days** — "create custom widget, refresh, widget exists but no data" — root caused here: even with the parsed blob ferrying everything cross-device, the widget's extras were never persisted to LS by the bake because of the two-loop reference aliasing. Without this fix, the only way to see widget data after refresh was to do a full re-Sync. **Sub-decision tranchée live**: option (b) substitute → (a) strip orphans fallback, never (c) — keeps the bucket clean and lets mapping changes on dim screenshots survive on fresh devices. **Tested in preview (mocked Supabase)**: helpers callable ✓, substitution preserves non-image props + external URLs + `__NO_MAPPING__` ✓, schedule respects `fromBlobDownload` (no loop) + skip demo mode ✓, boot regression reproduced (rawCount=0 after hard refresh with LS cache) ✓, fix verified end-to-end (rawCount=N, extras in LS = baked value) ✓, guard skips re-download when raw already populated ✓. Cache `?v=562` → `?v=574`. **Remaining cross-device gap**: Supabase Realtime push for "PC A → 30s → PC B updates without F5" — still deferred to Session 4 / optional.

- [x] **Cross-device sync overhaul — Sessions 2+3 of the M1 roadmap closed in one shot, including 14 sub-tasks of defensive cascading fixes** (2026-05-23) — Biggest architectural delivery since M1. Closes Max's core vision: « PC A → 2 min → PC B identique sans rien faire ». **(1) Layout + hidden widgets cross-device** ([dashboard.js:279](dashboard.js)) — `_SYNC_KEY_PREFIXES` gains `gs_layout_active_` + `gs_hidden_widgets_`, migration flag bumped v1→v2, `htfSource` added to `_SYNC_KEYS` to prevent view-source drift. Validated end-to-end Brave↔Safari (LS isolés). **(2) Trades blob on Supabase Storage** — new private bucket `notion-trades` with user-scoped RLS, path `<uid>/<profile>_<src>.json.gz`. Helpers `_compressString` / `_decompressBlob` via CompressionStream (gzip), `_SW.uploadTradesBlob` / `downloadTradesBlob`. Debounced upload (1.5s) hooked into `setCachedAPIData` covers all 3 entry points (sync, mapping change, widget bake). Boot flow inverted: `_reloadJournalProfileSelection` tries blob download BEFORE Notion fetch — on fresh device, 595 trades load in ~200-500 ms via 34 KB compressed payload. **(3) Custom widget DOM injection cross-device** — new `_ensureCustomWidgetInGrid(def)` companion to `_injectNewCustomWidget` that respects saved position from `_liveSectionLayouts.global` (vs fresh maxBottom). Called from `_reloadProfileScopedState` + post-init safety net in `initGridstack`. **(4) Create Widget property list post-blob-load** ([dashboard.js:30866](dashboard.js)) — `getApiAvailableProperties` no longer filters by `coverageCount > 0` when raw cache is empty, so the synced `apiFieldNames_v1_<id>` falls through visibly. **(5) Defensive cascade for the silent-upsert bug** — Task #3.11 pending-writes queue catches pre-auth `_user`-null race, #3.12 ungated `migrateProfileScopedKeys` runs every sign-in with bulk SELECT + .catch on upsert, #3.13 hooks reconcile via `getSession().then()` for Safari ITP, #3.14 `_saveCustomWidgetDefs` refuses unsuffixed writes (redirects to `_pending_*`) + `_recoverPendingCustomWidgetDefs` merges orphan/pending payloads into the active profile's scoped key on every `_reloadProfileScopedState`. **(6) Auto-trigger background full sync after widget create** ([dashboard.js:1161](dashboard.js)) — `_maybeTriggerFullSyncAfterWidgetCreate` fires `_loadNotionTrades({force:true, background:true})` when raw count < parsed count, populating extras across the full dataset so the blob upload carries complete data for the next device. **Architecture pivot** — Supabase now plays 2 distinct roles: (a) `user_data` table for metadata (mappings, presets, widget defs, layouts, hidden, htfSource) via monkey-patched LS sync, (b) `notion-trades` Storage bucket for bulky parsed-trade payload via debounced compressed blobs. Free tier covers ~6000 profiles for metadata + ~3000 for trades blob (no hard limit reached). Manual Sync button still hits Notion as the live source of truth; blob is the cross-device propagation layer. **Cache** JS `?v=534` → `?v=562`. **Commits** 11 in this session (`233fb1f` layout sync, `93368ef` blob upload hook, `85ce22d` boot inversion, `5a1e331` DOM injection, `90a730c` create-widget property list, `4a2e580` widget vanishing, `723353d` full sync auto-trigger, `84ab881` pending queue, `c168cbe` self-healing reconcile, `3733267` Safari ITP getSession path, `8d77ba9` orphan write recovery, plus this `/end` commit). **Known limitation deferred to Task #3.10** — raw cache itself is still in-memory only; widget creation on a fresh device shows "no data" until the auto-trigger full sync completes (~5-10s). Permanent fix in next session.

- [x] **DS session 2: cross-device sync for profile-scoped data + custom-widget post-refresh resilience + H4 layout overhaul + UX polish** (2026-05-23) — Long session closing 1 Deferred audit + multiple Max-reported regressions. **(1) Clickable partial-coverage chip in Mapping panel** ([dashboard.js:30008](dashboard.js)) — orange `X/Y trades` chip becomes a `<button data-action="open-mapping-missing-trades">` when `populated < total`. New action handler ([dashboard.js:2596](dashboard.js)) filters `appState.trades.items` via `_JOURNAL_DIM_EXTRACTORS[dimKey]` and calls `openWidgetDrawer('Missing <Label>', 'Trades without a <Label> value', missing)` — reuses the canonical right-side trades drawer. CSS `.is-clickable` variant ([dashboard.css:1956](dashboard.css)): cursor pointer, dotted underline, focus ring. Verified live on demo (setup 95/100, h4 94/100, obstacles 93/100 → 5/6/7 cards). **(2) Outcome value-mapping UI survives refresh** ([dashboard.js:30237](dashboard.js)) — `_renderOutcomeValueReportHTML` was gated behind `rawReady` at the call site, and `_persistRawAPIToSession` is a no-op by design (signed-URL quota). Removed the gate + added a fallback in `_getOutcomeValueReport` that walks `appState.trades.items[].outcomeRaw` when raw cache is empty. Required `outcomeRaw` to survive serialization → added to all 3 `_STORAGE_*_KEYS` ([dashboard.js:7802](dashboard.js)). Max's mapping (e.g. `TP 1 → Take Profit`) now persists across refreshes without forcing a Sync. **(3) ORR "sans rrMax" → English "missing RR Max"** ([dashboard.js:18524](dashboard.js)) — French string in the Optimal RR Analysis widget. **(4) Red data-warning header in ORR + PO `?` tooltips** ([index.html:2742](index.html), [2776](index.html); [dashboard.css:12372](dashboard.css)) — new `.tt-data-warn` class (red `--r`, weight 600, bottom border) flagging that empty RR Max values will produce inaccurate widget output. Placed FIRST in each tooltip body (above the existing assumptions/modes content). **(5) H4 templates layout overhaul** — `TEMPLATE_LAYOUTS.h4` ([dashboard.js:35801](dashboard.js)) rebuilt from Max's live `_serializeSectionLayouts()` export (13 visible widgets + 5 hidden: `w-m15, w-session, w-hour, w-heatmap, w-pair-session`). All 5 declared in `hiddenWidgets` sidecar so `gs_hidden_widgets_<id>` seeds correctly on profile creation. `h4-pro` / `h4-beg` `TEMPLATE_MAPPINGS` keep `session: '__NO_MAPPING__'` and `hour: '__NO_MAPPING__'` (Max confirmed H4 journals don't track intraday data). New profiles get clean layout; existing profiles keep theirs (TEMPLATE_LAYOUTS is seeded-once). **(6) Custom heatmap X/Y inversion fix** ([dashboard.js:30839](dashboard.js)) — render convention is `def.field`=rowKey (Y axis) and `def.field2`=colKey (X axis), but `_handleCreateWidgetContinue` was writing `xSelected → field`. Swap applied: xSelected → field2, ySelected → field. Label kept as user-friendly "X × Y". `_renderCreateWidgetList` sub line ([dashboard.js:30883](dashboard.js)) also flipped to `${field2Label} × ${fieldLabel}` for consistency. **(7) Numeric chip sort** ([dashboard.js:33218](dashboard.js), [15694](dashboard.js)) — `_scanUniqueValuesFor` (custom prop chips) + `_orderHeatmapAxisValues` (heatmap axis fallback) now detect when every value passes `Number.isFinite(Number(v))` and sort numerically ascending. Strings keep `localeCompare`. **(8) extras key persistence in MINIMAL/ULTRA** ([dashboard.js:7809](dashboard.js)) — `_hasHeavyImageFields` forces signed-URL-heavy datasets through `['minimal','ultra']`, both of which had `extras` stripped from `_STORAGE_*_KEYS`. Added — custom widget/filter values now survive refresh in production caches. **(9) Notion formula/rollup flatten** ([dashboard.js:30143](dashboard.js)) — `_flattenApiWidgetValue` gained handlers for the type-discriminated formula/rollup shapes (`{type:'formula',formula:{type:'string',string:'…'}}`, `{type:'number',number:N}`, `{type:'boolean',boolean:B}`, `{type:'date',date:{start:'…'}}`) plus the unwrapping wrappers. Before this, baking via `_bakeCustomWidgetExtrasIntoCache` returned `''` for formula raw values → `_extractWidgetValueForExtras` returned `undefined` → extras unwritten → no data on refresh. 19/19 tests (8 new shapes + 11 regression). **(10) audit M1 closed — cross-device sync for profile-scoped LS keys** ([dashboard.js:271](dashboard.js)) — new `_SYNC_KEY_PREFIXES` (5 patterns: `apiFieldOverrides_v1_`, `apiFieldNames_v1_`, `outcomeValueMapping_v1_`, `flipping_notion_properties_`, `flipping_custom_widgets_`) + `_isProfileScopedSyncKey()` helper with positive shape match `prefix + (jp_<chars> | __demo)` — filters out one-shot LS flags like `*_profile_migration_v1` that would otherwise false-positive. `_isSync` extended to match prefixes alongside exact `_SYNC_KEYS`. New `_SW.migrateProfileScopedKeys()` one-shot pushes each local LS key to Supabase ONLY when `maybeSingle()` shows no remote row — preserves cross-device last-write-wins. Wired after `syncFromRemote` in `SIGNED_IN` and standalone in `INITIAL_SESSION` handlers. Idempotent via `flipping_profile_scoped_sync_migration_v1` flag, with retry-on-partial-failure (flag only set when `failed===0`). HTF dimension preserved via existing `_dashId('h4')` / `_normKey` strip logic. Cache `?v=534`. **Profile/trade mismatch bug** removed from ROADMAP Active and persisted to memory (`memory/project_profile_trade_mismatch.md`) per Max's request — diagnostic stays live in code, surfaced only if Max raises it.

- [x] **Re-apply template defaults — strict partial-raw guard + inline "Resync needed" confirm popover** (2026-05-23) — Surfaced when Max reproduced "screenshots disappear after refresh" on the **Sync + Reapply + Refresh** path (vs. the Full-resync path which worked). Root cause traced through the cache pipeline: after a page refresh, `_rawAPICacheMemory` is wiped by design (in-memory only — see [dashboard.js:5953](dashboard.js:5953), `_persistRawAPIToSession` is a no-op). A subsequent **incremental** Sync uses `last_edited_after=<cursor>` and only refetches trades modified since the cursor (typically 0–5 of Max's 595). In `_loadNotionTrades` ([dashboard.js:9564](dashboard.js:9564)), `mergedRaw = _mergeRawNotionRowsById(existingRawCache=[], fetchedRawTrades)` = just those few trades. The LS parsed cache stays at 595 (merged via `_mergeTradesByNotionId(existingCache, parsed)`) but raw is now **partial**. When Reapply runs, the existing safety guard at the top of `_reapplyTemplateMapping` ([dashboard.js:7070](dashboard.js:7070)) only blocked the EMPTY-raw case — a partial raw passed through. `_saveAPIFieldOverrides({})` → `_invalidateNormalizedTradesCache` wiped LS + session + memory parsed caches (595 trades gone), template defaults rewritten, then `_reapplyAPIOverrides` re-normalised just the few raw rows and `setCachedAPIData(parsed)` overwrote LS with that small set. On the next refresh the dashboard loaded with only those few trades — 590 trades vanished, and with them every screenshot they held. Full resync worked because `force: true` ([dashboard.js:5218](dashboard.js:5218)) bypasses the cursor and refetches all rows, leaving raw and parsed at parity. **Fix (strict guard)** — new partial-raw check at [dashboard.js:7079](dashboard.js:7079): if `_getRawAPICache().length < getCachedAPIData().length`, abort the Reapply with the same recovery affordance as the empty-raw case. Console: `[ReapplyTemplate] aborted — raw cache partial (N trades) vs cached parsed (M). Full resync required.`. **Fix (inline confirm UX)** — replaced both `showThemeToast(...)` calls (empty-raw + partial-raw branches) with a new inline popover `_openReapplyResyncConfirm` ([dashboard.js:36277](dashboard.js)) anchored directly under `#ljp-reset-btn`. Markup follows the existing `.gs-hide-confirm-popup` pattern (DA-coherent) with a gold-tinted variant `.ds-reapply-resync-popup` for non-destructive intent (vs. the red used by the widget hide/delete confirms). Title `Resync needed` in red `var(--r)`, body row `Resync ?  [No] [Yes]` with gold accent on Yes. Yes → `_handleNotionProfileFullResync(activeProfileId)`; No / outside-click → close. **CSS z-index gotcha fixed live** — the popover initially didn't show on Max's screen because the inherited `.gs-hide-confirm-popup` z-index is 1100 while the data-hub-panel uses `--z-panel: 2000`. Bumped the variant to `z-index: 2100` so it surfaces above the panel that anchors it. Other usages of `.gs-hide-confirm-popup` (widget hide/delete) unaffected. **Verified live** — both abort paths fire (console warnings confirmed), popup created with correct markup and computed styles (`titleColor: rgb(239, 83, 80)` = `--r`, row flex, z-index 2100), no console errors. Action handlers wired in the click router at [dashboard.js:2342](dashboard.js). **Note** — the original "F&M screenshot pipeline still broken after refresh" TODO is closed: Max validated screenshots stay present after a Full-resync + multiple refresh cycles, confirming the 2026-05-22/23 stack of fixes (Reapply preservation, `_notionId` fallback chain, F&M-preferred auto-detect, post-refresh boot-time auto-detect, migration purge) collectively fixed the symptom; the user-visible regression he kept hitting was actually the partial-raw bug fixed here.

- [x] **Re-apply template defaults — preserve screenshot overrides + `_notionId` + safety guard** (2026-05-23) — Surfaced after the `_notionId` fallback shipped: Max's H4 Perso ended up with empty dashboard after Reapply + refresh, and a second Reapply crashed localhost. Three nested bugs in `_reapplyTemplateMapping` and `_reapplyAPIOverrides`. **(1) Reapply wiped screenshot overrides** ([dashboard.js:7048](dashboard.js:7048)) — `_saveAPIFieldOverrides({})` cleared everything, then `_maybeAutoApplyTemplateMapping` rewrote template defaults (`'URL H4 Before'` / `'URL H4 After'`) that don't exist in Max's custom DB. The user had to manually pencil-pick `'H4 Before'` / `'H4 After'` (F&M properties) again every time. Fix: snapshot `img_m15` / `img_h4_before` / `img_m15_after` overrides + their auto-mapped flags BEFORE the wipe, restore them AFTER the template apply so they win the merge. Net effect: every NON-screenshot dim resets to template baseline; screenshots stay as the user / previous auto-detect resolved them. **(2) `_reapplyAPIOverrides` stripped `_notionId`** ([dashboard.js:7028](dashboard.js:7028)) — `_normalizeAPITrade` doesn't include `_notionId` in its return object (intentional: `_notionId` is added externally by the caller). The Reapply re-normalize loop wasn't doing that addition, so every Reapply wiped `_notionId` from cached trades. Without `_notionId` the media queue can't match trades to their Supabase migrations. Fix: same fallback chain as the sync path at [dashboard.js:9456](dashboard.js:9456): `t._notionId || t.notionId || t.id || ''`. **(3) Reapply without raw cache produced empty dashboard** — Post-refresh, raw cache is wiped (no sessionStorage persistence by design). `_saveAPIFieldOverrides({})` triggered `_invalidateNormalizedTradesCache` purging LS + session parsed caches BEFORE checking raw. `_reapplyAPIOverrides` then early-returned because no raw → no refill → empty dashboard. A second Reapply on top of this state crashed localhost (`_reapplyAPIOverrides` couldn't produce anything to render, downstream code blew up). Fix: safety guard at the top of `_reapplyTemplateMapping` — if `_getRawAPICache()` is empty, toast "Click Sync first — Re-apply needs fresh data from Notion." and return WITHOUT wiping overrides or invalidating caches. **Verified live**: (a) safety guard fires when raw empty, all overrides preserved; (b) Reapply with raw + existing screenshot mapping → non-screenshots reset to template (outcome → `'Position Result'`, pair → `'Pair '`), screenshots preserved (`'H4 Before'` / `'H4 After'` / `__NO_MAPPING__`), auto-mapped flags kept. Cache `?v=503`.

- [x] **F&M screenshot pipeline — `_notionId` fallback chain (root cause of "screenshots disappear after refresh" on custom Notion DBs)** (2026-05-23) — Surfaced when Max ran a diagnostic on his H4 Perso profile post-fix and found `_notionId: ""` on all 595 cached trades + console warning `[MediaQueue] missing notionId on 595/595 trades`. Root cause traced through the full pipeline: (1) Sync produces trades with signed Notion S3 URLs in memory (~1h expiry); (2) `_serializeTradesForStorage` ([dashboard.js:7764](dashboard.js:7764)) intentionally STRIPS Notion URLs when writing to LS cache (`return (val && !_isNotionFileUrl(val)) ? val : ''`) because (a) they expire and (b) the payload would exceed the 2MB LS guard with 595 trades × 6 img fields × ~1700 chars (~6.7 MB); (3) `_mediaQueue` is designed to upload Notion URLs to permanent Supabase Storage via `POST /api/notion/media-sync`, keyed by `_notionId` per trade; (4) On refresh `_mergeMediaCache` rehydrates trades with Supabase URLs from the separate media cache LS slot. **The broken link** — at [dashboard.js:9448](dashboard.js:9448), the post-normalize loop only read `t._notionId` from the raw Vercel proxy response: `if (norm && t._notionId) norm._notionId = t._notionId`. Max's proxy version (and apparently others) exposes the Notion page UUID under `t.id` instead — used at [dashboard.js:7155](dashboard.js:7155) to construct `notionUrl` from the same field. With `t._notionId` undefined, `norm._notionId` stayed unset, `_mediaQueue.enqueue` skipped every trade (`if (!trade._notionId) continue`), the media cache stayed empty, and on every refresh the parsed LS cache had `imgM15: ''` / `imgH4Before: ''` / `imgM15After: ''` with nothing to rehydrate from. M15 profiles using TV `/x/` URLs were unaffected because `_normalizeTvImageUrl` converts them to permanent `https://s3.tradingview.com/snapshots/...` PNGs that pass the `_isNotionFileUrl` check and survive LS serialization. **Fix** — fallback chain `const notionId = t._notionId || t.notionId || t.id || ''` at [dashboard.js:9456](dashboard.js:9456). Captures the Notion UUID regardless of which field the proxy version exposes. **User action required** for existing Notion profiles: the cache holds 595 trades with `_notionId: ""`; incremental sync only re-processes NEW trades. Users must click **"Full resync"** (profile row ⋯ menu, `_handleNotionProfileFullResync` → `force:true`) once per profile to re-fetch the full DB and populate `_notionId` from `t.id`. After that, the media queue will migrate all signed URLs to Supabase in background batches of 50, and subsequent refreshes hydrate screenshots from the permanent media cache. **Verified**: no console errors after deploy, syntax clean. Cache `?v=501`. Also surfaced (separate issue, not fixed): Max's H4 Perso profile has `type: 'm15'` despite the H4-themed journal — `_getJournalProfilePreferredSource` returns 'm15' → HTF stays 'm15' on this profile. Doesn't directly cause the screenshot bug (override-based normalization is HTF-independent for screenshots) but worth fixing later (allow `type` edit on existing profiles, or infer from DB title at sync time).

- [x] **Screenshot auto-detect — multi-trade merged sample (fixes "H4 Before works but H4 After doesn't" on partial F&M coverage)** (2026-05-22) — Surfaced when Max reported that on his H4 perso profile, auto-detect correctly swapped `URL H4 Before` → `H4 Before` (F&M) but left `URL H4 After` stuck on the stale template default with "no value detected", even though manually picking `H4 After` worked. Root cause: `_findScreenshotCandidateByName` was scanning a SINGLE sample (the first trade in raw cache). If that trade happened to have a Before file but no After file yet (very common — users don't always take an after-screenshot on every trade), `_classifyScreenshotValue` returned null for the empty `H4 After` array → no F&M candidate found → my F&M-prefers logic fell through to the camel-alias fallback or kept the stale URL override. **Fix** — new `_buildScreenshotMergedSample(rawTrades)` helper ([dashboard.js:6804](dashboard.js:6804)) walks every raw trade and records the FIRST non-empty screenshot-classifiable value seen per property name. The auto-detect now sees the H4 After file from whichever later trade happened to have one. Pass 2 backfills empty-but-present keys from the first trade so existence checks (used by stale-override validation) still recognise empty-but-present columns. Replaces the single `sample = rawTrades.find(...)` line at the top of `_autoMapScreenshotsByRawKeys`. **Verified live**: seeded H4 template defaults + 3 fake trades where trade 1 has Before only, trade 2 has both, trade 3 has After only → both slots correctly auto-swapped to F&M (`H4 Before` and `H4 After`), `__NO_MAPPING__` preserved. Idempotent — Re-apply no longer regresses the After slot. Cache `?v=500`.


---

## Update rules for agents

At the end of every coding session:
- Move completed items → Done (keep last 10 max, oldest fall off)
- Add any new TODO discovered during the session (with context)
- Update "In Progress" if work is mid-flight
- Commit `ROADMAP.md` in the same commit as the related code change
