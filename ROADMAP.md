# ROADMAP.md — Flipping Market Dashboard

> Single source of truth for task tracking. Updated by agents at the end of every session.
> Supersedes `docs/current-roadmap.md` (kept for historical reference only).

---

## In Progress

_(none)_

---

## TODO — Active

- [ ] **Migrate remaining ~26 inline `connectionType === 'notion'` checks to `INTEGRATIONS` registry** — Strangler Fig follow-up to the 2026-05-19 registry refactor. One micro-fix done at [dashboard.js:5336](dashboard.js:5336) (`_reloadJournalProfileSelection` `isNotionWithDb` now uses `getIntegration(p).isReady(p)`). Rest deferred until MT5 ships — abstracting from a single concrete implementation risks getting the methods wrong.

- [ ] **Extend `.claude/hooks/post-edit.sh` to bump `share.html` / `share.js` / `share.css` cache-bust** — Today the hook only handles `dashboard.css` / `dashboard.js`. Manual `?v=N` bumps in `share.html` per edit during Phase 2/3 worked but are error-prone. Mirror the existing `dashboard.js` branch with a `share.js` branch (no `node -c` validation needed since share.js is small + parsed live in Preview) and same for `share.css`.

- [ ] **Install `pg_cron` extension + schedule weekly purge of expired shares** — Spec'd in [docs/share-link-feature.md §7](docs/share-link-feature.md). RLS already hides expired rows (`expires_at > now()` SELECT policy) so this is pure garbage-collection. Run `select cron.schedule('shares-purge-expired', '0 3 * * 0', $$delete from public.shares where expires_at < now() - interval '1 day';$$)` after enabling the extension via Supabase dashboard. Defer until row count justifies it (~10k shares).

- [ ] **Journal Evolution P1 — Custom props on existing trades** — next phase after P0. Scope : UI "Add custom property" (name, type, validation) dans le Data Setup → écrit `user_schemas` ; rendu inline des custom props dans la trade card du drawer ; edit-in-place (click → input → save → upsert `trade_overlays` via `tradeId`) ; enrichissement de `_normalizeAPITrade` avec les overlays au read. Estimé : 3–4 sem focalisées. **Bloquant aucun** — l'infra Supabase est en place depuis 2026-06-09 (P0). Spec : [docs/journal-evolution.md](docs/journal-evolution.md) §4.

- [ ] **Journal Evolution P2 — Native trade CRUD** — phase suivante après P1 : form de saisie complet (date, pair, setup, RR, screenshots upload), édition inline, soft-delete + UI Corbeille. À ce moment-là le dashboard est vraiment autonome (Notion devient optionnel). Estimé : 4–6 sem. Spec : [docs/journal-evolution.md](docs/journal-evolution.md) §4. **Ne pas démarrer avant P1 stabilisé** (les overlays valident le pattern jsonb sans risquer des données natives).

- [ ] **Journal Evolution P0 follow-up — Retention cron body** — `pg_cron` extension pas encore installée (TODO Active séparé : "Install pg_cron extension"). Une fois installée, écrire le body du cron weekly (cf. [docs/journal-evolution.md](docs/journal-evolution.md) §10 retention policy) : hard-delete profiles `deleted_at < now() - 30 days`, warning email à 180 j d'inactivité, soft-delete à 365 j. Sans le cron, la retention reste passive (le schéma supporte l'état, rien ne GC automatiquement). Pas urgent — tant que la base n'a pas de comptes inactifs, no-op.

- [ ] **Journal Evolution P0 follow-up — Server-side quotas (trades_native + storage)** — déclaré dans spec (10 000 trades/user, 500 Mo media/user) mais pas enforcé en BDD. Implémenter via trigger BEFORE INSERT sur `trades_native` (count check) + edge function de pré-signature pour les uploads Storage. Pas urgent tant que P2 n'a pas démarré (aucun trade natif possible aujourd'hui).

- [ ] **Preset id remap migration — repair presets already stuck at an out-of-range id (≥ PRESET_MAX)** — follow-up au fix `_nextPresetId` du 2026-06-11. Le fix empêche tout NOUVEAU preset de dépasser la plage `[0, PRESET_MAX)`, mais un membre qui a déjà aujourd'hui un preset bloqué à un id ≥ 10 (via cycles création/suppression sous l'ancien allocateur `maxId+1`) garde ce preset **vierge** (son snapshot/override n'a jamais été persisté) tant qu'il ne le supprime + recrée pas manuellement. Migration optionnelle au boot (après les loaders) : pour chaque preset `p.id >= PRESET_MAX` ou `< 0`, trouver le plus petit id libre `< PRESET_MAX`, migrer les slots runtime survivants (le live slot survit — `loadPresetLiveFilters` le charge par clé string ; le snapshot/override sont déjà perdus → migrent en blank), réassigner `p.id`, mettre à jour `appState.presets.activeId` si concerné, re-persister. **Proposé à Max le 2026-06-11, pas tranché** — plus sensible (touche les ids + la persistance de l'active id + dropdowns compare) donc demander confirmation avant. Le cas du reporter (Max) est déjà couvert par le seul fix `_nextPresetId` (ses 7 presets sont en plage 0..9).

- [ ] **Migration optionnelle des valeurs détail bloquées dans le chip natif `setup`** (noté 2026-06-14, suite du fix « exclusion mode détail → chip custom ») — Avant le fix de ce jour, les exclusions de valeurs setup-detail atterrissaient dans le chip natif `setup` (qui dual-filtre `[t.setup, t.setupDetail]` ~[dashboard.js:15393](dashboard.js:15393)). Les valeurs déjà présentes chez un membre (observé sur la donnée de Max : `NEM type A/B` dans `chips.setup.excluded`) continuent de filtrer mais restent dans le natif, **pas** dans la propriété custom → résiduel de désync pour l'ancien état. Le fix ne migre rien (ne touche pas l'état live existant, volontairement). Option : au boot, pour chaque valeur de `chips.setup.{excluded,included}` qui n'est **pas** une valeur de `t.setup` mais **est** une valeur de `t.setupDetail` ET dont la colonne est déclarée comme propriété custom, la déplacer vers le chip `custom:<key>`. Sensible (touche l'état de filtre live + persistance preset/snapshot) → demander confirmation avant. Contournement simple en attendant : le membre retire ces valeurs du chip Setup et les ré-exclut depuis le graphique (route désormais correctement).

- [ ] **PDF export — handle non-CORS (Notion-hosted) screenshots in the jsPDF download** — the trade-card PDF export (`_pdfBuildDoc`/`_exportTradesPdf`, 2026-06-09) embeds screenshots via html2canvas, which can only rasterise images whose host allows cross-origin reads. Supabase-public-bucket screenshots embed fine; a screenshot still hosted directly on Notion (upload to Supabase not yet done) comes out blank in the downloaded PDF. The in-tab preview still shows them (plain `<img>`, no CORS needed). Options to fix: (a) route Notion image URLs through the Vercel proxy with permissive CORS before capture, (b) force-migrate any pending screenshots to Supabase before allowing export, (c) fetch→dataURL each image with a CORS-allowed fallback. Low urgency — most synced profiles have migrated screenshots; flagged so it isn't mistaken for a bug.

---

## TODO — Deferred

- [ ] **Clarification UX du widget Optimal RR (contrefactuel vs perf réelle)** (noté 2026-06-16, proposé à Max non retenu cette session) — Un membre a confondu le « Cumul +163R / Optimal 2.4R » du panneau ORR avec sa perf réelle (+391R) affichée en haut, croyant à une incohérence/bug. En réalité le panneau est un **contrefactuel** : « si tu fermais 100% à un seul TP fixe » — il plafonne les gagnants et, en mode Strict Loss, compte toute sortie sous le TP comme perte pleine → **forcément < perf réelle** quand le trader laisse courir. Pas un bug, mais trompeur visuellement (gros « +163.20R » sous le « +391.5R » sans étiquette « hypothèse »). Proposer un sous-titre/tooltip explicite (ex. « contrefactuel : TP fixe unique fermé à 100%, distinct de tes sorties réelles ») pour couper court à la confusion chez les 600 membres. Max a préféré ne traiter que l'affichage de la colonne source en Data Setup cette session.

- [ ] **Revoir le système « Locked » et tout ce qu'il recouvre** (à reprendre un jour, noté 2026-06-11) — Aujourd'hui le compteur « LOCKED » du hero Data Setup ([dashboard.js:34065](dashboard.js:34065) `_updateDataSetupHero`) ne reflète **qu'une seule chose** : le mode **Personalised TP** (`appState.ui.tpConfig.mode === 'personalised'`) avec un mapping TP incomplet (`_getPersonalisedMappingStatus().complete === false`) → `lockedCount = 1`, pastille rouge `is-bad`, sinon masquée. C'est piloté par `FEATURE_REQS['personalised-tp']`. **À revoir** : (1) la sémantique exacte de `FEATURE_REQS` et quelles features devraient « locker » (aujourd'hui quasi rien d'autre que personalised-TP ; Multi-TP est explicitement exclu — cf. pitfall #12) ; (2) l'UX : un membre voit « 1 LOCKED » sans comprendre quoi est verrouillé ni comment le débloquer (pas de drill-down, pas de lien vers le mapping manquant) — surfacer *quelle* feature et *quel* champ manque ; (3) cohérence avec le ring de couverture + le badge « X to fix » de l'attention pipeline ; (4) décider si « Locked » doit rester un simple compteur ou devenir une liste actionnable. **Déclencheur** : revu quand un membre a remonté un « 1 LOCKED » incompris (2026-06-11). Pas urgent — informatif, ne bloque aucune donnée. Faire un audit complet de `FEATURE_REQS` + tous les sites qui lisent/écrivent « locked » avant de retoucher.

- [ ] **Grouped filters — quick-add depuis les widgets (clic-droit, option D)** (proposé 2026-06-12, non retenu cette session) — point d'entrée alternatif pour créer un groupe directement depuis un graphique : clic-droit sur une barre (Setup, Pair, Session…) → « Exclure ce setup en groupe » / « Inclure ». Seed un groupe pré-rempli avec la dim+valeur cliquée, ouvre le drawer pour affiner. Plus gros chantier (hook context-menu sur tous les renderers de barres + donut + heatmaps). Le système de groupes (drawer builder, opérateurs, mini-stats) est déjà en place — il ne manque que ce raccourci de création. À reprendre si Max veut accélérer la création depuis l'analyse.

- [ ] **MT5 / MT4 / cTrader — actual implementation** — registry stubs (`available: false`) exist since 2026-05-19. Next steps before code: (1) decide MetaApi.cloud SaaS bridge vs. self-built MQL5 EA (cost vs. control); (2) extend Vercel proxy or move to Supabase edge functions for per-broker secret handling (current static-page architecture can't hold per-user API keys safely); (3) spike one platform end-to-end on a single test account before promising to community. cTrader is the easiest technically (OAuth Open API) but smaller audience; MT5 covers most of Max's prop-firm users.

- [ ] **Backend OAuth: dynamic frontend redirect** — `notion-dashboard-api-2` Vercel function uses `DASHBOARD_URL` env var hardcoded to a single front URL (`https://lemarsuoff.github.io/Flipping-Market-Dashboard/`). Conséquence : impossible de tester l'OAuth Notion en local sans flipper la variable (et casser la prod). Fix : à `/api/notion/oauth/start`, lire l'`Origin`/`Referer` de la requête, valider contre une allowlist (`github.io/Flipping-Market-Dashboard/`, `localhost:*`), encoder dans le `state` OAuth, puis y rediriger au callback au lieu de toujours utiliser `DASHBOARD_URL`. Garder `DASHBOARD_URL` comme fallback. Nécessite accès au repo backend `notion-dashboard-api-2` (pas sur cette machine). Contexte complet dans [DECISIONS.md](DECISIONS.md) § 2026-05-19 — Vercel `DASHBOARD_URL` is single-tenant prod.

- [ ] **Drawdown Intelligence section** — DD by context (setup/session/pair decomposing max DD), Recovery Factor (Total R / |Max DD|), DD Duration distribution, Risk of Ruin estimate. Optional: Underwater curve, Streak/DD scatter, MAR/Calmar (~14 months of data, low priority). Deferred pending community priority signal.

- [ ] **Hardcoded font-sizes (~180 remaining)** — deferred pending community feedback on typography editor coverage.

- [ ] **BYOT + export/import config** — open the dashboard to all users without requiring their Notion credentials. Recommended step before full OAuth rollout. ~1.5 days dev vs weeks for OAuth. Deferred until post-Notion overhaul stabilization.


- [ ] **Orphaned localStorage keys after profile deletion (audit M2)** — `_deleteJournalProfileById` does not purge scoped keys (`apiTradesCache_*`, `outcomeValueMapping_*`, etc.).

- [ ] **Share view — bottom summary strip** — `WIN DAYS / LOSS DAYS / NO TRADES / BEST DAY / WORST DAY / AVG PER TRADE`. Visible in reference designs, not yet implemented.

- [ ] **Yellow border highlighting on clicked cells** — P&L Calendar, Pair × Session, Monthly P&L in Hover mode.

- [ ] **Security migration — repo privé + hosting Cloudflare Pages + minification bundle (Niveau 1+2)** — surfaced 2026-05-20. Aujourd'hui le repo est public sur GitHub → un voleur fait `git clone + deploy` en 5 min. Plan en 2 étapes faisables en ~3 h en une session : (a) passer le repo en privé + migrer le hosting GH Pages → Cloudflare Pages (free tier, accepte repos privés via GitHub Actions auto-deploy) ; (b) ajouter un step `npx terser dashboard.js -o dashboard.min.js -c -m` dans le CI, `index.html` pointe sur `.min.js` en prod, source reste lisible en local. Workflow dev inchangé (édition locale + refresh). Bénéfice : passe de "clone trivial en 5 min" à "reverse-engineer pénible d'un bundle minifié". **Timing libre — pas urgent aujourd'hui** (audience = 600 community members distribuée par Max, pas viral, pas monétisé, pas indexée). À faire **avant** un des 4 triggers : (1) lancement public visible (Twitter / YouTube / Product Hunt), (2) monétisation, (3) lancement MT5 (élargit l'audience), (4) repost externe non contrôlé. Niveaux 3 (auth gate via Supabase Auth) et 4 (secret sauce côté serveur via Edge Functions) restent réservés au pivot SaaS payant — sur-engineering tant que freemium community.

- [ ] **Add LICENSE file (MIT recommended)** — currently the public repo has no license, so legally all rights reserved by default. Not urgent since users don't access GitHub directly, but worth adding for legal clarity.

- [ ] **Add README.md** — public-facing landing page on GitHub. Low priority since users access the dashboard via GitHub Pages link, not the repo itself. Would matter if/when the repo becomes a public-facing project (contributions, forks, etc.).

- [ ] **Auto-rename session on `/end` — feature request to revisit if Anthropic ships the hook** — investigated 2026-05-21 via the claude-code-guide agent: programmatic auto-rename of the current Claude Code session is **not possible today**. (1) No CLI command like `claude session rename <id> "<title>"` exists. (2) Stop hooks have no access to the agent's final message text — they only receive tool execution events. (3) Claude Code does not parse OSC codes or any agent-emitted marker that would trigger a rename. The current pattern (agent proposes `Tu peux remplacer le titre par : …` line, user copy-pastes into `/rename`) is the only reliable path. Revisit if Anthropic ever exposes (a) a CLI/IPC endpoint for session-rename, (b) enriches the `Stop` hook payload with the agent's final turn, or (c) introduces a marker convention agents can emit. Until then, status quo.

---

## Done — Last 10

- [x] **Share-view PC follow-up — lightbox stays clear of trade cards + no nav-arrow pile-up during screenshot loading** (2026-06-23) — Deux régressions sorties du live test PC du fix mobile de la veille (commits `5eb0a03` puis `cf57c41`). **Bug 1 — les 4 flèches de nav s'empilent au centre pendant le chargement d'un nouveau screenshot** : entre deux navs, `_showLightbox` faisait `img.removeAttribute('src')` pour reset l'état. Conséquence : l'`<img>` collapse à 0×0 le temps que le nouveau src se charge, et comme `.img-lightbox-stage` est `display: inline-block` sized to image, le stage collapse aussi à 0×0 → les 4 flèches `position: absolute` au coins du stage s'écrasent toutes à un même point central de l'écran (visuellement « 4 flèches qui pointent vers l'intérieur »). **Fix** : ne plus toucher `img.src` au reset — le navigateur garde la vieille image visible jusqu'à ce que la nouvelle finisse de loader ; **belt-and-braces** : classe `.is-loading` ajoutée au stage avant le src swap, retirée dans `img.onload`/`img.onerror` ; CSS cache les flèches quand `.is-loading` (couvre le cas fallback → image où `<img>` était bien display:none et flasherait quand même 0×0). **Bug 2 — la lightbox déborde sur les trade cards à droite côté PC** : régression ciblée par Max sur un PC dont la fenêtre Chrome n'était pas maximisée → viewport tombait dans la plage 881-1200 que j'avais ajoutée la veille comme breakpoint « fullscreen lightbox sur tablettes ». **Double cause** : (a) ce breakpoint forçait `inset: 0` (fullscreen) au lieu de l'`inset: 0 340px 0 0` de base qui réserve les 340px du drawer ; (b) `.img-lightbox-img` avait `max-width: 96vw` (viewport-relatif) → 1056px alors que `.img-lightbox-stage` est limité à 96% de l'overlay → 729px sur écran 1100, donc l'image débordait le stage de ~327px **dans la zone du drawer**. **Fix** : (a) suppression du breakpoint 881-1200 — la lightbox suit maintenant strictement le grid layout (≤880 = mobile single-column → fullscreen ; >880 = drawer side-by-side → inset 340px à droite respecté) ; (b) `max-width: 100%` sur l'image (relatif au stage) au lieu de `96vw` (relatif au viewport) → image inhérite proprement du cap du stage. **Vérifié en preview headless** : à 1100×800 — image 730 × 412, ends at x=745, drawer libre à partir de x=760 (`imgFitsInOverlay: true`, `imgOverlapsDrawer: false`) ; à 1440×900 — image 1056, ends at x=1078, drawer libre à partir de x=1100, **AUDUSD/GBPNZD/EURUSD lisibles avec leurs noms complets** ; mobile 375 inchangé (overlay full viewport, image 360×206). 0 erreur console. **Validé live PC par Max** (le rapport du bug, donc rendu final à reconfirmer côté tel/iPad si jamais). Cache `share.css ?v=15→18` · `share.js ?v=16→17` · `share.html` 3 bumps.

- [x] **Share-view mobile lightbox — fullscreen + arc-shaped zones + active card snap-to-top + camera CTA** (2026-06-22) — Itération multi-étapes pilotée par les feedbacks live de Max sur iPhone, 3 commits (`aa3ef1a` → `f6deaad` → `7b6121c`). **Bug initial** : sur mobile la lightbox `inset: 0 340px 0 0` réservait 340px à droite pour le drawer desktop → sur 375px de large, l'overlay ne faisait que ~35px et l'image était invisible. **Fix 1 — fullscreen lightbox** (commit `aa3ef1a`) : breakpoint mobile (≤880) + breakpoint tablette (881-1200) → `inset: 0` + `max-width: 96vw`. Le drawer reste à droite uniquement au-dessus de 1200px (vrais grands écrans). **Fix 2 — count visibility + 4 zones** (commit `f6deaad`) : `.img-lightbox-count` opacity .45 → .85, color #fff, font-size aligné table-body ; 4 boutons directionnels flottants (↑↓ trade prev/next, ←→ step prev/next) + zones triangulaires via diagonales sur toute l'image ; auto-hide quand 1 seul trade ou 1 seul step ; suppression des ‹ › redondants du sel bar. **Fix 3 — refinement complet** (commit `7b6121c`) : (a) **flèches déplacées ON l'image** via wrapper `.img-lightbox-stage` (inline-block, sized to image) — avant elles étaient dans le backdrop noir, à côté de l'image ; (b) **flèches discrètes** : 28×28, fond transparent, juste glyphe + text-shadow `0 1px 4px rgba(0,0,0,.85)` (avant 44×44 avec fond `rgba(0,0,0,.55)` et border) ; (c) **zones cliquables en half-ellipse** au lieu de rectangulaires — chaque direction est une demi-ellipse ancrée à son bord, apex à 25% au milieu, tapering à 0 aux coins (formule `(nx/0.25)² + ((ny-0.5)/0.5)² ≤ 1`), centre + corners pockets dead zones, tiebreaker = bord le plus proche ; (d) **icône 📷 64px** ajoutée à la watermark au-dessus du « Click a camera icon … » avec `pulse-camera` animation (mobile + desktop) ; (e) **active card snap-to-top** : `target.scrollIntoView({block:'start', behavior:'auto'})` à chaque `_navOpen` ; padding-bottom 100vh sur drawer-body via classe `share-lightbox-open` toggle sur body → même la dernière trade card peut être au sommet du viewport, avec du vide en dessous ; (f) **fix scroll mobile bloqué** : html/body passent de `height: 100% + overflow: hidden` (base desktop) → `height: auto + overflow: auto/visible` sur mobile, sinon le `document.scrollingElement` (HTML) ne pouvait pas scroller car son contenu (body) avait la même hauteur fixe que lui — `scrollIntoView` était silencieusement no-op. **Vérifié en preview headless aux 3 breakpoints** (375 mobile / 1024 tablette portrait / 1440 laptop) : overlay full vs drawer-side OK, half-ellipse zones (centre dead ✓, 30% dead ✓, 5%/15% bord = nav ✓, mid-edges = nav ✓), camera 64px visible mobile + desktop, scroll-to-top fonctionne pour card 0/1/10 (last). 0 erreur console. **Validé live par Max** sur iPhone. Cache `share.css ?v=7→15` · `share.js ?v=10→16` · `share.html` 3 bumps.

- [x] **Data Setup affiche la propriété Notion source sur chaque ligne mappée + enquête « Optimal RR faux » (faux bug)** (2026-06-16) — **Départ** : un membre (profil `jp_exejcl6b`, 204 trades) trouvait le widget Optimal RR « faux » — top bar +391.5R / 64.7% WR vs panneau Optimal 2.4R → +163.2R / 52.9%. **Enquête sur sa vraie donnée** (lecture via le proxy Notion + ses réglages synchronisés Supabase, avec son accord) : **aucun bug** — `presetLiveFilters_v1_jp_exejcl6b = {}` → il n'a **jamais activé la bulle 2.4R** ; la barre montre sa perf réelle (il laisse courir ses gagnants), le panneau est un **contrefactuel « TP fixe unique, clôture 100% »** (plafonne les gagnants + compte toute sortie sous le TP comme perte pleine en Strict Loss → forcément < perf réelle). Convergence du code re-vérifiée en preview (clic bulle → la barre plafonne bien à ~163R). **Faux « bug rrMax » écarté** : `rrMax` n'utilise pas `_pick` dans `_normalizeAPITrade`, MAIS le bridge `_applyMappedFieldAliases` ([dashboard.js:8489](dashboard.js:8489), appelé avant normalize dans tous les chemins) copie `t[override] → t.rrMax` — l'override fonctionne en prod ; fix tenté puis **annulé** (redondant, cassait le design établi). Ma repro initiale appelait `_normalizeAPITrade` en direct, sans le bridge → faux négatif. **Livré** : chaque ligne mappée du panneau Data Setup affiche désormais `(field: <colonne Notion>)` — override explicite (= ce que le membre a relié) sinon **colonne source auto-résolue** depuis le raw cache (nouveau `_DIM_SOURCE_CANDIDATES` + `_resolveDimSourceColumn`, réutilise `_resolveNotionAlias` — zéro duplication des chaînes de `_normalizeAPITrade`). Avant : seules les ~6 dims remappées montraient la colonne ; désormais ~14 (auto-détectées incluses). **Vérifié live sur la vraie donnée H4 du membre** (panneau réel rendu : `Position Result` / `RR TP 1` / `Pair` / `Order` / `BE Management` auto-détectées ; `H4 Confirmation / Continuation` / `Type` / `RR max` / `Day` remappées ; `__NO_MAPPING__` → rien). `node -c` OK, accolades delta 0, 0 erreur console. Cache JS `?v=1092 → 1094`.

- [x] **Exclusion via widget Setup Performance (mode détail) non reflétée dans Custom Filters** (2026-06-14) — Bug signalé par Max : en mode détail du widget Setup Performance, un clic-droit « Exclure » sur une barre filtrait bien les données mais ne marquait **pas** la valeur comme exclue dans la propriété custom correspondante (« M15 Type Detailed ») du panneau Custom Filters. **Cause racine** : le menu contextuel des barres (`_BAR_CONTAINER_TO_CHIP` ~[dashboard.js:32934](dashboard.js:32934)) routait **toujours** vers le chip natif `setup`, codé en dur et aveugle au mode détail. Le chip natif filtre bien via le dual-filter `[t.setup, t.setupDetail]` (~[dashboard.js:15393](dashboard.js:15393)), d'où l'illusion « ça marche à moitié » — mais la propriété custom déclarée par l'utilisateur sur la même colonne Notion restait neutre. Le widget Setup intégré était le **seul** resté sur l'ancien système ; les widgets custom ont déjà `_cwResolveChipKey` (~[dashboard.js:17324](dashboard.js:17324)) qui sait router vers `custom:<key>`. **Diagnostic appuyé sur la donnée live de Max** (lecture seule via Chrome) : `setupDetail === extras["M15 Type Detailed"]` sur 697/697 trades, propriété custom `m15TypeDetailed` déclarée, et chip natif `setup` déjà pollué par des valeurs détail (`NEM type A/B`). **Fix (3 edits)** : (1) `_setupDetailChipKey()` — en mode détail, route vers le chip de la propriété custom **uniquement si** une propriété déclarée porte prouvablement les valeurs détail (vérif `extras[src] === t.setupDetail` sur un échantillon), sinon retombe sur natif `setup` ; réutilise `_cwResolveChipKey`. (2) `_showBarContextMenu` passe par `_resolveChipEntry` (gère `custom:<key>`) + alloue l'entrée custom si pas encore matérialisée (prop déclarée mid-session). (3) `_refreshAllChipDisplays()` après mutation → pastilles natives **et** custom à jour immédiatement. **Vérifié en preview** (scénario fidèle : colonne injectée dans extras + propriété custom déclarée via `addCustomProp` + **vrai** clic-droit dispatché sur une `.bar-row`) : mode détail → chip custom (`excluded:["NEM type A"]`, panneau `chip excluded`, dataset 100→99, **chip natif intact**) ; mode normal → chip natif `setup`, custom inchangé (zéro cross-contamination) ; `node -c` OK, brace delta 0, 0 erreur/warning console. Rendu final live à confirmer par Max. **Résiduel tracké** (nouveau TODO Active) : les valeurs détail déjà bloquées dans le chip natif d'un membre ne sont pas migrées (le fix ne touche pas l'état existant). Cache JS `?v=1088 → 1092`.

- [x] **Check « Supabase Preview » rouge sur chaque commit GitHub — intégration GitHub Supabase désactivée** (2026-06-14) — Le repo affichait un ❌ « Supabase Preview / Failing after 6s » sur chaque commit. **Cause** : l'intégration GitHub de Supabase (Settings → Integrations) comparait l'historique de migrations distant (10 migrations appliquées via MCP/dashboard) au dossier local `supabase/migrations/` (1 seul fichier, mal horodaté) → « Remote migration versions not found in local migrations directory ». Le Branching n'était même pas actif (Pro-gated, plan Free) — c'est le validateur de l'intégration qui postait le check. **Action** (dashboard Supabase, via Chrome MCP, choix validé avec Max : désactiver) : bouton « Disable integration » sur la carte GitHub Integration → plus aucun check Supabase sur les futurs push, intégration Vercel intacte, zéro changement de schéma. La croix figée sur le commit historique `4b48854` (résultat de check non effaçable rétroactivement) a été chassée par un **commit vide `0f25de5`** → HEAD repassé tout vert (build/deploy/report-build-status). **Si passage Pro un jour** pour les vraies preview branches : réconcilier `supabase/migrations/` avec le distant (option GitOps) avant de reconnecter. Aucun changement de code repo (action purement config/dashboard).

- [x] **Grouped filters (multi-dim AND'ed) + Unified Filters hub refactor** (2026-06-12) — Gros chantier multi-itérations, validé étape par étape avec Max. Spec : [docs/grouped-filters.md](docs/grouped-filters.md). **(A) Système de filtres groupés** — nouvel état `appState.filters.groupedFilters[]` (séparé des `comboFilters` heatmap, contrairement à ce que la TODO suggérait — leur shape est trop figée). Un groupe = conditions AND'ées ; plusieurs groupes du même mode OR'és ; include/exclude (exclude wins, include OR). Évaluateur `_applyGroupedFiltersToTrade` branché en queue de `filterChips` (~14393) + miroir dans `_filterTradesBySnapshot`. Persistance per-preset dans `presetLiveFilters_v1` + snapshot `flipping_preset_snapshots_v2` (sémantique override `null`/`[]`/`[…]`), hooks Update/Reset/Clear-all. Opérateurs : `is`/`is not` (select/checkbox), `has any`/`has none`/`has all` (multiselect), `≥`/`≤`/`between` (number), + **dim Date** avec comparateurs ISO (string). **Builder drawer** (pattern Theme editor) + **pastilles sidebar** (rouge exclude / vert include / grisé draft+disabled). **(B) Améliorations groupes** : compteur d'impact + **mini-stats edge par groupe** (compte · Total R · Win Rate · Avg R, couleurs alignées sur les seuils KPI via `_kpiTooltipColor` — WR/AvgR 6 paliers, Total R signe seul) ; **toggle activer/désactiver** (`enabled`, skip eval, pastille grisée) ; **dupliquer** ; header de carte sur **2 lignes** (titre / contrôles+stats). **(C) Refonte Hub Filters unifié** (mockup validé avant code) — le volet gauche regroupe désormais tout le filtrage en **3 cartes-accordéons repliables** (fermées par défaut, état persisté `filtersZoneCollapsed_v1`, badge d'actifs par zone) : **Grouped → Standard → Custom**. La création de propriétés custom est **rapatriée de Data Setup vers un drawer « Custom properties »** (bouton « + New property » dans la zone Custom) ; l'**onglet « Filters » de Data Setup est supprimé** (ne reste que Mapping + Create Widget). `_applyNativeFilterOrder`/`_persistFilterOrderFromDOM`/`_initFilterSortables` re-pointés sur le corps de la zone Standard. **(D) 2 bugs corrigés en fin de session** : (1) pastille de divergence absente pour un groupe non sauvegardé — `_getLiveFilterCountForPreset` ne comptait pas `groupedFilters` + `_gfPersistAndRender` n'appelait pas `_refreshChipDerivedUI` ; (2) Clear laissait le groupe actif — désormais Clear **garde** les groupes mais **masque** (enabled=false) les non sauvegardés, les sauvegardés restaurent leur état. **Vérifié intensivement en preview** (évaluateur tous opérateurs, round-trips preset/reload, persistance, ordre des zones, création custom end-to-end, les 2 fixes) ; 0 erreur console, `node -c` OK. Le screenshot du preview headless timeout systématiquement → **rendu visuel final à confirmer live par Max**. Cache JS `?v=1028 → 1088` · CSS `?v=315 → 329`.

- [x] **Ordre d'affichage des screenshots configurable par profil (Data Setup → reorder ▲▼)** (2026-06-11) — Demande Max (remontée membre) : l'ordre des 3 screenshots était **codé en dur** (`h4Before → m15Before → m15After`) sur tous les sites de rendu, donc imposé — un membre ayant remappé ses propriétés custom ne pouvait pas le changer. **Implémentation centralisée** autour d'un helper unique `_getScreenshotStepOrder()` ([dashboard.js](dashboard.js), près de `_LB_STEPS`) : ordre stocké **par profil** (`getProfileScopedKey('screenshotOrder_v1')`) et **synchronisé** cross-device (préfixe `screenshotOrder_v1_` ajouté à `_SYNC_KEY_PREFIXES`, comme le mapping). Sanitation stricte (déduplique + complète → renvoie toujours une permutation valide des 3 steps). **Appliqué partout** : les 3 sites de rendu des boutons (trade-row drawer `_selBtn`, widget drawer `createTradeMediaButton`, PDF `_wdBtn` — chacun via un map step→html ré-ordonné), la **navigation clavier ◄►** de la lightbox (`_lbGetStepOrder`), et l'**export PDF** (les `.shots` + le sélecteur d'image, `SHOT_ORDER` baké au build). **UI** : contrôle **▲▼ compact** (pile verticale thémée) sur chaque ligne de la section Screenshots du mapping (`_renderMappingTableRow`, dim `tier:'screenshot'` only), désactivé aux extrémités ; le tri de la section suit l'ordre configuré ; dispatcher `screenshot-order-up/down` → `_moveScreenshotStep` + `updateJournalPanel()`. **Les libellés restent pilotés par le mapping utilisateur** (`_getTradeMediaStepLabel`) — seul l'ordre est configurable, jamais hardcodé. **Vérifié en preview** : round-trip ordre (défaut/move/persist/sanitation garbage), lightbox suit, ▲▼ rendus + désactivés aux bouts, capture du contrôle dans la charte. `node -c` OK, accolades delta 0, boot sans erreur. Cache JS `?v=994 → 1006` · CSS `?v=314 → 315`.

- [x] **Screenshots dupliqués (M15 Before ≡ M15 After) figés dans le stockage — fix backend `force` + bouton « Rebuild screenshots »** (2026-06-11) — Bug signalé par un membre : ses 3 boutons screenshots d'une trade card affichaient le même graphique, alors que son Notion a bien 3 images distinctes et que son Data Setup montre 3/3 mappés (216/216). **Diagnostic (données dures Supabase)** : 215 trades / 1577 ont ≥2 slots au **contenu strictement identique** (même eTag/MD5), motif dominant **m15_before ≡ m15_after** (214 cas). Cause racine : ces trades ont été migrés vers Supabase Storage sous un mapping initial incorrect (les dims `img_m15` / `img_m15_after` résolvaient vers la même source), gravant la **même image dans 2 fichiers aux noms distincts**. Le membre a ensuite corrigé son mapping, mais (a) le backend `media-sync` faisait `upsert:false` (**0/3612 objets jamais réécrits**) + une table de cache DB `notion_media_cache` vérifiée en premier, et (b) côté front `_preservePermanentImageUrls` réinjectait les anciennes URLs permanentes → **un Full resync ne pouvait jamais défaire l'état figé**. Le trade-exemple du Data Setup (`37c063b4`) était sain → d'où l'illusion « tout est OK ». **Fix backend** ([notion-dashboard-api-2](https://github.com/LeMarsuOff/notion-dashboard-api-2), déployé prod + source sauvegardée dans un nouveau repo privé — elle n'existait que dans le déploiement Vercel) : flag `force` additif sur `media-sync` → bypass cache DB + existence storage + `upsert:true`. Rétrocompatible à 100 % (comportement strictement inchangé sans le flag). Vérifié live : `/api/ping` 200, sans force = hit cache inchangé, avec force = bypass confirmé. **Fix front** (`dashboard.js`) : bouton **« Rebuild screenshots »** dans le menu `…` du profil (profils Notion only, à côté de Full resync). Au clic : resync Notion avec `_screenshotRebuildForce` → `_preservePermanentImageUrls` sauté (URLs Notion fraîches), la media queue vide son `_done` et ré-enqueue **tous** les slots taggés `force:true`, `_processBatch` envoie `force:true` au backend + appose un cache-buster (`?cb=<stamp>`) sur l'URL patchée pour forcer le refetch navigateur, et un nouveau `_mediaQueue.onDrain` re-render (applyPreset) quand tous les uploads ont atterri (toast « Screenshots rebuilt »). Un clic, zéro manip technique pour le membre ; les ~30 autres membres concernés se réparent via le même bouton. **Suivi même session (3 itérations)** : (a) la confirmation est passée du `confirm()` natif → modal thémé → **morph inline « Rebuild? Yes No »** dans le menu profil (même pattern que « Delete source », demandé par Max « petit popup sous le bouton ») ; (b) le flag de rebuild, d'abord global, a été **scopé au profil ciblé** (`_screenshotRebuildProfileId` + `_isRebuildingProfile`) — un sync parallèle d'un autre profil n'hérite plus du `force` ; (c) **fix « screenshots vides si reload en plein rebuild »** (remonté par un membre : « no values detected » + « 1 LOCKED ») — le rebuild saute la préservation donc les champs portent des URLs Notion fraîches que le serializer stripait à `''` → un reload mid-rebuild perdait tout ; corrigé en persistant l'**URL Supabase déterministe** (`<id>_<slot>.png`) pendant un rebuild au lieu de striper. Données Supabase confirmées intactes (objets + `notion_media_cache`) — récup membre = Full resync. `node -c` OK, accolades delta 0, boot preview sans erreur. Cache JS `?v=980 → 1008` · CSS `?v=314 → 315`.

- [x] **Partial Optimizer — internal sections spilled outside the widget; the tile now auto-grows to contain them** (2026-06-11) — Demande Max : en mode édition du PO, agrandir/descendre une section interne (ex. SAVED PRESETS) la faisait **sortir du widget** (coupée en bas). **Root cause** : le layout interne draggable agrandit son nombre de rangées (`--po-grid-rows = max(280, maxRow)`) quand une section dépasse, mais la **tuile externe GridStack gardait sa hauteur fixe** → le contenu débordait (mesuré : -586px sous la tuile). **Fix** ([dashboard.js:27903](dashboard.js:27903), dans `_poInternalApplyLayout`) : après le calcul de `maxRow`, si `maxRow > 280` (canevas par défaut), agrandir la tuile via `_grid.update(poItem, { h })` proportionnellement (ancrage registre : 280 rangées ↔ gs-h 236 → `neededH = ceil(236 * maxRow / 280)`). **Grow-only** : ne rétrécit jamais auto (ne contrarie pas un resize manuel du widget). No-op gracieux si le widget n'est pas géré par le moteur (`typeof _grid` + `poItem` gardés). **Vérifié proprement** en preview en ré-intégrant le widget au moteur GridStack (= cas réel) : section poussée à 359 rangées → gs-h 236→303, tuile 2360→3030px, section **ne déborde plus** (3009 < 3030) ; réduire la section → gs-h reste 303 (grow-only) ; layout défaut → reste 236 (pas de grow parasite). `node -c` OK, brace delta 0, 0 erreur console. Moteur draggable intact. Cache JS `?v=968 → ~970`.

- [x] **Partial Optimizer — stat-tile labels/subtitles truncated on narrow widths + Ranking "Model" column squeezed** (2026-06-11) — Demande Max (deux fixes minimes, CSS-only). **(1) Tuiles best-stats** : `.po-block-bandeau .po-stat-lbl` (titre) et `.po-stat-name` (sous-titre) étaient en `white-space: nowrap` + ellipsis → tronquaient en « BEST T… » / « 1R/2.4R … » sur petit écran. Passés en `white-space: normal` (wrap au mot) → libellés/sous-titres entièrement lisibles (1 ligne en largeur normale, 2-3 lignes en étroit). La valeur numérique reste `nowrap`. **(2) Ranking colonne Model** : `.po-ranking td.po-row-name` avait le trick `max-width: 1px` + nowrap + ellipsis qui la comprimait → passé à `overflow: visible; max-width: none` → la colonne se dimensionne au contenu, nom de modèle entier. Vérifié en preview (clone aux largeurs 600/900px : `clip:false` sur les 6 tuiles). `dashboard.js` **non touché** (moteur draggable intact). Cache CSS `?v=308 → ~328`. **Contexte important** (cf. DECISIONS.md) : une refonte plus large du widget (remplacer la grille interne draggable par un layout fixe responsive) a été **tentée puis entièrement annulée** dans la même session — Max a confirmé préférer le système draggable où il redimensionne/organise chaque section. Le draggable reste la direction validée ; ne pas re-tenter de le remplacer.

---

## Update rules for agents

At the end of every coding session:
- Move completed items → Done (keep last 10 max, oldest fall off)
- Add any new TODO discovered during the session (with context)
- Update "In Progress" if work is mid-flight
- Commit `ROADMAP.md` in the same commit as the related code change
