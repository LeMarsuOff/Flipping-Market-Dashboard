# ROADMAP.md — Flipping Market Dashboard

> Single source of truth for task tracking. Updated by agents at the end of every session.
> Supersedes `docs/current-roadmap.md` (kept for historical reference only).

---

## In Progress

_(none)_

---

## TODO — Active

- [ ] **🔥 F&M screenshot pipeline still broken after refresh — TOP PRIORITY next session** (surfaced 2026-05-23, Max explicit: "Le problème n'est pas du tout réparé"). 4 shipped fixes this session didn't resolve the user-visible symptom: (a) `_notionId` fallback chain ([dashboard.js:9456](dashboard.js:9456)) — Max's diagnostic confirmed all 595 trades now have `_notionId` populated; (b) backend `/api/notion/media-sync` confirmed working (17+ batches, 200 OK, 583/595 trades patched with Supabase URLs in memory per diagnostic `memSupabase: 583`); (c) Reapply preservation of screenshot overrides + safety guard + `_notionId` preservation in `_reapplyAPIOverrides` ([dashboard.js:7028, 7048](dashboard.js)) — verified live; (d) F&M-preferred auto-detect + multi-trade merged sample. **Yet post-refresh Max's LS parsed cache is `null`** (`lsParsed_format: null` in his diagnostic) while sessionStorage media has 583 Supabase URLs (`sessSupabase: 583`). The merge-on-boot path SHOULD restore screenshots but doesn't. **Open hypotheses for next session**: (1) The migration's debounced `setCachedAPIData` (line 9021) never writes to LS — maybe the 2MB guard rejects because Supabase URLs are too long (595 × ~200 chars × 3 fields ≈ 360 KB — should fit but worth re-verifying with payload size logs); (2) `_isNotionFileUrl` matches Supabase URLs incorrectly (e.g. if Supabase exposes raw `amazonaws.com` URLs through S3 backend — need a sample of Max's actual Supabase URL); (3) Active profile cache context wrong at time of debounced write so it lands at a different key than where boot reads; (4) `_invalidateNormalizedTradesCache` fires AFTER migration completes (via some override write triggered downstream) and wipes the just-written LS; (5) Session media merge logic uses `_notionId` keys but boot path's trades have empty `_notionId` (post-strip serialize/deserialize cycle losing the id). **Repro path for next session**: refresh page → click Full resync → wait 2 min → run diagnostic (have it print sample Supabase URL + LS parsed sample if any + check `_invalidateNormalizedTradesCache` log fires) → refresh → re-run diagnostic. Cache `?v=503`.

- [ ] **Migrate remaining ~26 inline `connectionType === 'notion'` checks to `INTEGRATIONS` registry** — Strangler Fig follow-up to the 2026-05-19 registry refactor. One micro-fix done at [dashboard.js:5336](dashboard.js:5336) (`_reloadJournalProfileSelection` `isNotionWithDb` now uses `getIntegration(p).isReady(p)`). Rest deferred until MT5 ships — abstracting from a single concrete implementation risks getting the methods wrong.

- [ ] **Profile/trade mismatch bug — diagnostic in place, awaiting next occurrence** — keep at the bottom: actionable only when Max next reproduces the bug live (he'll bring up the console payload). Symptom: profile row highlighted but dashboard shows trades from another profile (observed 1-2 times by Max, not reliably reproducible). Defenses in place (`wasActiveAtStart`, `stillActive()`, `_journalProfileCacheContextOverride`) didn't fully explain how. Diagnostic added 2026-05-20 at 3 strategic points: `_injectTrades` tags `appState.trades.__diagSourceProfileId`; `_diagAssertProfileTradeAlignment` ([dashboard.js:7118](dashboard.js)) fires `console.error('[ProfileTradeMismatch]', ...)` with full state if active profile ≠ tagged profile. When Max next reproduces, console payload captures: callSite, active vs tagged ids, override at inject, dsMode, label, age, inject stack. **2026-05-22 update**: the post-fetch override race in `_loadNotionTrades` (smoking gun for the cross-profile mapping corruption reproduced this session) has been fixed via the post-await re-pin + explicit `profile` param to `_maybeAutoApplyTemplateMapping`. If the trade-mismatch symptom recurs after this fix, the cause is something else — keep watching.

---

## TODO — Deferred

- [ ] **`gs_layout_active` + `gs_layout_<preset>` twin-write** — surfaced 2026-05-20 during Bloc 4. `_saveActiveSlotLive` ([dashboard.js:35937](dashboard.js)) writes the same ~13.5KB payload to two LS keys on every drag/resize (autosave slot + per-preset slot). Semantically different (autosave vs preset-bound state) so global dedup wrap can't help. 27 KB/drag is inherent cost. Reducing requires either: (a) lazy-write per-preset slot only on preset switch (risk of crash-loss), (b) ref `dynamicWidgets` separately to dedupe between slots (major refactor of preset/layout architecture). Touch when preset architecture is next visited.

- [ ] **`_saveActiveSlotLive` over-fires on drags without layout change** — surfaced 2026-05-20 during Bloc 4. ~50% of `gs_layout_*` writes are dedup'd by the LS wrap → meaning the GridStack `resizestop dragstop` handler fires from drag operations that didn't actually move/resize anything (snap-back, micro-clicks). The dedup wrap absorbs the IO cost but JSON.stringify of 13.5 KB still runs. Investigate whether GridStack emits these events spuriously or whether a `_syncGridToLive` no-op check would short-circuit cleanly. Low priority since dedup absorbs the storage cost.

- [ ] **Retention policy design — to spec before any server-side trade storage ships (e.g. MT5)** — today Supabase only holds metadata (~100KB/user) + media; trades live in localStorage so an abandoned user costs nothing. The moment MT5 EA pushes trades to a server endpoint, abandoned accounts start accumulating real volume (~6–30 MB/user). Direction agreed during the 2026-05-19 session: (a) disconnect integration = soft (token revoked, metadata/trades retained), (b) profile manually deleted = soft → hard at 30 days, (c) user inactive 6 months = warning email, archive at 7 months, hard-delete at 12 months, (d) "Delete my account" = soft with 30-day grace period. Implementation: Supabase `pg_cron` or scheduled edge function, runs weekly. Add `last_active_at` column to user/profile rows when the schema is touched next.

- [ ] **MT5 / MT4 / cTrader — actual implementation** — registry stubs (`available: false`) exist since 2026-05-19. Next steps before code: (1) decide MetaApi.cloud SaaS bridge vs. self-built MQL5 EA (cost vs. control); (2) extend Vercel proxy or move to Supabase edge functions for per-broker secret handling (current static-page architecture can't hold per-user API keys safely); (3) spike one platform end-to-end on a single test account before promising to community. cTrader is the easiest technically (OAuth Open API) but smaller audience; MT5 covers most of Max's prop-firm users.

- [ ] **Backend OAuth: dynamic frontend redirect** — `notion-dashboard-api-2` Vercel function uses `DASHBOARD_URL` env var hardcoded to a single front URL (`https://lemarsuoff.github.io/Flipping-Market-Dashboard/`). Conséquence : impossible de tester l'OAuth Notion en local sans flipper la variable (et casser la prod). Fix : à `/api/notion/oauth/start`, lire l'`Origin`/`Referer` de la requête, valider contre une allowlist (`github.io/Flipping-Market-Dashboard/`, `localhost:*`), encoder dans le `state` OAuth, puis y rediriger au callback au lieu de toujours utiliser `DASHBOARD_URL`. Garder `DASHBOARD_URL` comme fallback. Nécessite accès au repo backend `notion-dashboard-api-2` (pas sur cette machine). Contexte complet dans [DECISIONS.md](DECISIONS.md) § 2026-05-19 — Vercel `DASHBOARD_URL` is single-tenant prod.

- [ ] **[Post-readonly] Trading Journal evolution** — manual trade entry, custom schemas, Notion two-way sync. Spec complète dans `docs/journal-evolution.md`. **Ne pas démarrer avant que le read-only soit clos.** Bloquants identifiés : XSS fix ✅ (2026-05-19), Ghost trades ✅ (2026-05-19), Mappings cross-device sync (audit M1) — restent listés dans le doc §9.

- [ ] **Drawdown Intelligence section** — DD by context (setup/session/pair decomposing max DD), Recovery Factor (Total R / |Max DD|), DD Duration distribution, Risk of Ruin estimate. Optional: Underwater curve, Streak/DD scatter, MAR/Calmar (~14 months of data, low priority). Deferred pending community priority signal.

- [ ] **Hardcoded font-sizes (~180 remaining)** — deferred pending community feedback on typography editor coverage.

- [ ] **BYOT + export/import config** — open the dashboard to all users without requiring their Notion credentials. Recommended step before full OAuth rollout. ~1.5 days dev vs weeks for OAuth. Deferred until post-Notion overhaul stabilization.

- [ ] **Mappings not synced cross-device (audit M1)** — `outcomeValueMapping_v1_<profileId>`, `apiFieldOverrides_v1_<profileId>_m15`, `apiFieldNames_v1_<profileId>` absent from `_SYNC_KEYS`. Users reconfigure from scratch on each machine.

- [ ] **Orphaned localStorage keys after profile deletion (audit M2)** — `_deleteJournalProfileById` does not purge scoped keys (`apiTradesCache_*`, `outcomeValueMapping_*`, etc.).

- [ ] **Share view — bottom summary strip** — `WIN DAYS / LOSS DAYS / NO TRADES / BEST DAY / WORST DAY / AVG PER TRADE`. Visible in reference designs, not yet implemented.

- [ ] **Yellow border highlighting on clicked cells** — P&L Calendar, Pair × Session, Monthly P&L in Hover mode.

- [ ] **Security migration — repo privé + hosting Cloudflare Pages + minification bundle (Niveau 1+2)** — surfaced 2026-05-20. Aujourd'hui le repo est public sur GitHub → un voleur fait `git clone + deploy` en 5 min. Plan en 2 étapes faisables en ~3 h en une session : (a) passer le repo en privé + migrer le hosting GH Pages → Cloudflare Pages (free tier, accepte repos privés via GitHub Actions auto-deploy) ; (b) ajouter un step `npx terser dashboard.js -o dashboard.min.js -c -m` dans le CI, `index.html` pointe sur `.min.js` en prod, source reste lisible en local. Workflow dev inchangé (édition locale + refresh). Bénéfice : passe de "clone trivial en 5 min" à "reverse-engineer pénible d'un bundle minifié". **Timing libre — pas urgent aujourd'hui** (audience = 600 community members distribuée par Max, pas viral, pas monétisé, pas indexée). À faire **avant** un des 4 triggers : (1) lancement public visible (Twitter / YouTube / Product Hunt), (2) monétisation, (3) lancement MT5 (élargit l'audience), (4) repost externe non contrôlé. Niveaux 3 (auth gate via Supabase Auth) et 4 (secret sauce côté serveur via Edge Functions) restent réservés au pivot SaaS payant — sur-engineering tant que freemium community.

- [ ] **Add LICENSE file (MIT recommended)** — currently the public repo has no license, so legally all rights reserved by default. Not urgent since users don't access GitHub directly, but worth adding for legal clarity.

- [ ] **Add README.md** — public-facing landing page on GitHub. Low priority since users access the dashboard via GitHub Pages link, not the repo itself. Would matter if/when the repo becomes a public-facing project (contributions, forks, etc.).

- [ ] **Auto-rename session on `/end` — feature request to revisit if Anthropic ships the hook** — investigated 2026-05-21 via the claude-code-guide agent: programmatic auto-rename of the current Claude Code session is **not possible today**. (1) No CLI command like `claude session rename <id> "<title>"` exists. (2) Stop hooks have no access to the agent's final message text — they only receive tool execution events. (3) Claude Code does not parse OSC codes or any agent-emitted marker that would trigger a rename. The current pattern (agent proposes `Tu peux remplacer le titre par : …` line, user copy-pastes into `/rename`) is the only reliable path. Revisit if Anthropic ever exposes (a) a CLI/IPC endpoint for session-rename, (b) enriches the `Stop` hook payload with the agent's final turn, or (c) introduces a marker convention agents can emit. Until then, status quo.

---

## Done — Last 10

- [x] **Re-apply template defaults — preserve screenshot overrides + `_notionId` + safety guard** (2026-05-23) — Surfaced after the `_notionId` fallback shipped: Max's H4 Perso ended up with empty dashboard after Reapply + refresh, and a second Reapply crashed localhost. Three nested bugs in `_reapplyTemplateMapping` and `_reapplyAPIOverrides`. **(1) Reapply wiped screenshot overrides** ([dashboard.js:7048](dashboard.js:7048)) — `_saveAPIFieldOverrides({})` cleared everything, then `_maybeAutoApplyTemplateMapping` rewrote template defaults (`'URL H4 Before'` / `'URL H4 After'`) that don't exist in Max's custom DB. The user had to manually pencil-pick `'H4 Before'` / `'H4 After'` (F&M properties) again every time. Fix: snapshot `img_m15` / `img_h4_before` / `img_m15_after` overrides + their auto-mapped flags BEFORE the wipe, restore them AFTER the template apply so they win the merge. Net effect: every NON-screenshot dim resets to template baseline; screenshots stay as the user / previous auto-detect resolved them. **(2) `_reapplyAPIOverrides` stripped `_notionId`** ([dashboard.js:7028](dashboard.js:7028)) — `_normalizeAPITrade` doesn't include `_notionId` in its return object (intentional: `_notionId` is added externally by the caller). The Reapply re-normalize loop wasn't doing that addition, so every Reapply wiped `_notionId` from cached trades. Without `_notionId` the media queue can't match trades to their Supabase migrations. Fix: same fallback chain as the sync path at [dashboard.js:9456](dashboard.js:9456): `t._notionId || t.notionId || t.id || ''`. **(3) Reapply without raw cache produced empty dashboard** — Post-refresh, raw cache is wiped (no sessionStorage persistence by design). `_saveAPIFieldOverrides({})` triggered `_invalidateNormalizedTradesCache` purging LS + session parsed caches BEFORE checking raw. `_reapplyAPIOverrides` then early-returned because no raw → no refill → empty dashboard. A second Reapply on top of this state crashed localhost (`_reapplyAPIOverrides` couldn't produce anything to render, downstream code blew up). Fix: safety guard at the top of `_reapplyTemplateMapping` — if `_getRawAPICache()` is empty, toast "Click Sync first — Re-apply needs fresh data from Notion." and return WITHOUT wiping overrides or invalidating caches. **Verified live**: (a) safety guard fires when raw empty, all overrides preserved; (b) Reapply with raw + existing screenshot mapping → non-screenshots reset to template (outcome → `'Position Result'`, pair → `'Pair '`), screenshots preserved (`'H4 Before'` / `'H4 After'` / `__NO_MAPPING__`), auto-mapped flags kept. Cache `?v=503`.

- [x] **F&M screenshot pipeline — `_notionId` fallback chain (root cause of "screenshots disappear after refresh" on custom Notion DBs)** (2026-05-23) — Surfaced when Max ran a diagnostic on his H4 Perso profile post-fix and found `_notionId: ""` on all 595 cached trades + console warning `[MediaQueue] missing notionId on 595/595 trades`. Root cause traced through the full pipeline: (1) Sync produces trades with signed Notion S3 URLs in memory (~1h expiry); (2) `_serializeTradesForStorage` ([dashboard.js:7764](dashboard.js:7764)) intentionally STRIPS Notion URLs when writing to LS cache (`return (val && !_isNotionFileUrl(val)) ? val : ''`) because (a) they expire and (b) the payload would exceed the 2MB LS guard with 595 trades × 6 img fields × ~1700 chars (~6.7 MB); (3) `_mediaQueue` is designed to upload Notion URLs to permanent Supabase Storage via `POST /api/notion/media-sync`, keyed by `_notionId` per trade; (4) On refresh `_mergeMediaCache` rehydrates trades with Supabase URLs from the separate media cache LS slot. **The broken link** — at [dashboard.js:9448](dashboard.js:9448), the post-normalize loop only read `t._notionId` from the raw Vercel proxy response: `if (norm && t._notionId) norm._notionId = t._notionId`. Max's proxy version (and apparently others) exposes the Notion page UUID under `t.id` instead — used at [dashboard.js:7155](dashboard.js:7155) to construct `notionUrl` from the same field. With `t._notionId` undefined, `norm._notionId` stayed unset, `_mediaQueue.enqueue` skipped every trade (`if (!trade._notionId) continue`), the media cache stayed empty, and on every refresh the parsed LS cache had `imgM15: ''` / `imgH4Before: ''` / `imgM15After: ''` with nothing to rehydrate from. M15 profiles using TV `/x/` URLs were unaffected because `_normalizeTvImageUrl` converts them to permanent `https://s3.tradingview.com/snapshots/...` PNGs that pass the `_isNotionFileUrl` check and survive LS serialization. **Fix** — fallback chain `const notionId = t._notionId || t.notionId || t.id || ''` at [dashboard.js:9456](dashboard.js:9456). Captures the Notion UUID regardless of which field the proxy version exposes. **User action required** for existing Notion profiles: the cache holds 595 trades with `_notionId: ""`; incremental sync only re-processes NEW trades. Users must click **"Full resync"** (profile row ⋯ menu, `_handleNotionProfileFullResync` → `force:true`) once per profile to re-fetch the full DB and populate `_notionId` from `t.id`. After that, the media queue will migrate all signed URLs to Supabase in background batches of 50, and subsequent refreshes hydrate screenshots from the permanent media cache. **Verified**: no console errors after deploy, syntax clean. Cache `?v=501`. Also surfaced (separate issue, not fixed): Max's H4 Perso profile has `type: 'm15'` despite the H4-themed journal — `_getJournalProfilePreferredSource` returns 'm15' → HTF stays 'm15' on this profile. Doesn't directly cause the screenshot bug (override-based normalization is HTF-independent for screenshots) but worth fixing later (allow `type` edit on existing profiles, or infer from DB title at sync time).

- [x] **Screenshot auto-detect — multi-trade merged sample (fixes "H4 Before works but H4 After doesn't" on partial F&M coverage)** (2026-05-22) — Surfaced when Max reported that on his H4 perso profile, auto-detect correctly swapped `URL H4 Before` → `H4 Before` (F&M) but left `URL H4 After` stuck on the stale template default with "no value detected", even though manually picking `H4 After` worked. Root cause: `_findScreenshotCandidateByName` was scanning a SINGLE sample (the first trade in raw cache). If that trade happened to have a Before file but no After file yet (very common — users don't always take an after-screenshot on every trade), `_classifyScreenshotValue` returned null for the empty `H4 After` array → no F&M candidate found → my F&M-prefers logic fell through to the camel-alias fallback or kept the stale URL override. **Fix** — new `_buildScreenshotMergedSample(rawTrades)` helper ([dashboard.js:6804](dashboard.js:6804)) walks every raw trade and records the FIRST non-empty screenshot-classifiable value seen per property name. The auto-detect now sees the H4 After file from whichever later trade happened to have one. Pass 2 backfills empty-but-present keys from the first trade so existence checks (used by stale-override validation) still recognise empty-but-present columns. Replaces the single `sample = rawTrades.find(...)` line at the top of `_autoMapScreenshotsByRawKeys`. **Verified live**: seeded H4 template defaults + 3 fake trades where trade 1 has Before only, trade 2 has both, trade 3 has After only → both slots correctly auto-swapped to F&M (`H4 Before` and `H4 After`), `__NO_MAPPING__` preserved. Idempotent — Re-apply no longer regresses the After slot. Cache `?v=500`.

- [x] **Screenshot pipeline post-refresh persistence — boot-time auto-detect + one-shot parsed-cache migration** (2026-05-22) — Surfaced when Max hard-refreshed after the F&M-preferred fix and saw screenshots disappear again. Root cause was the cache-hit boot path: `_reloadJournalProfileSelection` either calls `_reapplyAPIOverrides()` (re-normalises from raw → uses new `_rawUrl`) when raw cache survives in sessionStorage, OR falls through to `getCachedAPIData()` + `_injectTrades()` which uses the parsed LS cache as-is. The parsed cache was written before the `_rawUrl` fix landed, so `imgM15`/`imgH4Before`/`imgM15After` are empty strings — and the cache-hit path never re-normalises. Two-pronged fix: (1) Boot path at [dashboard.js:5610](dashboard.js:5610) now runs `_autoMapScreenshotsByRawKeys(raw, _getCurrentHTFSource())` BEFORE `_reapplyAPIOverrides()` whenever raw is available, so stale URL template overrides get swapped to live F&M overrides on every page reload — Max's H4 Perso (custom DB with F&M files) gets corrected without needing a Sync. (2) One-shot LS migration `_runScreenshotPipelineV2Migration()` ([dashboard.js:33421](dashboard.js:33421)) wired into `window.load` purges parsed-trades caches for every Notion profile exactly once per machine (guarded by `flipping_screenshot_pipeline_v2_migration` flag, never re-fires). Forces the next boot for every Notion profile to either re-normalise from raw or fall through to `loadFromAPI({force:true})` — both paths use the new code. No data loss: raw caches untouched, profile metadata untouched. **Verified live**: migration purges stale parsed cache on first call, idempotent on second call (flag respected). Cache `?v=499`.

- [x] **Screenshot pipeline rewrite — F&M-preferred auto-detect + `_rawUrl` nested-shape fix (supports the 3 user personas)** (2026-05-22) — Surfaced when Max tested H4 After fix on his `Flipping Market H4` Notion journal: auto-detect picked the `URL H4` text property instead of the Files & Media property holding the actual screenshot. Root cause was a stack of bugs that explain why custom-DB H4 users were silently broken. **(1)** `_rawUrl` ([dashboard.js:7104](dashboard.js:7104)) assumed `[{url}]` shape and fell back to `String(raw[0])` = `'[object Object]'` when fed Notion F&M's nested `[{name, file: {url, expiry_time}}]`. Fix: handle `item.file.url` and `item.external.url` explicitly so Notion-hosted files extract cleanly. **(2)** `_autoMapScreenshotsByRawKeys` ([dashboard.js:6804](dashboard.js:6804)) walked camel aliases only — no preference between F&M and URL properties when both existed for the same slot. Custom DBs (like Max's) that have both `URL H4` (text URL) and `H4 Before` (F&M file) got the URL picked because the proxy may surface either via camel aliases. Fix: new 2-stage decision per dim. Stage 1 = name-keyword pass (`_findScreenshotCandidateByName` + `_classifyScreenshotValue` + `_getScreenshotDimHints`) scans sample properties, classifies as F&M/URL/none, filters by primary keyword (m15/h4) + disqualifies on negative position (before/after), scores F&M (100) >> URL (10). Stage 2 = legacy camel-alias fallback for DBs whose property names don't carry m15/h4+before/after keywords. F&M wins over any URL override or stale override; live URL override kept only when no F&M candidate exists. **(3)** `_autoMapScreenshotsByRawKeys` now takes `source` (m15/h4) so `img_m15_after` correctly biases toward H4-named properties on H4 profiles. Threaded from both call sites (`_loadNotionTrades` at [dashboard.js:9390](dashboard.js:9390), `_reapplyTemplateMapping` at [dashboard.js:7027](dashboard.js:7027)). **Three user personas now work end-to-end**: (a) **Canonical Flipping template** — text URLs in `URL <slot>` properties, TV `/x/` links → `_normalizeTvImageUrl` converts to permanent `https://s3.tradingview.com/snapshots/...png`; (b) **Custom DB with Files & Media** — Notion-hosted signed S3 URLs (~1h expiry) → `_rawUrl` extracts the URL, `_normalizeTvImageUrl` passes through unchanged, `_mediaQueue` detects via `_isNotionFileUrl()` and migrates to Supabase Storage in 50-trade batches (permanent); (c) **Custom DB with URL property containing TV link** — same as persona (a). **Verified live** in preview: H4 profile with both `URL H4` (TV string) + `H4 Before` (F&M file) → F&M wins, auto-set includes `img_h4_before`; nested F&M URL extracted cleanly (no more `'[object Object]'`); TV URL still converted to S3 PNG snapshot; `__NO_MAPPING__` preserved. Max can fix his existing H4 profiles by clicking **Re-apply template defaults** (raw cache in memory suffices), or wait for next sync.

- [x] **H4 After screenshot "no values detected" — stale template default detection + camel-alias expansion** (2026-05-22) — Surfaced by Max during the same session: on H4 profiles, the H4 After dim was auto-mapped (green dot, `'URL H4 After'` shown as override) but the Mapping panel reported "no values detected" — yet manually picking the column made values appear. **Root cause** — two issues stacked: (1) The H4 Pro / H4 Beg templates ship `img_m15_after: 'URL H4 After'` as the canonical mapping, but Max's actual Notion DBs have the column named differently (e.g. `'H4 After'` without the URL prefix). `_maybeAutoApplyTemplateMapping` writes the template default blindly, and `_autoMapScreenshotsByRawKeys` then **skips any dim that already has an override**, even when that override points to a non-existent column. The stale override survives forever until manual pencil-pick. (2) `_SCREENSHOT_CAMEL_ALIASES.img_m15_after` only listed M15-flavored aliases (`imgM15After`, `m15After`, `m15AfterImage`) — so even if the validation logic ran, the auto-detect would fail on H4 profiles because the Notion proxy's camel alias for `'H4 After'` is `h4After` / `imgH4After`, not `m15After`. **Fix** — (a) `_SCREENSHOT_CAMEL_ALIASES.img_m15_after` ([dashboard.js:6716](dashboard.js:6716)) now lists M15 variants first AND H4 variants after (`imgH4After`, `h4After`, `h4AfterImage`, `imageH4After`, `h4ImageAfter`). `find()` short-circuits on first match, so M15 profiles still pick the M15 variant and H4 profiles transparently pick H4. (b) `_autoMapScreenshotsByRawKeys` ([dashboard.js:6736](dashboard.js:6736)) gains a `_overrideIsLiveInSample` helper that checks whether the existing override name corresponds to a real raw-trade key with a non-empty value. The skip condition becomes: skip ONLY if override is `__NO_MAPPING__` (intentional opt-out) OR the override points to a real column with data. Stale overrides fall through to the auto-detect, which finds the real column via camel alias and replaces the dead override silently. DebugLog reports the swap with `"(replaced stale override: <old>)"`. (c) `_reapplyTemplateMapping` ([dashboard.js:6920](dashboard.js:6920)) now also runs `_autoMapScreenshotsByRawKeys` against the in-memory raw cache after writing template defaults — so the user can fix existing profiles in one click without needing a full Notion sync. Skips gracefully when raw cache is unavailable (e.g. after a fresh tab refresh). **Verified live** — seeded an LS override `img_m15_after: 'URL H4 After'` (h4-pro default) + fake raw trade with `'H4 After'` column + `imgH4After`/`h4After` camel aliases (mirroring Max's real data). Helper detected stale override, found the camel alias with data, walked back to the original column name `'H4 After'`, replaced the override, tagged the dim auto-mapped. `__NO_MAPPING__` dims preserved, valid overrides untouched.

- [x] **Fix `_loadNotionTrades` post-fetch override race (cross-profile mapping corruption + spurious cache purges + re-sync-on-switch)** (2026-05-22) — Diagnosed and fixed the smoking gun behind 4 symptoms Max reproduced: (a) M15 perso "n'avait plus de data" after H4→M15 switch, (b) mapping changes appearing on M15 (especially screenshot dims), (c) hero counter incoherence "16/16 mapped" → Re-apply template defaults → "14/14" (different denominator), (d) automatic re-sync triggered on every profile switch even though cache should suffice. **Root cause** — `_loadNotionTrades` pins `_journalProfileCacheContextOverride = profileId` BEFORE the network `await`, but a concurrent `_applyActiveJournalProfile` (user clicking another profile during the ~2s fetch) steals the override. Post-fetch synchronous processing then runs with the wrong override pinned: `_maybeAutoApplyTemplateMapping` writes H4 mappings to M15's LS slot (`apiFieldOverrides_v1_<M15id>`), and `_invalidateNormalizedTradesCache` (triggered transitively via `_saveAPIFieldOverrides`) purges M15's parsed cache. Independently, `_maybeAutoApplyTemplateMapping` was reading `getActiveJournalProfile()` directly for `templateChoice` + `notionDatabaseTitle` — same race window, same wrong-profile result. **Reproduced empirically** via mock-fetch in the preview: pinned override = H4.id, stole to M15.id mid-flight, resolved fetch with H4-shaped data → before fix, H4 overrides landed in M15 slot AND M15 parsed cache was purged. After fix, overrides land in H4 slot, M15 cache survives untouched. **Fix** — (1) Right after `await resp.json()` in `_loadNotionTrades` ([dashboard.js:9152](dashboard.js:9152)), re-pin `_journalProfileCacheContextOverride = profileId` so all subsequent synchronous LS reads/writes hit the correct slot regardless of what concurrent flows did during the await. Safe because JS is single-threaded — the post-fetch block runs atomically until the next `setTimeout(0)` (which only schedules background work, no further override-dependent ops). The existing outer `finally` still restores `_prevCacheCtx` as before. (2) `_maybeAutoApplyTemplateMapping` ([dashboard.js:6577](dashboard.js:6577)) now accepts an explicit `profileOverride` param; `_loadNotionTrades` passes its local `profile` so the helper reads `templateChoice` + `notionDatabaseTitle` off the profile we're actually syncing, not the currently-active one. User-driven callers (`_reapplyTemplateMapping`) skip the param — `getActiveJournalProfile()` is the truly-correct source for those paths since no async race is possible. **Bonus** — closes the suspected race vector hint in the Profile/trade-mismatch TODO; if the symptom recurs after this fix, the cause is something else.

- [x] **Per-profile Notion sync mutex** (2026-05-22) — Closes the TODO Active surfaced 2026-05-21. A 2nd `_loadNotionTrades` call targeting a profile already syncing now returns immediately with a debugLog instead of firing a redundant parallel fetch. Cross-profile concurrent syncs stay allowed (per-profile `Set`, not a single boolean) — caches are profile-scoped so they can't corrupt each other, and serializing them would force the user to wait on a slow profile before they can sync a fast one. **Implementation** — module-scoped `_notionSyncInFlight = new Set()` ([dashboard.js:8935](dashboard.js:8935)) holding profile ids currently in flight. Duplicate-skip check at [dashboard.js:9039](dashboard.js:9039), placed AFTER the cache-hit path (a duplicate that would have been a cache-hit still gets to re-render — idempotent and good UX) but BEFORE any state mutation (cache-context pin, loading flag, syncState write), so the skip is a clean return with nothing to undo. Mutex acquired just before the network try-block at [dashboard.js:9069](dashboard.js:9069), released in the matching inner finally at [dashboard.js:9350](dashboard.js:9350) so every exit path (success / error / await race / `wasActiveAtStart` short-circuit) clears it. **Pattern mirrors** the existing `_otherSourceFetching` boolean used by `_fetchOtherSourceBackground` ([dashboard.js:9355](dashboard.js)) — same shape, but a Set instead of a boolean because we gate per-profile not globally. **Verified live**: spied `_notionSyncInFlight.add` and called `_loadNotionTrades` with a profile id pre-seeded into the Set → confirmed only 1 add attempt (the manual seed); the 2nd call returned before reaching the acquire line. Mutex did not interfere with normal dashboard load (no console errors after reload).

- [x] **Parsed-trades cache auto-invalidation on mapping change** (2026-05-22) — Closes the TODO Active surfaced 2026-05-21. Root architectural fix: every mapping change (template auto-mapping, manual pencil-pick, Re-apply template defaults, TP1/2/3 backfill, screenshot auto-map, database change) now invalidates `apiTradesCache_v2_rrmax_<profile>_<src>` so the stale-normalization symptom (e.g. `h4: []` while rawTrade has `H4 Obstacles: [{name:"..."}]`) cannot persist. **Implementation** — single hook in `_saveAPIFieldOverrides` ([dashboard.js:6157](dashboard.js:6157)) catches the 5 mapping-write paths since they all funnel through this canonical setter (lines 6607, 6620, 6697, 6812, 30624). Helper `_invalidateNormalizedTradesCache(profileId?, reason)` ([dashboard.js:6173](dashboard.js:6173)) wipes both HTF slots (m15 + h4) across LS + sessionStorage + `_parsedAPICacheMemory`. Plus a direct call in `_clearNotionProfileMappings` ([dashboard.js:4758](dashboard.js:4758)) for the DB-change path which bypasses `_saveAPIFieldOverrides` by deleting the override LS key directly. **Re-render contract** — invalidation is fire-and-forget; the caller is responsible for refreshing the cache: `pickAPIField` / `_reapplyTemplateMapping` call `_reapplyAPIOverrides()` synchronously after, `_loadNotionTrades` re-normalizes in the same flow. If neither runs (e.g. user closes the tab between an override write and any re-render), the next boot's `getCachedAPIData()` returns `null` and the boot path falls through to `loadFromAPI({force:true})` — the desired self-heal. **Verified live**: helper exists in scope, simulated wipe across LS + session + memory slots works, `_saveAPIFieldOverrides` triggers `_invalidateNormalizedTradesCache(null, 'overrides-saved')` confirmed via spy.

- [x] **DS session: custom Notion props profile-scoped + hero stats auto-update + CSV integration card (Integrations tab) + auto-hide `__NO_MAPPING__` chip groups + JOURNAL_DIMS.desc dead-data cleanup + CSV overlay re-routed + Health-tab refs removed + Download Notion Template menu + CSV card refactored (description + ⋯ menu + "File connected" badge)** (2026-05-22) — Long session spanning bug fixes + 2 active TODOs closed (Template auto-mapping b/c + CSV access path) + UX polish. **(1) Custom Notion properties were leaking across all profiles** — root cause: `CUSTOM_PROPS_LS_KEY = 'flipping_notion_properties'` was a single global key shared by all profiles. Fix: route load/save through `getProfileScopedKey(CUSTOM_PROPS_LS_KEY)` ([dashboard.js:980, 994](dashboard.js)) — same pattern already used for `CUSTOM_WIDGETS_LS_KEY`. Cache invalidation hook added to `_reloadProfileScopedState` ([dashboard.js:7586](dashboard.js)) for the in-memory `_customPropsCache`, plus auto-rerender `_renderNotionPropsPanel` + `_updateDataSetupHero` so the open panel refreshes on profile switch. `_deleteJournalProfileById` purges the scoped slot (`flipping_notion_properties_<id>` and `flipping_custom_widgets_<id>` — bonus close on audit M2 for these two keys). One-shot boot migration `_runCustomPropsProfileScopeMigration` purges the legacy global key under flag `flipping_notion_properties_profile_migration_v1` — per Max's call ("reset les tous"), no copy of legacy payload, all profiles start empty. **(2) Hero stats (Custom fields / Widgets) stayed stale when mutated with the Data Hub open** — the `dsh-stat-custom` / `dsh-stat-widgets` counters read `loadCustomProps()` / `_loadCustomWidgetDefs()` via `_updateDataSetupHero` but that was only called on `toggleDataHub`. Fix: hook `_updateDataSetupHero()` inside `_saveCustomWidgetDefs` ([dashboard.js:712](dashboard.js)) and `_saveCustomProps` ([dashboard.js:994](dashboard.js)) — both are the single persistence funnels, so add/update/delete all flow through them. **(3) Auto-hide `.filter-group` parents when the dim is mapped to `__NO_MAPPING__`** (Option A from b decision, mapping-driven rather than count-driven) — new helper `_isDimNoMapping(dimKey)` ([dashboard.js:28347](dashboard.js)) + map `_FILTER_KEY_TO_DIM_KEY` for the `h4obs` → `h4` legacy chip naming mismatch. `buildChips` ([dashboard.js:11288](dashboard.js)) hides the closest `.filter-group` parent when the resolved override is `__NO_MAPPING__`. Hooks automatically through the existing render path (`renderDashboardChrome` → `buildChips`) — any pickAPIField / Reset / Re-apply template re-renders and refreshes visibility. Custom chips (`custom:*` filterKeys) skipped — outside the JOURNAL_DIMS system. **(4) JOURNAL_DIMS[].desc dead-data deleted** (Option Delete from c decision) — 23 `.desc` strings stripped via single PowerShell regex pass, ~3 KB saved. The doc-comment above `JOURNAL_DIMS` updated to drop its dead `desc` reference. Confirmed pre-strip: `.desc` access count was 0. **(5) CSV virtual integration card** — initially shipped in the Profiles list (alongside Demo, per the "similar to Demo row" wording in the original TODO); after Max flagged he expected it in **Integrations**, moved to the second card via new `_renderCsvIntegrationCard()` ([dashboard.js:4131](dashboard.js)) wired into `_renderIntegrationsSection`. **(6) Smart 3-state CSV button handler** (`journal-profile-row-switch-csv` ([dashboard.js:2197](dashboard.js)) — (a) active+cached → reopen overlay (replace), (b) cached+inactive → `setDataSource('csv')` switch, (c) no cache → open overlay (first import). Smart fallback for the case (a) closes a latent bug: on desktop with CSV active there was NO way to replace the file (empty-state CTA only renders when no data, mobile import button is mobile-only, and switching away purges the cache via `setDataSource(mode!=='csv')`). **(7) CSV `Import CSV file` opens the guide overlay rather than skipping straight to the OS file picker** — the existing `#csv-overlay` ([index.html:156-384](index.html)) had the full property reference + drop-box but was being bypassed by `data-action="open-csv-overlay"` for legacy mob/empty-state buttons. The new card's button routes through `openCsvOverlay()` so the user sees the property guide before picking a file. Legacy `open-csv-overlay` action kept as direct-picker shortcut for mob-import-btn + empty-state-cta (unchanged). **(8) Removed 3 stale "Health tab" references** in property-naming callouts across `#csv-overlay`, `#empty-state`, `.lgp-callout-host` ([index.html:176, 416, 757](index.html)). Health tab was merged into Mapping (Phase 2) — text was misleading. **(9) New `INTEGRATION_ICONS.csv` entry + `NOTION_TEMPLATES_URL` constant + "Download Notion Template" menu item in Notion ⋯ menu** ([dashboard.js:4960-4965](dashboard.js)) — opens the Notion templates hub in a new tab via `window.open(URL, '_blank', 'noopener,noreferrer')`. Wired the same URL to a new template CTA at the top of the CSV guide (`#csv-overlay` + `.lgp-callout-host`, green-tinted via new `.csv-fh-template-cta` CSS rule). Single constant means future link changes touch one spot. **(10) CSV card refactor** per Max's spec — empty state shows NO badge (was "○ No file"), cached state shows single "🟢 File connected" badge (was "🟢 Imported / Active" pair), button moved INTO a ⋯ manage menu (new `_csvIntegrationManageOpen` state + `_toggleCsvIntegrationManageMenu` mutually exclusive with Notion's via cross-close, both outside-click listeners scoped to their own card via `.notion-integration-card .int-manage-wrap` / `.csv-integration-card .int-manage-wrap` selectors to prevent click cross-contamination), description footer added (mirror MT5 pattern). **(11) Profile/trade mismatch bug moved to the BOTTOM of Active** per Max's request — actionable only when he next reproduces the bug live with console payload in hand. **Cache final**: JS `?v=477`, CSS `?v=225`. **Files touched**: 4 modified + 1 new asset (`assets/logos/csv.png`).

---

## Update rules for agents

At the end of every coding session:
- Move completed items → Done (keep last 10 max, oldest fall off)
- Add any new TODO discovered during the session (with context)
- Update "In Progress" if work is mid-flight
- Commit `ROADMAP.md` in the same commit as the related code change
