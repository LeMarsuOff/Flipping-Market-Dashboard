# ROADMAP.md — Flipping Market Dashboard

> Single source of truth for task tracking. Updated by agents at the end of every session.
> Supersedes `docs/current-roadmap.md` (kept for historical reference only).

---

## In Progress

_(none)_

---

## TODO — Active

- [ ] **Concurrent Positions widget — v2 follow-ups restants** (noté 2026-06-29, MAJ 2026-06-29 après la v3) — (1) ~~frise heat navigable~~ **fait** (mode Overall scrollable, axe mois) ; (3) ~~rollup global multi-mois~~ **fait** (mode Overall = tous les widgets agrègent l'historique). **Restant** : (2) **granularité minute** (heure de sortie) — toujours jour-level car le modèle stocke `date`/`exitDate` en `YYYY-MM-DD` ; (4) **teinte du pic par corrélation des paires** ouvertes (3 paires USD-corrélées = un gros pari déguisé, pas une vraie diversification). Idées v3 nées du mockup ChatGPT non retenues : KPIs supplémentaires (Longest position, Total positions, Won/Loss/Open split). Non urgent.

- [ ] **Concurrent Positions — comportement du clic sur une barre Gantt** (proposé 2026-06-29, à trancher avec Max) — Aujourd'hui un clic sur une barre ouvre le drawer du **trade unique** (`_expoOpenTrade`). Alternative proposée : ouvrir **tous les trades ouverts en même temps que lui** ce jour-là (contexte d'exposition simultanée). Demander à Max lequel est le plus utile avant de changer. NB : depuis la v3, un clic sur un **label de paire** (mode By pair) ouvre déjà le drawer listant toutes les positions de cette paire sur la période (`_expoOpenPair`), et un clic sur une colonne en mode Bars ouvre le drawer du jour — donc plusieurs points d'entrée drawer existent déjà.

- [ ] **Migrate remaining ~26 inline `connectionType === 'notion'` checks to `INTEGRATIONS` registry** — Strangler Fig follow-up to the 2026-05-19 registry refactor. One micro-fix done at [dashboard.js:5336](dashboard.js:5336) (`_reloadJournalProfileSelection` `isNotionWithDb` now uses `getIntegration(p).isReady(p)`). Rest deferred until MT5 ships — abstracting from a single concrete implementation risks getting the methods wrong.

- [ ] **Extend `.claude/hooks/post-edit.sh` to bump `share.html` / `share.js` / `share.css` cache-bust** — Today the hook only handles `dashboard.css` / `dashboard.js`. Manual `?v=N` bumps in `share.html` per edit during Phase 2/3 worked but are error-prone. Mirror the existing `dashboard.js` branch with a `share.js` branch (no `node -c` validation needed since share.js is small + parsed live in Preview) and same for `share.css`.

- [ ] **Install `pg_cron` extension + schedule weekly purge of expired shares** — Spec'd in [docs/share-link-feature.md §7](docs/share-link-feature.md). RLS already hides expired rows (`expires_at > now()` SELECT policy) so this is pure garbage-collection. Run `select cron.schedule('shares-purge-expired', '0 3 * * 0', $$delete from public.shares where expires_at < now() - interval '1 day';$$)` after enabling the extension via Supabase dashboard. Defer until row count justifies it (~10k shares).

- [ ] **Journal Evolution P1 — Custom props on existing trades** — next phase after P0. Scope : UI "Add custom property" (name, type, validation) dans le Data Setup → écrit `user_schemas` ; rendu inline des custom props dans la trade card du drawer ; edit-in-place (click → input → save → upsert `trade_overlays` via `tradeId`) ; enrichissement de `_normalizeAPITrade` avec les overlays au read. Estimé : 3–4 sem focalisées. **Bloquant aucun** — l'infra Supabase est en place depuis 2026-06-09 (P0). Spec : [docs/journal-evolution.md](docs/journal-evolution.md) §4.

- [ ] **Journal Evolution P2 — Native trade CRUD** — phase suivante après P1 : form de saisie complet (date, pair, setup, RR, screenshots upload), édition inline, soft-delete + UI Corbeille. À ce moment-là le dashboard est vraiment autonome (Notion devient optionnel). Estimé : 4–6 sem. Spec : [docs/journal-evolution.md](docs/journal-evolution.md) §4. **Ne pas démarrer avant P1 stabilisé** (les overlays valident le pattern jsonb sans risquer des données natives).

- [ ] **Journal Evolution P0 follow-up — Retention cron body** — `pg_cron` extension pas encore installée (TODO Active séparé : "Install pg_cron extension"). Une fois installée, écrire le body du cron weekly (cf. [docs/journal-evolution.md](docs/journal-evolution.md) §10 retention policy) : hard-delete profiles `deleted_at < now() - 30 days`, warning email à 180 j d'inactivité, soft-delete à 365 j. Sans le cron, la retention reste passive (le schéma supporte l'état, rien ne GC automatiquement). Pas urgent — tant que la base n'a pas de comptes inactifs, no-op.

- [ ] **Journal Evolution P0 follow-up — Server-side quotas (trades_native + storage)** — déclaré dans spec (10 000 trades/user, 500 Mo media/user) mais pas enforcé en BDD. Implémenter via trigger BEFORE INSERT sur `trades_native` (count check) + edge function de pré-signature pour les uploads Storage. Pas urgent tant que P2 n'a pas démarré (aucun trade natif possible aujourd'hui).

- [ ] **Preset id remap migration — repair presets already stuck at an out-of-range id (≥ PRESET_MAX)** — follow-up au fix `_nextPresetId` du 2026-06-11. Le fix empêche tout NOUVEAU preset de dépasser la plage `[0, PRESET_MAX)`, mais un membre qui a déjà aujourd'hui un preset bloqué à un id ≥ 10 (via cycles création/suppression sous l'ancien allocateur `maxId+1`) garde ce preset **vierge** (son snapshot/override n'a jamais été persisté) tant qu'il ne le supprime + recrée pas manuellement. Migration optionnelle au boot (après les loaders) : pour chaque preset `p.id >= PRESET_MAX` ou `< 0`, trouver le plus petit id libre `< PRESET_MAX`, migrer les slots runtime survivants (le live slot survit — `loadPresetLiveFilters` le charge par clé string ; le snapshot/override sont déjà perdus → migrent en blank), réassigner `p.id`, mettre à jour `appState.presets.activeId` si concerné, re-persister. **Proposé à Max le 2026-06-11, pas tranché** — plus sensible (touche les ids + la persistance de l'active id + dropdowns compare) donc demander confirmation avant. Le cas du reporter (Max) est déjà couvert par le seul fix `_nextPresetId` (ses 7 presets sont en plage 0..9).

- [ ] **Migration optionnelle des valeurs détail bloquées dans le chip natif `setup`** (noté 2026-06-14, suite du fix « exclusion mode détail → chip custom ») — Avant le fix de ce jour, les exclusions de valeurs setup-detail atterrissaient dans le chip natif `setup` (qui dual-filtre `[t.setup, t.setupDetail]` ~[dashboard.js:15393](dashboard.js:15393)). Les valeurs déjà présentes chez un membre (observé sur la donnée de Max : `NEM type A/B` dans `chips.setup.excluded`) continuent de filtrer mais restent dans le natif, **pas** dans la propriété custom → résiduel de désync pour l'ancien état. Le fix ne migre rien (ne touche pas l'état live existant, volontairement). Option : au boot, pour chaque valeur de `chips.setup.{excluded,included}` qui n'est **pas** une valeur de `t.setup` mais **est** une valeur de `t.setupDetail` ET dont la colonne est déclarée comme propriété custom, la déplacer vers le chip `custom:<key>`. Sensible (touche l'état de filtre live + persistance preset/snapshot) → demander confirmation avant. Contournement simple en attendant : le membre retire ces valeurs du chip Setup et les ré-exclut depuis le graphique (route désormais correctement).

- [ ] **PDF export — handle non-CORS (Notion-hosted) screenshots in the jsPDF download** — the trade-card PDF export (`_pdfBuildDoc`/`_exportTradesPdf`, 2026-06-09) embeds screenshots via html2canvas, which can only rasterise images whose host allows cross-origin reads. Supabase-public-bucket screenshots embed fine; a screenshot still hosted directly on Notion (upload to Supabase not yet done) comes out blank in the downloaded PDF. The in-tab preview still shows them (plain `<img>`, no CORS needed). Options to fix: (a) route Notion image URLs through the Vercel proxy with permissive CORS before capture, (b) force-migrate any pending screenshots to Supabase before allowing export, (c) fetch→dataURL each image with a CORS-allowed fallback. Low urgency — most synced profiles have migrated screenshots; flagged so it isn't mistaken for a bug.

---

## TODO — Deferred

- [ ] **Clarification UX du widget Optimal RR (contrefactuel vs perf réelle)** (noté 2026-06-16, proposé à Max non retenu cette session) — Un membre a confondu le « Cumul +163R / Optimal 2.4R » du panneau ORR avec sa perf réelle (+391R) affichée en haut, croyant à une incohérence/bug. En réalité le panneau est un **contrefactuel** : « si tu fermais 100% à un seul TP fixe » — il plafonne les gagnants et, en mode Strict Loss, compte toute sortie sous le TP comme perte pleine → **forcément < perf réelle** quand le trader laisse courir. Pas un bug, mais trompeur visuellement (gros « +163.20R » sous le « +391.5R » sans étiquette « hypothèse »). Proposer un sous-titre/tooltip explicite (ex. « contrefactuel : TP fixe unique fermé à 100%, distinct de tes sorties réelles ») pour couper court à la confusion chez les 600 membres. Max a préféré ne traiter que l'affichage de la colonne source en Data Setup cette session.

- [ ] **Revoir le système « Locked » et tout ce qu'il recouvre** (à reprendre un jour, noté 2026-06-11) — Aujourd'hui le compteur « LOCKED » du hero Data Setup ([dashboard.js:34065](dashboard.js:34065) `_updateDataSetupHero`) ne reflète **qu'une seule chose** : le mode **Personalised TP** (`appState.ui.tpConfig.mode === 'personalised'`) avec un mapping TP incomplet (`_getPersonalisedMappingStatus().complete === false`) → `lockedCount = 1`, pastille rouge `is-bad`, sinon masquée. C'est piloté par `FEATURE_REQS['personalised-tp']`. **À revoir** : (1) la sémantique exacte de `FEATURE_REQS` et quelles features devraient « locker » (aujourd'hui quasi rien d'autre que personalised-TP ; Multi-TP est explicitement exclu — cf. pitfall #12) ; (2) l'UX : un membre voit « 1 LOCKED » sans comprendre quoi est verrouillé ni comment le débloquer (pas de drill-down, pas de lien vers le mapping manquant) — surfacer *quelle* feature et *quel* champ manque ; (3) cohérence avec le ring de couverture + le badge « X to fix » de l'attention pipeline ; (4) décider si « Locked » doit rester un simple compteur ou devenir une liste actionnable. **Déclencheur** : revu quand un membre a remonté un « 1 LOCKED » incompris (2026-06-11). Pas urgent — informatif, ne bloque aucune donnée. Faire un audit complet de `FEATURE_REQS` + tous les sites qui lisent/écrivent « locked » avant de retoucher.

- [ ] **Grouped filters — quick-add depuis les widgets (clic-droit, option D)** (proposé 2026-06-12, non retenu cette session) — point d'entrée alternatif pour créer un groupe directement depuis un graphique : clic-droit sur une barre (Setup, Pair, Session…) → « Exclure ce setup en groupe » / « Inclure ». Seed un groupe pré-rempli avec la dim+valeur cliquée, ouvre le drawer pour affiner. Plus gros chantier (hook context-menu sur tous les renderers de barres + donut + heatmaps). Le système de groupes (drawer builder, opérateurs, mini-stats) est déjà en place — il ne manque que ce raccourci de création. À reprendre si Max veut accélérer la création depuis l'analyse.

- [ ] **MT5 / MT4 / cTrader — actual implementation** — **SPEC WRITTEN 2026-07-04 : [docs/mt5-integration.md](docs/mt5-integration.md)**. Décision d'archi tranchée avec Max : **bridge côté serveur via MetaApi.cloud** (pas l'EA) car la majorité des membres sont **mobile-only** → un EA (qui tourne dans le terminal MT5 desktop) ne les sert pas ; seule une connexion serveur↔broker via **mot de passe investisseur lecture seule** (validé par Max) fonctionne device-independent. EA MQL5 reclassé bonus Phase 2 (power-users desktop). Modèle économique laissé ouvert (§5) : socle technique identique que ce soit BYO-token gratuit (chaque membre 1 compte MetaApi gratuit) ou managé/premium payant — à trancher à l'exposition (P5). Schéma : 2 tables (`mt5_connections` sans secret, `trades_synced` générique) + réutilise `trade_overlays` (couche discrétionnaire SMC). Limite produit clé : le R n'est auto que si le trader pose ses SL sur le broker (§7.3). **Prochaine étape = P1 spike sur le FTMO de Max** (nécessite accès Supabase auth + compte MetaApi — non faisable en session non-interactive). **Chantier non démarré — Max ne veut pas le lancer pour l'instant (2026-07-04).** cTrader = chemin différent (OAuth Open API natif, pas de coût bridge), accueilli par la table générique.

- [ ] **Backend OAuth: dynamic frontend redirect** — `notion-dashboard-api-2` Vercel function uses `DASHBOARD_URL` env var hardcoded to a single front URL (`https://lemarsuoff.github.io/Flipping-Market-Dashboard/`). Conséquence : impossible de tester l'OAuth Notion en local sans flipper la variable (et casser la prod). Fix : à `/api/notion/oauth/start`, lire l'`Origin`/`Referer` de la requête, valider contre une allowlist (`github.io/Flipping-Market-Dashboard/`, `localhost:*`), encoder dans le `state` OAuth, puis y rediriger au callback au lieu de toujours utiliser `DASHBOARD_URL`. Garder `DASHBOARD_URL` comme fallback. Nécessite accès au repo backend `notion-dashboard-api-2` (pas sur cette machine). Contexte complet dans [DECISIONS.md](DECISIONS.md) § 2026-05-19 — Vercel `DASHBOARD_URL` is single-tenant prod.

- [ ] **Drawdown Intelligence section** — DD by context (setup/session/pair decomposing max DD), Recovery Factor (Total R / |Max DD|), DD Duration distribution, Risk of Ruin estimate. Optional: Underwater curve, Streak/DD scatter, MAR/Calmar (~14 months of data, low priority). Deferred pending community priority signal.

- [ ] **Hardcoded font-sizes (~180 remaining)** — deferred pending community feedback on typography editor coverage.

- [ ] **BYOT + export/import config** — open the dashboard to all users without requiring their Notion credentials. Recommended step before full OAuth rollout. ~1.5 days dev vs weeks for OAuth. Deferred until post-Notion overhaul stabilization.


- [ ] **Orphaned localStorage keys after profile deletion (audit M2)** — `_deleteJournalProfileById` does not purge scoped keys (`apiTradesCache_*`, `outcomeValueMapping_*`, etc.).

- [ ] **Share view — bottom summary strip** — `WIN DAYS / LOSS DAYS / NO TRADES / BEST DAY / WORST DAY / AVG PER TRADE`. Visible in reference designs, not yet implemented.

- [ ] **Yellow border highlighting on clicked cells** — P&L Calendar, Pair × Session, Monthly P&L in Hover mode.

- [ ] **Security migration — repo privé + hosting Cloudflare Pages + minification bundle (Niveau 1+2)** — surfaced 2026-05-20. Aujourd'hui le repo est public sur GitHub → un voleur fait `git clone + deploy` en 5 min. Plan en 2 étapes faisables en ~3 h en une session : (a) passer le repo en privé + migrer le hosting GH Pages → Cloudflare Pages (free tier, accepte repos privés via GitHub Actions auto-deploy) ; (b) ajouter un step `npx terser dashboard.js -o dashboard.min.js -c -m` dans le CI, `index.html` pointe sur `.min.js` en prod, source reste lisible en local. Workflow dev inchangé (édition locale + refresh). Bénéfice : passe de "clone trivial en 5 min" à "reverse-engineer pénible d'un bundle minifié". **Timing libre — pas urgent aujourd'hui** (audience = 600 community members distribuée par Max, pas viral, pas monétisé, pas indexée). À faire **avant** un des 4 triggers : (1) lancement public visible (Twitter / YouTube / Product Hunt), (2) monétisation, (3) lancement MT5 (élargit l'audience), (4) repost externe non contrôlé. Niveaux 3 (auth gate via Supabase Auth) et 4 (secret sauce côté serveur via Edge Functions) restent réservés au pivot SaaS payant — sur-engineering tant que freemium community.

- [ ] **Add LICENSE file (MIT recommended)** — currently the public repo has no license, so legally all rights reserved by default. Not urgent since users don't access GitHub directly, but worth adding for legal clarity.

- [ ] **Add README.md** — public-facing landing page on GitHub. Low priority since users access the dashboard via GitHub Pages link, not the repo itself. Would matter if/when the repo becomes a public-facing project (contributions, forks, etc.).

- [ ] **Auto-rename session on `/end` — feature request to revisit if Anthropic ships the hook** — investigated 2026-05-21 via the claude-code-guide agent: programmatic auto-rename of the current Claude Code session is **not possible today**. (1) No CLI command like `claude session rename <id> "<title>"` exists. (2) Stop hooks have no access to the agent's final message text — they only receive tool execution events. (3) Claude Code does not parse OSC codes or any agent-emitted marker that would trigger a rename. The current pattern (agent proposes `Tu peux remplacer le titre par : …` line, user copy-pastes into `/rename`) is the only reliable path. Revisit if Anthropic ever exposes (a) a CLI/IPC endpoint for session-rename, (b) enriches the `Stop` hook payload with the agent's final turn, or (c) introduces a marker convention agents can emit. Until then, status quo.

---

## Done — Last 10

- [x] **Lightbox callout — largeur de bulle par mesure exacte `getBBox` (2-pass render)** (2026-07-04) — La bulle des étiquettes callout (`_lbAnnShapeSvg`) était dimensionnée par **estimation au nombre de caractères** (`text.length * fs * 0.62 + 16`) → coupait les libellés à caractères larges (`WWWWWW` : estimé 52px / réel 85px) et gonflait ceux à caractères fins (`iiiiii` : estimé 52px / réel 23px). Fix : géométrie extraite dans un helper partagé `_lbAnnCalloutInner(s, W, H, textW)` (source unique) ; le callout est wrappé dans `<g data-callout-id>` et rendu au pass-1 avec l'ancienne estimation **comme fallback** ; nouvelle passe post-render `_lbAnnMeasureCallouts(svg, W, H)` (appelée après `svg.innerHTML` dans `_lbAnnRender`) mesure le `<text>` réel via `getBBox().width` et régénère la bulle à la largeur exacte. `try/catch` → dégradation propre sur l'estimation si `getBBox` échoue. Se re-mesure à chaque frappe (bulle grandit pile avec le texte). **Vérifié en preview headless** (fonctions définies, géométrie `bw = max(28,textW)+16`, écart estimation↔réel démontré sur 3 libellés). Round-trip callout réel sur vrai screenshot **à valider live par Max** (data démo sans screenshots). `node -c` OK, brace delta 0, cache JS auto-bumpé.

- [x] **`_monthlyDisplayMonths` — garde anti-boucle infinie sur date malformée** (2026-07-04) — La boucle `while (cur <= last)` de construction de l'axe l12m (~[dashboard.js:20666](dashboard.js:20666)) comparait des chaînes `YYYY-MM` sans borne : une clé malformée (date → NaN → `_monthlyAddMonths` non-incrémentant) pouvait la rendre infinie → `keys` gonfle → `RangeError` qui crashait toute la chaîne `render()`. Fix : cap `guard++ < 240` (l'axe fait ≤12 mois par design, 240 est généreux). Comportement normal inchangé (`slice(-12)` final préservé). `node -c` OK, brace delta 0. Chip de tâche de fond associé retiré (obsolète).

- [x] **Screenshot annotations — Phase 2 : sync Supabase cross-device (blob-sync, mirror `po_saved_partials_v1`)** (2026-07-04) — Les annotations (mire + calque vectoriel, `_lbAnn*`) passent du **local pur** au **sync cross-device**. Archi tranchée avec Max : **blob-sync** — miroir exact du pattern `po_saved_partials_v1` (2026-06-23) — plutôt que table dédiée `trade_annotations` (que la spec Phase 1 envisageait) → réutilise 100 % de la plomberie `_SYNC_KEYS`/Realtime existante, **zéro schéma Supabase** à toucher. **6 blocs** : (1) blob LS scopé **par profil×HTF** via `_htfKey` (`_lbAnnStoreKey()`) au lieu de la clé plate globale ; `_lbAnnStoreRead`/`_lbAnnPersist` repointés. (2) préfixe `'lightboxAnnotations_v1_'` ajouté à `_SYNC_KEY_PREFIXES` (→ éligible au sync `user_data` + capté par le reconcile de boot `migrateProfileScopedKeys`, 3e filet) + `'lightboxAnnotations_v1'` à `_HTF_KEYS`. (3) `_lbAnnSyncToCloud()` — force-push à chaque save contournant le drop de la fenêtre apply-remote (miroir `_poSyncSavedPartialsToCloud`). (4) branche Realtime `isAnnotationsKey` dans `_onUserData` → `_lbAnnOnRemoteSync()` recharge + re-render la lightbox **si ouverte** sur le screenshot touché (skip si dessin en cours, pour ne pas clobber). (5) migration one-shot `_migrateLbAnnotationsToScoped()` — blob plat legacy → clé scopée du profil actif, flag-gated /profil×HTF, **non-destructive** (legacy laissé en place ; clés `tradeId::step` uniques par DB Notion → copie inter-profil inerte) + idempotente. (6) wiring boot aux 2 points d'entrée (`_reloadProfileScopedState` [dashboard.js:10721](dashboard.js:10721) + DOMContentLoaded), après la migration PO. **Clé résolue** : `lightboxAnnotations_v1_jp_<id>` (m15) / `…_jp_<id>_h4` (h4) — `_h4` en suffixe terminal → `_dashId`/`_isProfileScopedSyncKey` la classent comme presets/PO. **Vérifié headless** (0 erreur console, 26 widgets) : fonctions définies, préfixe+set enregistrés, scope résolu (`…___demo`) ; migration copie/non-destructive/flag posé/idempotente ; classification sync m15/h4/demo OK, clé `_migrated` **non** synchronisée ; `_lbAnnOnRemoteSync` safe lightbox fermée. **Round-trip complet (sign-in → dessin réel → écho Realtime 2e device) = live-only** (données démo sans screenshots + pas d'auth), même limite que le pattern PO. `node -c` OK, brace delta 0, cache JS `?v=1262`. Voir [DECISIONS.md](DECISIONS.md) 2026-07-04 (Phase 2). **À valider live par Max.**

- [x] **Screenshot lightbox — mire (crosshair) + calque d'annotation vectoriel non-destructif (Phase 1, local)** (2026-07-04) — Grosse session multi-itérations sur la lightbox des screenshots du drawer de trade (`_lb*` @~[dashboard.js:16620](dashboard.js:16620)). **(1) Mire type TradingView** : bouton toggle (préférence LS `lightboxCrosshair`), lignes pointillées qui suivent le curseur **bornées à l'image** (masquées hors screenshot), + **thème sombre/clair** de la mire (`lightboxCrosshairTheme`, lignes noires pour screenshots clairs / blanches pour sombres, halo opposé) via 2e bouton. **(2) Calque de dessin SVG non-destructif** (`_lbAnn*`) : bouton crayon (mode dessin) révélant une toolbar verticale gauche ; outils **rayon horizontal (S/R), trendline, rectangle, fibonacci, texte, callout (étiquette bulle), pinceau (dessin libre)**. Coordonnées **normalisées 0..1** (survivent au resize/tailles d'écran) ; **palette de couleurs littérale fixe** (indépendante du thème, comme le reste de l'overlay theme-independent) ; **persistance `localStorage['lightboxAnnotations_v1']`** clé `tradeId::step`, rechargée à l'ouverture, rendu **lecture seule hors mode dessin**. **Détails d'ergonomie** : trend/rect/fib/callout en **deux clics** (aperçu live entre les deux) ; **fib** = niveaux `-0.27 / 0 / 0.5 / 0.71 / 1` (1er clic = niveau 1, 2e = niveau 0), lignes **bornées à la largeur tracée**, **couleurs par niveau** (0.71 orange `#c25600`, -0.27 bleu `#14307a`, reste noir) ; **callout** façon TradingView = bulle + pointe **déplaçables indépendamment** (2 poignées) ; **texte/callout en saisie live** (input invisible capturant la frappe, texte rendu directement dans le SVG qui grandit + caret `|`) ; **double-clic pour éditer** un texte/callout ; **hit-strokes transparents 14px** pour re-sélectionner facilement les lignes fines ; **undo/redo** (boutons + Ctrl+Z/Ctrl+Maj+Z, snapshot avant chaque mutation) ; retour auto en mode **sélection** après pose d'une forme (fix : placer une étiquette ne réarme plus l'outil). **Fix racine notable** : en mode dessin le SVG passe au-dessus de l'image → le `click` fermait la lightbox ; corrigé par `stopPropagation` sur le SVG + garde `if (_lbDrawMode) return` sur le handler de fermeture de l'overlay + `preventDefault`/focus différé sur la saisie texte. **Vérifié intensivement en preview headless** (toggle mire, bornage, thèmes, création des 7 types de formes, two-click, persistance/rechargement, re-sélection via `elementFromPoint`, undo/redo, saisie live, callout move indépendant bulle/pointe, double-clic édition, 0 erreur console). **À valider live par Max** sur ses vrais screenshots. Cache JS/CSS auto-bumpés par le hook. **Phase 2 (sync Supabase cross-device) = TODO Active séparé.**

- [x] **Bug "profil M15 affiche la data du profil H4" — race de concurrence dans le switch de profil (root cause + fix)** (2026-07-03) — Bug dormant depuis ~7 semaines (diagnostic instrumenté 2026-05-20, jamais reproductible en TODO). Max l'a remonté live → déclencheur pour agir. **Cause racine** : le cache trades est scopé par `apiTradesCache_v2_rrmax_<profileId>_<htf>` où `profileId` vient du global mutable `_journalProfileCacheContextOverride`. Le chemin de switch le plus courant — `_reloadJournalProfileSelection` avec `await _SW.downloadTradesBlob(...)` puis `setCachedAPIData` + `_injectTrades` — n'avait **aucun garde de supersession ni re-pin post-await** (contrairement à `_loadNotionTrades`, corrigé le 2026-05-22). Vecteur crucial sans clic rapide : l'**echo Realtime** ([dashboard.js:1842](dashboard.js:1842)) lance ce reload depuis un autre device ; pendant son await, un switch de profil vole l'override → l'echo écrit ses trades dans le slot du **nouveau** profil et les **affiche** alors que l'autre profil est actif. Le détecteur `[ProfileTradeMismatch]` ratait le cas car `_injectTrades` taguait avec le profil actif *courant* (qui matchait). En plus, le `finally` de `_applyActiveJournalProfile` restaurait un `previousOverride` capturé à un instant transitoire → override bloqué sur le mauvais profil. **Fix** : deux jetons monotones `_profileApplySeq` / `_profileSelectSeq` (~[dashboard.js:5867](dashboard.js:5867)). `_reloadJournalProfileSelection` bump son jeton en entrée + **abort après chaque await** avant tout inject/écriture cache si un flux plus récent a pris le relais. `_applyActiveJournalProfile` bump son jeton + son `finally` **nettoie l'override à `''` seulement si toujours le plus récent** (sinon le laisse au flux qui l'a pris), au lieu de restaurer une valeur transitoire. `_injectTrades` gagne un 4e param optionnel `sourceProfileId` (capturé avant l'await) → le diagnostic tague désormais le profil qui a produit la donnée = comble l'angle mort. **Principe : le flux le plus récent gagne, les anciens avortent proprement.** Vérifié headless (logique des jetons, cycle de l'override — le périmé ne clobber pas / le dernier nettoie, tag explicite, boot 0 erreur, 26 widgets). **End-to-end (vrais profils Notion M15+H4 + Realtime) confirmable live uniquement par Max.** Cache JS `?v=1198`. Voir [DECISIONS.md](DECISIONS.md) 2026-07-03.

- [x] **Trade card drawer — libellés screenshots = nom de la propriété mappée + compteur de trade visible dans la lightbox** (2026-07-03) — Deux retours Max sur le drawer des trade cards. **(1)** Les libellés sous les vignettes screenshots ([dashboard.js:24832](dashboard.js:24832), `_wdTradeCard`) étaient abrégés par `.replace(' Before','').replace(' After','A')` → affichaient "H4 / TV Image 2 / H4A". Supprimé → affiche le **nom complet de la propriété mappée** ("H4 Before", "M15 Before", "H4 After") via `_getTradeMediaStepLabel` (qui résout déjà l'override Notion). Libellé + tooltip échappés (`_escapeHtml`) car nom de colonne Notion contrôlé par l'utilisateur (durcissement XSS, pitfall #7). Les 2 autres sites de rendu (Trade Log, Selection) n'utilisaient le label qu'en tooltip → non concernés. Le libellé de la lightbox elle-même utilisait déjà `_getTradeMediaStepLabel` sans abréviation → rien à changer. **(2)** Le compteur de trade "N / M" existait déjà dans la barre du haut de la lightbox mais avec `opacity: .45` → quasi invisible. Rendu proéminent (blanc vif `rgba(255,255,255,.95)`, gras). **Piège évité** : `var(--white)` résolvait en quasi-noir dans le thème clair de Max (token thème-dépendant) → invisible sur la barre sombre ; utilisé rgba littéral comme le reste de ce bloc overlay thème-indépendant (pitfall #6). Vérifié en preview : libellés drawer complets ("H4 Before"/"M15 Before"/"H4 After"), compteur "4 / 9" blanc net (capture), 0 erreur console. Cache JS `?v=1200` · CSS `?v=348`. **Validé live par Max à confirmer.**

- [x] **Concurrent Positions — rubrique dédiée + éclatement en 5 widgets (donut/barres/line) + modes Compact/By-pair + période Month/Overall scrollable** (2026-06-29) — Session en 3 itérations à partir du widget monolithique `w-exposure` (qui vivait dans Global Overview). **(v1 → rubrique)** : nouvelle **section `concurrent-positions`** insérée entre Global Overview et Optimal RR. Plomberie complète mirroir du pattern `optimal-rr` sur ~16 sites (`CONCURRENT_POSITIONS_LAYOUT`, `SECTION_LAYOUTS`, `DEFAULT_LAYOUT`, les ~8 seeds `_liveSectionLayouts`, `_serializeSectionLayouts`, `_saveSectionSlot`, `_loadSectionSlot`, `_migrateLegacySlot`, `_isLegacySlot`, `_applyTemplateLayoutsForProfile`, validation boot, `_SECTION_LABEL_MAP`, `_SECTION_ORDER`, `KIND_BY_ID`, view-tab). `_applySectionFilter` générique → aucune modif. Pas de rupture LS (anciens slots sans clé `concurrent-positions` retombent sur défaut via merge ; `w-exposure` orphelin ignoré). **(v2 → éclatement)** : `renderExposure` splitté en `_expoBuildModel` (compute partagé) + `renderExpoKpis` / `renderExpoDist` (→ **donut Chart.js** : tranches 1/2-3/4-5/6+, centre + légende latérale) / `renderExpoDuration` (**NOUVEAU**, barres horizontales Chart.js par tranche de durée 1j/2-3j/4-7j/8j+, durée moy en header) / `renderExpoTimeline` (Gantt) / `renderExpoOvertime` (**NOUVEAU**, line/area Chart.js + ligne de pic). Chaque widget a ses **propres sélecteurs synchronisés** via état module. Chart.js (déjà CDN) : pattern `tc('--var')`/`getByIdSafe`/`hasChartJs`/`renderCanvasEmptyState`/destroy. Rampe risque partagée (teal→ambre→rouge→rouge foncé) donut+barres. **(v3 → contrôles & modes)** : (a) Over Time **toggle Line/Bars** (Bars = bar-chart par jour cliquable → drawer jour) ; (b) Timeline **toggle Compact/By-pair** (By-pair = 1 ligne/paire alphabétique, labels gutter sticky) + bouton **"∅ pairs"** (afficher/masquer les paires sans trade) + **labels de paire cliquables → drawer** des positions de la paire (`_expoOpenPair`) ; (c) **période `[Month | Overall]`** synchronisée sur les 5 widgets — Overall = agrégation tout-historique + **Timeline scrollable horizontalement** (axe mois + colonne paires sticky, px/jour) ; (d) **menu mois dynamique** (uniquement les mois avec data) ; (e) **flèches ‹ ›** mois préc./suiv. (traversent les années). `_expoBuildModel` généralisé (range arbitraire `_expoComputeRange`, positionnement par index de jour → fonctionne à cheval sur les mois). **Vérifié intensivement en preview** (DOM, données démo + dates de sortie synthétiques injectées) : 4 onglets ordonnés, 5 widgets rendus, 3 charts Chart.js instanciés, synchro des 10 sélecteurs depuis n'importe lequel, donut centre/légende, durée "avg X.Xd", toggle Compact↔By-pair (6 paires tradées → 9 avec ∅), drawer paire ("EURAUD · 1 position · March 2026"), Over Time line↔bars, Overall (inner 10032px, 16 ticks de mois Jan2025→Avr2026, mois cachés), flèche 04→03, mois dynamiques 01-04 ; `node -c` OK, accolades delta 0, **0 erreur console**. Cache JS → `?v=1190` · CSS → `?v=346`. **Limite test** : capture screenshot headless en échec (souci outil, page réactive) — rendu visuel à valider live par Max. **À valider live par Max** (mapper sa colonne Exit date d'abord).

- [x] **Concurrent Positions — nouveau widget Global Overview : Gantt des trades simultanés + bande d'exposition** (2026-06-29) — Demande Max : à partir des dates d'**entrée** ET de **sortie**, visualiser combien de positions sont ouvertes **en simultané** (max, moyenne, quand). **Étage data** : nouveau dim de mapping optionnel `exitDate` (« Exit date », propType `date`) dans `JOURNAL_DIMS` → surface auto dans Data Setup ; extraction **additive** dans `_normalizeAPITrade` (jour, garde-fou sortie ≥ entrée), bridgée via `_applyMappedFieldAliases` (auto-détecte aussi `Exit Date`/`Date de sortie`/`Close Date`). Aucune rupture de schéma ; **état vide propre** tant que la colonne n'est pas mappée. **Étage widget** (`w-exposure`, calqué P&L Calendar) : container HTML ([index.html](index.html)) + registration (`GLOBAL_OVERVIEW_LAYOUT` + `KIND_BY_ID` + dispatch `renderPanels` + handlers selects + actions clic). `renderExposure` calcule la concurrence jour-par-jour (lane-packing greedy du Gantt), KPIs (Max simultané / Moy. en marché ≥1 / % temps ≥2 / fenêtres de pic), barre de répartition (% des jours en marché par nb de positions), Gantt cliquable → drawer du trade, bande d'exposition cliquable → drawer du jour. Namespace CSS `.expo-*` sur les tokens `--fs-cal-*`. **Survie au layout sauvegardé** : ajout à `GLOBAL_OVERVIEW_LAYOUT` suffit (`_loadSectionSlot` merge `{...GLOBAL_OVERVIEW_LAYOUT, ...saved}`). **Itérations live Max** : (1) **fill/scaling** — flex-fill + hauteur de couloir dynamique mesurée (`--expo-lane-h` = hauteur Gantt / nb couloirs, clampée 16–100px, post-insert comme `--calm-row-min-h`) → remplit la tuile et scale au resize en mode manuel ET auto (zoom `--widget-scale`). (2) **lisibilité jour** — grille verticale jour-par-jour, ombrage week-ends (Gantt + bande), axe de numéros de jour (haut Gantt + sous la bande), durée en jours sur les barres larges, liseré subtil. (3) **bande = bar-chart étiqueté** — axe Y à gauche (1 tick/étage 0..max + lignes de grille horizontales) au lieu des chiffres au-dessus + numéros de jour alignés sous les barres via gouttière. (4) **barres Gantt cliquables** → drawer du trade (titre paire · entrée→sortie · durée · R, barre surlignée). **Vérifié intensivement en preview** (dates de sortie synthétiques injectées, colonne pas encore mappée) : registration + état vide, rendu dense Nov 2025 (Max 6 / Moy 2.6 / ≥2 69 %, 6 couloirs, 22 barres), alignements au pixel, fill (emptyBelow 0) + scaling au resize, axe Y 0→6 + 7 gridlines, contraste labels **AA** dans le thème clair de Max ; 0 erreur console, `node -c` OK. **Limite test** : l'ouverture du drawer (`is-open` posé via `requestAnimationFrame`) ne s'anime pas en preview headless (rAF ne fire pas) — câblage vérifié correct (contenu peuplé), s'ouvre dans le vrai navigateur. Commit `4ac95e5`, cache JS/CSS auto-bumpés par le hook. **À valider live par Max** (mapper sa colonne Exit date d'abord).

- [x] **P&L Calendar (Global Overview) — mode Monthly repensé façon Share view (liste des paires tradées par jour)** (2026-06-29) — Demande Max : il n'aimait pas le rendu monthly actuel (heatmap horaire 24h par cellule-jour, peu lisible) et voulait **le même rendu que le P&L Calendar de la Share view** — celui qui montre les **noms de paires** tradées chaque jour — tout en gardant la **cliquabilité** côté dashboard. **Implémentation** : réécriture de `_renderCalendarMonthly` + `_calRenderMonthlyDayCell` ([dashboard.js:26482](dashboard.js:26482)). Chaque cellule-jour affiche désormais l'en-tête (n° du jour + R total signé) puis **une ligne par trade** (pastille win/loss colorée + nom de paire, triées par heure d'entrée), sur un **fond color-mix** dont l'intensité (10→45 %) suit le |R| du jour relatif au plus gros move du mois. En-tête de résumé (`MOIS ANNÉE NT WW LL` + total R) + colonne Week (R + T) repris du design share. **Hauteurs de rangées uniformes** mesurées post-insert (`scrollHeight` du plus gros jour → `--calm-row-min-h`, rangées en `minmax(row-min-h,1fr)`) comme la share view : remplit la tuile quand peu de trades, grandit + scrolle (`#calendar-grid` = `overflow:auto`) quand chargé. **Point d'archi** : **nouvelles classes `.calm-*` dédiées** ([dashboard.css](dashboard.css)) plutôt que réutiliser les `.sv-cal-month-*` — ces dernières lisent les tokens `--sv-fs-*` qui ne résolvent **que** sous `#share-view` (règle d'indépendance typo share-view, pitfall #6) ; les `.calm-*` utilisent les tokens `--fs-cal-*` du dashboard. **Cliquabilité préservée** : `data-date` + `data-action="cal-open-day"` conservés → `_calOpenDay` → `openWidgetDrawer` (inchangé) ; pastilles BE-TP/BE-SL gardent leurs couleurs sémantiques Notion-pures ; paires échappées via `_escapeHtml`. Mode **Annual** non touché. **Vérifié en preview** (OCT 2025 : 8T/5W3L/+9.0R, jour 14 `+4.8R` → `●USDCHF ●EURAUD`, fonds color-mix, `--calm-row-min-h:125px`, clic → drawer « 2025-10-14 · 2 trades » ouvert, liseré gold actif). `node -c` OK, brace delta 0, 0 erreur console. Cache JS `?v=1120 → 1125` · CSS `?v=331 → 333`. **Validé live par Max.**

- [x] **Monthly P&L — cap 12 barres + switch de période (12M | Year), dropdown année, regroupement des contrôles** (2026-06-29) — Demande Max : au-delà de ~24 barres le widget masquait les valeurs de perf par barre. **Fix racine** : cap de l'axe à **≤12 mois** → `N` ne dépasse jamais le seuil de masquage (`N <= 24`) donc **les labels sont toujours affichés**. **Deux modes** via toggle in-widget ([drawMonthly](dashboard.js:19750) réécrit autour d'un helper `_monthlyDisplayMonths`) : **`12M` (défaut)** = les 12 derniers mois jusqu'à la dernière data (rolling, clampé pour ne pas padder de mois pré-historiques) ; **`Year`** = les 12 mois calendaires de l'année sélectionnée, **mois futurs vides inclus** (ex. juin → mai-déc affichés vides). Mois manquants pré-remplis à zéro ; ligne avg calculée sur les mois actifs uniquement (pas diluée) ; overlays 3M Avg / Cumul bornés au dernier mois actif (pas de traînée dans le futur). **Sélecteur d'année** : d'abord flèches `‹2026›`, **remplacées (retour Max) par un menu déroulant** `.widget-select` (pattern du P&L Calendar) listant les années avec data, visible en mode Year seulement (`mc-year-sel` + handler `change`). **Contrôles regroupés** (retour Max « trop de boutons à plat ») en 3 groupes fonctionnels : `[12M｜Year]` + dropdown (période, segmenté), `[Stacked｜Net｜Volume]` (valeur, segmenté), `3M Avg`/`Cumul` (overlays, chips séparées) — nouvelles classes `.mc-seg`/`.mc-grp`/`.mc-controls`, gaps 14px entre groupes, joints à l'intérieur. État `appState.ui.monthlyPeriodMode`/`monthlyYear` non persisté (reset session, défaut 12M). **Vérifié en preview** (l12m 2025-05→2026-04 ; year 2026 jan→déc futurs vides ; dropdown peuplé `[2026,2025]` ; sélection 2025 → redraw ; géométrie des 3 groupes). `node -c` OK, brace delta 0, 0 erreur. Cache JS `?v=1109 → 1121` · CSS `→ 332`. **Validé live par Max.**

---

## Update rules for agents

At the end of every coding session:
- Move completed items → Done (keep last 10 max, oldest fall off)
- Add any new TODO discovered during the session (with context)
- Update "In Progress" if work is mid-flight
- Commit `ROADMAP.md` in the same commit as the related code change
