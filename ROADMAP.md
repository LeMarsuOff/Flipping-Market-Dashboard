# ROADMAP.md — Flipping Market Dashboard

> Single source of truth for task tracking. Updated by agents at the end of every session.
> Supersedes `docs/current-roadmap.md` (kept for historical reference only).

---

## In Progress

_(none)_

---

## TODO — Active

- [ ] **Migrate remaining ~26 inline `connectionType === 'notion'` checks to `INTEGRATIONS` registry** — Strangler Fig follow-up to the 2026-05-19 registry refactor. One micro-fix done at [dashboard.js:5336](dashboard.js:5336) (`_reloadJournalProfileSelection` `isNotionWithDb` now uses `getIntegration(p).isReady(p)`). Rest deferred until MT5 ships — abstracting from a single concrete implementation risks getting the methods wrong.

---

## TODO — Deferred

- [ ] **Retention policy design — to spec before any server-side trade storage ships (e.g. MT5)** — today Supabase only holds metadata (~100KB/user) + media; trades live in localStorage so an abandoned user costs nothing. The moment MT5 EA pushes trades to a server endpoint, abandoned accounts start accumulating real volume (~6–30 MB/user). Direction agreed during the 2026-05-19 session: (a) disconnect integration = soft (token revoked, metadata/trades retained), (b) profile manually deleted = soft → hard at 30 days, (c) user inactive 6 months = warning email, archive at 7 months, hard-delete at 12 months, (d) "Delete my account" = soft with 30-day grace period. Implementation: Supabase `pg_cron` or scheduled edge function, runs weekly. Add `last_active_at` column to user/profile rows when the schema is touched next.

- [ ] **MT5 / MT4 / cTrader — actual implementation** — registry stubs (`available: false`) exist since 2026-05-19. Next steps before code: (1) decide MetaApi.cloud SaaS bridge vs. self-built MQL5 EA (cost vs. control); (2) extend Vercel proxy or move to Supabase edge functions for per-broker secret handling (current static-page architecture can't hold per-user API keys safely); (3) spike one platform end-to-end on a single test account before promising to community. cTrader is the easiest technically (OAuth Open API) but smaller audience; MT5 covers most of Max's prop-firm users.

- [ ] **Backend OAuth: dynamic frontend redirect** — `notion-dashboard-api-2` Vercel function uses `DASHBOARD_URL` env var hardcoded to a single front URL (`https://lemarsuoff.github.io/Flipping-Market-Dashboard/`). Conséquence : impossible de tester l'OAuth Notion en local sans flipper la variable (et casser la prod). Fix : à `/api/notion/oauth/start`, lire l'`Origin`/`Referer` de la requête, valider contre une allowlist (`github.io/Flipping-Market-Dashboard/`, `localhost:*`), encoder dans le `state` OAuth, puis y rediriger au callback au lieu de toujours utiliser `DASHBOARD_URL`. Garder `DASHBOARD_URL` comme fallback. Nécessite accès au repo backend `notion-dashboard-api-2` (pas sur cette machine). Contexte complet dans [DECISIONS.md](DECISIONS.md) § 2026-05-19 — Vercel `DASHBOARD_URL` is single-tenant prod.

- [ ] **[Post-readonly] Trading Journal evolution** — manual trade entry, custom schemas, Notion two-way sync. Spec complète dans `docs/journal-evolution.md`. **Ne pas démarrer avant que le read-only soit clos.** Bloquants identifiés : XSS fix ✅ (2026-05-19), Ghost trades ✅ (2026-05-19), Mappings cross-device sync (audit M1) — restent listés dans le doc §9.

- [ ] **Drawdown Intelligence section** — DD by context (setup/session/pair decomposing max DD), Recovery Factor (Total R / |Max DD|), DD Duration distribution, Risk of Ruin estimate. Optional: Underwater curve, Streak/DD scatter, MAR/Calmar (~14 months of data, low priority). Deferred pending community priority signal.

- [ ] **Hardcoded font-sizes (~180 remaining)** — deferred pending community feedback on typography editor coverage.

- [ ] **BYOT + export/import config** — open the dashboard to all users without requiring their Notion credentials. Recommended step before full OAuth rollout. ~1.5 days dev vs weeks for OAuth. Deferred until post-Notion overhaul stabilization.


- [ ] **Orphaned localStorage keys after profile deletion (audit M2)** — `_deleteJournalProfileById` does not purge scoped keys (`apiTradesCache_*`, `outcomeValueMapping_*`, etc.).

- [ ] **Share view — bottom summary strip** — `WIN DAYS / LOSS DAYS / NO TRADES / BEST DAY / WORST DAY / AVG PER TRADE`. Visible in reference designs, not yet implemented.

- [ ] **Yellow border highlighting on clicked cells** — P&L Calendar, Pair × Session, Monthly P&L in Hover mode.

- [ ] **Security migration — repo privé + hosting Cloudflare Pages + minification bundle (Niveau 1+2)** — surfaced 2026-05-20. Aujourd'hui le repo est public sur GitHub → un voleur fait `git clone + deploy` en 5 min. Plan en 2 étapes faisables en ~3 h en une session : (a) passer le repo en privé + migrer le hosting GH Pages → Cloudflare Pages (free tier, accepte repos privés via GitHub Actions auto-deploy) ; (b) ajouter un step `npx terser dashboard.js -o dashboard.min.js -c -m` dans le CI, `index.html` pointe sur `.min.js` en prod, source reste lisible en local. Workflow dev inchangé (édition locale + refresh). Bénéfice : passe de "clone trivial en 5 min" à "reverse-engineer pénible d'un bundle minifié". **Timing libre — pas urgent aujourd'hui** (audience = 600 community members distribuée par Max, pas viral, pas monétisé, pas indexée). À faire **avant** un des 4 triggers : (1) lancement public visible (Twitter / YouTube / Product Hunt), (2) monétisation, (3) lancement MT5 (élargit l'audience), (4) repost externe non contrôlé. Niveaux 3 (auth gate via Supabase Auth) et 4 (secret sauce côté serveur via Edge Functions) restent réservés au pivot SaaS payant — sur-engineering tant que freemium community.

- [ ] **Add LICENSE file (MIT recommended)** — currently the public repo has no license, so legally all rights reserved by default. Not urgent since users don't access GitHub directly, but worth adding for legal clarity.

- [ ] **Add README.md** — public-facing landing page on GitHub. Low priority since users access the dashboard via GitHub Pages link, not the repo itself. Would matter if/when the repo becomes a public-facing project (contributions, forks, etc.).

- [ ] **Auto-rename session on `/end` — feature request to revisit if Anthropic ships the hook** — investigated 2026-05-21 via the claude-code-guide agent: programmatic auto-rename of the current Claude Code session is **not possible today**. (1) No CLI command like `claude session rename <id> "<title>"` exists. (2) Stop hooks have no access to the agent's final message text — they only receive tool execution events. (3) Claude Code does not parse OSC codes or any agent-emitted marker that would trigger a rename. The current pattern (agent proposes `Tu peux remplacer le titre par : …` line, user copy-pastes into `/rename`) is the only reliable path. Revisit if Anthropic ever exposes (a) a CLI/IPC endpoint for session-rename, (b) enriches the `Stop` hook payload with the agent's final turn, or (c) introduces a marker convention agents can emit. Until then, status quo.

---

## Done — Last 10

- [x] **Realtime self-write loop + Compare diff live + Custom Table right-click — 4 fixes in one session** (2026-05-26) — Suite de fixes Max-driven shippés en cascade. **(1) Realtime "Trades synced from another device" toast en boucle infinie** ([dashboard.js:429-455](dashboard.js:429) + [dashboard.js:9963-9974](dashboard.js:9963) + [dashboard.js:7902-7920](dashboard.js:7902) + [dashboard.js:1715-1733](dashboard.js:1715)). Diagnostic via inspection MCP de la RPC `bump_trades_blob_meta` + format JSON natif Postgres : `timestamptz` a deux représentations divergentes — PostgREST RPC return = ISO 8601 strict (`"2026-05-25T20:49:11.41542+00:00"`), Postgres natif (potentiel format Realtime payload) = espace + offset bare (`"2026-05-25 20:49:11.41542+00"`). Le `_isSelfWriteFn` comparait en string-strict via `Map.has(\`${table}|${composite}|${updatedAt}\`)` → mismatch quasi-systématique → tout self-write blob revenait comme "remote event". Amplificateur : `_onTradesBlobMeta` → `_reloadJournalProfileSelection({})` → si `DS_KEY === 'api'` + raw cache présent, early-return ligne 7324 sur `_reapplyAPIOverrides()` qui call `setCachedAPIData(preserved)` SANS `fromBlobDownload: true` → debounce 1500ms → upload → bump → nouvel event → boucle infinie (toast affiché throttled 10s mais re-déclenché en permanence). **Triple fix** : (a) `_normUpd(x)` helper qui normalise via `new Date(x).getTime()` (epoch ms) — robuste à ISO/Postgres natif/Z offset, validé via test direct `[ISO, Postgres natif, ISO+Z]` → tous mappent à `1779742151415`. (b) `setCachedAPIData` ligne 9942 lit maintenant `_isApplyingRemoteRealtime()` et skip `_scheduleBlobUpload` pendant la fenêtre 1.5s de remote-apply — coupe l'amplificateur même si Fix A rate un format inconnu. (c) Même garde sur `_setRawAPICache` ligne 7908 pour cohérence (raw blob pas dans le Realtime sub mais évite re-upload inutile). (d) `_onTradesBlobMeta` appelle `_enterApplyingRemote()` AVANT `_reloadJournalProfileSelection` (déjà fait par `_onUserData` pour les preset/theme paths, manquait sur blob). Validé via preview : 3 variantes `updated_at` produisent même epoch, 0 console errors au boot. End-to-end E2E live requires signed-in session (impossible côté preview). **(2) Compare Presets — Filter Difference section ne se mettait pas à jour sur live edit** ([dashboard.js:20350](dashboard.js:20350)). Bug analogue à `_getPresetStats` fixé le 2026-05-25 : `_buildPresetFilterDiff` lisait `presetSnapshots[idA/idB]` directement (snapshots permanents) au lieu d'utiliser l'état effectif (snapshot + live deltas + active chips). Quand Max ajoutait un chip live sur le preset actif, les nombres Compare (top) se rafraîchissaient via `_getPresetStats` (déjà fixé) mais le diff (bottom) restait figé. Fix : swap pour `_buildEffectiveSnapForPreset(idA/idB)` — même helper canonical, même source de vérité que les stats. Validé en preview : add `'SL'` aux includes outcome → `outcome (incl) TP, SL —` apparaît immédiatement dans la colonne diff (avant : `outcome (incl) TP —`). **(3) Custom Table widget — right-click sur cellule label pour include/exclude** ([dashboard.js:15429-15455](dashboard.js:15429) + [dashboard.js:15600-15622](dashboard.js:15600)). Feature ajoutée pour mirror le comportement des bars/heatmap. Nouveau helper `_cwResolveChipKey(def)` qui map `def.field` → chipKey (native dim → `field`, custom prop → `'custom:<key>'`). Listener `contextmenu` sur chaque `.cw-td-label` qui appelle `_showHeatmapContextMenu` avec un dims array de 1 (l'API qui supporte natifs + customs via `_resolveChipEntry`). Le title du row updated : `"Click to drill into N trades · right-click to include/exclude"`. Validé en preview : right-click "TP" sur Custom Table outcome → menu → "Include" → `chips.outcome.included = ['TP']` + mode `'include'`. **(4) Custom prop chipKey resolution via notionSourceName** ([dashboard.js:15429-15464](dashboard.js:15429)). Bug introduit par #3 puis fix immédiat après feedback Max : pour les custom Notion props (e.g. "H4 Type détail"), `def.field` est le raw notion source name (avec espaces/accents) mais `customChips` est indexé par la safe key (`h4TypeDetail`) que l'user assigne via Data Setup → Custom Fields. Le helper utilisait le raw → l'exclusion atterrissait dans `customChips['H4 Type détail']` (entry phantome) tandis que le sidebar chip lit `customChips['h4TypeDetail']` → l'exclusion s'appliquait au widget mais aucun chip sidebar ne reflétait. Fix : `_cwResolveChipKey` cherche maintenant dans `loadCustomProps()` une prop dont `notionSourceName === def.field` (ou `key === def.field`) et utilise sa safe key. Native dim wins par priorité, fallback raw préservé pour fields undeclared. Validé E2E en preview : custom prop déclarée + widget créé sur raw notion name + right-click Exclude "Liquidity" → `customChips.h4TypeDetail.excluded = ['Liquidity']` + sidebar chip `#chips-custom-h4TypeDetail .chip[data-val="Liquidity"]` passe à `class="chip excluded"`. **Limitation connue** : fields jamais déclarés comme Custom Field n'ont aucun chip sidebar à mettre à jour (pas une régression — même contrainte que tous les autres flows custom). **Cache JS** : `?v=786 → ?v=795` (9 bumps automatiques du hook PostToolUse). 0 console errors sur tous les scénarios validés en preview.

- [x] **Preset state correctness — 4 cascading bugs fixed across sign-in / Compare / Update / Share view "No Preset"** (2026-05-25) — Suite Max-driven sur la cohérence preset post-Realtime Push. **(1) Boot post-sign-in : presets pas visibles, switch profile aller-retour requis** ([dashboard.js:36156](dashboard.js:36156) + [dashboard.js:36277](dashboard.js:36277)). Diagnostic via les data Supabase de Max (profil `jp_iiqexza7` M15 Perso, 3 presets dont P1 avec `outcome.excluded=[BE-SL,BE-TP]` + 8 autres dims) : (a) `_runProfileScopeMigrationV3` au Boot 2 voit son FLAG `flipping_profile_scope_migration_v3` wipé par le logout (`_wipeLocalStateAndReload` fait `localStorage.clear()`) → re-tourne le wipe loop sur les prefixes legacy → essuie les `flipping_presets_jp_iiqexza7` que `syncFromRemote` venait de re-tirer. Fix : guard pré-wipe qui scanne LS pour toute clé matchant `_isProfileScopedSyncKey` ; presence = on est déjà post-v3 → set FLAG + return (premier commit `4f70ec0`). (b) Vrai root cause découvert ensuite : `dataSource='demo'` stale du Boot 1 (signed-out fallback) au Boot 2. Le `load` handler appelle `loadPresetsList()` AVANT `initDataSource()` corrige DS_KEY → `_htfKey('flipping_presets')` route via `getProfileScopedKey` → `dsMode==='demo'` → `__demo` comme profileId → cherche `flipping_presets___demo` (vide) au lieu de `flipping_presets_jp_iiqexza7` (3 presets). PRESETS reste à `[P0]` default in-memory. Fix : mirroir de `_hasNotionProfileButDemoBranch` au top du `load` handler — si profil actif est Notion-ready et DS_KEY='demo', flip vers 'api' AVANT les preset loaders. **(2) Compare Presets affiche fausses données** ([dashboard.js:13417](dashboard.js:13417) `_filterTradesBySnapshot` + [dashboard.js:20087](dashboard.js:20087) `_getPresetStats`). Bug 1 : `_filterTradesBySnapshot` ignorait silencieusement 4 dims (`outcome`, `direction`, comboFilters `sessionDay`/`pairSession`) que `filterChips` filtre normalement. Snapshot P1 de Max avait `outcome.excluded=[BE-TP,BE-SL]` qui passait à travers → Compare montrait plus de trades que P1 active. Fix : alignement strict sur `filterChips` (add outcome scalar, direction scalar, comboFilters via `snap.comboFilters`). Bug 2 : `_getPresetStats` utilisait `hasLiveContent(live) ? live : snap` → quand live a du contenu, n'utilisait QUE les deltas live (oublie les filtres snapshot baked). La live slot d'`_syncLiveSlotFromActiveChips` stocke `included.filter(v => !includedFromPreset.has(v))` = uniquement les deltas user. Fix : nouveau helper `_buildEffectiveSnapForPreset(presetId)` qui produit l'état effectif (mirror de ce que `applyPreset` met dans `appState`) — pour preset actif : lit `appState.filters.chips` directement (unsaved live edits visibles), pour non-actif : merge snapshot + live deltas + applique tombstones (`removedFromIncluded`/`removedFromExcluded`). + ajout `filterTemporal` au pipeline `_getPresetStats` pour matcher live à 100%. Bug 3 : pas de live update — `runPresetCompare` ne se déclenchait que sur change dropdown / TPM / open panel, pas sur chip clicks. Fix : ajout `runPresetCompare()` à la fin de `render()` gated sur `_presetCmpOpen`. Validation : test live avec parity check `filterChips` vs `_filterTradesBySnapshot` retournent EXACTEMENT le même set sur input identique, plus test live-update qui simule chip click → render → Compare re-flow. **(3) Update button — chips gardent leur look "live" au lieu de transitionner vers "permanent glow"** ([dashboard.js:19978](dashboard.js:19978) `_commitLiveFiltersConfirmed`). Le code-path utilisait `_refreshChipDerivedUI()` qui appelle `_refreshAllChipDisplays` — un fast-path qui met à jour `className` sur les `<div>` existants. Sur le papier ça marchait (test unitaire confirme classes correctes : `chip excluded` → `chip excluded is-from-preset`, `chip is-tombstoned` → `chip`) mais en live certains rendus restaient figés. Fix défensif : remplacé `_refreshChipDerivedUI()` par un full `render()` qui passe par `renderDashboardChrome → buildChips` pour chaque container — chemin canonique de rebuild. + appel `buildTunePanel(openTunePanel)` après car `render()` ne le touche pas. Coût ~5ms one-time sur Update click — négligeable. Validation : DOM réel + `toggleFilter` réel + `_commitLiveFiltersConfirmed` réel → XAUUSD tombstoned → neutre, GBPUSD/EURUSD live → permanent glow, snapshot persisté correctement. **(4) Share view "No Preset" garde les filtres du preset précédent** ([dashboard.js:42661](dashboard.js:42661) `_clearActivePreset`). Bug : la fonction faisait `setActivePreset(null)` mais NE clearait PAS `appState.filters.chips`. User sur P2 (pair.excluded=[XAUUSD]) → pick "No Preset" → activeId=null mais chips encore peuplés → render filtre encore XAUUSD → 10 trades au lieu de 12. Fix : ajout de `_removeAllPresetChips()` + `_removeAllLiveChips()` + `_clearComboFilters()` avant le `setActivePreset(null)` — même séquence qu'`applyPreset(id)` utilise pour wiper les chips. Validation : cycle P2 → No Preset → 12 trades / chips vides ✓ (était 10 / pair=[XAUUSD]) ; P1 → No Preset → 12 trades / chips vides ✓ (était 8 / outcome=[BE-TP,BE-SL]). **Cache JS cumulé** : `?v=778 → 786` (8 bumps sur 5 commits intermédiaires). 0 console errors sur tous les scénarios validés live par Max. Architecture insights : (a) le pattern `_buildEffectiveSnapForPreset` devient canonical pour tout tooling qui veut lire l'état effectif d'un preset sans muter `appState` (Compare, share preview, futurs widgets de comparaison). (b) `_clearActivePreset` doit clear les chips — pattern à mémoriser pour tout setActivePreset(null) ailleurs. (c) DS_KEY peut être stale entre boots — toute fonction qui résout des clés profile-scoped doit tolérer cet état transitionnel ou être appelée après `initDataSource`. Documenté dans DECISIONS.md § 2026-05-25 (Preset state correctness — 4 bugs).

- [x] **Supabase Realtime Push — zero-loss writes + live cross-device sync, shipped in 10 commits across 1 session** (2026-05-25) — Triggered by Max losing data on 2026-05-25: PC A made changes → PC B (tab open 2h, in-memory state frozen) triggered an autosave → wrote stale blob over fresh PC A blob → irrecoverable. Built in 5 phases + 5 hotfixes after live QA. **Phase 0** (commit `76c8231`) : 5 Supabase migrations — `BEFORE UPDATE` triggers auto-bumping `updated_at` on `user_data`/`journal_profiles`/`notion_connections` + 5 SECURITY DEFINER RPCs (`upsert_user_data_if_unchanged`, `delete_user_data_if_unchanged`, `upsert_journal_profile_if_unchanged`, `delete_journal_profile_if_unchanged`, `bump_trades_blob_meta`) that `RAISE 'stale_write'` when `existing.updated_at > p_seen_updated_at` + new `trades_blob_meta(user_id, profile_id, source, blob_size, updated_at)` watcher table with RLS (Storage has no native Realtime) + add 4 tables to `supabase_realtime` publication + hardening (`SET search_path = public` on trigger fn, `REVOKE EXECUTE FROM PUBLIC, anon` on RPCs). **Phase 1** (commit `a124ec1`, +384/-59) : optimistic-concurrency wrapper inside the `_SW` closure. New state `_lastSeenUpdatedAt` Map (keyed `ud|<dashId>|<key>` / `jp|<id>` / `blob|<profileId>|<source>`) + `_selfWrites` Map (5s TTL) for the Phase 2 self-event mute. 4 surfaces wrapped : (a) `_SW.set/_SW.remove` + the `migrateProfileScopedKeys` reconcile call the new RPCs with `p_seen_updated_at` ; on `P0001 stale_write` → re-pull row → retry once → on retry-stale, toast `"Sync conflict — refreshed from cloud"` + adopt cloud locally ; (b) `saveJournalProfilesToRemote` per-row via `Promise.allSettled` (bulk upsert replaced) ; (c) `uploadTradesBlob` + `uploadRawTradesBlob` bump meta FIRST then Storage upload — order critical because if bump fails (stale), Storage is never touched and the fresh cloud blob is preserved. Read-side : `syncFromRemote` + `loadJournalProfilesFromRemote` (added `updated_at` to SELECT) + `downloadTradesBlob`/`downloadRawTradesBlob` (parallel `Promise.all` for blob + meta SELECT) populate the seen-cache. **Phase 2** (commit `421d5fd`, +198/-1) : top-level `_swRealtime` module subscribes `postgres_changes` on 4 tables filtered by `user_id eq own`. 4 handlers : `_onUserData` write-through to LS via new `_lsSetUntracked` (exposed from monkey-patch IIFE to avoid echo into `_SW.set`) + dispatch `rt:user_data` CustomEvent ; `_onJournalProfiles` calls `hydrateJournalProfilesFromRemote` ; `_onNotionConnections` re-checks integration badge ; `_onTradesBlobMeta` re-pulls trades blob via `_reloadJournalProfileSelection` (only for active profile + parsed source `m15`/`h4`, skip raw). Self-event mute checks `_SW._isSelfWrite(table, composite, payload.new.updated_at)` against `_selfWrites` populated by every successful RPC ; without it, every write would echo back as an event and trigger render loops. Single throttled toast `"Synced from another device"` (10s cooldown). Lifecycle hooks in `onAuthStateChange` (`SIGNED_IN` after `syncFromRemote` completes, `INITIAL_SESSION`, Safari ITP `getSession` fallback ; `disconnect()` on `SIGNED_OUT`) ; idempotent connect. **Phase 3** (commit `924034f`, +73/-2) : `visibilitychange` listener registered once at module init, records `_hiddenSince` on hidden, force-pull on visible if hidden >10s. WS reconnect detection via `channel.subscribe()` status callback — first `SUBSCRIBED` = initial, subsequent = reconnect from CHANNEL_ERROR/CLOSED/TIMED_OUT → force-pull. `_forcePull(reason)` runs `syncFromRemote → hydrateJournalProfilesFromRemote → _reloadJournalProfileSelection` sequentially, debounced via `_pullInFlight`. **Phase 4** (commit `e10b25a`) : DECISIONS.md entry + ROADMAP QA checklist. **Hotfix 1** (`14241da`) : write-through alone didn't make remote changes visible — in-memory caches (presetSnapshots/presetsList/etc.) stay stale until something re-reads them. `_onUserData` now detects preset key family (`flipping_presets*`/`flipping_preset_snapshots_v2*`/`presetLiveFilters_v1*`/`flipping_preset_overrides*`/`gs_active_preset*`/`flipping_active_filter_preset_id*`) → calls `loadPresetsList + loadPresetSnapshots + loadPresetLiveFilters + loadPresetOverrides + renderPresetList + _restoreActivePresetForCurrentSlot` (which calls `applyPreset` → cascade re-render via `render()`). Theme key family → `loadSavedTheme + dispatch('themeTypoChanged')`. **Hotfix 2** (`bbf0534`) : Max hit infinite loop — `"Synced from another device"` + `"Sync conflict"` toasts in flood. Root cause : `applyPreset` cascade writes via `setActivePreset` / `_syncLiveSlotFromActiveChips` → monkey-patch → `_SW.set` → Supabase → new event → re-render → cascade → loop. Fix : private `_isApplyingRemote` flag in `_swRealtime` closure with 1500ms auto-clear timer (each enter resets the timer), exposed as `window._isApplyingRemoteRealtime()`. The monkey-patched `localStorage.setItem`/`removeItem` check the getter and skip the Supabase push while the apply-remote window is active. Trade-off : legitimate user writes within 1.5s of a remote event aren't synced — acceptable vs. infinite loop. **Hotfix 3** (`10fc7f0`) : after hotfix 2 broke the loop, chips still didn't update on B. `applyPreset`'s first step `_syncLiveSlotFromActiveChips` captures B's stale chip state into the live slot AND calls `savePresetLiveFilters` to persist → clobbers the freshly-pulled live filters from A. Then `_hydrateChipsFromLiveSlot` in the same `applyPreset` reads back the stale state. Fix : `_syncLiveSlotFromActiveChips` returns early when `_isApplyingRemoteRealtime()` is true, so `applyPreset` becomes a pure hydrate during remote apply (snapshot + live both read from the freshly-loaded in-memory caches). **Hotfix 4** (`eefd9f0`) : Max QA — apply live filters on A + Clear on A → B didn't reflect the clear. `savePresetLiveFilters` OMITS presets with `hasAny=false` from the JSON output. When A clears, B's LS receives `{}` (or partial). `loadPresetLiveFilters` was a merge-add (`Object.entries(parsed).forEach(...)`) so the stale in-memory slot for the cleared preset survived → `_hydrateChipsFromLiveSlot` re-applied dead overrides. Fix : blank every existing key in the in-memory `presetLiveFilters` map before the populate loop, so missing-from-LS keys are properly reset. **Hotfix 5** (`867c375`) : Max QA — filter + Update on A, remove filter + Update on A → B stuck on the pre-last state. Identical pattern to hotfix 4 but on `savePresetSnapshots` / `loadPresetSnapshots` — snapshots with `hasAny=false` also omitted from output, load merge-add survived stale snapshots. Same fix : blank in-memory `presetSnapshots` map via `_blankSnapshot()` before populate loop. `loadPresetOverrides` left alone (save side doesn't have the omit pattern). **Validated live by Max** : write propagation (preset change A → B within ~1-2s with single toast and full visual cascade) + filter+update / clear cycles both directions, all without infinite loop or sync conflict toast. Architecture documented in DECISIONS.md § 2026-05-25 (Phase 0, Phases 1-3, post-QA hotfixes — 3 entries total). Cache JS `?v=751 → 780` across 10 commits.

- [x] **Create Widget UX — search-input focus retention, heatmap axis swap, property-column manual resize on Table widgets** (2026-05-24) — Three fixes for the Data Setup → Create Widget panel reported live by Max. **(1) Search input focus** ([dashboard.js:32321](dashboard.js:32321)) : `_renderCreateWidgetBuilder` re-renders the entire host innerHTML on every keystroke via the document-level `input` listener at [dashboard.js:31235](dashboard.js:31235), which destroyed the focused `<input>` element each time — user could only type one character then had to click back into the field. Fix : snapshot `document.activeElement === searchInput` + `selectionStart/End` at the top of the function, then re-apply `.focus()` + `setSelectionRange()` on the freshly-rendered input at the bottom. Verified live via stubbed `activeElement` getter (caret position survived the swap — `caret: 3` after typing `pai`). **(2) Heatmap axis swap** ([dashboard.js:32546](dashboard.js:32546)) : `_setCreateWidgetHeatmapAxis` previously did `ySelectedId = newId` then checked `if (xSelectedId === ySelectedId) xSelectedId = ''` — so picking a property already on X for Y would WIPE the property previously on Y. New logic : if `match.id === currentOther`, swap (`thisKey = match.id; otherKey = currentThis`) so both selections survive. Toggle-off (re-click same axis) and mirror direction (X-pick of the Y-property) both verified. **(3) Property column manual resize on custom Table widgets** ([dashboard.js:14776](dashboard.js:14776) + [dashboard.css:3547](dashboard.css:3547)) : reached after one full reverted iteration. **Iteration 1 (reverted)** : built a full multi-column drag-resize with `<colgroup>`, per-column persistence, `table-layout: fixed` rewrite, `.is-label-fixed` class switch — worked technically (numerics stable, sync chain integrated, dblclick reset) but Max said "Je n'aime pas du tout le résultat" → `git checkout HEAD -- dashboard.css dashboard.js` to wipe everything, then re-applied only the two non-table fixes (#1 + #2). **Iteration 2 (shipped)** : minimal additive — single CSS variable `--cw-label-min` on the table element, picked up by `.cw-th-label` and `.cw-td-label` via `min-width: var(--cw-label-min, 0)`. Default fallback `0` = byte-identical to pre-feature behaviour (label still collapses via `max-width:0; width:1%`, numerics still at content widths). User drag sets the variable + persists to `def.settings.labelMinWidth` (validated 40–1600 px in `_normalizeCustomWidgetDef`). Drag handle visible ONLY on `.cw-th-label` (no handles on numeric headers). Sync chain (⛓) propagates the min-width across all tables — `toggleCustomTableSync` includes `labelMinWidth` in seed/commit. Double-click handle → reset to null + clear var + re-render. `overflow-x: hidden → auto` on `.cw-table-scroll` so widening past the widget shows a scrollbar instead of clipping silently. Capture-phase `click` listener swallows clicks on the handle so the `<th>` click→sort handler never fires (verified : drag = no sort flip). **Validated live in preview** : default state unchanged (label 43px / numerics 303/127/199/151/122 identical to revert state), drag +300 → label 343 persisted, dblclick → exact baseline restored, click without drag → sort unchanged. 0 console errors. Cache CSS `?v=270 → 282` · JS `?v=725 → 751`.

- [x] **Theme editor full sweep — 59 sites routed through theme vars in 4 phases, zero new pickers added** (2026-05-24) — Demande Max : "j'aimerais m'assurer que absolument toutes les couleurs puissent être modifiables, mais sans avoir 100 boutons différents". Audit Explore initial cartographie 39 vars éditables (Backgrounds, Borders, Text, Accent & Semantic, States, Chart Palette, Component Overrides) + l'architecture 3-couches (Layer A foundation → Layer B semantic alias → Layer C composant) + les dérivations automatiques (`--g2`/`--g3` = `--g` à 8%/15%, etc.) qui font déjà tout le boulot de "grouping" — confirme que la bonne stratégie est de boucher les fuites du système actuel plutôt que d'ajouter des pickers. **Phase 1 — 14 hex en dur CSS** : CSV type pills sur 3 panels (`logo-guide-panel`, `csv-format-hint`, `np-type` — number→`--t`, multiselect→`--streak-orange`, formula→`color-mix(--gold, --body)`, checkbox→`--streak-purple`, url→`--dim`), ORR important badge→`--streak-orange`, ts-v.r→`color-mix(--r, --body)`, reset-btn hover→`color-mix(--r, --body)`, csv-status.warn→`--streak-orange`. **Phase 2 — 9 canvas RGBA** : equity tooltip bg/text ([dashboard.js:15538-15546](dashboard.js:15538))→`tcRgba('--bg4', 0.88)`/`tc('--white')`, calendar dark-text-on-saturated ([22017](dashboard.js:22017))→`tc('--text-inverse')`, calendar empty day ([22140](dashboard.js:22140))→`color-mix(--dim, transparent)`, share-view crosshair line + dot stroke + tooltip + zero line + chart label bg/text ([40878-41897](dashboard.js:40878))→`tcRgba('--white', X)`/`tcRgba('--bg4', 0.88)`/`tc('--white')`. Helper réutilisé : `tcRgba(varName, alpha)` à [dashboard.js:13464](dashboard.js:13464). **Phase 3 — 8 alphas drift** : `--table-row-hover-bg` ([dashboard.css:246](dashboard.css:246)) + `.sel-table tr:hover`→`color-mix(--white, transparent)` (fixe invisible-sur-light), `.cw-hm-val`/`.cw-hm-n` heatmap cell text→`color-mix(--text-inverse, transparent)`, `.sv-cal-month-trade-dot.is-be-tp/sl` hex dups ([11714-11715](dashboard.css:11714))→`var(--oc-betp)`/`var(--oc-besl)`, empty hour cell bg ([dashboard.js:22158](dashboard.js:22158))→`color-mix(--dim, transparent)`, sv-calendar dark-text contrast ([41932](dashboard.js:41932))→`tc('--text-inverse')`. **Phase 4 — 28 sites** : `.wd-trade-meta-value` ([dashboard.css:11118](dashboard.css:11118), trouvé live par Max via screenshot des tradecards EURUSD qui montrait du jaune fixe qui ne suivait pas le thème)→`color-mix(--gold 70%, transparent)`, `.wd-trade-card-tp/sl/betp/besl` bgs + `.mob-tradelog-row.is-*` mobile mirror (8 sites)→`color-mix(--g/--r/--oc-betp/--oc-besl, transparent)`, `.ljp-required-badge` ([1832](dashboard.css:1832))→`--r`, `.lt-active-badge` ([8800](dashboard.css:8800))→`--streak-orange`, `.htf-toggle-btn.active` ([10664](dashboard.css:10664))→`--oc-betp`, `.chip[data-key]:hover` 6 catégories ([2895-2910](dashboard.css:2895)) routées vers `--b`/`--t`/`--gold`/`--oc-besl`/`--r`/`--gold`, 7 sites contraste blanc/noir (`#fff` sur gold→`--white` qui adapte light/dark, `#000` sur green→`--text-inverse`), JS BE-TP/BE-SL consts 5 paires ([14802/16610/16850/18034/18071](dashboard.js:14802)) + inline styles tooltip ([28427-28428](dashboard.js:28427)) + palette P3 ([26742](dashboard.js:26742))→`tc('--oc-betp')`/`tc('--oc-besl')`/`var(--oc-betp)`, bonus catch topbar separators `.ts` ([2484/2487](dashboard.css:2484))→`color-mix(--b, transparent)`. **3 catégories volontairement non touchées** (cf DECISIONS.md) : (a) lightbox + csv-overlay îlots dark intentionnels (pattern image viewer standard), (b) theme editor panel internals ~30 rgba lignes 16485-18883 (verre sombre intentionnel à la dev tools), (c) share-view "design canon" (`sv-cal-month-daynum-empty`, `.is-be` dot blanc, trade-pair text) qui affecte la canonicité du PNG cross-machine — à discuter séparément si Max veut y aller. **Validation** : `node -c` OK, 0 console errors au boot demo (28 body children, 100 trades), live theme-switch proof à chaque phase (mutate `--t` → CSV number pill vire au rouge ; mutate `--oc-betp` → trade-card betp bg vire au vert ; mutate `--gold` → `.wd-trade-meta-value` vire au magenta — toutes les surfaces suivent). Cache CSS `?v=250 → 271`, JS `?v=711 → 725`.

- [x] **Reapply Template Defaults fix + Supabase screenshot pre-swap (eliminate placeholder flash)** (2026-05-24) — Investigation déclenchée par Max : sur son profil M15 Pro, "Reapply template defaults" cassait 4 dims optionnels (`obstacles`, `h4`, `hour`, `badFeeling`) — widgets `w-m15`/`w-h4`/`w-hour` vides, mapping panel à 16/16 au lieu de 20/20. Repro confirmé : création profil M15 → 20/20 → hard refresh → Reapply → 16/16. **Root cause** : `templateChoice` jamais persisté côté Supabase. `saveJournalProfilesToRemote` ([dashboard.js:1029-1046](dashboard.js:1029)) buildait chaque row sans la clé, `loadJournalProfilesFromRemote` ne la SELECTait pas, `hydrateJournalProfilesFromRemote` merge ne préservait que `notionDatabaseTitle` du local. Au hard refresh → `hydrate` écrasait le LS local correct par un profil remote sans `templateChoice` → `_normalizeJournalProfile` fallback `'other'` → `_maybeAutoApplyTemplateMapping` branche 'other' overlay `TEMPLATE_MAPPINGS.other = { obstacles, h4, badFeeling, hour: '__NO_MAPPING__' }` ET tag auto-mapped → `_renderMappingSectionsHtml` skip `is-auto-no-mapping` → dims totalement invisibles dans le panel, seul "Reset all overrides" pouvait les ressusciter. **Quadruple fix** : (1) Migration Supabase `ALTER TABLE journal_profiles ADD COLUMN template_choice text` via MCP. (2) `saveJournalProfilesToRemote` ([dashboard.js:1043](dashboard.js:1043)) écrit désormais `template_choice` avec validation explicite `['m15','h4','other'].includes(...)`. (3) `loadJournalProfilesFromRemote` ([dashboard.js:1112-1143](dashboard.js:1112)) SELECT + mapping objet incluent `template_choice`. (4) Defensive merge ([dashboard.js:4706](dashboard.js:4706)) : `templateChoice: profile.templateChoice || local.templateChoice || ''` — préserve le LS local quand remote NULL (cas critique : tous les profils créés avant migration ont remote=NULL ; sans cette ligne le fix de persistence resterait inutile). (5) Hardening ([dashboard.js:7794](dashboard.js:7794)) : `_maybeAutoApplyTemplateMapping` filtre les valeurs `__NO_MAPPING__` avant `_setAutoMappedDims()` — si le bug 'other' overlay se redéclenche dans le futur, dims tombent en `is-no-mapping` (visible via toggle "Show N no-mapping fields") au lieu de `is-auto-no-mapping` (invisible). Validé end-to-end en preview : 23 keys total, 19 tagués auto-mapped, 4 exclus (obstacles/h4/hour/badFeeling) ; merge defensive avec remote vide + local 'm15' → 'm15' kept ; page boot clean 24 grid items 0 errors. Console snippet donné à Max pour fix manuel de son profil existant (option A) : update LS `templateChoice='m15'` + save → backfill auto Supabase. **Confirmé live par Max** : nouveaux profils protégés, fix manuel marche. **Suite Supabase screenshots — deuxième problème signalé même session** : sur ses anciens trades (sync >12h), cliquer sur un screenshot affichait ~1s un placeholder "Lien TradingView invalide" avant l'image vraie. Diagnostic via MCP : trade `3078c28c-...` avait son image dans `notion-screenshots` storage depuis 2026-05-17 (115KB PNG) + mapping dans `notion_media_cache`. Donc Supabase fonctionnait. Mais le sync de 03:47Z avait clobberé l'URL Supabase in-memory par un fresh Notion S3 URL (X-Amz-Expires=3600) ; 12h plus tard → 403 → `<img>` fail → placeholder. Le helper `_preservePermanentImageUrls` shippé tôt dans la journée prévient les futurs syncs de re-clobberer mais ne corrige pas rétroactivement les trades déjà cassés. **Fix en deux temps** : (a) `_tryRescueFromSupabase` ([dashboard.js:13740-13767](dashboard.js:13740)) — fallback réactif au load fail, mais laissait flash 1s en attendant le 403 Notion ; (b) après feedback Max, switch sur **pre-swap optimiste à `_lbShow` time** ([dashboard.js:13884-13895](dashboard.js:13884)) : si URL est Notion S3 et qu'on peut résoudre `_notionId` depuis appState (match par pair+date+URL via `_findTradeForLbRow`), construire l'URL Supabase déterministe `<base>/<notion_id>_<slot>.png` et set imgEl.src direct sur Supabase, stash l'URL Notion originale sur `imgEl.dataset.notionFallback`. Si Supabase 404 (trade jamais uploadé), onerror retry Notion une fois avant placeholder. Si load Supabase succès, onload patch `appState.trades.items[i][field]` + `[fieldOrig]` + `_lbData[idx][step]` via `_patchTradeAndLbRowToSupabase` — self-healing cascading : prochain setCachedAPIData persiste LS avec Supabase URL, prochain blob upload propage cross-device. Validé end-to-end avec le trade réel `3078c28c-...` de Max : pre-resolve construit l'URL correcte, image load (naturalWidth=2220), appState + _lbData patchés, dataset cleared. Fallback chain testé avec faux notionId : Supabase 404 → notionRetried=1 → retry Notion → placeholder. **Confirmé live par Max** : "C'est validé". Cache JS `?v=700` → `?v=711`. CSS inchangé. **Pattern architectural nouveau** : tout nouveau champ profil ajouté côté frontend doit simultanément être ajouté au SELECT + UPSERT Supabase + merge defensive dans hydrate (cf DECISIONS).

- [x] **Session marathon: signed-out fresh-state, new-user defaults, boot data hydration, donut → table widget, table polish** (2026-05-24) — Long stint shipping ten distinct improvements after the Refacto Total stabilized the extras pipeline. **(1) Logout + Delete Account wipe local state** ([dashboard.js:29798](dashboard.js:29798)) : both flows now `localStorage.clear() + sessionStorage.clear() + reload` after `_SW.signOut()` so the dashboard comes back up with no fantasm profiles / no stale integrations connected. New helper `_wipeLocalStateAndReload()` shared between the two paths. **(2) Topbar Sign In button + auto-open Account panel for signed-out boot** ([dashboard.js:29553](dashboard.js:29553) + [index.html:1000](index.html:1000) + [dashboard.css:5936](dashboard.css:5936)) : new gold-accented "Sign In" button in the topbar, visible only when `_SW.getUser()` is null (toggled in `_renderAccountPanelAuthState`). At boot via `getSession()`, if no session → auto-open the account panel on the Account tab. `toggleAccountPanel` forces the Account tab when opening for a signed-out user (covers both auto-open and manual button click). **(3) Email confirmation signup messaging hardened (Layer B)** ([dashboard.js:929](dashboard.js:929) + [29687](dashboard.js:29687)) : `_SW.signUp` now passes `emailRedirectTo: window.location.origin + window.location.pathname` so the confirmation link lands on the dashboard. `_handleAccountSignup` distinguishes 4 cases : email-already-exists (silent anti-enum, `data.user.identities = []`), pending-confirm (`data.user && !data.session`), auto-signed-in (Supabase setting drifted off — `console.warn` to flag operator), generic success. Max enabled the "Confirm email" toggle in Supabase Authentication → Providers → Email himself (Layer A) — Layer B is defensive. **(4) Dawn = default theme for new users** ([dashboard.js:34919](dashboard.js:34919)) : `loadSavedTheme` fallback (LS empty) swap TradingView → Dawn (via `.name === 'Dawn'` lookup, robust to preset reordering, belt-and-suspenders TradingView fallback if Dawn disappears). Existing users keep their LS theme untouched. **(5) Flipping BE = default BE Management mode for new users** ([dashboard.js:167](dashboard.js:167)) : `appState.ui.beMode` initial seed swap `'be-fallback'` → `'flipping-be'`. Existing users with persisted LS mode keep their choice (restore block lines 196-204 takes precedence). **(6) Remove Import/Export entirely** : `+ Import / Export` button retiré du menu More ([index.html:1019](index.html:1019)), backup panel + handlers + `BACKUP_EXCLUDE_KEYS/_PREFIXES` + `_isBackupExcludedKey` + `backupExportJSON/backupImportJSON` + `toggleBackupPanel/_closeBackupPanel` supprimés. Preset sidebar Export/Import buttons retirés ([index.html:1857](index.html:1857)) + `presetsExportJSON/presetsImportJSON` supprimés. ~350 LOC nettes supprimées, 0 référence orpheline. **(7) Notion auto-hydrate at boot — fix "no data on refresh"** ([dashboard.js:11062](dashboard.js:11062)) : `initDataSource` avait 3 branches selon LS state ; seule la `restoreAPI` (DS_KEY='api' + cache) déclenchait un async blob refresh. Les branches `prevWasAPI && !hasCache` (cache LS purgé) et demo-fallback (DS_KEY null post-logout) laissaient l'user sur un dashboard vide jusqu'à ce qu'il switche de profil manuellement. Nouveau helper inline `_notionAutoHydrate()` gated par `connectionType==='notion' && getIntegration(p).isReady(p)` câblé dans les 3 branches. Pour la branche demo-fallback, un check `_hasNotionProfileButDemoBranch` flip aussi `DS_KEY='api'` avant le render synchronously (évite le flash demo 200-500ms avant que Notion arrive). **(8) Custom Donut widget retiré + remplacé par Custom Table widget** ([dashboard.js:14248](dashboard.js:14248) + [14313](dashboard.js:14313)) : ~330 LOC donut supprimées (`renderCustomDonut`, `_drawCustomDonut`, `_customDonutHitTest`, `_customDonutStateFromCanvas`, `_customDonutStates`, hover + click listeners, `_renderCustomDonutToolbarHTML`, `set-donut-volume-mode` handler). Nouveau widget Table : un row par valeur de field (catégorie), 5 metrics par row (Total R / WR / Avg R / PF / Vol), sortable headers cliquables, click row → drawer avec trades filtrés. Migration boot-time `_migrateDropLegacyDonuts_v1` ([dashboard.js:35719](dashboard.js:35719)) purge les defs `type='donut'` du LS, idempotente. CSS `.cw-table-*` + `.cw-pf-inf` ajouté. **(9) Table widget DA cohérence avec Bar Chart / Heatmap** ([dashboard.js:30551](dashboard.js:30551) + [dashboard.css:14149](dashboard.css:14149)) : icône SVG refait en filled rects (header bar + grille 3×3 avec opacités variées, même langage visuel que la heatmap) au lieu de stroked. Border color du bouton aligné sur Bar Chart (modifier `--table` supprimé → hérite du default `--accent-primary`). Type badge `.cw-list-type-table` partage la même règle que `.cw-list-type-bars`. **(10) Table widget feature complete — bars toggle, sync chain, threshold colors, bigger ∞, Vol header** ([dashboard.js:14313-14545](dashboard.js:14313)) : toolbar header passe d'un 3-tab style switcher (Compact/Cards/Bars) à 2 boutons (⛓ sync + Bars toggle). Renderer unique sync-aware lit settings effectifs via `_getCustomTableEffectiveSettings(def)` (master state si chain on, else def.settings). State machine sync mirrors heatmap pattern : `_customTableSyncEnabled`, `_customTableMasterSettings`, `toggleCustomTableSync(cwId)` (seed master from clicked widget, commit to all defs au turn-off via delete-missing pattern). Bars mode toggle ajoute/retire le gradient bg horizontal sur la cellule Total R (`linear-gradient(to right, color-mix(--g/--r 22%) 0 <pct>%, transparent <pct>% 100%)` avec `<pct> = |sumR|/max(|sumR|) * 100`). Threshold colors WR/Avg R/PF mirror les KPI tiers (`_cwTableWrColor` / `_cwTableAvgColor` / `_cwTablePfColor` réutilisent les seuils canoniques de `_svProgressTone` + `_getWinRateTone`). PF=∞ wrap dans `<span class="cw-pf-inf">` (font-size 1.55em, font-weight 700) pour visuellement séparer le no-loss case. Column reorder final : `Category | Total R | WR | Avg R | PF | Vol` (`Vol` was `#`). Migration legacy `tableStyle` field → `barsMode` boolean idempotente dans `_normalizeCustomWidgetDef`. **Validation preview end-to-end** : tous les scénarios sync (toggle on/off, sort cross-widget, bars cross-widget, commit-on-disable), threshold colors validés sur 5 pairs avec colors mappées correctement (EURAUD: WR blue, Avg R blue, PF blue, Total green ; XAUUSD: tous red), ∞ rendu 18.6px parent 12px (1.55× confirmé), header order final OK. 0 console errors. Cache JS `?v=637 → 700` · CSS `?v=231 → 239`.

- [x] **Preset always-on invariant extended + RAW Baseline truly raw + legacy profile auto-seed pruned — 4 fixes new-user UX** (2026-05-24) — Suite de quatre micro-fixes ciblés signalés par Max sur deux runs distincts, tous validés en preview avec dataset démo. **(1) Delete preset actif → fallback P0** ([dashboard.js:11983](dashboard.js:11983)) : `deletePreset` mettait `activeId = null` quand le preset supprimé était actif, violant l'invariant "user jamais sur 'no preset'" établi le 2026-05-24. Pré-clear des chips (`_removeAllPresetChips + _removeAllLiveChips + _clearComboFilters`) puis `applyPreset(0)` pour basculer sur P0 RAW Baseline avec hydratation complète (snapshot, live slot, ORR bubble, tpConfig, TPM badge, persist LS via `setActivePreset(0)`). Le `_syncLiveSlotFromActiveChips` interne d'applyPreset(0) recrée temporairement un slot empty pour l'id supprimé via `_blankLiveSlot()` (l'activeId pointe encore sur l'id supprimé au moment du sync) — re-supprimé en post via `delete presetLiveFilters[_liveKey(id)] + savePresetLiveFilters()`. Try/catch fallback sur `appState.presets.activeId = null` si applyPreset throw, pour ne jamais bloquer une suppression. **(2) Sync incrémental & Full resync preservent le preset actif** ([dashboard.js:6048-6105](dashboard.js:6048)) : `_handleNotionProfileSyncNow` (force=false, syncNow=true) ET `_handleNotionProfileFullResync` (force=true) routent vers `_loadNotionTrades` qui call `_injectTrades(mergedParsed, 'Notion Live', null)` — entre la branche "first load" qui zappe `activeId = null` in-memory. Mais `setActivePreset` n'est jamais call sur ce path → LS garde l'id user. Fix : après l'await, appel `_restoreActivePresetForCurrentSlot()` gated sur "profile resync est encore actif" (`String(getActiveJournalProfile()?.id) === String(profileId)`). Lit LS → applyPreset(id) → restore complet (chips, ORR, tpConfig, glow). Skip propre si l'user a switché de profil mid-sync. **(3) P0 RAW Baseline = truly raw, plus de base exclusions** ([dashboard.js:11695-11710](dashboard.js:11695) + [35684-35713](dashboard.js:35684) + [35043-35047](dashboard.js:35043)) : Max nouveau user voyait 81/100 trades sur P0 au lieu de 100 — `BASE_M15_EXCLUDE` (`Volume - Faible`, `News Entry`, `Range`) baked dans `presetSnapshots[0].chips.obstacles.excluded` par `_migrateToV2` parce que `DEFAULT_PRESETS[0]` n'avait pas `noBaseExclusions: true`. Fix en 2 temps : (a) flag ajouté à DEFAULT_PRESETS[0] + nouvelle description "Every trade — no filters applied", `_migrateToV2` lit alors `withBase = false` → ne bake plus rien pour P0 (nouveaux users). (b) Migration one-shot idempotente `_migrateRawBaselineToTrulyRaw_v1` câblée au boot juste après `_migrateToV2()`, strip les valeurs `BASE_M15_EXCLUDE` / `BASE_SESSION_EXCLUDE` de `presetSnapshots[0].chips.obstacles.excluded` + `.session.excluded` pour les users existants v2 qui ont déjà migré. Force `p0.noBaseExclusions = true` + nouvelle description. Flag LS `flipping_raw_baseline_truly_raw_v1` empêche re-run. **(4) Profil fantôme "M15 Personnel" legacy pruned** ([dashboard.js:4621-4644](dashboard.js:4621) + [4585-4598](dashboard.js:4585)) : auto-seed pré-OAuth dans `ensureDefaultJournalProfile()` créait un profil pointant sur `https://notion-dashboard-api-2.vercel.app/api/trades` (database backend qui n'existe plus depuis l'OAuth Notion). Max le voyait apparaître en doublon à côté de son vrai profil Notion. Fix : (a) stop d'auto-seed pour nouveaux users (0 profils → land sur Demo + parcours "Connect Notion" manuel). (b) Prune universel via `_isDefaultJournalProfile` (signature exacte pristine : name='M15 Personnel', type='m15', source='api', apiUrl=API_URL_DEFAULT) — toute customisation user casse la signature et le prune skip. (c) Filtre aussi `mergedProfiles` dans `hydrateJournalProfilesFromRemote` avant l'écriture LS, sinon le remote ressuscite le fantôme à chaque sign-in. Le `saveJournalProfiles` interne de `ensureDefaultJournalProfile` push aussi la liste prunée vers remote (delete-missing + upsert pattern). **Validé en preview** : (1) createPreset → addChip TP → deletePreset → activeId=0 / LS="0" / DOM "P0" actif / chips reset à neutral / slot supprimé cleanup OK / P0 slot intact. (2) Simulation `_injectTrades` first-load qui wipe activeId → LS garde "1" → `_restoreActivePresetForCurrentSlot()` → activeId=1, outcome TP réhydraté, DOM bouton "1" actif. (3) Test nouveau user + test user v2 existant avec snapshot BASE baked → tradesShown=100 / filterDataset=100 / obstacles chip excludedSize=0 mode=neutral / migration flag set. (4) Inject 2 profils (real + legacy seed) → après boot, legacy pruné, real survit, `legacyStillPresent=false`. 0 console errors sur tous les scénarios. Cache JS `?v=637` → `?v=645`.

- [x] **Preserve Supabase permanent image URLs across fresh Notion sync** (2026-05-24) — Closes a bug Max reported live: ouvre le dashboard, voit des URLs `prod-files-secure.s3.amazonaws.com` dans le drawer trade au lieu des URLs Supabase permanentes que la media queue avait pourtant uploadées la veille. Diagnostic en 2 mécanismes superposés : **(a)** `_mergeTradesByNotionId(existingCache, parsed)` à [dashboard.js:10709](dashboard.js:10709) est LWW → les Supabase URLs de `existingCache` sont écrasées par les Notion S3 fraîches à chaque sync. **(b)** `_serializeTradesForStorage` à [dashboard.js:8877](dashboard.js:8877) strip déjà les Notion URLs à `''` au moment du LS persist (anti-bloat 2MB) → après un fresh sync, le LS perd brièvement les Supabase URLs jusqu'à ce que la media queue re-persiste 1s plus tard. **Fix** — nouveau helper `_preservePermanentImageUrls(freshParsed, source, priorTrades=null)` à [dashboard.js:8508](dashboard.js:8508) qui construit un lookup `notionId → permanent URLs` depuis `appState.trades.items` (in-memory, le plus à jour avec les patches media queue) + LS cache (cold-start safety net), ou un `priorTrades` explicite quand le caller en a un en scope. Pour chaque fresh trade dont un slot image est une Notion S3 URL ET que le lookup contient une URL non-Notion pour le même `notionId+slot`, swap. Shallow-copy uniquement pour les trades touchés (perf). 4 call sites patchés : `_reapplyAPIOverrides` ([:7993](dashboard.js:7993)), `loadFromAPI` ([:9450](dashboard.js:9450)), `_fetchAPICacheSilently` ([:10013](dashboard.js:10013)), `_loadNotionTrades` ([:10707](dashboard.js:10707) — le path principal pour les profils Notion, avec `existingCache` passé explicitement avant le `_mergeTradesByNotionId`). Bonus : `_mediaQueue.enqueue` reçoit `preservedParsed` au lieu de `parsed` → les trades déjà permanents sont skipped, la queue ne re-uploade que les vrais nouveaux trades. **Décision archi connexe** : Max a confirmé "tous les screenshots sont utiles, même ceux datant de 10 ans" → pas de TTL retention, pas de fallback "re-sync horaire à la place de Supabase". Garder Supabase Storage est non-négociable pour l'UX (un screenshot doit s'afficher instantanément au boot, même 6 mois après le trade, même si Notion est down). **Validé en preview** : 11/11 assertions principales (swap correct, refs préservées sur non-swap, mémoire conservée), 6/6 cas limites (null/undef input, no `_notionId`, prior vide, both-Notion → fresh kept), 0 console errors au boot demo, syntax `node -c` OK. Cache JS `?v=631` → `?v=637`. **Limite résiduelle acceptable** : pour un trade nouveau (jamais migré), la fenêtre [sync fini ↔ media queue passé] (~30s à 5min selon backlog) reste exposée aux Notion URLs ; si hard-refresh dans cette fenêtre, image vide jusqu'à la prochaine batch. Pas le bug que Max décrivait (qui était sur des trades anciens déjà migrés et écrasés par un nouveau sync).

- [x] **Wire Supabase MCP server at project scope + install agent skills** (2026-05-24) — Infrastructure setup so future Dashboard sessions have a project-pinned Supabase tool surface (vs. the account-wide `Claude.ai Supabase` managed MCP that was already loaded). **(1) `.mcp.json`** at project root registers `supabase` HTTP MCP at `https://mcp.supabase.com/mcp?project_ref=pjkilmmltbyugjxbvyvh` — scoped to this project only, no `list_projects` / `create_project` global tools, blast radius limited. Travels with the repo so other machines auto-inherit the entry on next `git pull`. **(2) OAuth completed** via in-CLI `/mcp` → Authenticate → browser flow → 20 `mcp__supabase__*` tools now live (apply_migration, execute_sql, list_tables, get_logs, get_advisors, deploy_edge_function, etc.). **(3) Agent skills** — `npx skills add supabase/agent-skills` installed two universal skills under `.agents/skills/`: `supabase` (general usage triggers: products, client libs, auth, RLS, schema/migrations) and `supabase-postgres-best-practices` (perf tuning, indexes, locks, pooling, RLS perf — ~35 reference files). Both symlinked to Claude Code's skill registry and visible in this session's available-skills list. **Trade-off**: duplicated Supabase tool surface in-session (managed claude.ai one keeps 29 account-wide tools, project-scoped one adds 20 project-pinned), accepted because (a) different scopes serve different use cases, (b) the project-pinned one is the canonical way to scope future Supabase work to this project, (c) the managed one stays useful for cross-project ops (list/create projects, org-level queries). Commit `95405ed` (40 files +2112 lines, all in `.agents/` + `.mcp.json` + `skills-lock.json`, no code touched).

---

## Update rules for agents

At the end of every coding session:
- Move completed items → Done (keep last 10 max, oldest fall off)
- Add any new TODO discovered during the session (with context)
- Update "In Progress" if work is mid-flight
- Commit `ROADMAP.md` in the same commit as the related code change
