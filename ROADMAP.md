# ROADMAP.md — Flipping Market Dashboard

> Single source of truth for task tracking. Updated by agents at the end of every session.
> Supersedes `docs/current-roadmap.md` (kept for historical reference only).

---

## In Progress

_(none)_

---

## TODO — Active

- [ ] **Template chooser — validation pending (errors observed by Max during initial test)** — feature shipped end-to-end across 4 commits (`a135788` data + UI, `ae71f99` delete-last-profile + screenshot backfill, `86319f8` chunks B/C/D wire-up). Max started testing at the end of the 2026-05-20 session, noticed unspecified errors, then closed the session for the day with intent to retest next session. **Next-session todo**: re-run the full test plan from the chunk B+C+D commit message (create M15 profile / create H4 profile / create Other profile / mismatch warning / DB immutable / DB error recovery) and capture the exact symptoms + console output of any error. Likely suspects given the surface area touched: (a) `_applyTemplateLayoutsForProfile` LS write race with `_applyActiveJournalProfile`'s grid load (the layout might be ignored if the grid initialises before the LS keys land — should add a stronger ordering guarantee) ; (b) `_maybeAutoApplyTemplateMapping` short-circuit on `existing && Object.keys(existing).length > 0` may still bite if a brand-new profile somehow has leftover overrides from a deleted profile sharing the LS key (unlikely thanks to profile-scoped keys, but verify) ; (c) `_autoMapScreenshotsByRawKeys` could write garbage if the Vercel proxy stops sending the dual-format payload (camelCase + original) on certain Notion property types — verify on Files & Media specifically. Begin by asking Max to share the exact error + reproduce steps.

- [ ] **Profile/trade mismatch bug — diagnostic in place, awaiting next occurrence** — symptom: profile row highlighted but dashboard shows trades from another profile (observed 1-2 times by Max, not reliably reproducible). Defenses in place (`wasActiveAtStart`, `stillActive()`, `_journalProfileCacheContextOverride`) didn't fully explain how. Diagnostic added 2026-05-20 at 3 strategic points: `_injectTrades` tags `appState.trades.__diagSourceProfileId`; `_diagAssertProfileTradeAlignment` ([dashboard.js:7118](dashboard.js)) fires `console.error('[ProfileTradeMismatch]', ...)` with full state if active profile ≠ tagged profile. When Max next reproduces, console payload captures: callSite, active vs tagged ids, override at inject, dsMode, label, age, inject stack. Suspected race vector: `_applyActiveJournalProfile` finally restores stale `previousOverride` if a sync was in-flight at switch time.

- [ ] **Migrate remaining ~26 inline `connectionType === 'notion'` checks to `INTEGRATIONS` registry** — Strangler Fig follow-up to the 2026-05-19 registry refactor. One micro-fix done at [dashboard.js:5336](dashboard.js:5336) (`_reloadJournalProfileSelection` `isNotionWithDb` now uses `getIntegration(p).isReady(p)`). Rest deferred until MT5 ships — abstracting from a single concrete implementation risks getting the methods wrong.

- [ ] **Bloc 11: mini grid sidebar polish + guide update** — retours d'usage Max attendus before closing. Code: `_msState`, `_msInitGrid`, `_msPreview`, `_msApplyChanges`, `_applyMagnetSnap`.

- [ ] **CSV access path after topbar cleanup** — CSV mode is now only reachable via the "⬆ Import CSV" overlay (empty state CTA + mobile button). Consider adding CSV as a virtual data source row in Data & Integrations, similar to the Demo row added on 2026-05-17.

- [ ] **Concurrent Notion sync mutex** — if the user clicks Sync on profile B while profile A's sync is in flight, both run concurrently with no mutex. Cache writes are profile-scoped so no data loss, but wasted bandwidth. Low priority — defer.

- [ ] **Template auto-mapping — minor fixes deferred from Phase A** — (a) ~~`_resetAPIFieldOverrides()` clears `flipping_detected_template_v1_<profile>`~~ ✅ shipped 2026-05-20 via `_setDetectedTemplate(null)` call. (b) ~~Verify filter chips for `__NO_MAPPING__` dims auto-hide~~ **Verified 2026-05-20 — NOT auto-hidden**. Native filter groups (`#chips-session`, `#chips-h4obs`, etc.) are static HTML in [index.html:1913-2053](index.html), populated by `buildChips()` ([dashboard.js:10806](dashboard.js:10806)) from hardcoded constants (`SESSIONS`, `ALL_H4OBS`, etc.) — not data-driven for visibility. When the active template marks a dim `__NO_MAPPING__`, the chip group still renders with all hardcoded values but every `.chip-n` count = 0. **Fix needed but design decision required**: add hide-when-all-zero logic in `buildChips` (risk: temporarily hides groups during initial load before data populates). Awaiting Max's call on the right trigger condition. (c) ~~`JOURNAL_DIMS[i].desc` surfaces in Mapping panel + elsewhere~~ **Verified 2026-05-20 — TODO was stale**. `JOURNAL_DIMS[].desc` field is **never read anywhere in `dashboard.js`** (confirmed via grep on `.desc`, `dim.desc`, `d.desc`, destructured `{desc}` — all zero hits). The 22 `.desc` strings (~3 KB total) are pure dead data. The CSV guide table in `index.html` (`.csv-doc-desc` cells) uses hardcoded HTML, not `JOURNAL_DIMS[].desc`. **Decision needed**: delete the dead `.desc` fields, or keep them as documentation reserved for a future help-tooltip feature.

- [ ] **Template auto-mapping — real-world end-to-end validation** — Phase A was validated via synthetic samples + mockup interaction round-trip, but the actual flow (community user OAuth-connects fresh → Vercel proxy returns raw trades → `_loadNotionTrades` hook fires → `_detectTemplate` matches → `_maybeAutoApplyTemplateMapping` writes overrides → banner renders + Mapping panel auto-populates) was not exercised because Max's active profile during the session was Demo. Validate by either: a fresh test Notion workspace duplicating one of the 5 templates, or a manual reset → re-sync of one of Max's existing profiles. Watch for any Vercel-proxy column-name normalization that might strip trailing spaces or alter casing — that would silently break detection.

---

## TODO — Deferred

- [ ] **`gs_layout_active` + `gs_layout_<preset>` twin-write** — surfaced 2026-05-20 during Bloc 4. `_saveActiveSlotLive` ([dashboard.js:35937](dashboard.js)) writes the same ~13.5KB payload to two LS keys on every drag/resize (autosave slot + per-preset slot). Semantically different (autosave vs preset-bound state) so global dedup wrap can't help. 27 KB/drag is inherent cost. Reducing requires either: (a) lazy-write per-preset slot only on preset switch (risk of crash-loss), (b) ref `dynamicWidgets` separately to dedupe between slots (major refactor of preset/layout architecture). Touch when preset architecture is next visited.

- [ ] **`_saveActiveSlotLive` over-fires on drags without layout change** — surfaced 2026-05-20 during Bloc 4. ~50% of `gs_layout_*` writes are dedup'd by the LS wrap → meaning the GridStack `resizestop dragstop` handler fires from drag operations that didn't actually move/resize anything (snap-back, micro-clicks). The dedup wrap absorbs the IO cost but JSON.stringify of 13.5 KB still runs. Investigate whether GridStack emits these events spuriously or whether a `_syncGridToLive` no-op check would short-circuit cleanly. Low priority since dedup absorbs the storage cost.

- [ ] **Retention policy design — to spec before any server-side trade storage ships (e.g. MT5)** — today Supabase only holds metadata (~100KB/user) + media; trades live in localStorage so an abandoned user costs nothing. The moment MT5 EA pushes trades to a server endpoint, abandoned accounts start accumulating real volume (~6–30 MB/user). Direction agreed during the 2026-05-19 session: (a) disconnect integration = soft (token revoked, metadata/trades retained), (b) profile manually deleted = soft → hard at 30 days, (c) user inactive 6 months = warning email, archive at 7 months, hard-delete at 12 months, (d) "Delete my account" = soft with 30-day grace period. Implementation: Supabase `pg_cron` or scheduled edge function, runs weekly. Add `last_active_at` column to user/profile rows when the schema is touched next.

- [ ] **MT5 / MT4 / cTrader — actual implementation** — registry stubs (`available: false`) exist since 2026-05-19. Next steps before code: (1) decide MetaApi.cloud SaaS bridge vs. self-built MQL5 EA (cost vs. control); (2) extend Vercel proxy or move to Supabase edge functions for per-broker secret handling (current static-page architecture can't hold per-user API keys safely); (3) spike one platform end-to-end on a single test account before promising to community. cTrader is the easiest technically (OAuth Open API) but smaller audience; MT5 covers most of Max's prop-firm users.

- [ ] **Backend OAuth: dynamic frontend redirect** — `notion-dashboard-api-2` Vercel function uses `DASHBOARD_URL` env var hardcoded to a single front URL (`https://lemarsuoff.github.io/Flipping-Market-Dashboard/`). Conséquence : impossible de tester l'OAuth Notion en local sans flipper la variable (et casser la prod). Fix : à `/api/notion/oauth/start`, lire l'`Origin`/`Referer` de la requête, valider contre une allowlist (`github.io/Flipping-Market-Dashboard/`, `localhost:*`), encoder dans le `state` OAuth, puis y rediriger au callback au lieu de toujours utiliser `DASHBOARD_URL`. Garder `DASHBOARD_URL` comme fallback. Nécessite accès au repo backend `notion-dashboard-api-2` (pas sur cette machine). Contexte complet dans [DECISIONS.md](DECISIONS.md) § 2026-05-19 — Vercel `DASHBOARD_URL` is single-tenant prod.

- [ ] **[Post-readonly] Trading Journal evolution** — manual trade entry, custom schemas, Notion two-way sync. Spec complète dans `docs/journal-evolution.md`. **Ne pas démarrer avant que le read-only soit clos.** Bloquants identifiés : XSS fix ✅ (2026-05-19), Ghost trades ✅ (2026-05-19), Mappings cross-device sync (audit M1) — restent listés dans le doc §9.

- [ ] **Drawdown Intelligence section** — DD by context (setup/session/pair decomposing max DD), Recovery Factor (Total R / |Max DD|), DD Duration distribution, Risk of Ruin estimate. Optional: Underwater curve, Streak/DD scatter, MAR/Calmar (~14 months of data, low priority). Deferred pending community priority signal.

- [ ] **Hardcoded font-sizes (~180 remaining)** — deferred pending community feedback on typography editor coverage.

- [ ] **BYOT + export/import config** — open the dashboard to all users without requiring their Notion credentials. Recommended step before full OAuth rollout. ~1.5 days dev vs weeks for OAuth. Deferred until post-Notion overhaul stabilization.

- [ ] **Mappings not synced cross-device (audit M1)** — `outcomeValueMapping_v1_<profileId>`, `apiFieldOverrides_v1_<profileId>_m15`, `apiFieldNames_v1_<profileId>` absent from `_SYNC_KEYS`. Users reconfigure from scratch on each machine.

- [ ] **Orphaned localStorage keys after profile deletion (audit M2)** — `_deleteJournalProfileById` does not purge scoped keys (`apiTradesCache_*`, `outcomeValueMapping_*`, etc.).

- [ ] **Share view — bottom summary strip** — `WIN DAYS / LOSS DAYS / NO TRADES / BEST DAY / WORST DAY / AVG PER TRADE`. Visible in reference designs, not yet implemented.

- [ ] **Yellow border highlighting on clicked cells** — P&L Calendar, Pair × Session, Monthly P&L in Hover mode.

- [ ] **Security migration — repo privé + hosting Cloudflare Pages + minification bundle (Niveau 1+2)** — surfaced 2026-05-20. Aujourd'hui le repo est public sur GitHub → un voleur fait `git clone + deploy` en 5 min. Plan en 2 étapes faisables en ~3 h en une session : (a) passer le repo en privé + migrer le hosting GH Pages → Cloudflare Pages (free tier, accepte repos privés via GitHub Actions auto-deploy) ; (b) ajouter un step `npx terser dashboard.js -o dashboard.min.js -c -m` dans le CI, `index.html` pointe sur `.min.js` en prod, source reste lisible en local. Workflow dev inchangé (édition locale + refresh). Bénéfice : passe de "clone trivial en 5 min" à "reverse-engineer pénible d'un bundle minifié". **Timing libre — pas urgent aujourd'hui** (audience = 600 community members distribuée par Max, pas viral, pas monétisé, pas indexée). À faire **avant** un des 4 triggers : (1) lancement public visible (Twitter / YouTube / Product Hunt), (2) monétisation, (3) lancement MT5 (élargit l'audience), (4) repost externe non contrôlé. Niveaux 3 (auth gate via Supabase Auth) et 4 (secret sauce côté serveur via Edge Functions) restent réservés au pivot SaaS payant — sur-engineering tant que freemium community.

- [ ] **Add LICENSE file (MIT recommended)** — currently the public repo has no license, so legally all rights reserved by default. Not urgent since users don't access GitHub directly, but worth adding for legal clarity.

- [ ] **Add README.md** — public-facing landing page on GitHub. Low priority since users access the dashboard via GitHub Pages link, not the repo itself. Would matter if/when the repo becomes a public-facing project (contributions, forks, etc.).

---

## Done — Last 10

- [x] **Template chooser feature shipped end-to-end (chunks A → D) + screenshot rename + screenshot auto-map backfill from raw Notion column names** (2026-05-20) — Closes the 5-question scope freeze from yesterday evening. Across 3 commits (`a135788` data + UI, `ae71f99` screenshot backfill + delete-last-profile, `86319f8` mapping/layout/remove-DB-UI hooks) : (1) `TEMPLATE_LAYOUTS` hardcoded for m15 / h4 / other buckets — m15 = Max's canonical layout, h4 = same minus w-m15 (totally excluded, not just hidden), other = canonical minus w-hour / w-m15 / w-h4 with w-pair-session + w-heatmap side-by-side ; (2) `TEMPLATE_MAPPINGS.other` adds the 4-dim exclusion overlay (`obstacles`, `h4`, `badFeeling`, `hour` → `__NO_MAPPING__`) ; (3) Add profile dialog gains a `<select>` Template field below the DB picker (3 options, immutable post-creation, persists on `profile.templateChoice` via `_normalizeJournalProfile` + `_getJournalProfileEditorDraft`) ; (4) `_maybeAutoApplyTemplateMapping` branches on `profile.templateChoice` — M15 / H4 buckets force their default with a toast on mismatch, Other layers exclusions atop detected mapping ; (5) `_applyTemplateLayoutsForProfile` seeds `gs_layout_active_<id>` + `gs_hidden_widgets_<id>` on profile creation so the grid renders with the chosen layout from frame 1 ; (6) "Change database" affordance removed from the profile editor block + the row's ⋯ menu (DB immutable rule) — the jp-db-error-block "Select another database" stays as the only escape hatch when a DB becomes unavailable. **Screenshot rename** (γ scope) : `JOURNAL_DIMS` labels `M15 Before screenshot` / `H4 Before screenshot` / `M15 After screenshot` renamed to `TV Image 2` / `TV Image 1` / `TV Image 3` ; `_LB_STEP_LABELS` mirrored ; both `_getJournalDimLabel` and `_getTradeMediaStepLabel` get an override-first short-circuit so the mapped Notion column name surfaces as the label everywhere (Mapping panel + modal + tradelog button tooltips). Removed the hardcoded H4-mode 'H4 After' branch — covered by the override path. The user-visible behavior keeps the Notion column name regardless of whether the file lives on Notion-origin or has been migrated to Supabase (the mapping is the canonical identifier of the slot, not the hosting URL). **Screenshot auto-map backfill** (`_autoMapScreenshotsByRawKeys`) runs after `_maybeAutoApplyTemplateMapping` in `_loadNotionTrades` : for each screenshot dim without an existing override, scans the raw sample for the camelCase alias with data, then matches its JSON-stringified value against every space-containing raw key to find the original Notion property name (e.g. "M15 before" or "Files M15 prout") — writes that as the override and tags the dim auto-mapped (green). Handles Files & Media properties whose names don't match any canonical `TEMPLATE_MAPPINGS` value. **PO mini-tiles drag fix** (`1f70352`) for Brave/Mac : the `_poInternalInitEdit` drag handles now stop `mousedown` + `touchstart` in addition to `pointerdown` so GridStack's outer-widget draggable doesn't grab the click. **Delete-last-profile** : `_deleteJournalProfileById` no longer refuses when `profiles.length <= 1` — when the last profile is deleted and it was active, falls back to Demo via `DS_KEY = 'demo'` + `loadBuiltinCSV()`, toast confirms the switch. **Status at session close** : Max started initial validation, noticed unspecified errors, closed the session with intent to retest. Surfaced as new TODO Active "Template chooser — validation pending" with debugging-suspect hints.

- [x] **Option A — Track template-auto-mapped overrides separately so Mapping panel renders them green (`is-auto-mapped`) instead of gold (`is-remapped`)** (2026-05-20) — User reported all dims showing gold/blue dots in the Mapping panel after a fresh sync triggered the template auto-detect, but those colours were originally designed for "user manually picked via pencil" — auto-mapping is not a user action so the visual was dissonant. **Architecture (Option A from 3 proposed):** new LS key `flipping_auto_mapped_dims_v1_<profile>` stores the `Set` of dim keys whose override was written by `_maybeAutoApplyTemplateMapping`. Helpers near `_setDetectedTemplate` ([dashboard.js:6135-6173](dashboard.js)): `_getAutoMappedDims` / `_setAutoMappedDims` / `_removeAutoMappedDim` / `_clearAutoMappedDims`. **4 lifecycle hooks**: (a) `_maybeAutoApplyTemplateMapping` writes the set when applying ([dashboard.js:6234](dashboard.js)) ; (b) `pickAPIField` removes the picked dim on user manual pencil-pick ([dashboard.js:29929](dashboard.js)) — flips green→gold instantly ; (c) `_resetAPIFieldOverrides` clears the set ([dashboard.js:6308](dashboard.js)) ; (d) `_clearNotionProfileMappings` adds the new LS key + the existing `flipping_detected_template_v1` key to its wipe list ([dashboard.js:4615-4621](dashboard.js)) — **bonus fix** for a separate stale-banner bug where changing a profile's database kept showing 'Auto-detected: <previous template>'. **Render**: 2 new statuses `is-auto-mapped` / `is-auto-no-mapping` routed at [dashboard.js:28846-28859](dashboard.js) via `autoMappedDims.has(dim.key)`, classified as 'mapped'/'hidden' in grouping at [28590-28598](dashboard.js). **CSS** ([dashboard.css:1777-1789, 1812-1815](dashboard.css)): new `.ljp-item.is-auto-mapped, .ljp-item.is-auto-no-mapping` (green border + 5% green bg) mirroring `is-ok`, and `.ljp-status-dot.is-auto-mapped, .ljp-status-dot.is-auto-no-mapping { color: var(--g); }`. **Validated end-to-end on Max's M15 Pro profile**: Reset+Sync wrote 20 keys to `autoSet`, panel renders green dots, manual pencil-pick on one dim would flip it to gold. **Final semantics**: 🟢 green = automatic (canonical via `is-ok` OR template-auto via `is-auto-mapped`), 🟡 gold = user manually picked (`is-remapped`), 🔵 blue = user picked 'No Mapping' (`is-no-mapping`), 🔴 red = missing. **Operational note**: PostToolUse cache-bust hook is installed in a different worktree (this branch was edited from `sharp-hoover-38e66d` then files copied to `stupefied-chatterjee-861be9` to feed the active preview on port 8000), so `index.html` was bumped manually (CSS 218→219, JS 396→397). Surfaced as TODO Deferred: hook install replication across worktrees.

- [x] **Database picker dropdown sorted alphabetically (Add profile + Edit profile)** (2026-05-20) — Added a single `.sort()` with `localeCompare` (`sensitivity: 'base'`) at the point where `_notionDbPickerDatabases` is filled from the Vercel backend ([dashboard.js:4506](dashboard.js)). Both render sites — `_renderJournalProfileEditorMarkup` ([dashboard.js:5197](dashboard.js)) and `_renderNotionEditorDbSection` ([dashboard.js:4722](dashboard.js)) — automatically benefit without code duplication. Notion's `/search` endpoint returns by recency which produced unpredictable order, painful for Max scanning a 4-entry list with 2 identically-named "Flipping Trading Journal" duplicates to find the right one. Case-insensitive so "h4" sorts identically to "H4".

- [x] **Dead `setSourceIndicator` chain cleanup (function + `_DS_LABELS` const + 32 call sites) + dead CSS (`.data-source-badge` + `.data-source-dot` variants) + verified Template Phase A sub-items (b) and (c)** (2026-05-20) — Follow-up to yesterday's `showDataStatus` cleanup. `setSourceIndicator` ([dashboard.js:7112](dashboard.js)) read `#data-dot` + `#data-source-label`, both gone from `index.html`. Same dead-chain shape. **JS**: removed the function + the `_DS_LABELS` const it consumed, then `sed -i '' '/setSourceIndicator(/d' dashboard.js` bulk-deleted all 32 call sites (29 standalone + 3 if-prefixed at [8484, 8525, 8542]). The 3 if-prefixed sites all had `setSourceIndicator(...)` as their only action so whole-line deletion was safe. **CSS**: removed `.data-source-badge` block + 5 `.data-source-dot` variant rules (~30 lines) — confirmed dead via grep (no element gets this class anywhere, and `@keyframes ds-pulse` had already been removed yesterday so the leftover `animation: ds-pulse` references were broken). Net JS ~-40 lines, CSS ~-30 lines. Brace balance 9384/9384, `node -c dashboard.js` clean. **Bonus** — autonomous verification of Template Phase A sub-items (b) and (c) (without code changes since they require design decisions): (b) `__NO_MAPPING__` dim auto-hide does NOT happen (filter groups static in HTML, `buildChips` not data-driven for visibility) — see Active item for details ; (c) `JOURNAL_DIMS[].desc` field is dead data, never read anywhere — see Active item.

- [x] **Dead status-chain cleanup (`showDataStatus` / `_updateLastSync` / `_syncDsButtonTitles`) + dead CSS + `_resetAPIFieldOverrides` clears detected-template key** (2026-05-20) — Surfaced from yesterday's `ds-refresh-btn` cleanup: `#ds-status-dot`, `#ds-status-text` and `.ds-toggle-btn` are all gone from `index.html`, so the entire status-indicator chain was no-op. **JS**: deleted the 3 function definitions plus all callers via sed (`showDataStatus` × 33 callers — 31 standalone + 2 if-prefixed; `_updateLastSync` × 16 callers; `_syncDsButtonTitles` × 1 call in `updateSourceUI`). No `else showDataStatus` patterns existed so bulk-delete was safe. **CSS**: removed entire `/* ══ DATA SOURCE SYSTEM ══ */` block (`.ds-toggle-group`, `.ds-toggle-btn` + variants, `.ds-btn-age`, `.ds-status-bar`, `.ds-status-dot` + variants, `@keyframes ds-pulse`, `.ds-last-sync`) ~85 lines, plus the dead rules inside `@media (max-width: 1680px)` (`.ds-btn-label`, `#ds-btn-age`, `.ds-toggle-btn`, `.ds-status-bar`, `.ds-last-sync`) and the `.ds-toggle-btn` rule inside `@media (max-width: 900px)`. Updated the topbar-compaction comment block to drop the stale `_syncDsButtonTitles()` reference and the obsolete "<=1680: data-source labels, status bar hidden" bullet. **Bonus fix**: `_resetAPIFieldOverrides` now calls `_setDetectedTemplate(null)` so the Mapping panel banner stops showing "Auto-detected: …" between reset and re-sync (Phase A sub-item (a)). Net JS -95 lines, CSS ~-90 lines. Verified: `node -c dashboard.js` clean, browser load identical (KPI strip + equity + outcome + monthly P&L render unchanged, no console errors). Surfaced for follow-up: `setSourceIndicator` is also dead (reads `#data-dot` + `#data-source-label`, both absent from HTML) — added to TODO Active.

- [x] **Dead `ds-btn-demo` / `ds-btn-csv` / `ds-btn-api` references + `_formatCooldown` orphan** (2026-05-20) — Two small dead-code cleanups in one pass. **(1)** `updateSourceUI` ([dashboard.js:7325](dashboard.js:7325)) had 8 lines of `getElementById('ds-btn-demo'|'ds-btn-csv'|'ds-btn-api')` + `classList.toggle('active')` guards over IDs that no longer exist in `index.html` since yesterday's `ds-refresh-btn` cleanup. The `if (btnX)` guards always short-circuited so the block was pure dead code — removed. **(2)** CSS rule `#ds-btn-api[data-cooldown]:not([data-cooldown=''])::after` ([dashboard.css:10630](dashboard.css)) targeted a non-existent element — removed (~8 lines incl. comment). **(3)** `_formatCooldown` ([dashboard.js:7104](dashboard.js)) had zero callers since `refreshAPISource` was removed yesterday — removed (5 lines). The TODO claimed `_syncDsButtonTitles` also calls `getElementById` on the dead IDs, but inspection showed it queries `.ds-toggle-btn` instead (a different dead selector, addressed in the follow-up status-chain cleanup above). Verified: JS syntax clean, browser load identical.

- [x] **Bloc 4: persistence/refresh investigation + global localStorage dedup wrap** (2026-05-20) — Audit instrument shipped at top of `dashboard.js` (gated `?lsAudit=1`, exposes `window.__lsAudit({sortBy:"total"|"redundant"|"bytes"})`). Wrapped `localStorage.setItem` globally so every call site short-circuits when the value matches what's already stored. Measured impact on 30s real usage (Max's session, 658-trade M15 profile): 232 setItem calls → only 80 actually reach storage (66% dedup'd) → **~800 KB of redundant writes neutralized**. Targeted fixes: (a) `loadSavedTheme` ([dashboard.js:32395](dashboard.js)) — boot merge only re-writes if `serialized !== saved`, kills the 4.6 KB redundant write per refresh; (b) sidebar state — unified sync (`_persistSidebarState`) + debounced (`_persistSidebarStateDebounced`) writers through a shared helper `_writeSidebarStateIfChanged` with first-call LS comparison; (c) `_saveBootSnapshot` ([15484](dashboard.js)) — removed dead `savedAt: Date.now()` from payload (only `topbarStatsHtml` is read on boot per index.html restore script), payload now stable when topbar HTML doesn't change; (d) `_commitLinkedSettingsToAllCustomHeatmaps` ([18058](dashboard.js)) — fixed `changed = true` bug that fired on every call regardless of whether settings actually differed (heatmap threshold slider was writing 22 KB on every move). All fixes verified live via the audit instrument (preview iframe + Max's real browser tab). The `safeSetLocalStorage` helper also got a transitive dedup short-circuit for defense in depth. Architecture decision logged in DECISIONS.md.

- [x] **Dead `ds-refresh-btn` topbar button + transitive dead code** (2026-05-20) — Hidden button at `index.html:985` (`display:none`, `updateSourceUI` always set visible=false, no caller toggled it true) removed. Cleaned the full chain: action handler `case 'refresh-api-source'` ([dashboard.js:2051](dashboard.js)), function `refreshAPISource()` (its only invoker was the dead button), `_setBtnCooldown()` (targeted `ds-btn-api` which also no longer exists in HTML), `getAPICooldownRemainingMs()` (only consumed by `refreshAPISource`), 4 spinning-class manipulations (`loadFromAPI` add/remove + `_loadNotionTrades` add/remove), `btnRef` reference in `updateSourceUI`. CSS: `.ds-refresh-btn`, `:hover`, `.spinning`, `@keyframes ds-spin`, mobile `.ds-refresh-btn` override. Preserved: `API_REFRESH_COOLDOWN_MS` constant (still used at [8855](dashboard.js)). ~110 lignes nettes. Adjacent finding: `ds-btn-demo/csv/api` IDs also gone from HTML but still referenced in JS+CSS — surfaced as a new TODO Active (same shape cleanup).

- [x] **Dead `_renderNotionSyncSection` + transitively dead `_getNotionSyncView` / `_formatNotionSyncAgo` + CSS** (2026-05-20) — Mutually-exclusive render gates: outer caller `_renderJournalProfileSwitchList` ([dashboard.js:5271](dashboard.js)) invokes `_renderNotionDbSection` when `pickerOpen || !hasDb`; inside that function the call to `_renderNotionSyncSection` ([5146](dashboard.js)) was in the `!isPickerOpen && hasDb` branch — never satisfied simultaneously. Function had been orphaned when the "↻ Sync now" button moved into the row-action icon ([5247](dashboard.js)) — the user-visible icon button is independent and remains. Removed the function, its only call site, plus `_getNotionSyncView` and `_formatNotionSyncAgo` (only callers were the dead function), plus 71 lines of CSS for `.jp-notion-sync-panel*` / `.jp-notion-sync-btn*`. ~110 lignes nettes. `_formatNotionSyncAgoCompact` is a different function and remains alive.

---

## Update rules for agents

At the end of every coding session:
- Move completed items → Done (keep last 10 max, oldest fall off)
- Add any new TODO discovered during the session (with context)
- Update "In Progress" if work is mid-flight
- Commit `ROADMAP.md` in the same commit as the related code change
