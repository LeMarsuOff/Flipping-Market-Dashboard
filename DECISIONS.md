# DECISIONS.md — Flipping Market Dashboard

> Append-only log of non-trivial architectural decisions.
> Format: ### YYYY-MM-DD — Title / Context / Decision / Consequence
> Never edit past entries. Add new entries at the bottom.

---

### 2026-03-10 — React prototype started

**Context:** Initial exploration of a dashboard for personal trade journaling. React chosen as a familiar starting point.

**Decision:** Begin with a React prototype.

**Consequence:** Prototype validated the data model and widget concepts but introduced toolchain complexity (bundler, hot reload, npm deps) that felt disproportionate for a single-user tool.

---

### 2026-03-17 — Pivot React → vanilla HTML/CSS/JS

**Context:** React prototype existed but the build step and component abstraction added friction. The project is maintained by one person with no CI pipeline.

**Decision:** Rewrite as a single-file vanilla JS/CSS/HTML stack — no bundler, no framework, no npm.

**Consequence:** Simpler deployment (open file in browser = done). No build step. Easier for community members to fork and self-host. Codebase now ~50k lines across 3 files (`index.html`, `dashboard.js`, `dashboard.css`). Trade-off: no tree-shaking, no type safety, monolithic files are hard to navigate — mitigated by the function map in `docs/file-map.md`.

---

### ~2026-04 — BYOT over OAuth as recommended first step for multi-user access

**Context:** Explored opening the dashboard to all Flipping Market community members (~600 users). Full OAuth integration with Notion would require user account management, RGPD compliance review, and Notion integration approval.

**Decision:** Recommend BYOT (Bring Your Own Token) + export/import config as the first step, before committing to full OAuth.

**Consequence:** ~1.5 days dev vs weeks for OAuth. RGPD/Notion review avoided in the short term. Still deferred pending post-Notion-overhaul stabilization.

---

### ~2026-04 — Raw API trade cache moved in-memory only

**Context:** The raw Notion API response cache was stored in localStorage, causing quota overflow errors (~4.6 MB raw cache exceeded the 5 MB browser limit for some users).

**Decision:** Keep the raw trade cache in-memory only (`appState.trades.items`). Store only `apiFieldNames_v1` (field mapping, small) in localStorage. Trade data is refetched on page refresh via incremental sync.

**Consequence:** No more localStorage quota errors. Cache is lost on page refresh (acceptable — incremental sync is fast). Agents must remember to purge `apiTradesCache_v2_rrmax_*` after structural normalization changes.

---

### ~2026-04 — Dataset B as primary analysis reference

**Context:** Two datasets were defined: Dataset A (excludes BE-TP/BE-SL trades from all calculations) and Dataset B (includes all outcomes including BE). The dashboard historically used Dataset A as primary for "clean edge analysis."

**Decision:** Dataset B is primary (operational truth). Dataset A is retained as a crosscheck lens available via filter.

**Consequence:** All EV calculations, KPIs, and default filter presets operate on Dataset B. This matches how a live account actually performs — BE trades are real outcomes with real risk. Dataset A analyses are opt-in via the outcome chip filter.

---

### ~2026-04 — Schema v2 for preset snapshots (unified Filters ↔ Presets)

**Context:** Preset system v1 stored filter state in multiple separate keys (`flipping_preset_overrides`, per-dim arrays on the preset object itself). Adding custom Notion chip support required a unified snapshot format.

**Decision:** Introduce `flipping_preset_snapshots_v2` — a single JSON blob per preset containing all chip state (`chips`, `customChips`, `tpConfig`). Boot-time `_migrateToV2()` silently upgrades v1 data. v1 fields on preset objects are zeroed to prevent drift.

**Consequence:** Cleaner preset isolation. Custom chips and TP config are fully preset-scoped. Breaking this key without a migration silently destroys all user presets — treat as a contract.

---

### 2026-05-15 — ROBUST-11: two-layer CSS injection hardening in `importTheme`

**Context:** The theme import function (`importTheme`) accepted arbitrary CSS values from a JSON blob (user-controlled or pasted). A malicious or malformed theme could inject `url(https://evil.com)` or `expression(...)` into CSS custom properties.

**Decision:** Add `_isSafeCssValue(val)`: a blacklist check (`url(`, `expression(`, `javascript:`, `data:`) with a 200-char cap. Add a second layer: normalize CSS unicode escapes (`\HH` through `\HHHHHH`) before the blacklist check, preventing bypass via `\75 rl(...)` → `url(...)` at parse time. Fail closed on normalization errors.

**Consequence:** Theme import is now hardened against CSS injection. Values failing the check are dropped with `console.warn`. Legitimate theme values (hex colors, pixel values, font-weight numbers) are unaffected. PR #22.

---

### 2026-05-17 — Workflow restructure: repo-level markdown as shared agent memory

**Context:** Multiple agents (Claude Code, Codex), two machines (Mac + Windows), no shared persistent memory. Context drift between sessions caused repeated re-explanations of architecture and repeated mistakes with localStorage keys and preset schema.

**Decision:** Introduce `CLAUDE.md`, `AGENTS.md`, `ROADMAP.md`, and `DECISIONS.md` at the repo root as the single source of truth for all agents. Every session starts with reading `CLAUDE.md` + `ROADMAP.md`. Every session ends with updating `ROADMAP.md` and optionally appending `DECISIONS.md`.

**Consequence:** Agents start each session with full architectural context without relying on conversation history. Roadmap and decision history are version-controlled alongside code. Trade-off: files must be actively maintained — a stale `ROADMAP.md` is worse than none.

---

### 2026-05-17 — Audit post-Notion overhaul: zero fixes applied, findings deferred

**Context:** Notion OAuth integration, journal profiles, MediaQueue/Supabase storage, incremental sync, outcome value mapping, and cache mode changes added ~9k lines between mid-April and mid-May 2026. A full read-only audit was conducted.

**Decision:** Apply zero fixes during the audit session. Dashboard conserved as-is. All findings documented in `docs/current-roadmap.md` and backlogged in `ROADMAP.md` for prioritization.

**Consequence:** SEV-HIGH findings (XSS at 6 sites, ghost trades) are known but unpatched. SEV-MEDIUM findings (mappings not synced cross-device, orphaned LS keys) are documented. The audit confirmed the calc engine passes (84 test cells, 6 probes, 0 regressions). Future sessions must treat the SEV-HIGH items as priority blockers before any community-facing distribution push.

---

### 2026-05-17 — Race-guard pattern for async profile-scoped writes

**Context:** `_loadNotionTrades` does a multi-second `await fetch` against the Notion proxy. If the user switches the active journal profile while the fetch is in flight, the response landed in the *new* active profile's cache slot (because `getProfileScopedKey()` reads the live active profile at call time) and `_injectTrades` overwrote the dashboard the user was now looking at. Cache pollution + UI pollution.

**Decision:** Adopt a two-part guard inside every async function that touches profile-scoped state:
1. **Capture the profile context at function entry** — `const profileId = String(profile?.id || '').trim()`.
2. **Pin the cache scope after the await** — set `_journalProfileCacheContextOverride = profileId` so all subsequent `getProfileScopedKey` resolutions go to *the captured profile* regardless of who is currently active. Restore in a `finally` block.
3. **Guard UI side-effects with `stillActive()`** — a helper closure that compares the captured `profileId` against `getActiveJournalProfile()?.id`. UI writes (`_injectTrades`, `setSourceIndicator`, `showDataStatus`, `_updateLastSync`, etc.) only fire when the captured profile is still the active one.

**Consequence:** A's cache always lands in A's slot. B's UI is never overwritten by A's response. Profile meta (`notionTradeCount`, `notionSyncState`) was already safe because `_updateJournalProfileSyncMeta(profileId, ...)` takes an explicit ID. **Apply this pattern** to any future async function that mixes profile-scoped storage writes with UI updates (e.g., `_loadFromAPI`, media queue handlers). The mutex against *concurrent* syncs (two profiles syncing at once) is **not** covered by this pattern — that's a separate deferred issue.

---

### 2026-05-17 — Demo as virtual data source row (vs. real journal profile)

**Context:** User asked to move the Demo data source from the topbar's mode toggle into the Settings → Data & Integrations data source list, as the first entry. Two paths considered: (A) introduce a new `connectionType: 'demo'` journal profile that synthesizes 100 sample trades, (B) render a virtual non-persisted row at the top of `_renderJournalProfileSwitchList` with a dedicated `setDataSource('demo')` action.

**Decision:** Path B — Demo is **not** a real profile. It's a virtual row injected at render time with `data-profile-id="__demo"` and `data-action="journal-profile-row-switch-demo"`. Clicking it bypasses `_handleJournalProfileSelectChange` entirely and calls `setDataSource('demo')` directly. The existing `setActiveJournalProfile` state is untouched when Demo is active; the `is-active` highlight is computed from `localStorage.getItem(DS_KEY) === 'demo'` and overrides the per-profile highlight in the list (`!demoActive && active?.id === profile.id`).

**Consequence:** No new persisted state. No migration. No Supabase row. Demo card has no sync button, no menu, no editor (because it's not a profile — those actions don't apply). The 3 topbar mode buttons (Demo/CSV/API) and the `ds-status-bar` were removed in the same session — all data-source switching now flows through this panel. **Caveat:** CSV mode is now only reachable via the "⬆ Import CSV" overlay (empty state CTA + mobile button). Adding CSV as a sibling virtual row is tracked in `ROADMAP.md` under "CSV access path after topbar cleanup".

---

### 2026-05-18 — Local automation layer: spec-in-CLAUDE.md, files gitignored

**Context:** The user works on two machines (Windows + Mac) and is non-technical (does not read code, verifies changes by refreshing localhost). Two recurring frictions were identified: (1) forgetting to bump `?v=N` in `index.html` after editing `dashboard.js`/`dashboard.css` → user refreshes and sees the old cached version, wastes minutes diagnosing; (2) syntax errors slipping through to delivery → user sees a blank/broken dashboard and has to flag it back. Three options for fixing this: (A) commit hook scripts and slash commands to git so both machines share them, (B) keep all local config gitignored under `.claude/` and require per-machine setup, (C) split — keep scripts under `scripts/` (tracked) and only `settings.local.json` (gitignored).

**Decision:** Path B — full per-machine setup, all under `.claude/` (already gitignored). The **spec** for hooks + slash commands lives in `CLAUDE.md` §12 / `AGENTS.md` §12 (committed). On a fresh machine, the user says "install hooks and commands here" and the agent regenerates everything from the spec. Six slash commands are canonical: `/start`, `/end`, `/localhost`, `/status`, `/undo`, `/push`. The hook script is OS-specific (PowerShell on Windows, bash on macOS) — the spec describes the behavior, not the syntax, so each machine gets a native script.

**Consequence:** Local config never collides between machines (no LF/CRLF flame wars, no Mac scripts in a Windows checkout, no permission-mode drift). The price: a one-shot install per machine (~2 minutes). The benefit: the agent on a fresh machine reads CLAUDE.md §12 and rebuilds everything without ambiguity — the spec is sufficient (no git-tracked script source needed). This pattern should generalize to any future per-machine automation (test runners, browser launchers, etc.) — always keep `.claude/` gitignored, always put the spec in CLAUDE.md §12.

---

### 2026-05-19 — Integrations vs Profiles split: registry pattern, gradual migration

**Context:** Long-term direction is multi-source support beyond Notion: MetaTrader 4/5 (via Expert Advisors), cTrader (Open API OAuth), and potentially Match-Trader / DXtrade / direct broker REST APIs. Today the dashboard's ~30 dispatch sites all branch inline on `profile.connectionType === 'notion'` (else fallback to legacy 'api'). Adding a new source would require finding and editing every one of those sites — high risk of partial migration, easy to miss edge cases. Two surfaces will eventually emerge in the UI: an **Integrations** page (one-time setup per source type) and a **Profiles** page (N profiles per integration, day-to-day switching). The data model split (integrations table + profiles table with FK) is anticipated but not yet implemented; today's `journal_profiles` rows still embed all integration data.

**Decision:** Introduce a runtime registry `INTEGRATIONS[type]` in `dashboard.js` (~line 4625, just after `_renderNotionEditorDbSection`) as the single source of truth for the set of supported / planned data sources. Each entry follows a fixed interface (`id`, `label`, `description`, `icon`, `available`, `isLegacy`, plus methods `isReady(p)`, `getStatusLabel(p)`, `renderEditor()`). Methods are arrow functions so referenced helpers resolve at call time (no hoisting issues across the 27k-line file). A helper `getIntegration(profileOrType)` returns the registry entry, falling back to the `notion` entry for unknown types rather than throwing. Five entries are seeded: `notion` (active, full methods), `api` (active, marked `isLegacy: true`, no UI), `mt5` / `mt4` / `ctrader` (planned, metadata only — `available: false`). The ~30 existing dispatch sites are **not** all migrated at once — only 3 highest-leverage ones in this initial commit (`_renderJournalProfileEditorMarkup` legacy branch + Notion section render, `_getJournalProfilesConnectedCount`, and the connection label at line 27916 area). Remaining sites migrate incrementally (Strangler Fig pattern) as new integrations land or as touched files are revisited for other reasons.

**Consequence:** Adding a fourth or fifth integration (e.g. when MT5 ships) is now bounded: write a new registry entry implementing the interface, no need to chase 30 call sites. The 3 migrated sites already benefit (`_getJournalProfilesConnectedCount` works for any future integration without a code change). Trade-off: until the remaining ~27 sites migrate, the registry is "leaky" — some code still branches inline on `connectionType === 'notion'`, so a new integration would partially work (count + editor + label) but fail at sync/lifecycle paths. The mockup `mockups/integrations-vs-profiles.html` documents the eventual UI target (Integrations page iterates `Object.values(INTEGRATIONS)` showing planned ones as "Coming soon" cards). CSV and Demo are deliberately **not** in the registry — CSV is a one-shot import (no auth, no sync engine) and Demo is a fixed dataset; both stay on their existing code paths until the Integrations page UI is built and we decide whether to model them as pseudo-integrations.

---

### 2026-05-19 — Ghost-trade reconciliation: 24h auto-trigger + invisible background full sync

**Context:** Notion's incremental sync endpoint (`?last_edited_after=<cursor>`) cannot surface deletions — a deleted page just stops appearing in subsequent responses, with no tombstone, no `_deleted` flag, no signal. The local cache (`apiTradesCache_v2_rrmax_<profile>_<source>`) therefore accumulates "ghost trades" indefinitely: rows the user removed in Notion that keep polluting equity curves, KPIs, and heatmaps. Severity tagged SEV-MED in the 2026-05-17 audit. Three cadence options were considered: (a) full sync on every Sync click — quadruple-digit cost for a deletion that happens once a week at most; (b) manual-only button — discoverability is near zero, 90%+ of community users will never use it and the bug persists; (c) periodic auto-trigger — balanced cost / detection latency, invisible to the user.

**Decision:** Implement option (c) with a 24-hour cadence (`_FULL_SYNC_INTERVAL_MS`). After every successful non-force `_loadNotionTrades` call, `_maybeQueueGhostSyncReconcile(profile, force)` checks `Date.now() - _getLastFullSyncAt() > 24h`; if true, it schedules a follow-up `_loadNotionTrades(profile, { force: true, background: true })` via `setTimeout(0)`. The `setTimeout(0)` defer is load-bearing: it lets the outer call's `finally` block restore `_journalProfileCacheContextOverride` first, otherwise the recursive call would read a stale cache scope after the outer's finally fires post-`await`. The reconcile leverages the existing `force: true` path, which already replaces the cache wholesale (`mergedParsed = parsed` when `fullSync`) — purge is implicit, no separate diff/delete logic needed. A new `background: true` option to `_loadNotionTrades` suppresses every transient UI artifact: no "Loading…" toast, no "syncing…" pill flash, no success toast, no source indicator change, no `_injectTrades` if `purgedCount === 0`. The user sees nothing unless a ghost is actually purged. Power-user escape hatch: a manual "Full resync" item in the profile row's ⋯ menu (`dashboard.js:5252`), gated on `connectionType === 'notion'`, calls `_loadNotionTrades(profile, { force: true })` with normal UI feedback and a completion toast (`N ghost(s) purged · M total` / `complete · M trades · no ghosts` / `failed`). Result communicated to the manual handler via a module-level `_lastFullSyncResult = { purgedCount, tradeCount, ranAt }` sentinel — chosen over modifying `_loadNotionTrades`'s return signature (would touch too many call sites).

**Consequence:** Ghost trades self-purge with at most ~24h latency, fully transparent in the common case. Bandwidth cost: one extra full Notion fetch per user per day (~200–500 trades = ~50–200 KB), each user's own OAuth token (no global rate limit) — negligible at 600-user scale. Edge case: if the user deletes ALL trades in Notion, the empty-trades branch (`dashboard.js:8606`) sets the profile to error state — acceptable, that's a destructive op the user shouldn't accidentally trigger. Concurrent sync mutex still missing (already a TODO Active) — clicking manual "Full resync" while a background reconcile is in flight runs both concurrently; cache writes are profile-scoped so no data loss, just wasted bandwidth. The initial implementation placed the manual button inside `_renderNotionSyncSection` — which turned out to be unreachable dead code (contradictory render gates between `dashboard.js:5267` and `dashboard.js:5136`). Moved to the ⋯ menu in the profile row; the dead-code finding logged as a TODO Active cleanup item.

---

### 2026-05-19 — Vercel `DASHBOARD_URL` is single-tenant prod (no dev/prod split)

**Context:** Mid-session, OAuth Notion redirected to `localhost:8766` after authorization (Max's local front runs on `localhost:8000`). Investigation : la function Vercel `notion-dashboard-api-2` lit `DASHBOARD_URL` (env var) pour rediriger après le callback OAuth. La variable était `http://localhost:8766` scope **Production AND Preview** depuis 2 jours → la prod (github.io users, ~600) était silencieusement cassée pour toute nouvelle connexion Notion. Aucun mécanisme dans le code backend pour distinguer prod vs dev front.

**Decision:** Remettre `DASHBOARD_URL = https://lemarsuoff.github.io/Flipping-Market-Dashboard/` (Production + Preview) et redéployer. Pour le dev OAuth local : **option pragmatique** — tester l'OAuth sur la prod uniquement, garder localhost pour le reste (UI, calculs, widgets). L'option propre (dynamic redirect via `Origin`/`Referer` + allowlist + state encoding) est trackée dans `ROADMAP.md` § Deferred, nécessite l'accès au repo backend qui n'est pas sur cette machine.

**Consequence:** Prod restaurée immédiatement (deploy `dpl_HA7pr4HH7EasGdYBUMXSpL2r2cjV`). Local OAuth flow inutilisable sans flipper temporairement la variable Vercel (ce qui casse la prod) — accepté comme trade-off jusqu'à ce que le backend repo soit accessible. **Règle pour les futurs agents** : ne jamais modifier `DASHBOARD_URL` côté Vercel sans confirmer que c'est volontaire et temporaire — la variable contrôle directement le redirect des 600 utilisateurs prod.

---

### 2026-05-20 — Global `localStorage.setItem` dedup wrap (Bloc 4 persistence/refresh investigation)

**Context:** Bloc 4 of the ROADMAP — "identify which localStorage writes are redundant on refresh and whether a debounce strategy is needed." Started by instrumenting `localStorage.setItem` at the very top of `dashboard.js` (gated `?lsAudit=1`) to capture per-key write counts, redundant calls (where the new value matches what's already stored), and payload sizes. Real-usage measurement on Max's 30-second interaction with a 658-trade M15 profile: **230 setItem calls, of which ~152 were exact duplicates of the value already in storage — about 800 KB of fully-redundant writes**. The redundancy was structural, not bursty: `presetLiveFilters_v1_<profile>` fired 28× with 100% identical payloads, `gs_layout_active_<profile>` and `gs_layout_default_<profile>` fired in tandem with identical 13.5 KB payloads on every drag/resize, `flipping_custom_widgets_<profile>` rewrote 22 KB on every heatmap threshold-slider move (due to a `changed = true` bug — see consequence). Targeted fixes per-callsite would have required touching 5-7 unrelated code areas with different ownership patterns. The 122-site grep made it clear a centralized fix was much higher leverage.

**Decision:** Wrap `localStorage.setItem` globally at the very top of `dashboard.js` (before any code runs, even before `DEMO_TRADES`). On every call, compare the proposed value against `localStorage.getItem(key)` — if identical, short-circuit and return without invoking the original setter. Cost is one extra `getItem` per `setItem` call (cheap, no I/O). The audit instrument was kept (still opt-in via `?lsAudit=1`) but merged into the same IIFE — the wrap always dedups; the audit toggle just adds stats tracking on top. Targeted fixes complementary to the wrap (none of these are strictly necessary now that dedup is global, but they avoid wasted CPU on `JSON.stringify` of large payloads): (a) `loadSavedTheme` — only re-writes the merged theme if `JSON.stringify(currentTheme) !== saved`, avoiding a 4.6 KB stringify+write per refresh; (b) sidebar state — unified sync (`_persistSidebarState`) + debounced (`_persistSidebarStateDebounced`) writers through a shared helper `_writeSidebarStateIfChanged` that caches the last payload in memory + compares against localStorage on the first call of the session; (c) `_saveBootSnapshot` — removed the dead `savedAt: Date.now()` field from the payload (only `topbarStatsHtml` is read on boot, per the index.html restore script), so the payload becomes stable when the topbar HTML doesn't change and the dedup wrap catches it; (d) `_commitLinkedSettingsToAllCustomHeatmaps` — replaced the `defs.forEach(def => { changed = true; def.settings = {...} })` bug with a real `prev.metricMode === _sdMetric && prev.threshold === _heatmapThreshold && prev.hardMode === _hmHardMode` short-circuit, which prevents the 22 KB stringify entirely on every heatmap-slider tick. Also added a transitive dedup short-circuit inside `safeSetLocalStorage` itself for defense in depth (most call sites go through it but not all). The `storage` event behavior is unchanged — the browser already only fires it when the value actually changes, so cross-tab listeners are unaffected.

**Consequence:** No call site needs to know about the dedup — it's transparent infrastructure. Future code that writes to localStorage automatically benefits without ceremony. Measured impact on the same 30-second user session, after all fixes: 232 setItem calls → 80 actually reach storage (66% short-circuited), ~444 KB legitimately written + ~800 KB redundancy neutralized. The remaining "true" writes are dominated by GridStack layout persistence (~270 KB across `gs_layout_active` and `gs_layout_<preset>`) and custom widget defs (~107 KB) — both are semantically required state changes, not bugs. **Caveats** logged as deferred TODOs: (i) `gs_layout_active` and `gs_layout_<preset>` write the same 13.5 KB payload to two LS keys on every drag (semantically different: autosave vs per-preset state — global dedup can't help between distinct keys); (ii) ~50% of `gs_layout_*` writes are dedup'd, meaning the GridStack `resizestop dragstop` handler fires even on drags that didn't move anything (snap-back, micro-clicks) — the dedup absorbs the I/O but `JSON.stringify` of 13.5 KB still runs. Both deferred to the next preset/layout architecture pass. The audit instrument stays installed (gated `?lsAudit=1`, zero overhead when disabled) for future investigations — call `__lsAudit({sortBy: "total"|"redundant"|"bytes"})` in the console.

---

### 2026-05-20 — `/end` branch safety guard + orphan-branch policy (worktree-isolation agents)

**Context:** Audit during the 2026-05-20 session revealed 6 stale `claude/*` branches on the repo (5 with worktrees in `.claude/worktrees/`, 2 on origin) created by agents running in `isolation: "worktree"` mode — cloud agents, ultrareview, and explicit Agent-tool calls with the isolation parameter. Two of those branches carried real unmerged work: `awesome-dubinsky-f03119` (1 doc commit about a prod incident) and `sharp-hoover-38e66d` (the entire Template chooser feature, 7 commits, +375 LOC, never merged for 24+ hours). The risk surface: an isolated agent commits + pushes to its own branch, expects a human to review and merge, but the branch is silently forgotten — meanwhile a future `/end` from the user is harmless because they're on `main`, but a future `/end` from an agent that itself checked out a side branch mid-session (e.g. for testing, then forgot to return) would push to the wrong branch. CLAUDE.md §11 already says "No worktrees. Small tasks → `main` directly." but that rule is enforced by the agent reading CLAUDE.md, not by the harness — and worktree-isolation invocations don't read the project's CLAUDE.md.

**Decision:** Add a defensive step 0 to `/end` (slash command at `.claude/commands/end.md`): run `git branch --show-current`, refuse to proceed if the result is not `main`, surface `git log --oneline main..HEAD` + `git status --short` and ask the user explicitly whether to (a) merge/rebase back onto main before closing, (b) push the side branch as-is, or (c) abort `/end`. Document the same guard in canonical `CLAUDE.md` §12.2 `/end` table cell + `AGENTS.md` mirror, so fresh-machine "install hooks and commands here" regenerates it. Backed by a memory file `feedback_local_commands_customizations.md` as a defensive fallback if spec regeneration misses table-cell nuance. The same `/end` step also folds in step 7 (session rename via `/rename Dashboard - DD/MM - <context>`), which was previously a TODO entry from 2026-05-20.

**Consequence:** From this point forward, `/end` cannot accidentally push the session's commits to a non-main branch on GitHub — the agent stops and asks. Orthogonal complement: the deeper "no worktree" rule in CLAUDE.md §11 stays as-is, since enforcing it would require changing how agents are invoked (a harness-level concern outside CLAUDE.md's reach). Orphan-branch cleanup policy: at the start of any session that touches branch state, the agent should list `git branch -a | grep claude` and surface anything not on main (~3 commands, low cost) — added as soft guidance to future audits. The 6 branches found this session were all eliminated: 4 vacuous (0 ahead of main, just stale HEAD pointers), 2 with content (cherry-picked + merged before deletion). All worktrees in `.claude/worktrees/` removed; the dir is now empty besides the main repo.

---

### 2026-05-21 — Template detection: pivot schema-based → title-based + Multi-TP as bonus tier + hero-stat / section-counter alignment

**Context:** During real-browser validation of the Template chooser feature (TODO Active "validation pending" from 2026-05-20), four UX bugs surfaced on Max's 6 live Notion profiles. (1) The Data Setup banner displayed the wrong template label ("H4 Pro Template" for an actual H4 Beginner DB): `_detectTemplate(rawTrades)` fingerprinted by inspecting trade VALUES (e.g. `"External Fib Touched"` vs `"Fib - X Touched"`), but Notion's multi_select serialization can vary (array-of-strings vs array-of-`{name}` vs Notion-API-format with `{multi_select: [...]}`) and the option strings themselves shift between template versions — the detection was right at first sync, then locked into the LS via `_setDetectedTemplate(...)`, then drifted out of sync forever. (2) Section bars showed e.g. Optional 11/14 with the 3 `__NO_MAPPING__` dims rendered as is-no-mapping rows behind a footer toggle — Max wanted them gone entirely for template-auto-applied no-mapping (user-set no-mapping should keep the toggle so they can revert). (3) Hero coverage ring read 15/16 → 94% while every section counter showed 3/3, 11/11, 3/3, 3/3 (100% each) — because hero counted "mapped" by `_inspectDimInTrades.populated > 0` while sections counted by override-validity; a mapped column with zero data dragged the ring without touching the sections. (4) Multi-TP section flashed `has-missing` red on every profile that didn't have all 3 TP dims mapped, even though Max considers Multi-TP entirely optional/bonus — including the attention badge's "X to fix" pill, which was incrementing for users who had no intention of tracking tp1/tp2/tp3.

**Decision:** Four coordinated architectural changes, validated end-to-end via Claude in Chrome piloting Brave on all 6 of Max's live profiles. **(A) Template detection pivots to title-based.** New helper `_detectTemplateFromTitle(title)` at `dashboard.js:6243` replaces the entire body of `_detectTemplate(rawTrades)` and uses adjacent-qualifier regex on `profile.notionDatabaseTitle`: `/\bm15[\s\-:()]+pro\b/`, `/\bm15[\s\-:()]+beg(?:inner)?\b/`, `/\bh4[\s\-:()]+pro\b/`, `/\bh4[\s\-:()]+beg(?:inner)?\b/`, with a looser whole-title fallback prioritising Pro over Beg (so "Flipping M15 Pro - beginner-friendly edition" correctly resolves to m15-pro, not m15-beg). 22 unit-test cases pass including canonical names, pathologic mixes of pro+beg in the same title, separators (`-`, `:`, `()`), casing, and no-qualifier fallback. `_getDetectedTemplate()` reads live from `profile.notionDatabaseTitle` rather than the LS cache `flipping_detected_template_v1_<profile>`, so the banner always reflects today's truth (the LS is still written by `_setDetectedTemplate` for backward-compat / audit purposes but no longer read). **(B) Multi-TP is a bonus tier.** `_updateDataSetupHero` unconditionally excludes `tier === 'multi-tp'` from the coverage denominator (was: only excluded when `mode !== 'multi'`). The locked-count branch for multi-TP was deleted entirely (only personalised-tp still gates a locked pill). `_renderMappingSectionsHtml` neutralises the section header for `sect.tier === 'multi-tp'`: forces `hasMissing = false` (no red CSS class) and uses a new `is-bonus` (text-muted gray) count chip instead of `is-bad` / `is-partial`. `_isWidgetSuppressedByNoMapping` now uses `_hasDimDecidedMapping`: returns true when the dim has ANY override (valid mapping OR `__NO_MAPPING__`), not just `__NO_MAPPING__` — so widgets whose dim is mapped-but-empty-in-trades no longer raise the attention badge (the per-widget empty state handles no-data). Special-case for `w-heatmap` (multi-dim, `WIDGET_DIM_KEY[w-heatmap] = null`): suppress when EITHER session OR day is decided. **(C) Auto-no-mapping is fully invisible.** `_renderMappingSectionsHtml` `for (const dim of sectDims)` loop now hard-skips `st.status === 'is-auto-no-mapping'` rows (continue before `total++` and before any group accumulation). `_updateDataSetupHero` excludes any dim with `_isNoMappingValue(apiOverridesForCov[dim.key])` from the denominator. User-set `is-no-mapping` (manual pencil-click) still goes into `hiddenDims` and surfaces in the footer toggle — design intent: user decisions deserve reversibility, template decisions are noise. **(D) Hero coverage stat aligns with section counts.** `_updateDataSetupHero` mapped count switches from `populated > 0` to override-validity semantics: a dim counts as mapped iff `overrides[dim.key]` is a non-empty value (override-set: user OR template auto), OR — when no override is set — the canonical column is auto-detected via `populated > 0`. Plus a backfill exception in `_maybeAutoApplyTemplateMapping`: even when overrides already exist (normally short-circuits to preserve user customisation), backfill `tp1_rr / tp2_rr / tp3_rr` if missing — needed because these keys were added to `TEMPLATE_MAPPINGS` after the initial template release; without backfill, legacy profiles would need a Reset all overrides to get Multi-TP pre-mapped.

**Consequence:** All 6 of Max's live profiles validated (banners correct, badges OK, hero stats 100%, multi-TP gray, tp1/tp2/tp3 auto-populated). The schema-based `_detectTemplate(rawTrades)` is gone — 50 lines of fragile fingerprinting removed, replaced by ~25 lines of pure-string regex that's testable in 0 ms without any rawTrades sample. The title becomes the **single source of truth** for template identity, which means: (1) renaming a Notion DB to "Mon journal" silently disables the banner (acceptable — the user can rename back, or the manual `templateChoice` in the profile editor still drives mapping); (2) the Vercel proxy could stop sending raw column data tomorrow and detection would still work; (3) any future template additions only need to add a new branch in `_detectTemplateFromTitle` + an entry in `TEMPLATE_LABELS` + an entry in `TEMPLATE_MAPPINGS`. **Trade-offs:** Multi-TP being bonus means a user who genuinely WANTS Multi TP mode but has missing tp dims gets zero alert from the Data Setup panel — they only find out when the Multi-TP widget shows an empty state. Acceptable because Multi-TP is opt-in via `appState.ui.tpConfig.mode = 'multi'`; if the user has opted in, they know what they're doing. The hero-stat alignment also means a mapped-but-empty column now reads as "100% configured" even though the data is missing — the Data Setup is about mapping correctness, not data completeness; widget empty states surface the latter. **Known limitation surfaced as TODO Active**: profiles synced before today's template fixes have a stale `apiTradesCache_v2_rrmax_*_<source>` containing trades normalized without h4/obstacles populated (the cache layer stores normalized output, not raw, so it cannot benefit from a code-side mapping fix without a re-sync). All 6 of Max's profiles were force-synced during validation; future users on legacy profiles will need to click Sync once. Ideally `_setAutoMappedDims` or `_maybeAutoApplyTemplateMapping` should invalidate the cache on mapping changes — deferred.

---

### 2026-05-21 — Template H4/Other layouts: exclude widgets via `hiddenWidgets` + filter UI re-entry paths; `/end` spec gains session-summary table + drops the inert `/rename` auto-run

**Context:** Two unrelated but coupled architecture decisions came out of the same session. **(1) The H4 template's design intent** ("widget `w-m15` doesn't exist on H4 profiles") was undermined by the layout-load pipeline: `_loadSectionSlot` ([dashboard.js:36456](dashboard.js:36456)) merges `GLOBAL_OVERVIEW_LAYOUT` defaults into the saved layout (so new widgets added in future releases automatically appear for existing users). Side-effect: any widget the template explicitly OMITS from `layout.global` gets re-injected from defaults — `w-m15` ended up as an orphan in `_liveSectionLayouts.global` (18 entries) while the LS only had 17. Then `_applySectionFilter` saw `w-m15` not in `_hiddenWidgets` → tried to place it in the grid at its HTML default position → layout chaos. The comment "the dashboard never tries to render w-m15 for an H4 profile" in `TEMPLATE_LAYOUTS.h4` was demonstrably false. Symmetric problem for `TEMPLATE_LAYOUTS.other.hiddenWidgets = {}` which claimed to exclude `w-hour / w-m15 / w-h4`. **(2) The `/end` slash command spec** claimed that the agent would `Run /rename Dashboard - DD/MM - <context>` as its final step. Max reported that it had NEVER actually run — the rename always required manual intervention. Investigation via the claude-code-guide subagent confirmed the architectural reality: the Claude Code CLI only parses slash commands from USER input (the chat box), not from agent output. An agent emitting `/rename ...` in its response is inert text. There is no CLI command (`claude session rename ...`), no IPC endpoint, no marker / OSC sequence the CLI listens for, and Stop hooks have no access to the agent's last message content.

**Decision:** **(A) Templates own their exclusion set via `hiddenWidgets`.** `TEMPLATE_LAYOUTS.h4.hiddenWidgets` now contains `{ 'w-m15': { w:12, h:82 } }` (was `{}`). `TEMPLATE_LAYOUTS.other.hiddenWidgets` contains all 3 exclusions (`w-hour`, `w-m15`, `w-h4`). `_applyTemplateLayoutsForProfile` writes the hidden set to `gs_hidden_widgets_<profile.id>` at creation time, so `_loadHiddenWidgets` picks it up on first activation and `_applySectionFilter` honors it (display:none + removed from grid). The widgets stay in `_liveSectionLayouts.global` (the default merge still pollutes it) but are visually gone — the LS pollution is dead data, harmless. **(B) New helper `_getTemplateExcludedWidgetIds()` ([dashboard.js:35400](dashboard.js)) derives the exclusion set from `TEMPLATE_LAYOUTS[choice].hiddenWidgets` keys** and is read by the two UIs that could resurrect an excluded widget: `_collectAllManagedWidgets()` (unhide popover) and `_msGetSectionWidgetIds()` (mini-sidebar parking). Both filter out excluded ids so the user has no way to bring `w-m15` back on an H4 profile. The `hiddenWidgets` declaration in the template now does double duty: persistence seed AND UI-filter source-of-truth. **(C) `/end` step 7 drops `/rename` entirely.** Agent emits exactly one final French line `Tu peux remplacer le titre par : DS - DD/MM - <context>` and the user copy-pastes into `/rename` themselves. No more pretending the agent runs it. **(D) `/end` gains a new step 7 BEFORE the title proposal: session-summary table.** 3-col `Surface | Problem | Change` table with colored-square section separators (`🟩 FIXED, 🟪 ADDED, 🟦 CHANGED, 🟥 REMOVED, 🟧 SPEC`) inside the table itself (skip empty categories), preceded by a `Scope · N changes · cache JS bumps · CSS bumps · M files touched` header line and followed by a `✓ Validated live by Max — X changes · Y spec-only` footer. Spec graved in `.claude/commands/end.md`, `CLAUDE.md` §12.2, memory `feedback_local_commands_customizations.md` (so fresh-machine regen carries it).

**Consequence:** Templates that exclude widgets now actually work end-to-end on profile creation — Max validated by creating a fresh H4 profile, confirming `w-m15` invisible everywhere (grid + unhide popover + mini-sidebar parking). Pattern is declarative: future template additions just need to list their exclusions in `hiddenWidgets`, no UI code changes. **Trade-off**: the `_liveSectionLayouts.global` pollution from the default-merge is still there (the H4 template's saved LS gets an extra `w-m15` entry on every drag/resize via auto-save), but it never renders since `_hiddenWidgets` wins at `_applySectionFilter` time. Cleaning the source pollution would require teaching `_loadSectionSlot` to respect a "template-excluded" hint — over-engineering for now. **Spec-side trade-off**: agent can no longer "do the work" of rename — it's a manual user step. Acceptable because (a) the old "auto-run" never worked anyway, (b) the propose-and-copy pattern is 100% reliable, (c) Max prefers the explicit copy-paste UX, (d) the auto-rename feature request is tracked as a Deferred TODO with the exact 3 architectural changes Anthropic would need to ship for it to become possible. Session-summary table format is the third iteration in one sitting (bullets too messy → mini-tables per section → single big table with colored-square separators) — the iteration cost was paid up-front so future `/end` runs have unambiguous formatting guidance.

---

### 2026-05-23 — Cross-device sync overhaul: Supabase Storage gzipped blob as parsed-trades propagation layer, monkey-patched LS sync for metadata, defensive cascade against silent upsert failures

**Context:** Max's core complaint at the start of session: "j'ai changé d'ordinateur et pourtant je n'ai récupéré aucun widget created" — and the deeper vision: a dashboard where signing in on PC B 2 minutes after editing on PC A shows the exact same state without any manual Sync click. Investigating revealed the existing M1 sync (closed earlier 2026-05-23 via `_SYNC_KEY_PREFIXES` + the `user_data` table) covered metadata (mappings, presets, custom widget defs, custom Notion props, outcome value mappings) but NOT (a) the gridstack layout positions (`gs_layout_active_*` + `gs_hidden_widgets_*` per-profile + per-HTF), (b) the actual parsed trades (`apiTradesCache_v2_rrmax_*` — too bulky for the user_data table at ~300 KB per profile uncompressed × N profiles), (c) the raw Notion response (`apiTradesRaw_session_v2_*` — sessionStorage-only by design, in-memory wiped on refresh). Result: layout drift between devices (Brave widgets at different positions than Safari), custom widgets visible on PC A but absent on PC B even with the def synced (no DOM injection on profile switch — `_injectNewCustomWidget` only ran at widget CREATION), and the "no Sync needed" promise broken because trades had no transport mechanism cross-device. Further hardening: across the session Max discovered that even M1's monkey-patched LS sync was silently failing for some writes — Supabase showed only the initial 14:23 burst of profile-scoped writes, then nothing despite Max creating multiple widgets in subsequent sessions. The bug had multiple compounding causes: pre-auth race (`_user` null when `localStorage.setItem` fired), Safari ITP not always firing `onAuthStateChange` events, profile context unresolved at widget-creation time causing writes to land at the unsuffixed legacy key `flipping_custom_widgets` (which isn't in `_SYNC_KEY_PREFIXES` → no upsert).

**Decision:** Supabase plays **two distinct roles** in the new cross-device architecture, each chosen for size/cost characteristics. **(A) `user_data` Postgres table for metadata** — small per-key JSONB payloads (mappings ~30 bytes, widget defs ~200 bytes, layout JSON ~2 KB, hidden sidecar ~150 bytes). Synced via the existing monkey-patched `localStorage.setItem` → `_SW.set` → upsert pipeline. Extended `_SYNC_KEY_PREFIXES` with `gs_layout_active_` and `gs_hidden_widgets_`, added `htfSource` to `_SYNC_KEYS` for view-source consistency, bumped migration flag v1→v2 to force one-shot re-push of pre-existing layouts on every existing user's next sign-in. Free tier 500 MB DB easily covers 5000+ users of typical size. **(B) `notion-trades` Storage bucket for the parsed-trades blob** — gzipped JSON file at `<uid>/<profile_id>_<source>.json.gz`, ~34 KB compressed for Max's 595 trades (observed). User-scoped RLS via `(storage.foldername(name))[1] = auth.uid()::text` policies on `storage.objects` (4 policies: SELECT/INSERT/UPDATE/DELETE). Helpers `_compressString` / `_decompressBlob` use the native CompressionStream API (Chromium 80+, Safari 16+) with magic-byte sniffing for mixed compressed/uncompressed payloads. `_SW.uploadTradesBlob(profileId, source, trades)` / `downloadTradesBlob` wrap the Supabase Storage SDK. Upload is debounced 1.5 s per `(profile, source)` pair and triggered via a SINGLE hook in `setCachedAPIData` (line 8154) that catches all three write paths (Notion sync, mapping change re-normalization, custom widget extras bake). Boot flow inverted: `_reloadJournalProfileSelection` tries `_SW.downloadTradesBlob` BEFORE falling back to `loadFromAPI` Notion fetch — `fromBlobDownload: true` flag passed to `setCachedAPIData` prevents the immediate re-upload loop. Free tier 1 GB Storage covers ~3000 profiles at 34 KB each.

**Cascade of defensive fixes against silent upsert failures** (Tasks #3.6 through #3.14, all in one session): #3.6 `_ensureCustomWidgetInGrid(def)` companion to `_injectNewCustomWidget` that respects the saved layout position (vs computing maxBottom) — called from `_reloadProfileScopedState` so synced widget defs get DOM injection on every profile activation; #3.7 `getApiAvailableProperties` no longer filters by `coverageCount > 0` when raw cache is empty, so synced `apiFieldNames_v1_<id>` falls through to the Create Widget UI; #3.8 invalidate `_customWidgetDefsCache` before `_injectCustomWidgetHTML` in `initGridstack` + safety-net `_ensureCustomWidgetInGrid` loop after `_applySectionFilter`; #3.9 `_maybeTriggerFullSyncAfterWidgetCreate` fires `_loadNotionTrades({force:true, background:true})` when raw count < parsed count (typical post-blob-load), populating extras across the full dataset so the blob upload carries complete data for the next device; #3.11 `_pendingSyncWrites` queue catches LS writes before `_user` resolves, flushed in SIGNED_IN/INITIAL_SESSION handlers; #3.12 `migrateProfileScopedKeys` ungated (was one-shot via v2 flag), runs every sign-in with bulk SELECT for performance + `.catch` on the upsert promise to surface previously-silent rejections; #3.13 same flush + reconcile hooked into `getSession().then()` callback for Safari ITP edge cases where `onAuthStateChange` events never fire; #3.14 `_saveCustomWidgetDefs` refuses writes to the unsuffixed legacy key (no profile context), redirects to `_pending_*` namespace, and `_recoverPendingCustomWidgetDefs` merges orphan/pending payloads into the active profile's scoped key on every `_reloadProfileScopedState` + getSession callback.

**Consequence:** Max's cross-device experience went from "create widget on A, sign in on B, see nothing, must click Sync" to "create widget on A, hard refresh B, widget appears with full data at the correct position, no manual action". Validated end-to-end Brave↔Safari (isolated localStorage on same Mac, logically equivalent to 2 PCs since the sync path goes through Supabase). The architecture supports the freemium community at scale: free tier covers ~3000 active users on Storage and 5000+ on the metadata table, well above the current ~600-member audience. **Trade-offs:** (1) The raw Notion cache itself is still in-memory only — a custom widget created on a fresh device shows "no data" for ~5-10 s while the auto-trigger background full sync (#3.9) refetches the full dataset and populates extras. Permanent fix planned as Task #3.10 in next session: parallel raw-cache blob at `<uid>/<profile>_<src>_raw.json.gz`, ~200-400 KB compressed. Sub-decision deferred: how to handle Notion S3 File & Media URLs (~1 h expiry) in the raw payload — most likely (b) replace with permanent Supabase URLs from the media cache before upload. (2) The defensive cascade is wide (8 sub-tasks) because the bug surface was diverse — auth races, browser-specific event firing, profile-context resolution, in-memory cache staleness, layout vs def divergence. Each layer is independently small (~20-100 LOC) and the layers compose: even if one layer fails, the next catches the residual case. Trade-off: cognitive complexity for future maintainers — but the self-healing pattern (reconcile on every sign-in, recovery on every profile activation) means orphaned writes can never permanently survive once a fix lands. (3) Last-write-wins semantics across devices accepted as-is — no merge logic. If a user customises layout differently on PC A and PC B without syncing in between, the first device to sign in after the deploy "wins" the layout for both. Acceptable for single-user multi-device use; not designed for collaborative editing. (4) `notion_media_cache` table found NOT scoped by `user_id` — surface-area for future multi-user data leakage if two users somehow share a `notion_id`. Documented as a follow-up in Task #3.6 description but not actioned this session.



---

### 2026-05-23 — Raw Notion cache cross-device sync (Task #3.10) — option (b) URL substitution + boot-time hydration + post-bake LS persist

**Context:** Task #3.4 (parsed-trades blob) shipped 2026-05-23 morning solved cross-device propagation of normalized trades and screenshots, but `_rawAPICacheMemory` (the unparsed Notion response used by `_bakeCustomWidgetExtrasIntoCache` for new widget creation and by `_reapplyAPIOverrides` for mapping changes) was left in-memory only. Consequence on a fresh device after sign-in: parsed cache hydrates → dashboard renders → user creates a custom widget → bake reads raw cache → empty → silently returns → widget shows no data until the auto-trigger Task #3.9 background full sync (~5-10 s) repopulates raw. Max reported this regression for 3 consecutive days: refresh → create widget → refresh → widget exists but no data.

**Decision:** Ship the raw blob as a sibling of the parsed blob in the same `notion-trades` bucket — path `<uid>/<profile>_<src>_raw.json.gz`, reusing the gzip helpers and RLS rules. Three sub-decisions tranchées live:

1. **Image URL handling — option (b) with (a) fallback.** Notion S3 signed URLs in File & Media properties expire in ~1 h, so uploading them verbatim would pollute the bucket with dead links that consumers (mapping change re-normalize) would extract on a fresh device. New helper `_substitutePermanentImageUrls(rawTrades, source)` ([dashboard.js:8100](dashboard.js:8100)) walks each trade's screenshot props (resolved via current field overrides), looks up the permanent Supabase URL in the media cache via `(_notionId, slot)`, and substitutes the URL inside the canonical `[{name, file:{url}}]` shape. Orphan trades (no media cache entry — typically <30 s old, before the media queue migrated) get their image props stripped to `[]` rather than left as Notion URLs. Non-image props and external URLs (imgur, cloudinary, TradingView `/x/`) are untouched. Option (c) — upload Notion URLs verbatim — rejected because it pollutes the bucket and breaks the mapping-change-on-fresh-device flow.

2. **Boot hydration as fire-and-forget IIFE** ([dashboard.js:6326](dashboard.js:6326)). Inserted at the top of `_reloadJournalProfileSelection`, BEFORE the LS-cache early-return. Rejected: awaiting the raw blob inside the parsed blob fast-path (would block first paint). Rejected: hoisting raw download into the network fallback branch (skipped when LS cache hits, which is the common case → original regression). Chose: an `(async () => {...})()` block that runs in parallel with the synchronous `_injectTrades` from LS cache. Two guards (pre-fetch + post-await) detect already-populated raw cache and skip the download to avoid clobbering a concurrent Sync's fresher data.

3. **Post-hydration bake catch-up + forced LS persist.** After raw hydration, re-bake every custom widget def (`_loadCustomWidgetDefs` → `for def: _bakeCustomWidgetExtrasIntoCache(def)`), then **explicitly** call `setCachedAPIData(parsedMem, src, { fromBlobDownload: true })`. The explicit call is non-obvious but load-bearing: `_bakeCustomWidgetExtrasIntoCache` has a `changed` flag that only flips when Loop 2 (`_parsedAPICacheMemory[slot]`) writes new extras. Loop 1 (`appState.trades.items`) mutates extras in place FIRST, and when both arrays hold the same trade references (the common case after `_injectTrades(cached)` aliases them), Loop 1's mutation makes Loop 2's `hasOwnProperty` check skip → `changed` stays false → bake's internal `setCachedAPIData` is never called → in-memory cache has the new extras but LS does not. On the next hard refresh, LS-derived parsed cache returns empty extras, widget renders empty. The forced LS write closes that gap. This was the root cause of Max's 3-day regression — even with all the cross-device plumbing in place, the bake's `changed`-flag aliasing trap prevented LS from being updated.

**Consequence:** Max's "create widget, refresh, widget has no data" scenario fixed end-to-end. Custom widgets and mapping changes now work on a fresh device without a manual Sync. Free tier impact: raw is ~5-8× larger than parsed (~200-400 KB compressed per profile), well below the 1 GB Storage limit (supports ~2500-5000 profiles). Trade-offs: (1) The bake's `changed`-flag aliasing trap is a latent footgun — any future code path that relies on `_bakeCustomWidgetExtrasIntoCache` persisting to LS must compensate by calling `setCachedAPIData` explicitly, or refactor the bake to track "extras filled" independent of which loop set them. (2) Orphan trades (created on PC A < 30 s before raw upload) lose their screenshot in the blob — mapping change on a fresh device shows empty image until the next Sync. Acceptable: rare timing, self-heals on next sync. (3) The IIFE runs unconditionally on every Notion-with-DB profile load — one extra Storage GET per device per session. ~30 KB transfer, ~100 ms latency, fine. (4) Custom-widget extras testing required eval-time access to lexical `_customWidgetDefsCache` to bust the boot-cached value — confirmed the bake works correctly when defs are loaded fresh from LS at boot (Max's real-device flow), but the test reproduces required the eval trick. (5) Supabase Realtime push for "PC A → 30 s → PC B updates without F5" still deferred to Session 4. **Cache** JS `?v=562` → `?v=574`.

---

### 2026-05-23 — Eager-bake ALL detected Notion props into extras (Task #3.11)

**Context:** Max flagged a mismatch between his mental model ("all Notion properties detected = all data permanently in Supabase, like standard filters") and the actual behaviour after the Task #3.10 raw blob sync shipped. Standard journal dims (pair, outcome, setup, h4, obstacles, etc.) were eagerly populated via `_normalizeAPITrade` at sync time — always available cross-device, no manual action. Custom Notion properties were only baked LAZILY: `_bakeCustomWidgetExtrasIntoCache` only processes properties referenced by an existing custom widget def. The user-chips path (`_repopulateExtrasForProp` via `loadCustomProps()`) was eager. Custom widgets were not. Result: a fresh device that downloaded the parsed blob saw extras only for widget-referenced props from the source device. Creating a new widget on a different Notion property on the fresh device produced "no data" until a full Sync (or the auto-trigger background sync) repopulated raw and re-baked.

**Decision:** Promote custom widget extras to the same eager-bake tier as standard dims and custom props. New `_bakeAllDetectedPropsToExtras(trades, rawCache, source)` walks every raw Notion property for every trade and writes the flattened value to `t.extras[propName]`. Hooked into 3 hot paths: (1) `_loadNotionTrades` after `mergedParsed` is built — bakes BEFORE `setCachedAPIData` so the LS write + blob upload carry the full extras payload. (2) `_injectTrades` when raw is available — covers boot-from-LS-cache (post-refresh same device) and the per-widget bake on creation. (3) The post-hydration IIFE in `_reloadJournalProfileSelection` — covers cross-device first paint, replaces the per-widget bake from #3.10, forces `setCachedAPIData` after the bake to bypass the `_bakeCustomWidgetExtrasIntoCache` `changed`-flag aliasing trap.

**Skip list** to keep the parsed blob lean and avoid double-payload:
- **System keys**: `id`, `_notionId`, `notionId`, `created_time`, `last_edited_time`, `created_by`, `last_edited_by`, `icon`, `cover`, `parent`, `archived`, `url`, `public_url`, `object`, `in_trash`.
- **Notion File & Media shapes** (detected via `[{file:{url}}]` / `[{external:{url}}]` heuristic): image URLs are already routed through the dedicated `parsed.imgM15` / `imgH4Before` / `imgM15After` + media cache pipeline. Doubling them into extras would inflate the blob without any consumer.
- **Oversized values (>2KB)**: any flattened string longer than 2048 chars is dropped. Notes / long text properties hit this cap. Trade-off: custom widget on such a column will show no data unless the user adds it as a filter chip (which uses `_repopulateExtrasForProp` per-prop, no global cap). Acceptable: long-text widgets are rare; cap protects the blob.

**Empty-value semantics**: when raw returns empty/null/empty-array for a previously-present extras key, the key is **deleted** (mirrors `_repopulateExtrasForProp`'s writeOrDelete). Prevents stale extras when the user clears a Notion column.

**Consequence:** Mental model now matches reality. Detected Notion property → baked at sync → persists in parsed blob → cross-device 24/7 without manual Sync. Creating a custom widget on ANY detected Notion prop on a fresh device returns data immediately, without waiting for the auto-trigger background sync. **Trade-offs**: (1) Parsed blob size grows by ~30-50% (every trade carries every Notion prop value, not just widget-referenced) — for typical 595 trades × ~10 extra props × ~50 bytes avg ≈ +300 KB raw, ~+80 KB compressed. Still within Supabase Storage free-tier budget by orders of magnitude. (2) LS parsed cache grows similarly — fallback modes (compact/minimal/ultra) already strip extras when payload exceeds the 2MB guard, so degradation is graceful on quota-stressed devices. (3) The 2KB-per-value cap means widgets on long-text Notion props (Notes, Description, full markdown body) won't auto-populate — explicit user action required (add as filter chip). Acceptable: text widgets are an edge case, and the user-chips path remains uncapped. (4) Raw blob cross-device sync (Task #3.10) is now technically redundant for the widget creation use case — the parsed blob alone is sufficient. BUT raw is still needed for `_reapplyAPIOverrides` (mapping changes need to re-normalize from raw with a new override map). So #3.10 stays. (5) The eager bake fires multiple times per session (every sync, every inject, every IIFE hydration) — each pass is idempotent (already-equal extras short-circuit via `if (t.extras[propName] !== flat)`) so the overhead is one Object.keys walk + flatten per trade per pass. Negligible vs the network IO. Cache JS `?v=574` → `?v=578`.

---

### 2026-05-23 — Custom widget DOM cleanup on profile switch

**Context:** Max reported that custom widgets created on Profile A remained visible after switching to Profile B, despite each profile being a separate database with separate widget defs. The `flipping_custom_widgets_<id>` LS key is correctly profile-scoped via `getProfileScopedKey`, and `_reloadProfileScopedState` ([dashboard.js:9093](dashboard.js:9093)) properly invalidates `_customWidgetDefsCache` and `_customPropsCache` on profile switch. The defs reload from the new profile's LS slot. But the GridStack DOM was never cleaned up — `_ensureCustomWidgetInGrid` ([dashboard.js:38669](dashboard.js:38669)) is additive only (`return false` when the item already exists in the DOM) and no code path ever removed widgets that belonged to the previous profile. Result: Profile B's `#gs-container` accumulated A's leftover widget DOM elements on top of B's own, with no clear way to undo it short of a hard refresh + cache clear.

**Decision:** Add a § 3.4 cleanup block in `_reloadProfileScopedState` ([dashboard.js:9146](dashboard.js:9146)) that runs BEFORE the existing `_ensureCustomWidgetInGrid` loop. Walks every `#gs-container .grid-stack-item[gs-id]`, identifies custom widgets via the `data-cw-id` attribute or `.cw-widget` class (markers set by `_ensureCustomWidgetInGrid` / `_injectNewCustomWidget`; standard widgets like `w-equity` / `w-monthly` don't have these and are profile-agnostic, so they're skipped), and removes any whose `gs-id` isn't in the freshly-loaded def list via `_grid.removeWidget(item, true)` (same pattern as the delete-widget path at line 38806). The next inject loop then rebuilds only what the new profile actually owns.

**Consequence:** Profile A → B → A round-trip now keeps each profile's custom widgets fully isolated (verified end-to-end in preview with 2 mocked profiles, each with its own widget def). **Trade-offs**: (1) The cleanup fires on every `_reloadProfileScopedState` call, not just profile switches — also runs on HTF source change and on boot. Idempotent (no widgets to remove if defs match) but means an extra DOM walk per state reload. Negligible cost (<5ms with typical 10-20 grid items). (2) The cleanup is scoped to custom widgets only — standard widgets aren't touched. Future refactors that change how custom widgets are marked in the DOM (replace `.cw-widget` class or `data-cw-id` attribute) must update the cleanup's identification check at the same time, or the cleanup silently no-ops and the leak returns. (3) Orphan layout entries (`_liveSectionLayouts.global['w-cust-AAA']` from the previous profile) aren't actively purged here — they sit inert because no def references them, but they slightly bloat the layout LS slot. Acceptable: a future `_saveActiveSlotLive` pass after a drag would prune them naturally as the live grid state replaces the saved one. Cache JS `?v=578` → `?v=579`.

---

### 2026-05-24 — Refacto total du pipeline extras au lieu de patches incrémentaux

**Context:** Marathon de 12 commits incrémentaux pour résoudre le symptôme reporté par Max — chips custom-field affichent "no values found / 0/658" malgré data présent en mémoire. Chaque commit corrigeait un angle (eager-bake `_notionId` fallback, suppression `_repopulateExtrasForProp`, anti-corruption guard sur upload, anti-stale guard sur boot, recovery hook au moment du Add, explicit `renderCustomFilterChips` post-bake, bake sur appState en plus de parsedMem) mais aucun n'a résolu définitivement. Diag montre `item0_extrasCount: 124` puis `extrasKeysCount: 0` entre deux refresh consécutifs sans action utilisateur évidente — système non-déterministe. L'audit Explore identifie une architecture extras à 8 étages (LS cache → 4 storage modes → blob parsed → blob raw → `_injectTrades` clone → bake parsedMem OU appState → render → `renderCustomFilterChips`) avec races de timing entre l'inject sync et l'IIFE async post-blob-download. Chaque patch ajoute un guard sans résoudre le défaut conceptuel. Côté serveur tout est sain (blob Supabase 397 KB / 148 props / 658 trades, anti-corruption guard tient depuis 00:35, Vercel proxy clean, Notion connection OK).

**Decision:** Stop des patches incrémentaux. Refacto total du pipeline extras dédié à la prochaine session, en démarrant par un audit obligatoire de 100% du code touchant extras (sites d'écriture, sites de lecture, conditions de race, modes de storage, fonctions de bake, séquence boot complète) AVANT toute modification. Architecture cible : (1) source unique de vérité = blob Supabase Storage, (2) un seul point d'entrée au boot (DL blob → inject → render — pas de chemin LS prioritaire qui shortcuit), (3) readers déterministes lisant une clé unique (pas de chain `prop.name ?? prop.key`), (4) zéro guards de fallback (le système doit être correct par construction), (5) séparation `notionSourceName` (immuable, identité Notion canonique) du `name` (label éditable côté user), (6) recovery hook au Add supprimé (plus nécessaire avec architecture déterministe).

**Consequence:** ~2-3h de travail en session fraîche et dédiée — Max et l'agent sont fatigués (Max 20h ces 3 derniers jours, agent 12 tentatives de fix dans la journée), faire un refactor complexe en l'état empire. Pendant le refacto, le blob serveur reste filet de sécurité (anti-corruption guard tient). Edge functions diag (`diag-blob-peek`, `diag-bake-fix`) restent déployées sur Supabase pour usage de la next session — réutiliser ou supprimer au début. Code prod (`?v=597`) contient les 9 commits d'aujourd'hui — peuvent être revertés en bloc au début de la next session si le refacto les rend obsolètes. **Anti-pattern reconnu** : accumulation de patches défensifs sur une architecture mal conçue produit un système non-déterministe pire que le bug initial. Préférer un revert + refacto que continuer.

---

### 2026-05-24 — Preserve permanent Supabase URLs before LWW merge on Notion re-sync

**Context:** Max ouvre son dashboard après un sync de la veille et voit dans le drawer trade des URLs `prod-files-secure.s3.amazonaws.com` (Notion signed S3, expiry ~1h) au lieu des URLs Supabase Storage permanentes que la media queue avait pourtant uploadées. Les images sont cassées. Investigation : (a) l'archi media queue migre bien Notion → Supabase en background (POST `/api/notion/media-sync`, patch in-place `item.trade[item.field] = perm`, debounced `setCachedAPIData` 1s plus tard pour persister en LS). (b) Le LS persist via `_serializeTradesForStorage` ([dashboard.js:8877](dashboard.js:8877)) strip déjà les Notion URLs à `''` (anti-bloat 2MB) — donc LS reste propre. (c) Au boot, `getCachedAPIData` → trades from LS avec Supabase URLs → injecté en appState — chemin correct. (d) **Mais** dès que le user re-sync, `_loadNotionTrades` rappelle `_mergeTradesByNotionId(existingCache, parsed)` à [dashboard.js:10709](dashboard.js:10709) qui est last-write-wins : le `parsed` fresh écrase les URLs Supabase de `existingCache` par les Notion S3 fraîches. Le media queue redémarre derrière mais entre-temps (et surtout si le user n'attend pas que le batch se termine), appState contient des URLs Notion qui expireront 1h plus tard.

**Alternatives considérées:** (1) **Ne pas uploader vers Supabase, re-sync horaire à la place** — rejeté : exige le dashboard ouvert en permanence + crash si Notion down + UX cassée pendant les 5-10s du re-sync. (2) **TTL retention sur Supabase Storage pour limiter le coût** — Max a tranché "tous les screenshots sont utiles même 10 ans après" → pas de retention. Free tier Supabase Storage (1 GB) couvre ~5000 trades × 2 images × 200 KB ≈ 2 GB ; au-delà $25/mois Pro plan avec 100 GB. Pas un blocker financier vu l'audience actuelle (~600 community members peu actifs). (3) **Fix dans `_mergeMediaCache`** ([dashboard.js:8740](dashboard.js:8740)) en inversant la priorité de `t.imgM15 || m.imgM15` à `m.imgM15 || t.imgM15` — rejeté : `_mergeMediaCache` lit le session storage media cache (ephemeral, vide au boot) et n'intervient pas sur le path live-sync de toute façon. (4) **Fix dans `_normalizeAPITrade`** ([dashboard.js:8325](dashboard.js:8325)) en injectant le lookup à la normalisation — rejeté : force un lookup synchrone par trade dans le hot path de normalisation, coûteux. (5) **Fix au sync time via un helper dédié** — retenu.

**Decision:** Nouveau helper `_preservePermanentImageUrls(freshParsed, source, priorTrades = null)` à [dashboard.js:8508](dashboard.js:8508). Build un lookup `notionId → {imgM15, imgH4Before, imgM15After, imgM15Orig, imgH4BeforeOrig, imgM15AfterOrig}` depuis le `priorTrades` explicite quand le caller en a un en scope (cas `_loadNotionTrades` qui détient `existingCache`), sinon depuis `appState.trades.items` (in-memory, le plus à jour avec les patches media queue) + LS cache `apiTradesCache_v2_rrmax_<src>` (cold-start safety net). Le lookup ne retient QUE les URLs non-Notion (via `_isNotionFileUrl` filter) — pas de risque de propager une expired URL. Pour chaque fresh trade dont un slot image est une Notion S3 URL **ET** que le lookup contient une URL permanente pour le même `notionId+slot`, swap. Shallow-copy uniquement pour les trades touchés (refs préservées sinon, négligeable côté GC). 4 call sites patchés : `_reapplyAPIOverrides` ([:7993](dashboard.js:7993)), `loadFromAPI` ([:9450](dashboard.js:9450)), `_fetchAPICacheSilently` ([:10013](dashboard.js:10013)), `_loadNotionTrades` ([:10707](dashboard.js:10707) — `existingCache` passé explicite avant `_mergeTradesByNotionId`). `_mediaQueue.enqueue` reçoit `preservedParsed` au lieu de `parsed` → la queue ne re-uploade pas les trades déjà permanents.

**Consequence:** Bug fermé sur le chemin principal Notion sync. **Trade-offs**: (1) Pour un trade strictement nouveau (jamais vu, jamais migré), la fenêtre [sync fini ↔ media queue passé] (~30s à 5min selon backlog) reste exposée aux Notion URLs en appState ; hard refresh dans cette fenêtre → image vide jusqu'à ce que la queue patche et re-persiste 1s plus tard. Acceptable, c'est le coût inhérent du premier upload. (2) Le helper itère sur `appState.trades.items` ET LS cache à chaque sync — pour 658 trades c'est 2 × 658 = 1316 iterations + le scan du `freshParsed`, négligeable (<10ms). Aucun caching mémo car le lookup change après chaque batch de la media queue. (3) Nouveau pattern architectural "preserve permanent URLs from prior cache before LWW merge" — à généraliser si d'autres LWW merges sur des champs migrés émergent (ex: si on stocke un jour des fichiers PDF par trade, même logique applicable). (4) Le helper ne touche PAS `_substitutePermanentImageUrls` ([:8445](dashboard.js:8445)) qui couvre un cas différent (substitution AVANT upload du blob raw cross-device) — séparation explicite : ce helper protège la vie en mémoire / LS post-sync, l'autre protège le blob raw uploadé. Cache JS `?v=631` → `?v=637`.


---

### 2026-05-24 — RAW Baseline = truly raw (no base exclusions) + legacy "M15 Personnel" auto-seed removed

**Context:** Deux signalements de Max en flux d'onboarding nouvel utilisateur. **(A)** Le preset par défaut P0 RAW Baseline n'affichait pas l'intégralité du dataset : 81/100 trades visibles côté demo (et l'équivalent sur ses 658 trades Notion), à cause de `BASE_M15_EXCLUDE = ['Volume - Faible', 'News Entry', 'Range']` baked dans `presetSnapshots[0].chips.obstacles.excluded` par `_migrateToV2`. La description elle-même mentait à moitié — "No obstacle/context filters — base exclusions only" — un user débutant qui voit "RAW Baseline" attend zéro filtre, pas trois exclusions invisibles. **(B)** À côté du profil démo + son profil Notion réel, Max voyait un troisième profil fantôme nommé "M15 Personnel" pointant sur `https://notion-dashboard-api-2.vercel.app/api/trades` (l'ancien endpoint pré-OAuth dont la database n'existe plus). Auto-seedé pour tout nouveau user par `ensureDefaultJournalProfile()` depuis l'époque pré-OAuth — héritage d'un parcours d'onboarding où l'user collait son API URL à la main. Aujourd'hui le parcours canonique est "Connect Notion" qui crée un profil OAuth complet — le seed legacy n'a plus aucun rôle utile et crée juste de la confusion (deux entrées dont une morte).

**Alternatives considérées:** (A1) **Garder le baseline tel quel et éduquer le user via tooltip / onboarding** — rejeté : la description prétend déjà "base exclusions only" mais Max lui-même (créateur du dashboard) n'a pas réalisé que P0 filtrait. Si l'auteur ne le voit pas, aucune chance qu'un nouveau user le voie. (A2) **Renommer P0 en "Filtered Baseline" + créer un "P-1" Truly Raw** — rejeté : multiplie les presets seedés alors qu'on en a justement réduit le nombre il y a peu (un seul P0 par défaut). (A3) **Garder base exclusions par défaut côté config dashboard mais documenter** — rejeté : le mot "RAW" perd tout son sens. **(B1)** **Garder le seed pour éviter le scénario "0 profils" qui pourrait casser des chemins de code** — rejeté : `getActiveJournalProfile()` retourne déjà null proprement quand vide, et tous les call sites usent `?.id` / `if (!profile) return`. Aucune régression mesurée en preview avec 0 profils. (B2) **Ne pruner que pour les nouveaux users mais conserver pour les existants par compat** — rejeté : un user existant qui n'a jamais utilisé ce profil (Max et ses 600 community members typiquement) en hérite quand même comme ghost row dans son UI. La signature `_isDefaultJournalProfile` (name + type + source + apiUrl exact) garantit que toute customisation user préserve le profil.

**Decision:** **(A) P0 RAW Baseline devient truly raw, zéro filtre.** (1) `DEFAULT_PRESETS[0]` à [dashboard.js:11695-11710](dashboard.js:11695) reçoit `noBaseExclusions: true` (lu par `_migrateToV2` ligne 35556 → `withBase = false` → ne bake plus `BASE_M15_EXCLUDE` / `BASE_SESSION_EXCLUDE` pour les nouveaux users) + nouvelle description "Every trade — no filters applied" (factuellement vraie). (2) Migration one-shot idempotente `_migrateRawBaselineToTrulyRaw_v1` à [dashboard.js:35684-35713](dashboard.js:35684), câblée au boot juste après `_migrateToV2()` à [dashboard.js:35043-35047](dashboard.js:35043), strip les valeurs `BASE_M15_EXCLUDE` / `BASE_SESSION_EXCLUDE` de `presetSnapshots[0].chips.obstacles.excluded` + `.session.excluded` pour les users v2 existants dont le snapshot a été matérialisé avant ce changement. Force aussi `p0.noBaseExclusions = true` + nouvelle description en LS pour cohérence. Flag LS `flipping_raw_baseline_truly_raw_v1` empêche re-run. Conserve uniquement les valeurs BASE — toute exclusion user-ajoutée sur P0 (théoriquement impossible vu que P0 est protégé, défensif quand même) survit. **(B) Auto-seed legacy supprimé + prune universel des profils matchant la signature pristine.** (1) `ensureDefaultJournalProfile()` à [dashboard.js:4621-4644](dashboard.js:4621) : retrait du bloc `if (!profiles.length) { saveJournalProfiles([{ name: 'M15 Personnel', ... }]) }`. Nouveaux users → 0 profils → land sur Demo, créent leur premier profil via "Connect Notion". Prune universel en remplacement : filtre `profiles` via `!_isDefaultJournalProfile(p)`, push via `saveJournalProfiles(pruned)` (= LS + remote upsert via le delete-missing pattern du `_SW`). Early-return `if (!profiles.length) return null` au cas où le user n'aurait que le seed legacy. (2) `hydrateJournalProfilesFromRemote` à [dashboard.js:4585-4598](dashboard.js:4585) : ajoute un `.filter(p => !_isDefaultJournalProfile(p))` sur `mergedProfiles` avant le `localStorage.setItem` — sinon le remote ressuscite le fantôme à chaque sign-in (la prune locale serait défaite par la prochaine hydratation cross-device). Le `_isDefaultJournalProfile` à [dashboard.js:4661](dashboard.js:4661) vérifie les 4 champs en AND (name === 'M15 Personnel' && type === 'm15' && source === 'api' && apiUrl === API_URL_DEFAULT) — toute customisation user (rename, connexion Notion, import CSV qui change `source`) casse la signature et le prune skip.

**Consequence:** **(A)** P0 affiche désormais 100/100 trades sur demo (validé en preview, dataset démo). La sémantique "RAW = vraiment raw" matche enfin le mot. Trade-off : les users qui se reposaient implicitement sur le filtrage silent de `Volume - Faible / News Entry / Range` via P0 vont voir +N trades dans leurs vues globales — soit ils créent un preset custom dédié pour ce filtrage (parcours explicite, conforme à l'usage moderne des presets), soit ils ignorent ces obstacles via les chips (mode include/exclude). Aucune perte de donnée, juste une visibilité plus large. La migration s'auto-applique une seule fois et est invisible (pas de toast, pas de prompt). **(B)** Onboarding nouveau user : Demo seul jusqu'à connexion Notion explicite — état propre, pas de profil mort à explorer. Trade-off : la fonction `ensureDefaultJournalProfile` ne "ensure" plus rien si l'user n'a aucun profil — son nom devient un peu trompeur, mais renommer aurait élargi le scope du changement (callers à grep). Conservée comme prune + normalize. Le profil legacy disparaît à la fois côté LS (immédiatement au prochain reload) et côté Supabase remote (au prochain save, qui fire automatiquement via `saveJournalProfiles`). Pour les users existants qui avaient ce seed inutilisé en remote (~600 community members + Max), suppression silencieuse via le delete-missing pattern de `_SW.saveJournalProfilesToRemote`. Cache JS `?v=637` → `?v=645`. **Side fixes shippés dans le même commit** (preset lifecycle, traités via ROADMAP) : (1) `deletePreset` bascule sur P0 au lieu de null quand on supprime le preset actif (extension de l'invariant "user jamais sur 'no preset'" établi le 2026-05-24). (2) `_handleNotionProfileSyncNow` + `_handleNotionProfileFullResync` re-appellent `_restoreActivePresetForCurrentSlot()` après l'await pour restaurer le preset actif depuis LS (le wipe `activeId = null` de `_injectTrades` est in-memory only, LS reste source de vérité).
